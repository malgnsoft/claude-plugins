# backlog-consistency-20260825 리뷰 보고서 (최초 리뷰, 풀패널)

- **대상**: 워크트리 `/Users/hopegiver/workspace/claude-plugins/.claude/worktrees/agent-ade8ffe123b60d6e0`, 브랜치 `trainer/backlog-consistency-20260825`, `git diff f62a0e5 08b06d4` (3파일, 순증 +888B)
- **등급**: Sensitive (전역 배포 자산 상시 본문 + 승인·재검토 게이트 절차)
- **target_id**: `backlog-consistency-20260825` (신규 발급 — 최초 리뷰이므로 재검토 판정 절차 미적용)
- **모드**: 풀패널(수렴 3 + 발산 1)
- **날짜**: 2026-08-25

## 요약 (2분 규칙)

PM이 지시한 결함 3건은 **전건 착지**했고 스코프 확장은 0건이다(변경 라인이 정확히 6곳: pm.md 40·108·153, reviewer.md 47·51, SKILL.md 판정표+주석). 결함2의 "재검토 3요소" 문구는 pm.md:36과 **바이트 단위로 동일**함을 문자열 비교로 확인했다. 이번 diff가 식별자·이력 금지 규칙을 새로 어긴 것은 0건이고, `pnpm run check-assets`는 ERROR 0 · WARN 18로 베이스(f62a0e5)와 동일하다.

**병합 차단 사유는 없다.** 다만 결함1이 겨냥한 문제(정본 `skills/project-standards` §3 "id는 적지 않는다"와 사본의 드리프트)는 pm.md 밖에서 아직 살아 있다 — `knowledge/` 3파일 5곳이 여전히 "STATUS.md에 id를 남긴다"고 지시하며, 그중 하나는 자기 문장에 "`skills/project-standards` §3과 동일 규칙"이라고 적어 놓고 정반대 지시를 한다. 이번 위임 스코프 밖이라 trainer 잘못은 아니지만, 이 라운드를 "id 드리프트 해소"로 종결 처리하면 안 된다(RV-001, 후속 위임 필요).

**종합 판정: 🟡 Amber — 병합 가능. 병합 전 필수 수정 0건, 후속 위임 필수 1건(RV-001).**

## 페르소나 재사용 판정

| 페르소나 | 유형 | 재사용/신규 | 사유 |
|---|---|---|---|
| `persona-spec-implementation-conformance-auditor.md` | 수렴 | **재사용** | INDEX "명세를 계약서, 커밋을 납품물로 놓고 조항 1:1 대조" — 이번 라운드(수용기준 3건 대조)와 동일 역할개념. §5 참고파일은 직전 라운드 고정이라 역할개념 수준으로 적용 |
| `persona-semantic-force-preservation-auditor.md` | 수렴 | **재사용** | INDEX "삭제·치환 후 규칙의 경계·강제력이 조용히 약해지지 않았는지 원문 대조" — 결함1이 정확히 "종속절 삭제" 작업이라 동일 역할개념 |
| `persona-product-body-portability-auditor.md` | 수렴 | **재사용** | INDEX "조회 불가 근거(식별자·이력)가 제품 본문에 유입됐는지 목적 기준 감사" — PM 지시 (d)와 동일 역할개념 |
| `persona-process-mechanism-zero-based-challenger.md` | **발산** | **재사용** | INDEX "이 정도 장치가 애초에 필요한가"를 묻는 발산형 — 결함3(표 열 신설)이 구조 추가라 동일 역할개념 |

신규 페르소나 0건. INDEX.md의 "역할개념(1줄)" 열 전량(32행)을 스크리닝해 위 4개가 이번 리스크 표면을 모두 덮는다고 판정했다. 4개 파일 모두 "적용 이력"에 이번 라운드 항목을 append했고 INDEX "최근 재사용" 열을 갱신했다.

## 스펙-구현 대조 (수용 기준 검증)

| 결함 | 수용 기준 | 확인 방법 | 결과 |
|---|---|---|---|
| 1 | `grep -n "decision id" agents/pm.md`의 모든 줄이 STATUS.md에 id를 적으라는 지시를 담지 않을 것 | 워크트리에서 실행 | **0줄** — 조건 충족(:40 종속절+`+ decision id로`, :108, :153 세 곳 모두 제거·치환) |
| 2 | pm.md와 reviewer.md의 재검토 진입 요소 개수(3)·이름이 정확히 일치 | 두 파일의 해당 구간을 python 문자열로 잘라 `==` 비교 | **IDENTICAL: True** — 괄호 예시("최초 위임 시 발급한 안정 슬러그", "blast radius 카테고리, 예: …")까지 동일 |
| 3 | 각 등급 값이 pm.md 기존 권한 구분과 모순 없음, 새 카테고리 발명 금지 | pm.md 70~80행 권한표와 5행 1:1 대조 | 5행 전건 대응 확인. 다만 2행에서 압축 손실(RV-002/003) |
| 공통 | 스코프 확장 금지 | `git diff --stat` + 변경 라인 열거 | **0건** — 3파일, 변경 라인 6곳뿐 |
| 공통 | 식별자·이력 금지 | 추가줄 대상 패턴 grep 3종 | **0건** |
| 공통 | `check-assets` ERROR 0 | 워크트리·베이스 양쪽 실행 | ERROR 0 · WARN 18 (베이스와 동일, 신규 WARN 0) |

### 결함3 상세 대조 (판정표 ↔ pm.md 권한 참조표)

| 등급 | SKILL.md 신설 셀 | pm.md 권한표 원문 | 판정 |
|---|---|---|---|
| Micro | 승인 불요(PM 직접 처리, 사후 기록만) | PM 직접 처리, 승인 불요, 사후 STATUS.md 1줄 + `decision_record`/`work_record`만 | ✅ 대응 |
| Standard | PM 단독 승인(사람 승인 불요) | PM이 위임 + **evaluator 약식 검증 확인 후** PM 단독 승인(사람 승인 불요) | ⚠️ 전제조건 탈락 (RV-002) |
| Sensitive | 사람 승인 필수(PM 단독 승인 불가) | reviewer 풀패널 필수 + 사람 승인 필수. PM 단독으로 승인 불가 | ✅ 대응 |
| Exploration | 승인 대상 아님(변경 산출물 없음, PM은 결론 채택 여부만 판단) | PM은 조사 **결론의 채택 여부만 승인**(파일 미변경 상태이므로 원 조사 자체엔 승인 불요) | ⚠️ 용어 변형 (RV-003) |
| Refactor | 사람 승인 필수(PM 단독 승인 불가) | (Sensitive/Refactor 동일 행) | ✅ 대응 |
| — (표 아래 주석) | 정본은 `agents/pm.md` "PM 권한 참조표", 등급 무관 사람 승인 행위·전역 자산 승격 담당은 그 표를 따름 | 비가역·대외·정책 신설 행 / 전역 자산 승격 실행 행 | ✅ 새 카테고리 발명 없음 |

## 지적 사항 (통합)

| ID | 심각도 | 위치 | 문제 | 확인 방법 | 권고 |
|---|---|---|---|---|---|
| RV-001 | 🟠 Major (이번 diff 밖) | `knowledge/common/project-management.md:10`, `knowledge/common/project-folder-structure.md:26`, `knowledge/leadership/progress-status-templates.md:25,33,36` | 결함1이 고친 것과 **같은 지시가 knowledge 5곳에 살아 있다**. 특히 `project-management.md:10`은 "완료 섹션은 1줄 요약(**+기록 id**)으로 축약(`skills/project-standards` §3과 **동일 규칙**)"이라며 정본과 같다고 선언하고 정반대를 지시한다. `progress-status-templates.md:33`은 "1줄 요약 + MCP id 참조가 **원칙**"이라 못박고 예시까지 든다. `:36`은 pm.md:40에서 방금 지운 문구("1~2줄 + 최신 decision id로 즉시 재압축")와 사실상 같은 문장이다 | `grep -rn "STATUS.md" agents skills knowledge \| grep -E "\bid\b\|아이디"`(3개 디렉토리 전량). 도달성도 확인 — 세 파일 모두 agents/skills에서 직접 참조는 0건이나 진입점 `knowledge/README.md:31,32,62`에 등재돼 있어 도달 가능 | **병합 차단 아님**(이번 위임 스코프 밖, 이번 diff가 악화시킨 것 아님). 별건으로 trainer에 위임해 5곳을 정본 §3에 맞추고, pm.md만 고쳐 놓고 "id 드리프트 해소"로 종결하지 말 것 |
| RV-002 | 🟡 Minor | `skills/common-task-grading-and-verification-depth/SKILL.md:18` | Standard 행 승인 주체가 정본의 전제조건("evaluator 약식 검증 확인 후")을 떨어뜨렸다. 이 표만 읽는 에이전트는 검증 없이 PM 단독 승인이 성립한다고 읽을 수 있다 | pm.md:73 원문과 셀 문자열 대조 | 셀을 `PM 단독 승인(evaluator 약식 검증 확인 후, 사람 승인 불요)`로. 1셀 수정 |
| RV-003 | 🟡 Minor | 같은 파일 `:20` | Exploration 행이 정본의 **승인**을 "승인 대상 아님 … 결론 채택 여부만 **판단**"으로 바꿨다. pm.md는 "다른 에이전트가 'PM 승인'을 언급할 때 이 표 하나만 대조하면 된다"는 용어 통제를 걸어 두었으므로, 요약본에서 승인→판단 치환은 "Exploration엔 승인 절차가 없다"는 해석 여지를 만든다 | pm.md:76 원문과 셀 문자열 대조 | `PM 승인(조사 결론의 채택 여부만 — 파일 미변경이라 원 조사 자체엔 승인 불요)`로. 1셀 수정 |
| RV-004 | 🟡 Minor | 같은 파일 `:17`,`:18` | Micro/Standard 셀이 "승인 불요"/"사람 승인 불요"를 단정형으로 적는다. 등급 무관 사람 승인 오버라이드(비가역·대외·정책 신설)는 표 **아래** 주석에만 있고, 이 스킬의 머리말(`:8~11`)은 "Micro 등급 배포 설정 변경"을 예로 든다 — 즉 오버라이드가 실제로 물리는 조합이 문서 스스로 예시돼 있다 | `:8~11` 머리말과 `:17`,`:23` 대조 | 두 셀에 `†` 등 표식을 달아 주석과 연결(약 60B). 주석 자체가 인접해 있어 위험은 제한적 |
| RV-005 | 🟡 Minor (선재 결함) | `agents/reviewer.md:49` | 재검토 판정 2단계가 "**§1.2** 동일대상 4조건"을 가리키는데 §1.2가 실재하지 않는다. 4조건은 `common-task-grading-and-verification-depth` "동일 대상 재검토" 절(번호 없음)에 있고, `reviewer-persona-panel-standard`의 절 번호는 0/1/1.5/2/2.5/3~7로 §1.2가 없다 | `git show f62a0e5:…/reviewer.md`로 선재 확인(49행 동일) + panel-standard 헤딩 전량 나열 + `grep -rn "§1.2"` | 이번 diff가 만든 문제가 아니므로 병합 차단 아님. 다만 **이번에 고친 47·51행 바로 사이에 낀 줄**이라, 후속 위임 시 함께 처리 권고. 참고: `scripts/validate-agent-assets.mjs`의 `ANCHOR_REF`는 `Skill \`x\` §N` 형태만 잡아 맨몸 `§1.2`는 검출되지 않는다 |
| RV-006 | 🟡 Minor | `skills/common-task-grading-and-verification-depth/SKILL.md:40` | pm.md:36이 "상세: Skill … '동일 대상 재검토' 절"로 가리키는 그 절은 아직 "PM은 위임 시 `target_id`를 발급/재사용하고"라고만 적어 3요소를 모르는 상태다. 포인터를 따라간 독자는 3요소 규칙을 만나지 못한다 | pm.md:36 포인터 → SKILL.md:38~46 정독 | 이번 위임이 금지한 스코프 확장에 해당하므로 **이번 커밋에서 고치지 말 것**. 후속 위임에서 1줄 추가(또는 pm.md·reviewer.md로의 포인터) 권고 |
| RV-007 | ⚪ Nit | 같은 파일 전체 | 상시비용 자산(`common-*`)이 4,183B → 4,724B(+541B, +13%). reviewer.md +346B, pm.md +1B | `git show f62a0e5:<file> \| wc -c` 대 현재 파일 대조 | 조치 불요. 이 저장소의 "1순위 성능, 2순위 토큰" 원칙상 승인 주체 조회 1회를 위해 pm.md(37.6KB) 전문을 열지 않아도 되는 효과가 비용을 상회한다고 판단. 기록 목적 |

## 기각된 지적

| 후보 지적 | 기각 사유 |
|---|---|
| pm.md:40에서 "이때는 decision id가 발급되지 않았으므로"를 지워 요구의 근거가 사라졌고, 문장이 앞뒤와 어색해졌다 | 기각. ①요구 강도 불변 — "줄 내용 자체로 재개 가능해야 한다"가 그대로 남아 있고 서술어도 의무형 유지. ②문맥 정합 — "…압축 규율을 그대로 적용하며, 줄 내용 자체로 재개 가능해야 한다"로 매달린 조사·괄호 파편 없음. ③삭제된 근거가 **이미 무효** — 이제 어떤 경우에도 id를 적지 않으므로 "이때는 발급되지 않았으므로"라는 대조가 성립하지 않는다. PM 지시 (a)에 대한 답: trainer의 "종속절만 제거" 주장은 사실이며 문맥도 자연스럽다 |
| 자족성 요구("줄 내용 자체로 재개")가 hub 미가용 절에만 있어, id를 못 쓰게 된 정상 케이스에는 빠졌다 | 기각. 정본 `project-standards/SKILL.md:43`이 id를 빼는 근거로 "hub 이력으로 상세 조회가 가능하므로"를 든다 — hub가 살아 있는 정상 케이스는 조회로 복원되고, hub 미가용 케이스만 줄 자체의 자족성이 필요하다. 현행 배치가 정본 의도와 정합 |
| 이번 diff가 제품 본문에 식별자·이력을 새로 들였다 | 기각. 추가줄 대상 3종 패턴 grep 전건 0(8자리 hex·26자 ULID / `v\d+\.\d+\.\d+`·`20\d\d-\d\d-\d\d`·이전엔·예전엔·직전 라운드·이관·폐기·신설됨 / 자사 배선·실측치) + 신규 문장 3종 정독. 전부 현재형 규칙 서술 |
| 결함3이 "새 카테고리를 발명"했다(승인 대상 아님 등) | **강등 후 잔존** — 발명 수준은 아니라고 판정해 🟠에서 🟡로 내렸다(RV-003). pm.md 원문의 두 반절("결론 채택 여부만 승인" + "원 조사 자체엔 승인 불요")을 모두 담고 있어 모순이 아니라 압축이다. 다만 표제어가 정본에 없는 표현이라 Minor로 남긴다 |
| `check-assets` 회귀 | 기각. ERROR 0 유지, WARN 18로 베이스와 동일(신규 WARN 0). 판정표 아래 정본 포인터 문장은 오히려 `CANONICAL_CLAIM` 정규식에 부합해 기계 검증 대상으로 편입됨 |

## 페르소나별 관점

### [스펙-구현 정합성 감사관] — 판정: 🟢 Green
계약 조항 3건 + 공통 준수사항 3건을 전부 실물로 대조했다. 결함1은 수용 기준(grep 0줄)을 문자 그대로 충족하고, 지시서에 없던 :40까지 trainer가 스스로 찾아 처리한 것도 확인했다(원 지시는 :108·:153만 특정). 결함2는 요구가 "개수·이름 일치"였는데 실제 산출물은 그보다 강한 **바이트 동일 복제**다 — 이후 라운드에서 기계 대조가 가능해졌다는 뜻이라 요구 이상으로 착지했다. 결함3만 5행 중 2행에서 압축 손실이 있다(RV-002/003). 스코프 확장 0건.

### [의미강도 보존 감사관] — 판정: 🟢 Green
이번 diff에서 **경계가 열린 곳은 없다**. reviewer.md의 재검토 진입 게이트는 2요소 AND → 3요소 AND로 좁아졌고, 안전측 기본값(누락 시 풀패널)의 적용 범위는 넓어졌다 — 즉 실수했을 때 비싼 쪽으로 틀리게 설계돼 있다. id 삭제 3곳은 모두 의무형 서술어와 적용 조건을 보존했다. 유일한 미세 약화는 판정표 Standard/Exploration 셀의 압축 손실이며(RV-002/003), 둘 다 셀 1개 수정으로 복원된다.

### [제품 본문 이식성 감사관] — 판정: 🟢 Green
패턴별 결과: 식별자 0건(`\b[0-9a-f]{8}\b`, `\b01[0-9a-hjkmnp-tv-z]{24}\b`) / 이력 0건(`v\d+\.\d+\.\d+`, `20\d\d-\d\d-\d\d`, 이전엔·예전엔·직전 라운드·이관·폐기·신설됨) / 자사 배선·실측치 0건. 신규 문장이 규칙의 이유를 현재형으로만 적어("id는 적지 않는다 — 상세는 hub 이력으로 조회한다") 항구 규칙 [이력 금지]의 모범에 부합한다. 다만 RV-001에서 드러난 knowledge 5곳 중 `progress-status-templates.md:33`의 예시 `(decision #142)`는 **형식 예시로서의 id**라 이식성 위반은 아니다 — 그 줄의 문제는 id 형태가 아니라 "id를 적으라"는 지시 자체다.

### [프로세스 메커니즘 제로베이스 도전자, 발산형] — 판정: 🟡 Amber
아래 🔵 Rethink 참조. 결론만 적으면: 이번 커밋은 **드리프트를 고치면서 드리프트 표면을 하나 늘렸다**. 이번 병합을 막을 근거는 아니지만(정본 포인터가 함께 들어갔고 변경 동결 원칙상 구조 변경은 백로그), 이 저장소가 같은 실패를 정확히 세 번째 반복 중이라는 사실은 기록해 둘 가치가 있다 — 정본 1 + 사본 N 구조에서 사본이 조용히 낡는 것이 RV-001의 원인이기도 하다.

## 구조적 제언 (Rethink) — 발산형 🔵

| ID | 현재 구조 | 제안 구조 | 왜 더 나은가 / 왜 일부만 나은가 | 예상 비용·리스크 |
|---|---|---|---|---|
| RT-001 | 등급별 승인 주체가 pm.md 권한표(정본)와 grading 판정표(사본) **두 곳**에 값으로 존재. 사본에는 "정본은 pm.md" 주석이 달려 있으나 강제력은 없다 | **(A)** 열은 두되 값 대신 `→ pm.md 권한표` 포인터만. **(B) 권장** — 등급 축의 소유권을 스킬로 이전: 5등급 승인 주체는 이 판정표가 정본이 되고 pm.md의 등급 4행은 포인터로 축약. pm.md는 등급 무관 행(비가역·대외·정책 신설, 전역 자산 승격, 위임 실패 2회, 직접 작성)만 보유. **(C)** 사본 유지 + `scripts/validate-agent-assets.mjs`에 두 표의 등급별 승인 주체 셀 대조 검사 추가 | (A)는 드리프트 0이지만 이번 결함3의 목적(표 하나로 조회)을 되돌린다. (B)는 단일 소스 + 조회 편의를 **동시에** 얻는다 — pm.md:68이 이미 "등급은 Skill `common-task-grading-and-verification-depth`의 5등급만 쓴다"고 선언해 등급 축의 정본을 스킬로 인정하고 있으므로 방향도 정합적이다. 부수 효과로 pm.md(37.6KB, 예산 15KB 초과 WARN)가 줄고 상시비용이 4.7KB 스킬로 이동한다. (C)는 구조를 안 건드리고 드리프트만 막지만 사본 자체는 남는다 | (B)는 pm.md "다른 에이전트 MD와의 정합 규칙"·"이 표 하나만 대조하면 된다" 서술을 함께 개정해야 하는 **구조 변경** → 변경 동결 원칙상 백로그, 사용자 판단 필요. (C)는 배포되지 않는 저장소 전용 스크립트 수정이고, 그 스크립트에 이미 `checkCanonicalClaims`(정본 선언 파싱)·`checkAnchors`(§참조 대조) 같은 크로스파일 검사 함수가 있어 **같은 패턴의 함수 1개 추가** 형태다. 다만 마크다운 표 파싱이 필요해 정확한 공수는 **미실측 추정** |
| RT-002 | 재검토 3요소 all-or-nothing: 리스크 범주가 위임문에 없으면 나머지 2요소가 멀쩡해도 풀패널(발산형 포함 2~4명) 전량 재실행 | reviewer가 직전 리뷰 문서를 어차피 Read하므로, 리스크 범주 누락 시 **직전 리뷰에서 읽어와 보고서 첫 표에 명시하고 PM에 확인 요청**하는 경로를 허용. target_id·직전 리뷰 경로 2개는 지금처럼 필수(이건 파생 불가) | 리스크 범주는 직전 리뷰 문서에 이미 적혀 있는 **파생 가능 정보**다. 그것의 서식 누락에 풀패널 비용 전액을 매기는 것은 비례하지 않는다 — 실제로 벌을 받는 것은 위임문을 덜 쓴 PM이 아니라 토큰이다 | **반대 논거를 인정한다**: (1) 리스크 범주 "불변" 여부는 축소 모드 판정의 입력이라, reviewer가 직전 리뷰에서 스스로 읽어오면 PM 판단을 대신 만들어 주는 셈이 되어 "PM 판단을 그대로 신뢰하지 않는다"는 2단계 원칙과 반대 방향으로 샐 수 있다. (2) 안전측 기본값은 틀렸을 때 비싼 쪽으로 틀리는 것이 맞다. (3) 지금 pm.md가 이미 3요소로 정해 두었고 이번 위임은 그 통일이 목적이었다. → **이번 병합에 영향 없음, 백로그** |

## 트레이드오프 (페르소나 간 충돌)

- **정합성 감사관(결함3 착지 = 합격) vs 발산형(사본 신설 = 구조적 후퇴)**: 두 판단 모두 옳다. PM 요구는 "값이 pm.md와 정합할 것"이었고 그건 충족됐다. 발산형이 문제 삼는 것은 요구 자체의 구조다. 이 충돌은 **이번 병합에서 해소하지 않는 것이 맞다** — 변경 동결 원칙상 구조 변경은 백로그이고, 지금 되돌리면 결함3이 미해결로 남는다. 권고: 병합하고 RT-001(B/C)을 백로그에 올린다.
- **이식성 감사관(diff 청결) vs 정합성 감사관(RV-001 잔존)**: 이번 diff만 보면 🟢, 문제의 전체 표면을 보면 🟠. 보고서는 후자를 판정에 반영하되 병합 차단으로는 쓰지 않는다 — 원인이 이번 커밋 밖에 있고, 병합을 미룬다고 knowledge 5곳이 고쳐지지 않기 때문이다.

## 잘 된 점

- **바이트 동일 복제**: 결함2에서 요구는 "개수·이름 일치"였는데 산출물은 괄호 예시까지 동일한 완전 복제다. 다음 라운드에서 문자열 비교 한 줄로 드리프트를 잡을 수 있게 됐다.
- **지시에 없던 세 번째 지점을 스스로 찾았다**: 원 지시는 pm.md :108·:153만 특정했는데 trainer가 :40의 두 곳(종속절 + "+ decision id로")까지 찾아 처리했고, 그 사실을 보고에 명시했다.
- **정본 포인터를 기계가 읽는 형식으로 썼다**: `승인 주체의 정본은 \`agents/pm.md\`의 "PM 권한 참조표"다`가 `check-assets`의 `CANONICAL_CLAIM` 정규식에 부합해, 앞으로 그 대상 파일이 사라지면 정적 검사가 잡는다.
- **게이트를 닫는 방향으로만 움직였다**: reviewer.md 변경이 전부 안전측(2요소→3요소, 누락 시 풀패널)이라, 이 변경으로 리뷰가 헐거워지는 경로가 없다.
- **스코프 규율 준수**: 변경 라인 6곳뿐. RV-006처럼 "손대고 싶었을 인접 지점"에 손대지 않았다.

## 평가기준 충족 현황

| 항목 | 결과 |
|---|---|
| 결함 1 수용 기준 | ✅ 충족 (grep 0줄) |
| 결함 2 수용 기준 | ✅ 충족 (바이트 동일) |
| 결함 3 수용 기준 | ✅ 충족 (5행 대응, 새 카테고리 0) — 단 2행 압축 손실 Minor |
| 스코프 확장 금지 | ✅ 0건 |
| 식별자·이력 금지 | ✅ 0건 |
| `check-assets` ERROR 0 | ✅ 유지 (WARN도 베이스 동일) |

## PM에게 권고

1. **병합 진행 가능(🟡 Amber). 병합 전 필수 수정 0건.** 이번 diff에는 Critical도, 병합을 막을 Major도 없다.
2. **선택 사항**: RV-002·RV-003은 판정표 셀 2개 수정이다(각 1셀). 같은 브랜치에서 처리하려면 trainer에 반환하고, 그렇지 않으면 후속 백로그로 넘겨도 무방하다. **PM이 직접 고치지 말 것** — `skills/` 하위 `.md`는 trainer 전담이다.
3. **후속 위임 필수(RV-001)**: knowledge 3파일 5곳의 "STATUS.md에 id를 남긴다" 지시를 정본 `skills/project-standards` §3에 맞추는 별건을 trainer에 위임할 것. 특히 `knowledge/common/project-management.md:10`은 정본과 "동일 규칙"이라 선언하면서 정반대를 지시하므로 결함 성격이 명확하다(변경 동결 원칙의 "채택 대상: 실증 가능한 결함"에 해당). 이번 라운드를 "id 드리프트 해소 완료"로 기록하지 말 것.
4. **함께 넘길 것(RV-005·RV-006)**: `reviewer.md:49`의 죽은 앵커 `§1.2`(선재 결함, 이번에 고친 47·51행 사이에 낀 줄)와 grading 스킬 "동일 대상 재검토" 절의 3요소 미기재. 둘 다 후속 위임의 같은 배치에 넣기 좋다.
5. **백로그(사용자 판단)**: RT-001(B 소유권 이전 / C 검사기 가드) — 구조 변경이라 변경 동결 원칙상 자동 승격하지 않는다. RT-002(3요소 all-or-nothing 완화)도 백로그.
6. **reviewer가 실행한 것 / 하지 않은 것**: 실행한 것은 읽기·grep·`pnpm run check-assets` 2회(워크트리·베이스)와 자기 산출물 4개 페르소나 파일의 "적용 이력" append + `INDEX.md` "최근 재사용" 열 갱신 + 이 보고서 작성뿐이다. **리뷰 대상 3파일은 한 글자도 수정하지 않았고, 커밋·병합·push·배포는 하지 않았다.**

## 생략한 관점 / 확인하지 않은 것

- **화면 캡처 없음** — 대상이 전부 에이전트/스킬 마크다운 본문이라 렌더링 화면이 존재하지 않는다(UI 리뷰 비해당).
- **런타임 실행 검증 없음** — 이번 변경은 실행 코드가 아닌 지시 텍스트라 재현 실행 대상이 없다. 대신 정적 검사기(`check-assets`)를 베이스·워크트리 양쪽에서 돌려 대조했다.
- **RV-001의 5곳을 "전부"라고 단정하지 않는다** — 검색 범위는 `malgn-agent/agents`·`skills`·`knowledge` 3개 디렉토리에 대한 `grep -rn "STATUS.md" … | grep -E "\bid\b|아이디"` 한 가지다. "STATUS.md"라는 문자열 없이 완료 섹션 서식을 지시하는 문장이 있으면 이 그물에 걸리지 않는다.

---

# 2차 재검토 보고서 (모드: 축소)

target_id: `backlog-consistency-20260825`
직전 리뷰: 이 문서의 1차 섹션 (판정: 🟡 Amber)
이번 리뷰 대상: `git diff 08b06d4 ad8aeaf` — 6파일 +14/−14 (`agents/reviewer.md`, `knowledge/common/project-folder-structure.md`, `knowledge/common/project-management.md`, `knowledge/leadership/progress-status-templates.md`, `skills/common-task-grading-and-verification-depth/SKILL.md`, `skills/reviewer-persona-panel-standard/SKILL.md`)
리스크 범주: 전역 배포 자산 + 승인·재검토 게이트 절차 텍스트 — 직전 리뷰의 값과 대조해 **불변** 확인
모드 판정 근거: 풀패널 강제 승격 조건 5개 전건 미해당(직전 Major RV-001은 **이번 diff가 해소 대상**이며 아래에서 해소 확인 / 새 실행경로 없음 — 신규 3파일은 전부 1차 RV-001이 지목한 그 파일들 / 다른 Sensitive 하위도메인 미진입 / 2차 누적 / 풀패널 요청 없음). 동일 대상 인정 4조건도 직접 대조: 동일 target_id ✅ · 7일 이내(같은 날) ✅ · 리스크 범주 불변 ✅ · 대상 파일 실질 중첩(reviewer.md·grading SKILL.md 연속) ✅ → **축소(C) 모드**.

## 페르소나 재사용 판정

4개 전원 재사용(6대 요소 무변경, 적용 이력만 append). 신규 0명 — 이번 diff에 새 리스크 표면이 없어 축소 모드 규정대로 신규 페르소나를 만들지 않았다.

## 직전 Critical/Major 해결 여부

| ID | 심각도 | 직전 지적 | 해결 | 근거 |
|---|---|---|---|---|
| RV-001 | 🟠 Major | knowledge 5곳이 "STATUS.md에 id를 남긴다"고 지시 | ✅ **해소** | 재스윕 `grep -rn "STATUS.md" agents skills knowledge \| grep -E "\bid\b\|아이디"` 결과 잔존 히트 7건이 **전부 부정형 지시**("id는 적지 않는다")이거나 무관 건(`project-standards/SKILL.md:38` = `repository_id` 필드 미저장 규칙). `project-management.md:10`은 "`skills/project-standards` §3과 동일 규칙"이라는 자기 선언이 이제 **사실**이 됐고(정본 §3 = `SKILL.md:24 "## 3. 진행 상태 = 루트 STATUS.md"` 실재, 내용 일치), `progress-status-templates.md`의 템플릿 예시 `(decision #123)`·`(decision #142)`도 함께 제거됨(`grep -rn "decision #\|issue #" malgn-agent/` 잔존 2건은 각각 다른 스킬의 무관 예시) |
| RV-002 | 🟡 Minor | Standard 셀이 "evaluator 약식 검증 확인 후" 전제조건 탈락 | ✅ 해소 | `SKILL.md:18` → `evaluator 약식 검증 확인 후 PM 단독 승인(사람 승인 불요)`. pm.md:73과 대응 |
| RV-003 | 🟡 Minor | Exploration 셀이 정본의 "승인"을 "판단"으로 치환 | ✅ 해소 | `SKILL.md:20` → `PM이 조사 결론의 채택 여부만 승인(파일 미변경 상태이므로 원 조사 자체엔 승인 불요)`. pm.md:76과 조사 하나(`은`/`이`) 빼고 동일 |
| RV-005 | 🟡 Minor | `reviewer.md:49`의 죽은 앵커 `§1.2` | ✅ 해소 | `grep -rn "§1\.2" malgn-agent/agents malgn-agent/skills` = **0건**. 치환문이 가리키는 절 제목("동일 대상 재검토 — 축소/증분 모드")과 라벨("동일 대상 인정 조건")이 대상 파일에 문자 그대로 실재(`SKILL.md:38`, `:42`) |
| RV-006 | 🟡 Minor | grading 스킬 "동일 대상 재검토" 절에 3요소 미기재 | ✅ 해소 | `SKILL.md:40`에 3요소 + "하나라도 누락되면 reviewer는 최초 리뷰(풀패널)로 처리" 추가. pm.md:36·reviewer.md:51과 모순 없음 |
| RV-004 | 🟡 Minor | Micro/Standard 셀 단정형 ↔ 등급 무관 사람 승인 오버라이드 연결 표식 부재 | ⬜ **미반영(열림)** | 이번 위임 대상이 아니었음. 1차와 동일하게 비차단 — 주석이 표 바로 아래 인접 |
| RV-007 | ⚪ Nit | 상시비용 증가 기록 | — | 이번 diff는 ±0에 가까움(순증 0줄, 문자 치환 위주) |

## 이번 diff 신규 지적

| ID | 심각도 | 위치 | 문제 | 확인 방법 | 권고 |
|---|---|---|---|---|---|
| RV-008 | 🟡 Minor | `skills/reviewer-persona-panel-standard/SKILL.md:167` | **같은 줄의 반쪽만 고쳐졌다.** 앞쪽 `§1.2`는 절 이름 참조로 정정됐으나 뒤쪽 `§3 에스컬레이션 미해당 확인`은 그대로다. 이 파일의 §3은 "발산형 페르소나 — 패널에 최소 1명 필수"이고, 실제 에스컬레이션 조건은 grading 스킬의 **번호 없는** "풀패널 강제 승격 조건"(`SKILL.md:44`)이다. RV-005와 정확히 같은 결함 유형이 한 토큰 옆에 남았다. 용어도 갈린다 — `reviewer.md:48`은 "풀패널 강제 승격 조건", 이 줄은 "에스컬레이션" | `grep -n "^## 3\." reviewer-persona-panel-standard/SKILL.md` → `61:## 3. 발산형 페르소나…` + grading `SKILL.md:44` 원문 대조 | `"풀패널 강제 승격 조건"(같은 절) 미해당 확인`으로. 1줄 수정. **병합 차단 아님** |
| RV-009 | 🟡 Minor (선재, 정본 내부) | `skills/project-standards/SKILL.md:41` | 이번 라운드가 사본들의 완료 섹션 개수를 3~5로 통일한 결과, 이제 **정본 자신**이 마지막 불일치 지점이다 — `:41`이 "압축 규율(완료 섹션 **5~7개** 유지)을 지켜도"라고 적고 두 줄 아래 `:43`이 "최근 **3~5개**만 유지(5~7개에서 축소)"라고 적는다 | `grep -rn "5~7\|3~5" agents skills knowledge` 전량 스캔 — STATUS.md 문맥 히트는 `project-standards:41`(5~7) / `:43`,`:145`(3~5) / knowledge 3파일(3~5)뿐 | 이번 diff가 만든 문제가 아니고 위임 스코프 밖. RV-008과 함께 후속 배치에 넣을 것. `:41`의 괄호를 "완료 섹션 개수 유지"로 바꾸거나 3~5로 정정 |
| — | ⚪ Nit | `knowledge/common/project-folder-structure.md:26` | "완료 항목은 1줄 요약만으로(id는 적지 않는다), 완료 섹션은…" — "만으로"가 살짝 겉돈다. 원문의 생략 구조를 그대로 물려받은 것이라 의미 손상은 없다 | 원문(`git show 08b06d4:…`)과 나란히 대조 | 조치 불요 |

## 기각된 지적 (2차)

| 후보 | 기각 사유 |
|---|---|
| `progress-status-templates.md:33`이 여전히 "기록 id"를 언급한다 | 기각. 부정형 지시("기록 id는 적지 않는다")이며 이유까지 현재형으로 붙었다 — 정본 `project-standards:43`과 같은 형태 |
| `project-management.md:10`의 `§3` 앵커도 RV-005와 같은 죽은 앵커다 | 기각. `skills/project-standards/SKILL.md:24`에 `## 3. 진행 상태 = 루트 STATUS.md (단일 소스)`가 실재하고, 이번 수정으로 "동일 규칙"이라는 주장 자체도 참이 됐다 |
| knowledge 3파일 수정이 각 파일 내 다른 서술과 모순된다 | 기각. `progress-status-templates.md`를 STATUS.md 관련 전 라인(1~39) 통독 — 템플릿 예시(`:20`)·리스크 서술(`:25`)·아카이빙 규칙(`:33~36`)·헤더 규칙(`:37`)이 모두 "1줄 요약, id 없음, 3~5개"로 한 방향. `:36`의 이관 트리거도 "5개를 넘거나"로 3~5 상한과 정합 |
| 이번 diff가 식별자·이력을 새로 들였다 | 기각. 추가줄에 식별자·날짜·버전 0건. 오히려 템플릿 예시의 id 2건이 제거됐다 |
| `check-assets` 회귀 | 기각. **직접 실행** ERROR 0 · WARN 18 (베이스 동일). 위임자 보고를 그대로 받지 않고 재확인함 |

## 종합 판정 (2차)

**🟢 Green — 병합 승인.**

직전 Major 1건(RV-001)과 Minor 4건(RV-002/003/005/006)이 전건 해소됐고, Critical·Major 0건이다. 남은 것은 Minor 3건뿐이며 전부 **이번 diff 밖 원인**이거나(RV-009 정본 선재, RV-004 이번 위임 대상 아님) **1줄 수정으로 끝나는 후속**(RV-008)이다. 특히 RV-001은 1차에서 "pm.md만 고치고 종결하면 안 된다"고 지적한 건인데, trainer가 재스윕에서 1차 보고서에 없던 `project-folder-structure.md`까지 스스로 찾아 처리했고 완료 섹션 개수(5~7→3~5)까지 함께 통일했다 — 지시 범위보다 넓게, 그러나 같은 축 안에서 마무리한 것이라 스코프 확장으로 보지 않는다.

## PM에게 권고 (2차)

1. **병합 진행(🟢). 병합 전 필수 수정 0건.**
2. **후속 1배치**(trainer 위임, 각 1줄): RV-008(`reviewer-persona-panel-standard/SKILL.md:167`의 `§3 에스컬레이션` → `"풀패널 강제 승격 조건"(같은 절)`), RV-009(`project-standards/SKILL.md:41`의 `5~7개` 잔존). 둘 다 이번 라운드가 정리한 축의 마지막 꼬리다.
3. **백로그 유지**: RV-004, RT-001(승인 주체 열 = pm.md 권한표 사본 구조), RT-002. 1차와 동일하게 자동 승격하지 않는다.
4. **reviewer가 실행한 것**: 읽기·grep·`pnpm run check-assets` 1회(직접 재확인)와 이 문서·페르소나 이력·INDEX 갱신뿐. **리뷰 대상 6파일 무수정, 커밋·병합·push 없음.**
