# 백로그 결함 2건 정정 초안(`969cde4`) 리뷰 보고서

리뷰 페르소나 패널: `personas/persona-spec-implementation-conformance-auditor.md`(재사용) · `personas/persona-decision-reversal-auditor.md`(신규) · `personas/persona-dead-reference-scope-challenger.md`(재사용, 발산형)
리뷰 대상: `git diff b20ffd0..969cde4` — `malgn-agent/agents/evaluator.md`(+1/−1) · `malgn-agent/skills/project-retrospective/SKILL.md`(+1/−0)
워크트리: `.claude/worktrees/agent-ac46e32dc566a7f20` (브랜치 `worktree-agent-ac46e32dc566a7f20`)
리스크 범주: 전역 배포 자산(제품 본문 MD — 전 직원 상시 로드)
target_id: `backlog-2items-path-workrecord-20260901` (PM 위임에 미발급 — 리뷰어가 부여, 최초 리뷰로 취급)
리뷰 일자: 2026-09-01
종합 판정: 🟡 **Amber** (Critical 0 · Major 1 · Minor 1 · Nit 1 · Rethink 1)

## 요약 (2분 규칙)

두 수정 모두 **위임 지시서의 수용 기준 문면대로 정확히 착지했고, 스코프 초과는 0건**이다(2파일 +2/−1, 정적 게이트 base와 동일). 다만 결함1의 **수정 방향**이 문제다 — 위임 지시서가 근거로 든 "과거 bare 경로 표준 결정"(`docs/reviewer/review-backlog-fix-round1-2026-08-26.md:235`)은 실재하지만, **그 뒤 커밋 `6e7a40f`가 정반대 규약("소스 clone을 고치는 자리는 접두를 붙인다")을 명시하며 `trainer.md`를 접두어 쪽으로 통일**했고, 초안·커밋 메시지 어디에도 이 사실이 없다. 게다가 그 선행 리뷰 자신이 "**단독 1줄 수정으로는 표기만 갈리므로 동종 15개 사이트를 한 라운드에 묶어야 한다**"(`:248`)고 못박았는데 이번이 정확히 그 단독 1줄 수정이다. 결과적으로 `evaluator.md`는 이제 같은 판정 절차 안에서 `:42`(bare) vs `:67`(접두어)로 갈린다. 실행이 깨지는 결함은 아니므로 병합 차단 사유는 아니지만, **PM이 방향을 명시적으로 결정·기록하기 전에 병합하면 프로젝트의 "변경이력 관리 원칙"이 금지한 왕복이 된다.**

## 페르소나 재사용 판정 (산출물 게이트)

| 페르소나 | 유형 | 판정 | 사유 |
|---|---|---|---|
| `persona-spec-implementation-conformance-auditor.md` | 수렴 | **재사용** | INDEX.md:35 역할개념 "명세를 계약서, 커밋을 납품물로 놓고 조항 1:1 대조" = 이번 "수용기준 2항 ↔ diff 대조"와 동일. 6대 요소 무수정, 적용이력만 append |
| `persona-dead-reference-scope-challenger.md` | 발산 | **재사용** | INDEX.md:36 역할개념 "문제 스코프를 형태로 정의한 것이 실제 판정 기준과 어긋나지 않는지 반증" = 이번 "1문장 형태 불일치 vs 경로 도달 가능성"과 동일 |
| `persona-decision-reversal-auditor.md` | 수렴 | **신규** | INDEX.md 전 41행의 "역할개념(1줄)" 열을 전수 스크리닝한 결과 **"이 변경이 과거 반대 결정을 확인 없이 뒤집는가"를 묻는 행이 없다.** 최근접은 `persona-mechanism-longevity-realist`(새 장치가 다음 라운드에 살아남는가)로, 대상이 "새로 도입한 메커니즘의 생존"이지 "기존 결정의 번복"이 아니라 겹치지 않는다. INDEX.md에 새 행 추가 완료 |

## 지적 사항 (통합)

| # | 심각도 | 관점 | 위치 | 확인방법 | 문제 | 개선안 |
|---|---|---|---|---|---|---|
| RV-01 | 🟠 Major | 결정 되돌림 | `malgn-agent/agents/evaluator.md:42` (커밋 `969cde4`) | `git log -L 42,42:malgn-agent/agents/evaluator.md` + `git show -s --format=%B 6e7a40f` + `grep -rn "malgn-agent/" malgn-agent/agents/` | 수정 방향이 **가장 최근에 명시된 규약과 반대**다. `6e7a40f` 커밋 메시지: "편집 대상 경로를 malgn-agent/ 접두 형태로 통일 — 경로 규약상 소스 clone을 고치는 자리는 접두를 붙여야 읽기 대상과 구분된다". 그 커밋이 `trainer.md`를 bare→접두어로 바꿔 현재 `trainer.md`에 접두어 9건이 있고, `evaluator.md:67`도 접두어다. `evaluator.md:42`의 판정 대상은 "trainer가 고친 소스 clone 파일"이므로 그 규약상 접두어 쪽이다. 초안·커밋 메시지에 이 규약이 전혀 언급되지 않았다 | 문면을 바꾸기 전에 **방향을 결정하고 그 근거를 남긴다.** (a) bare 유지 시: `6e7a40f` 규약이 이 자리에는 적용되지 않는 이유를 커밋 메시지·CHANGELOG에 적고, 남은 접두어 사이트(evaluator.md:67 + trainer.md 9건)를 백로그에 존치. (b) 접두어 유지 시: 선언 대신 **예시 3개**를 접두어로 맞추는 1줄 수정으로 방향을 뒤집는다. 어느 쪽이든 비용은 1줄 |
| RV-02 | 🟡 Minor | 정본 충돌 | `malgn-agent/skills/project-retrospective/SKILL.md:60` ↔ `malgn-agent/agents/trainer.md:65` | 정본 절 `skills/common-learning-loop-knowledge-management/SKILL.md:37-43` 원문 대조 + trainer.md 모드표 확인 | 새 포인터를 따라가면 정본은 "주어가 '이 프로젝트가 어디까지 갔는가'(여러 에이전트 결과를 종합한 진행 상태·다음 행동) → **PM**이 남긴다. 개별 에이전트는 이 행을 대신 쓰지 않는다"로 귀결된다. 그런데 이 `work_record`는 `nextAction=다음 프로젝트에 적용할 행동`이라 정확히 그 분기이고, `trainer.md:65`의 모드2 산출 열은 그 hub 기록을 **trainer가 하는 것**으로 적고 있다. 즉 애매성이 "누가?"에서 "정본과 모드표가 서로 다르다"로 옮겨갔다 | 이번 라운드에서 고치지 않는다(trainer.md 수정 = 스코프 확대). **백로그 신규 항목**으로 등재: "project-retrospective 모드2의 교훈 `work_record` 주체가 정본(PM)과 trainer.md:65(trainer)로 갈린다 — 둘 중 하나를 정정" |
| RV-03 | ⚪ Nit | 명세-구현 적합성 | `SKILL.md:60` vs 선례 `skills/reflect-lessons/SKILL.md:78` | 두 줄 나란히 대조 | **부분 동형**이다. 포인터 부분(`(기준: Skill \`common-learning-loop-knowledge-management\`의 "\`work_record\` 주인 판별")`)은 문자 단위로 동일하나, 선례는 "**주체(trainer)+예외(PM 몫)**+포인터" 3요소이고 신규는 "기준 요약+포인터" 2요소다 — 읽는 사람이 답을 얻으려면 다른 스킬을 열어야 한다 | 조치 불요. 주체를 적으려면 분기를 골라야 하고 그건 새 규칙 신설이라 수용기준 위반이 된다 — 회피가 타당. RV-02가 해소되면 자연히 채울 수 있다 |

## 기각된 지적

| 관점 | 지적 요지 | 처리 | 사유 |
|---|---|---|---|
| 결정 되돌림 | "`평면 경로`라는 표현이 `skills/<name>/SKILL.md`·`knowledge/<domain>/<file>.md`(하위 디렉터리 있음)와 모순된다" | **기각(스코프 밖)** | 실물 확인 결과 이 표현은 v1.0.0(`0cb2517`)부터 있던 기존 문구이고 이번 diff가 건드리지 않았다. 문맥상 "평면"은 디렉터리 깊이가 아니라 "에이전트별 하위 디렉터리·이중 구조가 없다"는 뜻으로 같은 문장이 이어서 설명한다 — 오독 아님 |
| 명세-구현 적합성 | "`project-retrospective/SKILL.md`가 `common-learning-loop-knowledge-management`를 처음 참조하는데 참조 목록·frontmatter 갱신이 없다" | **기각(실측 반증)** | `pnpm run check-assets` ERROR 0(참조 해석 성공), 이 스킬에는 별도 참조 목록 절이 없어 갱신 대상 자체가 없음 |
| 발산 | "`evaluator.md:42` 전제 문장 자체를 삭제하고 정본 1곳으로 보내는 게 낫다" | **강등 → 🔵 RT-01로 이동** | 방향은 유효하나 변경 동결 원칙상 구조 변경이라 이번 판정에 반영 불가. 아래 Rethink 섹션에 대안 구조로 기재 |

## 페르소나별 관점

### [명세-구현 적합성 감사관] — 판정: 🟢 Green

계약서(위임 수용기준 2항)와 납품물(`b20ffd0..969cde4`)을 1:1 대조한 결과 **전건 착지·스코프 초과 0건**.

- **결함1 조항**: "선언 문구가 자신의 예시(bare)와 일치하도록 고치되 예시나 파일의 다른 부분을 넓히지 않는다" → `malgn-agent/<카테고리>/<name>` → `<카테고리>/<name>` 단 하나만 변경, 예시 3개(`agents/pm.md`·`skills/<name>/SKILL.md`·`knowledge/<domain>/<file>.md`)와 문장 나머지 전부 무변경. 문면 충족.
- **결함2 조항**: "reflect-lessons:78과 같은 방식으로 정본을 가리키는 포인터 한 줄 추가, 새 규칙 금지" → `SKILL.md:60` 1줄 추가. 인용한 정본 절이 **실재**함을 확인: `skills/common-learning-loop-knowledge-management/SKILL.md:37` `#### \`work_record\` 주인 판별 — 기준은 "이 기록의 주어가 누구인가"다`. 저장소 전체에서 같은 포인터 문구를 쓰는 자리는 이미 5곳(`trainer.md:17`·`:52`·`:132`, `topic-learning:109`·`:119`, `agent-upskill:21`, `reflect-lessons:78`)으로 관용 표현이 확립돼 있다. 새 규칙 신설 없음.
- **스코프**: `git diff --numstat` 2파일뿐, `git status --porcelain` 무출력(워킹트리 clean).
- **정적 게이트**: `check-assets` ERROR 0 · WARN 14 · INFO 7 — **base(`b20ffd0`)에서 재측정한 값과 완전 동일**. `check-docs` 3/3 통과.
- **크기**: `evaluator.md` 24,808B → 24,796B(**−12B**, 상시 비용 소폭 감소) / `project-retrospective/SKILL.md` 4,648B → 4,838B(+190B, invoke 시에만 열리는 조건부 비용).
- 유일한 계약 외곽 검출은 RV-03(부분 동형)이며 Nit.

### [결정 되돌림 감사관] — 판정: 🟠 Amber

C1(이력 확인) 미충족으로 Major 1건. 상세는 RV-01.

`git log -L 42,42`로 그 줄의 전 생애를 확인했다: `0cb2517`(v1.0.0)에서 접두어 선언으로 태어나 `de0eea9`(재배치)에서 접두어 그대로 유지 → 이번 `969cde4`가 처음으로 bare로 바꾼다. 그 사이에 있었던 결정 두 개가 서로 반대다.

| 시점 | 근거 | 방향 |
|---|---|---|
| `docs/reviewer/review-backlog-fix-round1-2026-08-26.md:235` | "다른 조직에서는 clone 루트가 곧 malgn-agent이므로 접두어를 박으면 그쪽이 깨진다" | **bare** |
| 같은 문서 `:248` | "재서술된 형태는 '접두어를 박아라'가 아니라 **grep 명령의 기준 디렉터리를 한 곳에서 못박고** evaluator.md:42의 선언-예시 불일치를 함께 맞춘다. **단독 1줄 수정으로는 표기만 갈리므로 동종 15개 사이트를 한 라운드에 묶어야 한다**" | **표기 통일이 아니라 기준 디렉터리 명시** |
| 커밋 `6e7a40f`(위 리뷰보다 나중) | "편집 대상 경로를 malgn-agent/ 접두 형태로 통일 — 경로 규약상 소스 clone을 고치는 자리는 접두를 붙여야 읽기 대상과 구분된다" | **접두어** |

C4(잔존 사이트 실측): 이번 수정 후에도 접두어 표기는 `evaluator.md:67`(같은 판정 체크리스트 안!) + `trainer.md` 9건 = **10건 잔존**. 즉 자기모순은 문장 안에서 파일 안으로 자리를 옮겼을 뿐이다. `STATUS.md:22` 백로그 항목이 "bare↔접두어 경로혼재 **+** evaluator.md:42 자기모순"으로 두 개를 묶어둔 것도 이 때문이다 — **이번 수정으로 그 항목을 닫으면 안 된다.**

> 공정을 위해 반대 논거도 적는다: 설치본(`~/.claude/plugins/cache/.../malgn-agent/<ver>/`) 기준으로는 루트가 곧 malgn-agent라 bare가 맞다. 즉 **"편집·판정 컨텍스트(저장소 clone)"와 "읽기 컨텍스트(설치본)"에서 정답이 다르다.** 표기를 어느 한쪽으로 통일하려는 시도가 반복해서 되돌아오는 근본 원인이 이것이며, 선행 리뷰 `:248`이 "기준 디렉터리를 못박아라"라고 한 이유도 같다.

### [스코프 정의 도전자(발산형)] — 판정: 🔵 Rethink 1건

상세는 아래 구조적 제언.

## 구조적 제언 (Rethink) — 발산형 페르소나 🔵

| # | 현재 구조 | 제안 구조 | 왜 더 나은가 | 예상 비용/리스크 |
|---|---|---|---|---|
| RT-01 | 경로 표기 규약이 **어디에도 선언돼 있지 않고**, 15개 사이트에 접두어/bare가 흩어져 있다. 결함이 발견될 때마다 "이 줄을 어느 쪽으로 맞출까"를 매번 새로 판단한다(같은 판단이 최소 3회 반복된 것이 이력에 남아 있다) | 표기를 통일하려 하지 말고 **기준 디렉터리를 한 곳에서 못박는다.** 예: evaluator/trainer 공통으로 "이 문서의 모든 상대경로는 malgn-agent 소스 루트(= 플러그인 루트) 기준이다"를 1회 선언하고, 개별 사이트는 전부 bare로 둔다. 그러면 clone 루트가 claude-plugins든 malgn-agent든 해석이 한 가지로 고정된다 | 형태(접두어 유무) 기준으로는 매 라운드 새 사이트가 발견되지만, 목적(기준 디렉터리 확정) 기준으로는 **한 번에 닫힌다.** 선행 리뷰(`review-backlog-fix-round1-2026-08-26.md:248`)가 이미 같은 결론에 도달했고 미수행 상태로 남아 있다 | 2파일 + 15개 사이트 정정 = 1라운드. **변경 동결 중이므로 지금은 불가 → 백로그 존치.** 리스크: 사이트 일괄 치환이라 trainer 위임 필수(편집 권한 경계) |

## 트레이드오프 (페르소나 간 충돌)

- **명세-구현 적합성 감사관(🟢, "지시대로 정확히 했다")** vs **결정 되돌림 감사관(🟠, "지시 자체가 최근 규약과 반대다")**.
  - 두 판정 모두 참이다. 이번 초안의 문제는 **trainer의 실행이 아니라 위임 지시서의 방향 결정**에 있다 — 지시서가 근거로 든 2026-08-26 리뷰는 실재하지만, 그 리뷰의 결론 절반(`:248` "단독 1줄 수정 금지·기준 디렉터리 명시")과 그 이후 커밋(`6e7a40f`)이 지시서에 반영되지 않았다.
  - **권고: trainer 반려가 아니라 PM 결정으로 처리한다.** 초안을 되돌릴 필요는 없고, 병합 전에 (a)/(b) 중 하나를 골라 근거를 커밋 메시지·CHANGELOG에 남기면 된다.

## 잘 된 점

- **스코프 규율이 정확하다.** "예시나 파일의 다른 부분을 넓히지 않는다"는 제약을 문자 그대로 지켜 +2/−1로 끝냈다. 인접한 매력적인 수정거리(`evaluator.md:67` 접두어, `평면 경로` 표현)를 건드리지 않은 것은 변경 동결 원칙에 정확히 부합한다.
- **선례를 형태까지 베꼈다.** `SKILL.md:60`의 포인터는 저장소에 이미 5곳 있는 관용 표현과 문자 단위로 같은 모양이라, 다음 세션이 "이건 우리 저장소의 표준 포인터"라고 바로 알아본다.
- **커밋 메시지가 결함1의 "왜"를 적었다.** 다만 그 "왜"가 반쪽(2026-08-26 리뷰만, `6e7a40f` 누락)인 것이 RV-01이다 — 습관 자체는 유지할 만하다.
- **정본 절 인용이 실재한다.** 죽은 참조를 만들지 않았다(`check-assets` ERROR 0으로 기계 확인).

## 평가기준 충족 현황

| 기준 | 관점 | 중요도 | 충족 | 비고 |
|---|---|---|---|---|
| 수용기준 ①(결함1 문면) | 명세-구현 | 필수 | ✅ | 선언만 변경, 예시 무변경 |
| 수용기준 ②(결함2 포인터) | 명세-구현 | 필수 | ✅ | 정본 절 실재 확인, 새 규칙 없음 |
| 스코프 초과 0 | 명세-구현 | 필수 | ✅ | 2파일 +2/−1, 워킹트리 clean |
| 정적 게이트 무회귀 | 명세-구현 | 필수 | ✅ | check-assets base와 동일, check-docs 3/3 |
| C1 이력 확인 흔적 | 결정 되돌림 | 필수 | ❌ | `6e7a40f` 규약 미언급 (RV-01) |
| C2 근거 원문 실재 | 결정 되돌림 | 필수 | ✅ | 2026-08-26 리뷰 `:235` 실재 |
| C3 그 근거가 최신 결정 | 결정 되돌림 | 필수 | ❌ | 이후 `6e7a40f`가 반대 방향 (RV-01) |
| C4 잔존 사이트 명시 | 결정 되돌림 | 필수 | ❌ | 10건 잔존, 초안에 언급 없음 (RV-01) |
| C5 선례 동형성 | 결정 되돌림 | 권장 | ⚠️ | 부분 동형 (RV-03) |
| 형태 아닌 목적 기준 스코프 | 발산 | — | ⚠️ | RT-01 |

## PM에게 권고

1. **병합 자체는 가능하다(🟡 Amber).** 실행이 깨지는 결함 0건, 정적 게이트 무회귀, 스코프 초과 0건. 결함2(`SKILL.md:60`)는 그대로 병합해도 무방하다.
2. **단, 결함1은 병합 전에 방향을 결정하고 근거를 남길 것(RV-01).** 커밋 메시지의 "bare 경로가 표준"이라는 단정은 `6e7a40f`의 반대 규약을 모른 채 쓰인 것이다. 이 상태로 병합하면 `CLAUDE.md` "변경이력 관리 원칙"이 금지한 왕복의 다음 회차가 예약된다. (a) bare 유지 + 사유 기록(1줄 추가 없음, CHANGELOG만) 또는 (b) 예시를 접두어로 맞춤(1줄 수정) 중 택일 — 둘 다 저비용.
3. **`STATUS.md:22` 백로그 항목을 닫지 말 것.** 그 항목은 "경로혼재 + evaluator.md:42 자기모순" 두 개 묶음이고 이번에 닫힌 것은 후자뿐이다. 접두어 잔존 10건(`evaluator.md:67` + `trainer.md` 9건)과 RT-01(기준 디렉터리 명시)은 그대로 남는다.
4. **백로그 신규 1건 등재(RV-02).** "`project-retrospective` 모드2의 교훈 `work_record` 주체가 정본(→PM)과 `trainer.md:65` 모드표(→trainer)로 갈린다." 재조사 없이 처리 가능하도록: 위치 = `skills/common-learning-loop-knowledge-management/SKILL.md:42` vs `agents/trainer.md:65`, 고친 뒤 참이어야 할 상태 = 모드2 회고에서 나온 교훈성 `work_record`의 주체가 두 문서에서 같은 답으로 읽힐 것, 선례 = `skills/topic-learning/SKILL.md:119`(3분기 답을 인라인 요약해 같은 문제를 이미 해결).
5. **재검토는 불요.** RV-01은 문면 결함이 아니라 방향 결정이므로 reviewer 재투입 없이 PM 판단으로 닫을 수 있다. 방향을 (b)로 바꾸기로 하면 trainer에게 1줄 재위임(편집 권한 경계 유지).

## 생략·미확인 명시

- **화면 캡처 없음** — 문서(MD) 리뷰라 대상 화면이 없다.
- **malgnai-hub 조회 못 함** — 이 세션에서 MCP 인증이 안 돼 `project_search_history`로 과거 결정기록 원문을 확인하지 못했다. 결정 이력 추적은 전부 `git log -L`·커밋 메시지 본문·`docs/reviewer/` 보고서 원문으로 대체했다(RV-01의 근거는 모두 저장소 안에서 재현 가능).
- **실행 액션 없음** — 병합·push·버전업 등 어떤 실행도 하지 않았다. 리뷰 산출물(페르소나 3개 + INDEX.md 갱신 + 이 보고서)만 `docs/` 아래에 기록했다.
