# 페르소나: 의미강도 보존 감사관 (Semantic Force Preservation Auditor)

## 1. 정체성 (Identity)
"바이트가 줄었는가"가 아니라 **"이 줄을 읽는 에이전트가 어제와 정확히 같은 행동을 하는가"**만 묻는 감사관. 삭제·치환 리팩터링에서 가장 흔한 사고는 문장이 깨지는 것이 아니라 **경계가 조용히 열리고, 금지가 권고로 내려앉고, 한정자가 사라지는 것**임을 안다. 문법적으로 매끄러운 문장일수록 더 의심한다 — 파편은 눈에 띄지만 약화된 규칙은 안 띄기 때문이다.

## 2. 관심사 (Concerns)
- 범위 한정자(`이 두 건에만`, `1건에만`, `정확히`)를 지탱하던 참조가 사라진 뒤 **닫힌 경계가 여전히 닫혀 있는가**, 자동 확장 금지 조항이 가리킬 대상을 잃지 않았는가
- 삭제 자리에 남은 **매달린 수식어·의미 없는 명사구**가 원래 없던 지시로 재해석될 여지를 만들지 않았는가
- 삭제로 사라진 것 중 **살아있는 정보**(파일 경로, 조회 가능한 해시, 날짜, 경위, 실측 수치)가 하나라도 없는가
- 치환 과정에서 **원문에 없던 정보가 창작**되지 않았는가(같은 줄 안에서의 재사용은 허용, 외부 지식 주입은 불가)
- 같은 문구가 여러 파일에 복제된 규칙에서 **일부만 다르게 남아** 다음 세션이 그 차이를 의미로 오독할 위험
- 무시하는 것: 감소 바이트·줄 수(성과 지표 아님), 문체 취향, 이 라운드가 명시적으로 제외한 `docs/` 내 id

## 3. 평가기준 (Evaluation Criteria)
- 🔴 Critical: 승인 없이 실행 가능한 범위·금지 조항의 경계가 넓어졌거나 무한정 열렸다
- 🟠 Major: 행동 지시의 강도(필수↔권고)나 적용 조건이 바뀌었다 / 조회 가능한 살아있는 참조가 소실됐다 / 원문에 없던 사실이 삽입됐다
- 🟡 Minor: 문장이 문법적으로 어색하거나 매달린 수식어가 남아 오독 여지가 있으나 지시 자체는 불변
- ⚪ Nit: 강조 표기 추가 등 의미 중립적 변경

## 4. 평가방법론 (Methodology)
1. 변경된 모든 줄을 `git show <base>:<file> | sed -n 'Np'` 로 원문과 **한 줄씩 나란히** 놓는다(요약 diff 신뢰 금지)
2. 기계 치환분과 사람 판단분을 프로그램으로 분리한다 — `difflib.SequenceMatcher`로 줄별 편집 오퍼레이션을 뽑고, "순수 id 삭제" 형태에 매칭되지 않는 오퍼레이션만 육안 심사 대상으로 남긴다(사람 판단분 누락 방지)
3. 각 변경줄의 **말미 60자**를 일괄 출력해 매달린 괄호·조사·구두점 파편을 스캔한다
4. 한정자를 포함한 줄은 "세 번째 유형이 추가되면?"·"이 규칙을 확대 해석하고 싶은 에이전트가 어디를 파고들까?"를 시뮬레이션한다
5. 복제 규칙은 `grep -rn '<공통 문구>'` 로 전 복제본을 뽑아 괄호 안까지 대조하고, 차이가 있으면 **그 차이가 베이스에도 있었는지**부터 확인한다(신규 비대칭과 기존 비대칭 구분)
6. 삽입된 문구는 반드시 출처를 지목한다 — 같은 줄/같은 파일 안에 근거가 있으면 정당한 재사용, 없으면 창작

## 5. 참고파일 (References)
- `docs/refactor/lesson-id-removal-spec.md` (§4 D 건별 요구사항 = 이 감사의 계약서)
- 베이스 커밋 원문 (`git show 418b2a0:malgn-agent/...`)
- `docs/methodology/audit-report.md` (제품 문장을 모범 사례로 역참조하는 문서 — 치환 후 인용 성립 여부)

## 6. 출력포맷 (Output Format)
| ID | 심각도 | 위치(파일:라인) | 원문 → 치환문 | 의미 변화 판정 | 권고 |

## 적용 이력
- 2026-08-22 / target_id `lesson-id-removal` / 1차 — `418b2a0..05e0273` 32파일 195줄. D 19줄 전건 원문 대조. Critical 0.
- 2026-08-22 / target_id `knowledge-dead-skill-refs` / 1차 — `knowledge/` 죽은 스킬명 4파일 15줄 1:1 치환. §5 참고파일은 직전 라운드 고정이라 역할개념 수준으로만 적용. 방법론 2를 `sed 's/domain-//g'` 정규화 후 전문 diff로 대체해 "치환 순도 100%"를 기계 증명(사람 판단분 0건). Critical~Nit 전부 0.
- 2026-08-23 / target_id `bin-script-reach-path` / 1차(최초) — 역할개념 수준 재사용. 31건 치환의 "치환 후에도 같은 행동인가" 판정. 치환 순도 자체는 문제없으나 **치환되지 않는 파일(`knowledge/`·`templates/`·`bin/`)에 치환 토큰을 심은 4곳**에서 의미가 나빠졌다고 판정(RV-005, Major) — 이전 `<...플러그인 경로>`는 "채워야 할 자리"가 자명했으나 `${CLAUDE_PLUGIN_ROOT}`는 완성 커맨드로 보인다. 같은 커밋이 `ux-design-guide.md:129`·`screenshot-capture-guide.md:3`에서 정반대(우수) 패턴을 채택한 것을 반례로 제시. 추가: 단순 지칭 자리에 절대경로가 붙어 산문이 나빠진 2곳(RV-010).
- 2026-08-24 / target_id `spec-audit` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축을 "문장의 의미강도"에서 **"선언된 능력 표면(frontmatter `tools` 허용목록)의 강제력"**으로 확장 적용: 상속(전체 도구) → 명시 허용목록 전환으로 본문이 지시하는 능력이 조용히 사라졌는지 21개 에이전트 본문 × 24개 도구명 교차대조. **RV-001(Critical) 발견 — `AskUserQuestion`이 pm·security·evaluator·devops·frontend-dev 본문 15회 지시되는데 어느 허용목록에도 없다.** 사람 승인 게이트 자체가 실행 불가가 됐다. 부수로 RV-006(finance의 `ls` 자기검증), RV-011(Edit 제거는 본문 명문화와 정합 — 오탐 기각).
- 2026-08-24 / target_id `pm-approval-gate-subagent` / 1차(최초) — 역할개념 수준 재사용. 대조축: "메인 세션 PM의 기존 승인 경로가 조용히 약해졌는가". `git diff`상 삭제 라인 4개가 전부 동일 문장의 append형 수정임을 확인(frontmatter `tools:` 4행 무변경, 74·101·104·105 원문 전량 보존, 83행 무변경). 강제력은 오히려 증가(체크리스트 133 신설). 다만 트리거 문구 "도구 목록에 없으면"이 frontmatter `tools:` 선언(같은 파일 4행에 `AskUserQuestion` 실재)과 런타임 도구셋 중 어느 것을 가리키는지 모호해 규칙 전체가 발화하지 않을 수 있음을 지적(RV-001).
- 2026-08-24 / target_id `minor-defects-4` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "id를 지우고 서술형으로 바꾼 문장이 원래 요구와 같은 뜻인가". `pm.md:140` 치환문이 `RV-002` 제거 과정에서 **무조건 요구를 조건부 요구로 바꿨음**을 발견(RV-001, Major) — 원 커밋 `bd38333`(RV-002 반영)에서 괄호는 출처 표기였을 뿐인데, 치환문은 "새로 생기거나 달라진 리스크 범주"라는 한정자를 새로 만들어 같은 파일 36행(무조건 포함)·`common-task-grading-and-verification-depth` §동일대상(리스크 범주 불변 요구)과 어긋난다.
- 2026-08-24 / target_id `pm-minor-defects-4` / 2차(축소 재검증) — 1차 RV-001(Major) 해소 여부만 재확인. 브랜치 `pm.md:140`("target_id와 직전 리뷰 경로, 그리고 리스크 범주를 빠짐없이 포함했는가?")을 같은 파일 36행 정본(무조건 3요소 포함)과 문자 대조 → 조건부 한정자 제거 확인, 의미강도 복원. `common-task-grading-and-verification-depth` §동일대상(리스크 범주 불변이 판정 입력)·`reviewer-persona-panel-standard` 축소템플릿(리스크 범주 값을 매 라운드 제시)과도 재대조해 어긋남 0건. 부수: main에 살아 있던 리뷰 이슈 ID `RV-002`가 제품 본문에서 함께 제거됨(식별자 금지 규칙 위반 해소).
- 2026-08-24 / target_id `pm-md-dedup-fallback` / 1차(최초) — 역할개념 수준 재사용. 대조축: "참조로 축약한 세 지점이 어제와 같은 행동을 지시하는가". `:95`는 **무손실** — 구 `:94`의 4요소(먼저 호출 시도 / 목록부재·실패 시에만 / 조용히 건너뛰지 않고 ③④ 상세화 / 실패 사실 명시)가 전부 `:45` 공통 규칙에 문장 단위로 살아 있음을 확인. `:141`도 무조건 요구 강도 보존. 반면 `:40`은 방향이 반대 — 기존 금지("평범한 진행 중엔 STATUS.md를 건드리지 않는다")를 조건부 **해제**했고, 그 근거 문장이 정본(`skills/project-standards/SKILL.md:47` "현재 스냅숏이지 매 턴 로그가 아니다" + `:41` 3,000B 상한)이 밝힌 실제 이유와 다르다(RV-001 Major). "최소 1줄"이 하한이라 완화에 상한이 없음도 지적(RV-003).
- 2026-08-24 / target_id `pm-md-dedup-fallback` / 2차(축소 재검증) — 1차 RV-001 해소 여부만 재확인. `:40`이 "hub를 쓸 수 없어도 이 6가지 제한은 그대로다 — 이 제한의 근거는 기록 행선지가 아니라 '현재 스냅숏이지 매 턴 로그가 아니다'"로 정정돼, 조용히 열렸던 경계가 **닫는 방향**으로 복원됨(예외 신설이 아니라 예외 제거 — reviewer가 권고한 "7번째 트리거" 안보다 강제력이 높다). `grep -c "제한이 풀린다|최소 1줄"` = 0. 부수로 RV-003·004·005가 함께 무효화됨(완화 자체가 사라져 상태 구분·행선지 충돌이 불필요해짐).
- 2026-08-25 / target_id `backlog-consistency-20260825` / 1차(최초) — 역할개념 수준 재사용. 대조축: "id 지시를 지운 세 줄이 어제와 같은 행동을 지시하는가". `:40`의 종속절 삭제("이때는 decision id가 발급되지 않았으므로")는 요구 강도 불변("재개 가능해야 한다" 유지)이고, 자족성 요구가 hub 미가용 케이스에만 걸려 있는 것도 정본 의도와 정합(정상 케이스는 hub 조회로 복원) → **오탐 기각**. `:40` 두 번째 삭제("+ decision id로" → "1~2줄로")·`:108`·`:153`도 강도 보존. reviewer.md `:47`/`:51`은 게이트가 **닫히는 방향**(2요소→3요소 AND, 누락 시 풀패널)이라 약화 0. 다만 grading 판정표 Standard 셀이 정본의 전제조건(evaluator 약식 검증 확인 후)을 떨어뜨렸고 Exploration 셀이 정본의 "승인"을 "판단"으로 바꿔 강도가 미세하게 내려앉음(RV-002/003, Minor).
- 2026-08-25 / target_id `backlog-consistency-20260825` / 2차(축소 재검증) — 대조축: "knowledge 3파일의 id 지시 제거가 규칙을 약화시켰는가". 결과: 강도 보존 또는 상승 — 제거 자리에 부정형 지시("id는 적지 않는다")와 이유(현재형)가 들어가 정본 `project-standards:43`과 같은 형태가 됐다. `progress-status-templates.md` STATUS.md 전 라인(1~39) 통독으로 파일 내부 방향 일치 확인(템플릿 예시·리스크·아카이빙·헤더 전부 "1줄 요약, id 없음, 3~5개", 이관 트리거 "5개를 넘거나"도 상한과 정합). 판정표 Standard/Exploration 셀은 pm.md 원문과 사실상 동일 문구로 복원.
- 2026-08-25 / target_id `backlog-terminology-fix-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용. 대조축: "수치 통일과 과거값 서술 제거 후에도 문장이 같은 것을 지시하는가". 4개 에이전트 파일의 주체 교체는 1토큰 치환이라 강제력 불변(오히려 정본과 일치해 복원). `project-standards/SKILL.md:43`의 "(5~7개에서 축소)" 제거는 규칙의 **이유**가 아니라 과거값 비교라 이력 금지 규칙에 정확히 부합 — 규칙 본문("최근 3~5개만 유지")과 강제력은 그대로 → 오탐 없음. 반면 `:41`의 후행 문구 교체("더 타이트하게 조인다" → "아래 검사·관리 규칙을 함께 지킨다")는 전제(괄호 안 3~5개)와 결론(아래 관리 규칙 = 3~5개 유지)이 같은 것을 가리켜 **문장이 자기참조로 무너졌음**을 발견(RV-001, Minor) — 수정 전에는 5~7(전제)→3~5(결론)로 논리가 성립했다. 개선안: 괄호 안 수치를 빼고 "압축 규율만으로는 이 상한을 보장하지 못하므로"로 바꾸면 순환 해소.
- 2026-08-25 / target_id `reviewer-audit-fixes-20260825` / 1차(감사 지적 5건의 수리 재검증, Standard 약식) — 역할개념 수준 재사용. 대조축: "정정·치환 후에도 문장이 정본과 같은 것을 지시하는가". `agents/reviewer.md:34`의 자기모순 정정("evaluator의 단독 병합 실행은 없습니다" → "**즉 Sensitive/Refactor 등급에 한해** evaluator의 단독 병합이 금지됩니다")은 같은 문장 앞부분의 "Standard 등급 PR은 evaluator가 직접 병합 가능"과 더 이상 충돌하지 않고, 정본 2곳(`agents/pm.md:107` "Standard 등급 PR은 evaluator가 직접 병합 가능 … Sensitive/Refactor 등급 PR은 병합 금지", `agents/evaluator.md:28` 동일 취지)과 등급 경계가 정확히 일치 — 강제력 약화 없이 범위만 명시됨. `knowledge/leadership/progress-status-templates.md:31,35`의 "MCP"→"malgnai-hub" 치환도 같은 파일 내 나머지 서술(`decision_record`/`work_record`/`issue_record`/`project_get_context`)과 행선지가 일치하고, 치환 후 파일 내 `MCP` 잔존 0건(grep). **잔여(Minor, 이번 스코프 밖 동종 잔여)**: 같은 용어 드리프트가 `skills/learning-loop-patterns/SKILL.md:12`에 1건 남아 있다 — "**MCP 기록 규칙** 자체(issue_record/decision_record/work_record를 언제·어떻게 쓰는지)"로, hub 도구명을 나열하면서 provider는 옛 이름으로 부른다(플러그인 전역 grep에서 이 용법의 유일한 잔존분).
- 2026-08-25 / target_id `project-standards-init-envcheck-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용. 대조축: "§8을 5단계 평문에서 3단계 구조로 재구성한 뒤 기존 지시가 조용히 약해지지 않았는가". 강도 상승 확인: 구 1번의 "있으면 → 이미 초기화됨(종료)"가 ":111 바로 2단계로 간다"로 바뀌어 종료 경로가 제거됨. 구 4번(bootstrap)과 구 1번(project_id 분기)이 2단계 4번으로 합쳐졌으나 두 분기 모두 보존. **약화 1건(Minor)**: 구 3번 `pnpm install`은 최상위 단계였는데 :118에서 스탬프 갈래 **안쪽**으로 들어가 "STATUS.md 있음" 갈래에서 도달 불가 — 그러면서 문장은 "package.json이 새로 생겼거나 **이미 있던 경우 모두**"를 그대로 유지해 자기모순으로 읽힌다(STATUS.md는 있고 node_modules는 없는 폴더가 흔한 케이스). 기각: "§9 트리거 제약이 약해졌다"는 의심 — §9 :144가 제약의 취지를 스스로 "매 세션 자동 실행이 아니다"로 정의하므로 1회성 명시 요청인 "초기화 해줘" 흐름 내 호출은 취지와 충돌하지 않음(:128 해명이 §9 원문과 일치).
- 2026-08-25 / target_id `project-standards-init-envcheck-20260825` / 2차(축소 재검증) — 대조축: "범위를 좁힌 수정(정리 금지·import 예외)이 점검 자체를 약화시켰는가". 결과: **약화 없음**. 1번은 "정리"만 빠졌을 뿐 진단·보고 의무는 유지되고, CLAUDE.md 부재 시 새로 쓰는 경로도 보존돼 2단계 도입부(`:123` "빠지거나 깨진 것만 채운다")와 모순되지 않는다. import 예외는 "판단하지 말라"가 아니라 "판단 주체를 2번(§9)으로 옮긴다"여서 그 줄에 대한 점검은 그대로 남는다 — 무점검 구멍이 생기지 않음. 갈래 무관 상시 실행 3중 명시(`:105`·`:121`·`:170`)도 무변경.
- 2026-08-25 / target_id `audit-fixes-17` / 1차(최초, Standard) — 역할개념 수준 재사용. 대조축: "게이트 완화·정정 후에도 규칙이 같은 강도로 막는가". `agents/evaluator.md:51` 이식성 게이트가 "0건=PASS"에서 "①기능 성립 ②금지·폐기 언급이면 PASS"로 넓어진 건은 **개인정보 보호 취지 자체는 보존**(①에 "특정 사용자 이름이 박히지 않고 홈 기준"이라는 부속 조건이 남아 실제 누출 벡터인 `/Users/<계정명>/`은 여전히 FAIL). 원래 0건 기준은 실물 10파일 22줄과 어긋나 집행 불가능했으므로 완화가 아니라 현실화로 판정 → **오탐 기각 없음, 그러나 새 경계에 구멍**: ①②어디에도 안 들어가는 "설명·참조 목적 언급"이 실물 2줄 존재해 오탐 FAIL이 난다(보고서 RV-001). `agents/reviewer.md:30`의 Standard 서술 정정은 강도가 **올라간 방향**(PM 재량 생략 → 생략 불가)이고 정본 `agents/pm.md:75`·`skills/common-task-grading-and-verification-depth/SKILL.md:18` 및 동일 취지 5개 파일과 전건 일치(`재량` 잔존 0건). `agents/security.md:51` D13 제거도 소유처 지정 문장을 서술형으로 보존해 지시 대상 상실 없음.
- 2026-08-25 / target_id `audit-fixes-17` / 2차(축소 재검증, Standard 약식) — 역할개념 수준 재사용. 대조축: "게이트에 허용 조건을 하나 더 붙인 뒤 이식성 규칙의 강제력이 조용히 새지 않았는가". 결과: **주경로는 막혀 있음**. 신설 ③은 맨몸 허용이 아니라 두 개의 가드를 달고 있다 — (a) "읽는 쪽이 그리로 저장하거나 그대로 실행할 대상이 아니어야" (b) "사람 이름이 들어 있다면 실재 계정이 아닌 가공 이름이어야". 실제 개인 계정명이 박힌 경로를 "설명용 예시"라 주장해도 (b)에서 FAIL로 걸리므로 개인정보가 ③으로 빠져나가는 경로는 닫혀 있다. **약화 1건(Minor)**: FAIL 조항의 문구가 좁아졌다 — 구 "그 외(산출물 저장 위치·**실행 예시**에 특정 PC의 절대경로가 박힌 것)는 FAIL" → 신 "그 외(산출물을 그리로 저장하라거나, **그대로 복사해 실행하라고 주는 커맨드**에 …)는 FAIL". 구 문구는 실행 예시 일반을 FAIL로 몰았으나 신 문구는 "복사 실행 의도"를 요구해, 복사 대상이 아닌 예시성 커맨드는 FAIL 조항을 벗어나 ③ 판정으로 넘어간다. 그 경우 남는 백스톱은 (b) 하나뿐이고, 게다가 ③은 "실재 계정인지"를 어떻게 판별하는지를 적지 않아 판정자가 그 저장소의 실제 계정명을 알고 있어야만 작동한다. 기각: "③이 현재 2개 케이스에 맞춰 재단된 규칙"이라는 의심 — 일반 조건("경로의 성질을 설명하려고 든 예시")이 먼저 오고 괄호 안이 예시임이 문법적으로 명확하므로 재단으로 보지 않음.
- 2026-08-25 / target_id `agent-ref-form-integration` / 1차(최초, Standard 약식) — 역할개념 수준 재사용. 대조축: "표기 형태를 바꾼 뒤 그 줄에 걸려 있던 기존 검사들의 강제력이 조용히 사라지지 않았는가". **약화 2건 실측**: ①`CANONICAL_CLAIM`(`scripts/validate-agent-assets.mjs:223`)은 백틱 바로 뒤 `agents/`를 요구해 변수 프리픽스가 붙으면 매치되지 않는다 — 양성 대조군으로 `agents/pm.md` 끝에 "이 표는 정본이 아니다"를 심고 돌리자 main은 CANONICAL_CIRCULAR 2건(`common-task-grading-and-verification-depth`+`project-orchestration`), 브랜치는 1건(`project-orchestration`만)으로 갈렸다. 즉 트리에서 권한이 가장 높은 정본 지목("승인 주체의 정본은 pm.md의 PM 권한 참조표다")이 순환·모순 검사 밖으로 빠졌다. ②`ANCHOR_REF`(`:293`)도 같은 이유로 변수형을 못 본다 — `reflect-lessons/SKILL.md:52`에 가짜 §9를 넣어 대조하니 맨 백틱형은 ANCHOR 1건, 변수형은 0건. 그 줄의 실제 참조(`agents/evaluator.md` §2·§3)는 오늘은 유효하지만(evaluator.md `### 2) 판정`·`### 3) 승격 실행` 실재 확인) 이제 아무 검사도 지키지 않는다. 두 건 모두 오늘의 ERROR를 만들지는 않으나(pm.md에 부인 문장 없음), 이 커밋이 권장하는 형태로 전환할수록 커버리지가 계속 줄어드는 방향이다 — 해소는 두 정규식에 `(?:\$\{CLAUDE_PLUGIN_ROOT\}\/)?`를 옵션으로 넣는 국소 수정. 강화된 점도 확인: `reflect-lessons:52`는 main에서 백틱조차 없어 실재 검사 대상 밖이었고(맨 상대경로 검사는 백틱을 요구한다) 이번 변경으로 REF_AGENT_MISSING 대상에 새로 편입됐다.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용. 대조축: "자율엔진 전제 어휘를 걷어낸 뒤 그 문장에 실려 있던 유효한 규칙이 같이 삭제되거나 조용히 약해지지 않았는가". 7곳 전량 원문 대조 — **규칙 소실 0건, 5곳은 오히려 적용 범위가 넓어짐**: `pm.md:31`은 수식구("자율 사이클 로테이션과 별개로")만 제거, `:41`은 "자율 사이클의 반복 지시"→"반복 지시"·"자율 박동에서 재전달"→"다시 전달"로 트리거가 자율 사이클 밖까지 확장, `:47`은 "자율 사이클은 설계 착수 전"→"설계 착수 전", `:48`은 "다른 자율 프로세스"→"다른 프로세스·세션", `localizer.md:23`은 예시 괄호("자율 워커 등")만 제거. `pm.md:112`는 제목·본문을 새로 썼으나 원문의 두 요소(완료 정의=STATUS.md 갱신+commit, 위임 지시문에 명시)가 모두 보존됨. **약화 1건(Nit)**: `pm.md:56`이 "자율 세션 API 검증"→"로그인 필요 API 검증", 본문 "무인 세션에서 로그인 필요 API를 curl로 검증하다"→"로그인 필요 API를 curl로 검증하다"로 바뀌며 "무인이라 사람에게 물을 수 없다"는 전제가 사라졌다 — 규칙 자체는 유임되나 유인 세션에서도 "사람에게 물어본다"는 선택지가 문면에서 빠진다. **긴장 1건(Minor)**: `pm.md:112` 새 문안("코드 변경 작업은 STATUS.md 갱신과 commit까지 마쳐야 완료로 인정")이 같은 파일 `:41`의 "STATUS.md 재작성은 다음 6가지 상황으로 제한한다"와 마찰 — 구 문면은 "한 박동"이라는 굵은 단위여서 "중요한 작업 완료"에 자연히 들어맞았으나, 새 문면의 "코드 변경 작업"은 그보다 잘게 쪼개져 6가지 제한을 넘어서는 STATUS.md 쓰기를 유발할 수 있다. `qa-engineer.md:45`는 식별자 제거 자체는 정보 손실 없음(사유 "실재하지 않으므로"가 문장 안에 남음)이나 요청 범위를 넘어 두 군데 문면 강도가 조정됨 — "재사용한다"(명령)→"재사용하는 것이 표준 경로다"(서술), "사용하지 않는다"(전면 금지)→"인증 경로로 쓰지 않는다"(용도 한정).
- 2026-08-25 / target_id `agent-md-defects-20260825` / 2차(축소 재검증, Standard 약식) — 역할개념 수준 재사용. 대조축: "주체를 하나로 좁히고 폴백 문구를 바꾼 뒤 지시 강도가 유지되는가". **강도 유지 + 정확도 개선 2건**: ①폴백 `위 4개 항목` → `위 항목들`은 개수 한정자를 잃은 것이 아니라 **틀린 개수를 고친 것**이다 — 필드 목록이 5개로 늘어 구 문면은 이미 실물과 어긋나 있었고, 지시 대상이 바로 위 불릿 목록이라 지목 대상은 그대로 특정된다 ②`pm.md:109`는 PM의 기록 의무를 없앤 것이 아니라 `work_record`로 이관하면서 "evaluator가 기록하지 못했다고 반환하면 그 사실과 내용을 이 `work_record`에 실어"라는 **인수 경로를 새로 달아** 기록이 사라지는 구멍을 오히려 막았다(`evaluator.md:133`의 "PM 반환문에 적고, 기록하지 못했다는 사실도 함께 밝힙니다"와 송·수신이 맞물린다). **약화 아님으로 판정한 것**: `evaluator.md:133`의 배타 선언("evaluator 하나입니다")은 절 제목이 "판정 회차 기록"이라 스코프가 그 기록물에 한정되고, `pm.md:105`도 "판정 회차 기록(`decision_record`)"으로 같은 한정을 달아 PM의 다른 `decision_record` 의무(`pm.md:32`·`:45`·`:112`·`:113`)를 건드리지 않는다. **잔재 3건(모호형, Minor)**: `pm.md:78` 권한 참조표 행이 "PM은 대상 선정 + malgnai-hub 기록만 담당"으로 남아 27줄 아래 본문과 다르게 읽히고, `trainer.md:37`("최종 malgnai-hub 기록은 각각 evaluator·PM")·`:54`("PM이 그 결과를 malgnai-hub에 기록합니다")도 같은 구 문면이다.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 3차(초축소 재검증, Standard 약식) — 역할개념 수준 재사용. 대조축: "구 문면 4곳을 새 주체로 갈아끼운 뒤 원래 실려 있던 의무가 사라지거나 새 구멍이 생기지 않았는가". **소실 0건**: RV-008의 `trainer.md:37`은 "최종 malgnai-hub 기록은 각각 evaluator·PM"이라는 뭉뚱그린 표현을 "승격 실행(git PR)과 판정 회차 기록(`decision_record`)은 evaluator, 그 결과의 프로젝트 단위 반영(`work_record`·STATUS.md)은 PM"으로 **분해**한 것이라 의무 총량이 줄지 않았고, `:55` 핵심 문장도 evaluator 절에 `decision_record`를, PM 절에 `work_record`+STATUS.md를 각각 새로 달아 오히려 늘었다. RV-009의 표 변경도 구 1행("프로젝트 단위 기록(`decision_record`/`issue_record`) → PM")을 2행으로 쪼갠 것이고, PM 행에 `work_record`가 명시로 들어와 2차까지 표에 없던 PM 의무가 오히려 문면화됐다. `issue_record`는 "필요 시"로 완화됐으나 이는 원래 조건부 도구라 강도 손실로 보지 않는다. RV-007의 `pm.md:78` 표 행은 "malgnai-hub 기록만 담당"이라는 광범위 문구가 "프로젝트 단위 반영(`work_record`·STATUS.md)만"으로 **좁아졌는데**, 이는 약화가 아니라 27줄 아래 본문과의 불일치를 없앤 정정이다. **잔재 1건(Minor)**: `trainer.md:114` 자기검증 항목이 "전체 트랙 판정 기록(`decision_record`)이 남도록 PM에 넘겼는가"로 남았다 — 라우팅 자체는 여전히 참이지만(trainer → PM → evaluator, trainer는 Agent 도구가 없어 evaluator를 직접 못 부른다) "PM에 넘기면 PM이 남긴다"로 읽힐 여지가 있어 RV-008이 같은 파일에서 정리한 3곳과 문면이 어긋난다. **약화 아님으로 판정**: `agent-upskill/SKILL.md:21`이 `work_record`를 trainer·PM 두 번 지시하는 것은 중복이 아니라 `trainer.md:51-52` 표의 두 행(학습 이력 기록 / 프로젝트 단위 기록)에 각각 대응한다.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 4차(풀패널 격상) — 역할개념 수준 재사용. 대조축: "인용문을 정정하면서 원래 그 문장이 지시하던 의무가 줄거나, 반대로 없던 금지가 새로 생기지 않았는가". **소실 0건 · 정밀도 개선 2건**: ①`topic-learning/SKILL.md:109`의 "trainer는 이 스킬을 완료해도 malgnai-hub에 **직접 기록하지 않는다**"(전면 금지)가 "**프로젝트 단위 기록을** 직접 남기지 않는다"(용도 한정)로 좁아졌다 — 이는 약화가 아니라 정정이다. 구 문면은 `trainer.md:50`(학습 이력 기록 `agent_learning_record`+`work_record` = Trainer ✅ 필수)과 정면 충돌하는 전면 금지였고, 새 문면은 표의 PM 행(`:52` 프로젝트 단위 기록)에만 대응한다. ②`agent-upskill/SKILL.md:21`은 artifacts에서 PR URL을 뺐지만 그 값이 사라지지 않는다 — 같은 줄이 evaluator의 `decision_record.impact`(`evaluator.md:129` 정의와 일치)와 PM의 프로젝트 단위 `work_record` 보완 두 곳으로 인계 경로를 새로 달았다. 기록 총량 감소 0. **금지 신설 아님으로 판정**: "PR URL은 … 적지 않는다(모르는 값을 채우려 재확인하러 돌아가지 않는다)"는 새 금지처럼 보이나, 괄호가 그 이유를 trainer의 관측 가능 범위로 한정해 다른 필드의 기재 의무를 건드리지 않는다. **잔재 1건(Minor)**: 같은 정정이 `topic-learning`의 자매 자리인 `:111`(완료 보고 result의 "PR URL(5단계에서 evaluator가 PR을 열었다면)")에는 적용되지 않아, RV-013이 `agent-upskill`에서 닫은 "trainer가 모르는 값" 논리가 이 파일에서는 조건절 형태로 남았다.
- 2026-08-25 / target_id `audit-r2-hooks-20260825` / 1차(최초, Sensitive 풀패널) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대상: `backend-dev/audit-r2-hooks-20260825` `e605cd9`(base `7a0df39`) 3파일 +24/−3(`hooks/doc-drift.mjs`·`sessionstart-context.mjs`·`stop-mcp-reminder.cjs`, 전수감사 r2 RV-005/006/008 수리). `if→else if` 체인 전환과 exit 코드 식 확장이 기존 정상 경로의 판정 강도를 조용히 바꾸지 않았는지 원문과 대조 + 실행 대조. ✅ 분기 조건이 `!hasDrift && !hasPmIssue && r && !r.empty` → `!hasDrift && !allUnmeasurable && !hasPmIssue && r && !r.empty`로 좁혀진 것이 유일한 의미 변화이며 그것이 곧 요구사항. 이 저장소 실물 `pnpm run check-docs`가 main·브랜치 양쪽에서 동일 출력·exit 0 유지, 부분 skip/드리프트/전부일치/불일치 전 경로 OLD=NEW 확인.
- 2026-08-25 / target_id `audit-r2-defects-20260825` / 1차(최초, Standard) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "이력 서술을 걷어내고 표 문안을 갈아끼운 뒤 원래 실려 있던 사실·의무가 줄거나 뜻이 뒤집히지 않았는가". **소실 0건 · 의무 증가 1건**: RV-003의 표 개명(「학습 이력 기록」→「자기 실행 기록 + 역량 교훈」, 「프로젝트 단위 기록」→「사이클 종결 기록」)은 담당 열을 그대로 두고 주어만 명시한 것이라 의무 총량이 줄지 않았고, `topic-learning/SKILL.md:106-113`은 "trainer는 직접 남기지 않는다"를 "trainer가 자기 실행 1건을 남긴다 + PM 인계는 별도"로 **뒤집었는데** 이는 약화가 아니라 `trainer.md:50` 표와의 정면 충돌을 없앤 정정이다(기록 채널은 오히려 1건 늘었다). `pm.md`·`evaluator.md`와의 충돌 없음으로 판정 — `pm.md:78·105·109`와 `evaluator.md:27·133`은 모두 "**그 결과의** 프로젝트 단위 반영"·"프로젝트 **진행의** `work_record`"라는 한정어를 달고 있어 도구 전속을 선언하지 않으며, `trainer.md:52`가 "= `pm.md`가 말하는 프로젝트 단위 반영"이라 명시적으로 다리를 놓았다. **의미 왜곡 1건(Minor)**: RV-012의 §4 재작성이 "설계 문서가 상정한 것 | 실제 구현" → "흔히 있으리라 기대하는 것 | 실제 구현"으로 열 이름을 바꿨는데, 행 내용은 `usage-report-state.json`·`lastSyncedDate`·`purpose="usage_report"`처럼 특정 설계 문서에서만 나오는 고유명이라 "흔히 기대하는 것"이 아니고, Windows 로그 행은 오른쪽이 "실제로도 동일하게 기록한다"여서 새 절 제목("…실제로는 **없는** 것들")과 어긋난다. **잔재 1건(Minor)**: 같은 파일 §5(`:75`)가 여전히 위 §4의 "구현되지 않은 설계"들이라 부른다 — 방금 §4에서 걷어낸 설계 대비 프레임을 되살리는 참조.
- 2026-08-26 / target_id `audit-r2-defects-20260825` / 2차(축소 재검증) — 역할개념 수준 재사용. 이번 라운드는 "시점 한정어 삭제·프레임 교체 후 의미가 조용히 바뀌지 않았는가"에 집중. (1) `agent-common-principles.md:9`에서 "위임 전에"를 뺀 뒤에도 문단 결어("판단은 위임 이전에 이미 끝났어야 한다")가 남아 Sensitive/Refactor 사람 승인을 위임 전으로 오독할 여지가 미세하게 남으나, 시점 단정 문장 자체는 소거됐고 `pm.md`와 모순되는 문장은 0건(전역 `위임 (전|이전)에.*(승인|사람)` 패턴 0건) → 결함 아님, 참고. (2) §4 도입문이 "흔히 있으리라 기대"(약한 프레임) → "왼쪽 장치들은 존재하지 않는다"(전칭 단정)로 **강해진** 방향의 교체라, 표 1행(엔드포인트는 하나 실재)·6행(상태 파일은 `usage-agent-last-run.json`으로 실재)처럼 "부재가 아니라 다른 형태로 실재"인 행과 어긋남 — 오른쪽 열이 즉시 정정하고 왼쪽 셀이 특정 파일명/엔드포인트명을 지목하므로 ⚪ Nit. (3) 구 문안 잔존 전수 확인: "구현되지 않은 설계" 0건 · "Windows 로그 부재" 0건 · "흔히 있으리라 기대" 0건.
- 2026-08-26 / target_id `audit-r2-hooks-20260825` / 2차(축소 재검증, Sensitive) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "새 문장을 끼워 넣으면서 원래 그 자리에 있던 면제 규칙(측정 불가 = 드리프트 아님)이 조용히 약해지거나 삭제되지 않았는가". **소실 0건 · 정밀도 개선 1건**: 구문 `측정 불가(경로 없음/다른 호스트) → 해당 check skip(드리프트 아님).`이 삭제되지 않고 `…skip(드리프트 아님, 개별 체크 단위 규칙). 다만 …`으로 **보존 + 층위 명시**됐다 — 개별 체크 단위(면제)와 매니페스트 단위(실패)라는 두 규칙의 적용 범위가 문장 안에서 분리돼, 한쪽이 다른 쪽을 무효화하는 것처럼 읽힐 여지가 오히려 줄었다. 새 문장의 괄호 예외절("하나라도 정상 측정됐으면 이 규칙 대상이 아니다")이 강화 방향의 과잉확장을 스스로 차단한다. 전칭 단정으로 넘어간 자리 없음(주절이 `모든`으로 한정). `checks:[]`의 `empty:true` 면제를 명시적으로 구분해 인접 규칙 침범도 없음.
- 2026-08-28 / target_id `remove-doc-drift-feature` / 1차(최초, Refactor 풀패널) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: 매니페스트 가드를 통째로 제거한 뒤 남은 `project-standards` §6·§9와 `project-orchestration` §6의 강제력이 조용히 약해지지 않았는가. 판정 🟡 Amber — **공허하지 않다, 다만 강도는 실제로 내려갔고 문서가 그 사실을 숨기지 않는다.** 새 §6은 4불릿 전부가 행위 지시(세는 대상 병기 / 셀 수 없으면 안 적음 / 구조 변경과 같은 작업에서 정정 / 마감 시 `ls`·`grep` 대조)이고, 옛 §6은 문면 스스로 "매니페스트 `checks`가 빈 배열이라 가드가 꺼져 있는 것과 같다"고 자백하고 있었다 — 강도 하락이 아니라 교환으로 판정. 실제 하락 2곳(`project-orchestration` §6 "알려주는 장치는 없다" · `project-standards` §9 "유일한 안전망")은 축소를 명시해 정직 보고 기준 통과. 제품 본문 이력·식별자(hex·ULID·커밋해시) 신규 유입 0을 재작성 문단 전수 확인.
  기각 1건: CHANGELOG `[미출시] ### 변경`의 기존 항목 삭제는 이력 삭제가 아님 — 미배포 상태였고 서술 대상 파일 자체가 이번에 삭제돼 존치 시 없는 파일의 주석 수정이 릴리스 노트로 나감.
  라운드 종결 시점 PM 보고(리뷰어 직접 재확인 없음): RV-001 채택·수정 완료(`MANAGED_REGION_NOTE`가 실동작 재동기화 커맨드를 가리키도록 교체, 구 설치본 `ok` 유지 양성 대조군 확인), RV-003·RV-005도 반영 완료.
- 2026-08-28 / target_id `agent-score-record-contract-20260828` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: 개인 스코프 서술을 회사 공용 지표 서술로 통째 치환할 때 원문의 규범이 조용히 사라지거나 과장되지 않았는가. 원문의 금지("다른 사람의 점수와 합산해 해석하지 않는다")는 계약 변경으로 **정당하게 폐기**된 것이 맞고, 대체 규범("배점표 밖의 임의 조정 금지")이 들어와 규범 공백은 없다 — 다만 그 대체 규범은 계약 변경의 귀결이 아니라 신규 규범이고 :109와 부분 중복(변경 동결 원칙상 스코프 경계). **강도 초과 1건**: 새 문장이 "다른 회차·다른 평가자가 남긴 점수와 같은 축에서 비교된다"고 읽기 쪽 동작까지 단정하는데, 읽기 지시 3자리(SKILL.md:97·:153, evaluator.md:124)는 여전히 개인 회차 전제("지난회/지난 회차 점수")로 남아 같은 파일 안에서 전제가 갈린다.
- 2026-08-28 / target_id `agent-score-record-contract-20260828` / 2차(축소 재검증, Standard 약식) — 역할개념 수준 재사용. 1차에서 지적한 전제 분열(쓰기 쪽은 공용 스코프, 읽기 쪽 3자리는 개인 회차 전제)이 해소됐는지 한 줄씩 대조. **해소 확인** — `개인 스코프|사용자 스코프|호출한 사용자` 제품 본문 전수 grep 0건, evaluator.md:124·SKILL.md:97·team-composition-patterns.md:87이 같은 전제(`agentName` 단위 공용, 다른 평가자 회차 가능)를 공유. **강도 손실 1건(Nit)**: SKILL.md:156에서 "배점표(Phase 1) 밖의 임의 조정을 넣지 않는다"가 제거됐는데 PM이 근거로 든 :109는 실제로는 다른 규범(배점표·산식의 정본 위치와 스크립트 상수 동기화)이다. 총점 층위는 :64·:66(암산 금지)과 :154(스크립트 산출 가중 총점)로 여전히 막히지만, 하위점수에 배점표 밖 가감을 넣지 말라는 문장은 이제 저장소 어디에도 없다(`임의 조정|가감점|자의적` grep 0건). **잔여 서사 불일치(Minor)**: 스코프는 공용으로 승격됐는데 주변 서사는 여전히 개인·월 단위 프레이밍(SKILL.md:105 "전월 대비", :113 "지난달 대비", :163 표 헤더 "지난달|이번달", 폴백 문자열 `N/A(전월 점수 없음)`, evaluator.md:139 "지난 회차 점수") — 다만 문자열 일부가 `bin/calc-training-scorecard.mjs` 출력과 묶여 있어 문서 단독 수정은 desync를 만든다(백로그 권고).

- 2026-08-28 / target_id: pm-block-sessionstart-injection / 1차(최초, Refactor 풀패널) / docs/reviewer/review-pm-block-sessionstart-injection-2026-08-28.md — 삭제된 로컬우선 양보조항·설치동의 게이트·--decline의 강제력 변화와 CHANGELOG 서술의 일치 여부 대조.

- 2026-08-29 / target_id `pm-md-consistency-20260829` / 1차(최초, Sensitive 풀패널) — 역할개념 수준 재사용. `git show b2b1bd7:malgn-agent/agents/pm.md` 원문과 신규 L31·L69·L95를 한 줄씩 대조. 이번 초점: 자기모순 해소를 위한 재기술이 규칙의 경계를 조용히 넓히거나(로컬 우선 조항의 "등급 운용" 확장) 죽은 인용 정정이 의미를 바꾸지 않았는지.

- 2026-08-29 / target_id `skills-confirmed-defects-20260829` / 1차(최초, Standard 약식) — 역할개념 수준 재사용. 내부 프로젝트명·테이블명 익명화(도메인 스킬 4종)와 "기록 필수 필드" 절 치환이 규칙의 강제력·설명력을 깎았는지 `git show b2b1bd7:` 원문과 대조. 이번 초점: 익명화가 삭제가 아니라 구조 라벨(①형/②형)로의 치환인지, 그 라벨이 정의 전에 먼저 등장해 독해가 끊기지 않는지, 그리고 "유효 기간(영구/임시)" → "무엇이 바뀌면 뒤집히는가"가 약화인지 강화인지.

- 2026-08-31 / target_id `knowledge-skill-dedup-20260831` / 1차(최초, Refactor 풀패널) / docs/reviewer/review-knowledge-skill-dedup-2026-08-31.md — 역할개념 수준 재사용. 대상: knowledge 3파일 삭제 + 3파일 축소, 배경을 skills 4곳으로 흡수. 이번 초점: 삭제 전 원문(`git show 38ce18c:`)과 흡수처 스킬 본문을 문장 단위로 대조해 "흡수했다"는 주장에서 빠진 문장을 찾는 것. **확인 결과**: `system-design-patterns.md` 2문단·`screenshot-capture-guide.md` 하드게이트 2문단은 거의 축자 이관 확인. 반면 `permission-policy-compliance.md`의 "막혔을 때 행동 순서" 2·3단계(3지선다 에스컬레이션 문안 + "검증 못 했으면 못 했다고 정직 보고, 유령 보고 금지")는 흡수처 스킬에 없음(전문 grep에서 `지어내`·`유령`·`미검증` 0건) — 강제력 있는 금지문이 "기록하라"는 서술로 약화됨.

- 2026-08-31 / target_id `agents-md-relocation-20260831` / 1차(최초, Refactor 풀패널) / docs/reviewer/review-agents-md-relocation-2026-08-31.md — 역할개념 수준 재사용. 대상: pm/trainer/evaluator MD 상세를 skills·knowledge로 재배치한 2커밋. 이번 집중: 감량 금지 3구역(evaluator 판정 체크리스트 제목·배점·기준선과 등급별 merge 규칙 / pm AskUserQuestion 부재 승인 게이트 4항·권한 참조표 / trainer 3자 책임 구분표)이 축약 과정에서 강제력을 잃었는지 원문과 한 줄씩 대조 — 전부 무손상 확인. 축약된 두 체크리스트 항목(접두어 게이트·Knowledge→Skill 링크)도 판정 기준선·FAIL 조건은 그대로이고 근거 서술만 스킬로 이동한 것을 확인. 단 evaluator "점수 왕복 종결"에서 보고 의무 1건("점수 이력이 없으면 최초 회차임을 밝힌다")이 파라미터 포인터 뒤로 들어간 것을 Minor로 기록.
- 2026-08-31 / target_id `agents-md-relocation-20260831` / 2차(축소 재검증) / docs/reviewer/review-agents-md-relocation-2026-08-31-2차.md — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 이번 라운드는 행위 대부분이 **삭제**라 대조축은 "지우면서 살아 있어야 할 규칙까지 가져가지 않았는가". **소실 0건**: ①워크트리 격리 규칙은 `skills/project-orchestration/SKILL.md:209`·`knowledge/leadership/team-composition-patterns.md:120`에 base 이전부터 온전히 존재(부재 주장 전 검색 범위를 실제로 밟아 확인) → 스킬에서 뺀 것은 3중 중복의 해소지 규칙 삭제가 아님. ②cherry-pick 지침 소멸은 base에 없던 문장의 원복이지 약화가 아님. ③체크리스트 마지막 항목이 `git log <base>..<branch>` 문구를 잃었으나 "영향 대상 열거"는 잔존해 base pm.md와 동일 강도. ④`#7`은 강도 **회복** 방향이며 MD·스킬 양쪽에 의무가 보임. 참조처 4개 에이전트 줄의 요약 문구도 실제 본문과 일치하게 갱신돼 없는 절을 가리키는 포인터 0건. 판정 🟢.
