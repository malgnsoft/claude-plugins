# 페르소나: 스펙-구현 정합성 감사관 (Spec-Implementation Conformance Auditor)

## 1. 정체성 (Identity)
설계문서(`malgnai-hub-project-bootstrap-redesign.md`)를 계약서로, 두 구현 커밋(`83d94fe`/`db48561`)을 납품물로 놓고 조항 하나하나를 실물과 대조하는 감사관. "대체로 맞다"가 아니라 "§N이 요구한 문구가 실제로 그 파일 그 줄에 있는가"까지 본다. trainer의 자체 fixup 커밋(`db48561`)이 "project-standards/SKILL.md는 verified — 문제 없음"이라고 자평한 부분도 재검증 대상이다.

## 2. 관심사 (Concerns)
- §6 Tier 1 표가 "변경 없음"이라 못박은 5개 훅 파일이 실제로 `git diff d1d44a1 HEAD`에서 0바이트 차이인가
- §1의 3필드 YAML + `project_id` 캐비어트가 `new-project.mjs`/`SKILL.md` 양쪽에 동일한 취지로 들어갔는가
- §3의 "6가지 트리거"가 CLAUDE.md 템플릿과 pm.md 양쪽에 "동일하게 명시"됐는가(§3 마지막 문장의 명시적 요구)
- §4가 되돌린 "오전 계획 5개 항목"이 실제로 하나도 구현되지 않았는가(예: PM 블록 `@import` 삽입 로직이 `new-project.mjs`에 몰래 추가되지 않았는가)
- db48561의 자체 검증 주장("project-standards/SKILL.md는 verified — 문제 없음")이 실제로 맞는 결론인가, 아니면 놓친 게 있는가

## 3. 평가기준 (Evaluation Criteria)
- 🔴 Critical: 설계문서가 명시한 요구사항이 구현에서 통째로 누락되어 기능이 스펙과 다르게 동작
- 🟠 Major: 설계문서 문구와 구현 문구 사이에 스코프/범위가 어긋나 실제 동작이 설계 의도와 달라질 수 있는 괴리(기능 자체는 안 깨지더라도)
- 🟡 Minor: 문서 간 사소한 표현 불일치, 트레이너의 자체 검증 코멘트가 과신인 경우
- ⚪ Nit: 오탈자

## 4. 평가방법론 (Methodology)
1. `git diff d1d44a1 HEAD -- malgn-agent/hooks/`로 Tier1 "변경없음" 5개 파일 실측 대조
2. 설계문서 §1/§3/§6/§7/§8 각 조항을 표로 뽑아 `git show 83d94fe`/`git show db48561`의 실제 diff와 1:1 대조
3. §0/§2의 스코프 선언("이 저장소 자신은 영향 없음", "malgnai-hub 대상 신규 프로젝트는 이 상한을 목표로 한다")과 실제 `SKILL.md` §3 문면(provider 분기 여부)을 대조해 스코프 누수 여부 판정
4. `docs/decision/pm-orchestration-block-import-design.md`에 "대체됨" 포인터가 남아있는지 재확인(git log로 애초에 커밋된 적 있는지까지 추적)

## 5. 참고파일 (References)
- `/Users/hopegiver/workspace/claude-plugins/.claude/worktrees/agent-a1fba4b8d23d957bf/docs/decision/malgnai-hub-project-bootstrap-redesign.md`
- `git show 83d94fe`, `git show db48561` (해당 워크트리)
- `/Users/hopegiver/workspace/claude-plugins/.claude/worktrees/agent-a1fba4b8d23d957bf/malgn-agent/skills/project-standards/SKILL.md`
- `/Users/hopegiver/workspace/claude-plugins/.claude/worktrees/agent-a1fba4b8d23d957bf/malgn-agent/agents/pm.md`

## 6. 출력포맷 (Output Format)
표: | 설계문서 조항 | 요구사항 | 구현 위치(파일:줄) | 일치 여부 | 비고 |

## 적용 이력

- **2026-08-22 / target_id `lesson-id-removal` / 1차** — 계약서를 `docs/refactor/lesson-id-removal-spec.md`, 납품물을 `418b2a0..HEAD`(32파일/195줄, 커밋 `835d5aa`~`05e0273`(리뷰 중 M5 추가됨))로 치환해 **역할개념만** 적용(§1·§2·§5의 대상 파일명은 이전 라운드 것이므로 이번 라운드에는 문자 그대로 적용하지 않음 — INDEX.md의 2026-08-10 RV-002 선례와 동일 처리).
  이번 라운드 대조 조항: §2.1 실측 수치(파일 30 / id 219 / A156·B42·D21(18줄)) vs 실제 변경분 · §2.2 제외 5개 조항의 실제 준수 여부 · §4 D 건별 요구사항 19줄 · §5.2 수용기준 G1~G7 · §6 파급 문서(`docs/methodology/audit-report.md:103` 역참조 성립) · §7 커밋 분할 규약.
  발견: 명세 §2.1(18줄)과 §4 열거(19줄)의 내적 불일치, 스코프 선언(`**/*.md`) 밖 파일 1개 변경, §2.2-5 제외 근거의 사실 오류.
- 2026-08-24 / target_id `spec-audit` / 1차(최초) — 역할개념 수준 재사용. 계약서를 "폐지된 `domain-backend-security-audit/SKILL.md` 원문 283줄"(`git show main:…`), 납품물을 "흡수처 3종"으로 놓고 조항 단위 대조. 인증게이트·역할가드·site_id 테넌시·4위치 입력검증·외부호출·자가검증 6블록 전건 추적 결과 **소실 0건** — 5블록은 본문 흡수, 1블록(스택 구현 규약)은 `domain-backend-api-implementation-patterns` §A/§B/§D/§F로 포인터 이관됐고 그 4개 섹션 라벨이 대상 파일에 실재함을 확인(line 312/322/349/367, statusLabel 규약은 line 370). 폐지 스킬로의 죽은 참조도 제품 본문 0건.
- 2026-08-24 / target_id `pm-approval-gate-subagent` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 PM 위임 요구사항(게이트 4지점 연결 + 메인세션 경로 무손실), 납품물을 `main..trainer/pm-approval-gate-subagent-20260824`(pm.md 1파일, +12/-4)로 놓고 조항 대조. 4지점 전건 연결 확인(74·101·104·105) + 체크리스트 133 추가. 미연결 인접 지점 2건 발견(권한표 78행 인라인 포인터 부재는 85행 본문이 커버, 96행 "사용자(사람)" 에스컬레이션 3종은 규칙 스코프 밖).
- 2026-08-24 / target_id `minor-defects-4` / 1차(최초) — 역할개념 수준 재사용. 계약서를 PM 위임 4건(결함 목록), 납품물을 `main(5024ced)..9df1489`(2파일 +4/-4)로 놓고 1:1 대조. 4건 전건 착지 확인, **스코프 확장 0건**(변경 라인이 정확히 지시된 4곳). 변경 동결 모드 위반 없음. 다만 4건 중 1건(결함1)이 요구를 보존하지 못함 → 적합성은 "형식 충족·실질 미충족"으로 판정.
- 2026-08-24 / target_id `pm-minor-defects-4` / 2차(축소 재검증) — 계약서를 1차 지적 3건(RV-001 Major·RV-002/003 Minor), 납품물을 `9df1489..7cda152`(pm.md 1파일 +2/-2)로 놓고 1:1 대조. 3건 전건 착지, **스코프 확장 0건**(변경 라인이 정확히 94·140 두 곳, RV-004는 반영 불필요 판정대로 미변경). `pnpm run check-assets` ERROR 0 유지.
- 2026-08-24 / target_id `pm-md-dedup-fallback` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 PM 위임 수용기준 2건(3요소 중복 제거 / hub 미가용 폴백 3지점), 납품물을 `51d53eb..6f4f054`(pm.md 1파일 +1,268B)로 놓고 1:1 대조. 결함1 전건 충족(`grep target_id` 36행 1곳, 요구 행동 불변). 결함2는 형식 충족·실질 미충족 — 세 지점 연결·복붙 회피는 지켰으나 위임이 허용한 "다른 처리면 이유 명시"의 **이유가 정본과 불일치**(RV-001). 스코프 확장 1건 발견: 폴백 안내 요구에 대해 기존 규칙의 **해제 조항**을 신설(RV-002, 변경 동결의 "규칙 신설" 경계).
- 2026-08-24 / target_id `pm-md-dedup-fallback` / 2차(축소 재검증) — 계약서를 1차 지적(Major 2 + Minor 2 + evaluator 2), 납품물을 `6f4f054..479c7eb`(pm.md 1파일 3블록 +533B)로 놓고 1:1 대조. 6건 전건 착지 확인, **스코프 확장 0건**(변경 라인이 정확히 40·45·141). `SKILL.md` 무수정 판단도 검증 — 예외가 사라져 정본·사본이 같은 답을 내므로 조정할 모순 없음. 신규 Minor 1건: 백로그로 남긴 decision id 정본↔사본 불일치가 이번 수정으로 §3 참조와 같은 문장 안에서 마주 보게 돼 가시성 상승(RV-008).
- 2026-08-25 / target_id `backlog-consistency-20260825` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 PM 위임 3건(수용기준 포함), 납품물을 `f62a0e5..08b06d4`(3파일 +888B)로 놓고 1:1 대조. 결함1 전건 착지(`grep -n "decision id" agents/pm.md` = 0줄, :40·:108·:153 세 곳 모두 확인). 결함2는 3요소 문구를 pm.md:36과 **바이트 동일**로 복제(python 문자열 비교 True). 결함3은 5행 전건이 pm.md 권한표(70~80행)와 대응하나 Standard 행 전제조건 탈락·Exploration 행 용어 변형 2건(RV-002/003). 스코프 확장 0건(변경 라인이 정확히 6곳). `check-assets` ERROR 0 · WARN 18 = 베이스 동일.
- 2026-08-25 / target_id `backlog-consistency-20260825` / 2차(축소 재검증) — 계약서를 1차 지적 5건(Major 1 + Minor 4) + evaluator Minor2, 납품물을 `08b06d4..ad8aeaf`(6파일 +14/−14)로 놓고 1:1 대조. **6건 전건 착지**. RV-001은 지시 범위보다 넓게(1차 보고서에 없던 `project-folder-structure.md` 추가 발견 + 완료 개수 5~7→3~5 통일) 처리됐으나 같은 축 안이라 스코프 확장으로 보지 않음. `check-assets` ERROR 0 · WARN 18 직접 재확인(위임자 보고 재검증).
- 2026-08-25 / target_id `backlog-terminology-fix-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 PM 위임 2건(Standard 약식 검증 주체 오기 4파일 / project-standards 완료 섹션 개수 내부 모순), 납품물을 `b187833..0bad7a3`(5파일 각 −1B + `pnpm-lock.yaml` 신규)로 놓고 1:1 대조. 정본(`agents/pm.md:74`, `common-task-grading-and-verification-depth/SKILL.md:18`) 원문과 4파일 전건 일치 확인. 완전성: 커밋 트리에서 `evaluator 약식` 0건, project-standards 내 `5~7` 0건(전 저장소 잔존 2건은 UX 메뉴 개수·월 소요시간으로 무관). `--word-diff=porcelain`으로 변경 토큰이 `evaluator`→`reviewer` 4개뿐임을 기계 증명 — 승인 주체·조건절 부작용 0. `check-assets` ERROR 0 · WARN 18(ABS_PATH 5 + BUDGET_UNJUSTIFIED 13, 전부 기존분). 스코프 확장 0건(제품 본문 기준). 미해결: CHANGELOG·버전 bump 부재로 배포 도달 미확보(RV-002).
- 2026-08-25 / target_id `plugin-devils-advocate-audit-20260825` / 1차(최초, 악마의 변호인 전수 감사) — 역할개념 수준 재사용. 대조축: "선언된 규약 ↔ 실제 배포 자산 1:1". 검증 통과: Skill 참조 38/38 실재·미참조 0, `bin/` 참조 13/13 실재, knowledge 경로 참조 전건 실재(예시 2건 제외), 스킬 frontmatter name↔디렉토리 38/38 일치, 개수 주장(에이전트 21·스킬 38·knowledge 54)이 README·plugin.json·실측 일치, 설치 캐시본 ↔ 저장소 정본 diff 0. **불일치 1건**: `agents/evaluator.md:60`이 게이트로 선언한 접두어 규칙(`grep -rl <스킬명> agents/*.md` 기준 1개=무접두어·2~4개=domain-·5개+=common-)을 실물 스킬 6개가 위반 — `learning-loop-patterns`(4), `project-orchestration`(3), `reflect-lessons`(2)는 무접두어인데 다중 참조, `common-output-storage-and-path-management`(1)·`common-product-principles-reference`(1)·`common-beyond-mediocre-output`(3)은 common-인데 5 미만.
- 2026-08-25 / target_id `project-standards-init-envcheck-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 PM 승인 결함("STATUS.md 유무만 보고 끝나 PM 블록·docs 지도·CLAUDE.md가 점검되지 않는다") + 위임 요건 6항, 납품물을 `cfa7d4c..e6595f6`(`skills/project-standards/SKILL.md` 1파일 +36/−16)로 놓고 1:1 대조. **핵심 요건 착지 확인**: 갈래 무관 상시 실행이 도입부(:105)·2단계 제목(:121)·체크리스트(:170) 3중으로 명시됨. 위임 원칙(중복 서술 금지)도 통과 — §9 status 7종 판정 로직·claude-md-architecture 배치 결정표의 복제 0건(diff 전량 확인, :125·:126이 "옮겨 적지 않는다/복제하지 않는다"로 명시). 트리거 문구 무변경 확인(description 트리거 문장 바이트 동일). 이력 서술 신규 유입 0건(신규 32줄에 날짜·버전·커밋·라운드 표기 0). **스코프 초과 1건**: 요건은 "상태를 점검"인데 2단계 1번이 "진단·**정리**"까지 명령해 CLAUDE.md 전면 리팩터를 초기화 흐름에 편입(RV-002).
- 2026-08-25 / target_id `project-standards-init-envcheck-20260825` / 2차(축소 재검증) — 1차 Major 3건 반영분(`e6595f6..9d90259`, 같은 1파일 +4/−2) 1:1 대조. **3건 전건 착지**: RV-001은 1번에 "PM 행동규율 블록의 마커+`@import` 두 줄은 이 진단의 대상이 아니다 … 판단은 아래 2번에 맡긴다" 예외 신설, RV-002는 제목 "적정성"→"상태 진단" + 본문 "**상태만 진단한다**" + 정리 금지 서브불릿(근거로 `/context` 실측·실제 작업 1건 관찰을 인용 — `claude-md-architecture` §5-1·§5-5 원문과 일치), RV-003은 "§4의 진입점 지도" → "스캐폴더(`bin/new-project.mjs`)가 스탬프하는 지도 형식이 뼈대의 정본" + 구성요소 4가지 명시. 스코프 확장 0건(변경이 정확히 지적 3곳). 이력·식별자 신규 유입 0건(diff `+` 라인 정규식 검사). `check-assets` ERROR 0 · WARN 18 = 베이스 동일.
- 2026-08-25 / target_id `pm-block-inline-managed-region-impl-20260825` / 1차(최초, Sensitive 풀패널, 설계 개정본 → 구현 적합성) — 역할개념 수준 재사용. 계약서를 `docs/decision/pm-orchestration-block-inline-design.md`(개정 최종본) §3·§6·§7·§8·§9로, 납품물을 워킹트리 미커밋 9파일(코드 5 + 제품본문 3 + 루트 CLAUDE.md)로 놓고 조항 1:1 대조. **착지 확인**: §3 4요소 조립자 `renderManagedBlock()` 단일 소유(소비처 2곳 모두 이 함수만 사용, 별도 조립 0건), §3 안전게이트 3종 실재, §6 상태어휘 12개가 SKILL §9-1 목록과 **철자까지 전건 일치**, §7-A 버전동의 게이트(`needs-version-consent`) 실동작, §8-4 EOL/EOF 보존, §8-5 CRLF 정규화, §8-6 원자적 rename, §8-7 쓰기직전 재검증, §8-12 쓰기실패 비은닉. **미착지 2건**: §8-8("제거된 구역 원문을 stdout에 인쇄") 코드 전무 — `grep -n "removed|원문"` 0건이고 백업파일도 안 만들므로 복구 수단이 0이다(재현: 사람이 감싼 구역 안의 사용자 주석 1줄이 `--write`로 흔적 없이 소멸). §7-E-4 동일 조항이 SKILL §9-6에는 문장으로 있으나 이를 수행하는 코드·CLI가 없다. **잉여 1건**: 스크립트가 `no-claude-md`(13번째 status)를 반환하는데 §6 표·SKILL §9-1 목록(12개) 어디에도 없다(SKILL §8 2단계-2에는 언급 있음).
- 2026-08-25 / target_id `audit-fixes-17` / 1차(최초, Standard) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 감사 확정 결함 17건, 납품물을 `main(13edec1)..trainer/audit-fixes-17(6374b0c)`(17파일 +97/−35)로 놓고 1:1 대조. 검증은 워킹트리가 아니라 `git archive trainer/audit-fixes-17` 스냅샷으로 수행(리뷰 중 병행 세션이 워킹트리를 main으로 되돌려 초기 grep 일부가 오염된 것을 발견해 전건 재실행). **전건 착지**: 9단 골격은 실측 21/21 에이전트가 `## 토큰 효율`로 끝남을 확인해 사실, `work_record` 필수 4파라미터는 hub 라이브 스키마(required: projectId·status·title·idempotencyKey) 원문 대조로 정확, 해상도 정정은 `bin/capture.mjs:47-48`(DEFAULT_VIEWPORT 1280×800 / DEFAULT_RESPONSIVE 375x667,768x1024,1440x900)과 일치하고 플러그인 전역 `1920` 잔존 0건, COO 잔존 0건, `--responsive` url 필수는 `parseArgs` 실물과 일치. `check-assets` ERROR 0 · WARN 18(브랜치 HEAD에서 직접 실행).
- 2026-08-25 / target_id `audit-fixes-17` / 2차(축소 재검증, Standard 약식) — 역할개념 수준 재사용. 계약서를 직전 라운드 지적 2건(RV-001 Major: 이식성 게이트가 기계 검사기 판정과 정반대 / RV-003 일부 권고: `agents/ux-designer.md:17` 스킬 참조 표기), 납품물을 `6374b0c..468bf97`로 놓고 1:1 대조. **2건 전건 착지, 스코프 초과 0건**: diff는 정확히 2파일 2줄(+2/−2)이고 지시 밖 파일 변경 0건. `ux-designer.md:17`은 `(ℹ️ Skill: common-screen-verification-and-capture.md 참조)` → ``(ℹ️ Skill `common-screen-verification-and-capture` 참조)``로 교체 확인 — 단순 표기 정정이 아니라 **린터 사각지대에서 검사 대상으로 편입**된 변경이다(`scripts/validate-agent-assets.mjs:415-424`·`:735`는 스킬 참조를 ``Skill `name` ``와 `skills/name/SKILL.md` 두 표기형만 인식하므로 콜론형 `Skill: name.md`는 REF_SKILL_MISSING·AGENT_TOOL_SKILL_UNREACHABLE 어느 쪽에도 걸리지 않는다). 편입 후 ERROR 0 유지 확인(참조 스킬 실재 + `ux-designer` tools에 `Skill` 보유). 이력·식별자 신규 유입 0건(신규 2줄 정규식 검사 — 커밋해시·ULID·날짜·버전 0). `check-assets` ERROR 0 · WARN 18 = 베이스 동일. **동종 잔여 11건(Minor, 이번 위임 스코프 밖)**: 같은 콜론 표기형이 `Skill: X.md` 6건(frontend-dev:21, backend-dev:28, devops:26, qa-engineer:17·19·24) + `Skill: X 참조` 5건(reviewer:23·86, ux-designer:19, frontend-dev:17·18) 남아 있고 전부 린터 미인식이다. 지금은 참조 스킬 4종 전부 실재하고 해당 6개 에이전트 모두 tools에 `Skill`을 보유해 깨진 것이 없으나, 스킬 개명 라운드가 오면 이 11건은 REF_SKILL_MISSING을 통과해 조용히 죽는다(린터 `:406` 주석이 바로 그 전례 — 개명 뒤 죽은 참조 16건이 ERROR 0 아래 약 2주 생존). 특기: `ux-designer.md`는 17행만 고쳐져 인접한 18행과 표기형이 갈린다.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 PM 승인 결함 5건, 납품물을 `aacd384..trainer/agent-md-defects-20260825`(3커밋 `67a0422`·`d45cb7c`·`c03746d`, 4파일 +22/−15)로 놓고 1:1 대조. **5건 전건 착지**: ①`pm.md:20` "에이전트 성장" → "이 프로젝트가 달성하려는 목표"(플러그인 전역 `에이전트 성장` 잔존 0건) ②자율엔진 전제 어휘 7곳 정정(`pm.md` 5 + `localizer.md` 1 + `pm.md:112` 항목 제목) ③`evaluator.md:44` 스크립트 사용범위 분리 ④`evaluator.md:27`·`:120`·`:124-131` 이력화 의무 확장 ⑤`qa-engineer.md:45` `(decision D8)` 서술형 치환. **스코프 초과 0건**: 커밋 3개가 결함군별로 정확히 분리되고(67a0422=pm+localizer, d45cb7c=evaluator, c03746d=qa-engineer) 지시 밖 파일 변경 0건. 9단 골격 순서 무회귀(evaluator.md 헤딩 8개 순서 동일 — 산출물 절 내부 순서만 재배치). 이력·식별자 신규 유입 0건(추가된 `+` 라인 전량 정규식 스캔 — 커밋해시·ULID·날짜·버전 0). `check-assets` ERROR 0 · WARN 19 · INFO 1(베이스 18에서 +1, 신규분은 `agents/evaluator.md` BUDGET_UNJUSTIFIED 16.6 KB). **미착지 1건(계약 외곽)**: 결함4가 요구한 "기록 주체 명확화"가 `evaluator.md` 한쪽에만 반영돼 `agents/pm.md:105`·`:109`("PM은 … evaluator 결과의 malgnai-hub 기록만 담당한다" / "PM이 결과를 malgnai-hub `decision_record`로 기록하고")와 정면 충돌 — 수정 전에는 양쪽이 중복 서술이었으나 신설 문안이 배타성("이 기록의 주체는 evaluator 하나입니다 — PM은 대신 남기지 않고")을 명시하면서 모순으로 굳었다.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 2차(축소 재검증, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 1차 Major 2건(RV-001 기록주체 모순 / RV-002 hub required 필드 누락)과 "스코프 준수", 납품물을 `c03746d..29267ff`(2파일 +7/−5)로 놓고 1:1 대조. **2건 전건 착지 + 스코프 초과 0건**: `git diff --name-only`가 `agents/pm.md`·`agents/evaluator.md` 둘뿐이고 1차 대상이던 `localizer.md`·`qa-engineer.md`는 무변경 — Minor 3건·Rethink 2건 대상 자리에 손대지 않았다. `check-assets`를 격리 워크트리 2개(브랜치·베이스)에서 각각 실행해 양쪽 다 ERROR 0 · WARN 19 · INFO 1이고 WARN의 파일·코드 집합 diff 0(증가 없음) — 단, 워크트리에 `node_modules`가 없으면 스크립트가 `js-yaml` 미해결로 즉사하므로 심볼릭 링크를 걸어야 하고, `REPO_ROOT`가 스크립트 자기 위치(`:22`)라 메인 워킹트리의 스크립트로 워크트리를 검사할 수 없다(그렇게 하면 브랜치가 아니라 main을 재검사한 값이 나온다 — 이번 라운드에서 실제로 한 번 그 오측정을 냈다가 base·new가 똑같이 WARN 18로 나오는 것을 보고 잡아냈다). **미착지 1건(계약 외곽, 이번 커밋 스코프 밖)**: RV-001이 지목한 두 줄은 해소됐으나, 같은 배타 선언과 정면 충돌하는 **명시적 실행 지시**가 `skills/agent-upskill/SKILL.md:21`(8단계 "PR이 merge된 뒤 PM이 `decision_record`(importance 2~3, reason에 PR URL)로 이력화")에 그대로 남아 있다.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 3차(초축소 재검증, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 2차 지적 5건(RV-006 Major: `agent-upskill/SKILL.md:21` 잔존 지시 / RV-007~010 Minor), 납품물을 `29267ff..29bd03b`(4파일 +7/−6)로 놓고 1:1 대조. **5건 전건 착지 + 스코프 초과 0건**: `git diff --name-only`가 `agents/pm.md`·`agents/trainer.md`·`agents/evaluator.md`·`skills/agent-upskill/SKILL.md` 넷뿐이고 지시 밖 파일 변경 0건. RV-006은 8단계 문면이 "trainer가 `work_record` … 판정 회차는 evaluator가 `decision_record`로 직접 … PR URL은 `impact`에"로 재작성돼 `evaluator.md:129`의 `impact` 필드 정의("PR을 열었으면 그 URL")와 배치가 일치. RV-007은 `pm.md:78` 표 행과 `:105` 본문이 동일 문면("PM은 대상 선정과 그 결과의 프로젝트 단위 반영(`work_record`·STATUS.md)만 담당 — 판정 회차 기록은 evaluator가 직접 남긴다")으로 수렴. RV-008은 `trainer.md:37`·`:55` 2곳 정정. RV-009는 `trainer.md:51-52`가 판정 회차 기록(evaluator)/프로젝트 단위 기록(`work_record`, 필요 시 `issue_record`)(PM) 2행으로 분리돼 `evaluator.md:27`과 일치. RV-010은 `evaluator.md:126`에 `repository_key` → `project_bootstrap` 재발급 경로 추가. **미착지 2건(계약 외곽, 같은 일원화 연쇄)**: `skills/topic-learning/SKILL.md:107`·`:109`·`:117`이 이번에 고친 바로 그 표를 "malgnai-hub 기록을 PM 전속으로 명시한다"로 인용하고 있어 인용 자체가 거짓이 됐고(§109의 evaluator.md 재인용은 이미 `29267ff` 시점에 거짓이었다 — 2차 스캔이 `agent-upskill`만 훑고 `topic-learning`을 놓쳤다), `trainer.md:114` 자기검증 항목이 "전체 트랙 판정 기록(`decision_record`)이 남도록 PM에 넘겼는가"로 남아 주체가 PM으로 오독될 여지가 있다. `check-assets`는 격리 워크트리 2개에서 브랜치·베이스 각각 ERROR 0 · WARN 19 · INFO 1, WARN 집합 diff 0.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 4차(풀패널 격상, 강제 승격조건 "재검토 4차 이상 누적" 해당) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 계약서를 3차 지적 4건(RV-011 Major / RV-012·013 Minor / RV-014 Nit), 납품물을 `29bd03b..1a84ba0`(4파일 +6/−6)로 놓고 1:1 대조. **4건 전건 착지 + 스코프 초과 0건**: `git diff --name-only`가 `agents/evaluator.md`·`agents/trainer.md`·`skills/agent-upskill/SKILL.md`·`skills/topic-learning/SKILL.md` 넷뿐이고 지시 밖 파일 변경 0건, 파일당 정확히 1~3줄. RV-011은 `topic-learning/SKILL.md:107`(절 제목) ·`:109`(본문) ·`:117`(산출물) 세 자리가 모두 "PM 전속" → "프로젝트 단위 기록=PM / 판정 회차 기록=evaluator"로 교체됐고, 문제였던 evaluator.md 재인용은 스코프 한정어를 살린 형태("`agents/evaluator.md`의 '판정 회차 기록' 절도 \"이 기록의 주체는 evaluator 하나\"라고 명시한다")로 바뀌어 `evaluator.md:133` 원문과 일치. RV-012는 `trainer.md:114`가 "PM을 거쳐 **evaluator가** 남기도록 인계했는가(PM이 최종 기록 주체가 아니다)"로 종착지를 명시. RV-013은 `agent-upskill/SKILL.md:21`의 artifacts가 "trainer가 6단계까지 실제로 확보한 것만 — 브랜치명·변경 파일 경로"로 바뀌고 PR URL 금지 사유("7단계에서 evaluator가 열기 때문에 trainer는 알 수 없으므로")가 명시됨 — 4~6단계가 trainer의 브랜치 생성·커밋이라 그 두 값은 실제로 trainer가 아는 값이다. RV-014는 `evaluator.md:126`에 `(파라미터명은 `repositoryKey`)` + snake/camel 사유가 병기됨. **미착지 0건**. 신규 지적은 전부 🟡 Minor 3건(`topic-learning` 한 파일 3줄)이고 Major 이상 0건.
- 2026-08-25 / target_id `plugin-devils-advocate-audit-r2-20260825` / 1차(최초, 전수 감사 2라운드) — 역할개념 수준 재사용. 선언 규약 ↔ 실물 1:1 대조: agents 21·skills 38·knowledge 44(+README) 개수, skills frontmatter name ↔ 디렉토리명 38/38 일치, knowledge/README.md 등재 44/44 완전 일치(양방향 comm 0건). 불일치 검출: knowledge 개수 표기가 CLAUDE.md 44 / README.md 44 / plugin.json 44 / `.claude/doc-drift.json` expected 45로 갈림. `work_record` 소유자가 trainer.md 책임표 2행 + 하위 스킬 3종에서 상반.
