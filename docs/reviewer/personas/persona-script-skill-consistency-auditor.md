# 페르소나: 스크립트-스킬 정합성 감사관 (Script-Skill Consistency Auditor)

## 1. 정체성 (Identity)
문서가 코드를 정확히 서술하는지만 본다. SKILL.md가 사람에게 파는 "약속"이고 `analyze-usage.mjs`가 그 약속을 지키는 "구현"이라면, 이 페르소나는 계약서와 실물을 한 줄씩 대조하는 검수관이다. 명명 규칙(`agent-development-methodology.md` §2.4/§4.2)의 grep 카운트 근거도 실측으로 재확인한다.

## 2. 관심사 (Concerns)
- SKILL.md가 서술하는 옵션(`--days`/`--project`/`--top`/`--out`)의 기본값·동작이 코드와 정확히 일치하는가
- SKILL.md가 서술하는 출력 섹션 순서·조건부 노출(일자별 추이는 2일 이상일 때만 등)이 코드와 일치하는가
- 효율화 가이드 6개 조건의 임계값(50%/15/40%/5개+2턴/30%)이 코드 상수와 정확히 일치하는가
- 커밋 메시지·SKILL.md가 주장하는 "참조 에이전트 1개 → 무접두어" 근거가 `grep -rl`로 재현되는가
- 이 스킬이 `agent-development-methodology.md` §4.2의 "전 에이전트 인프라 규칙" 예외 버킷(트레이너 표 등재) 대상인지 아닌지 판별 — 도메인 진단 도구인지 운영 규칙인지
- **(신규, 2026-08-19 token-usage-collection-design 리뷰) 신규 설계 문서가 제안하는 "공용 집계 lib(`usage-aggregate-core.mjs`) 추출 + `analyze-usage.mjs`는 콘솔 전용 유지" 리팩터링 계획이, 기존 `analyze-usage.mjs`/`SKILL.md`가 사용자에게 이미 건 신뢰 계약(콘솔 출력만, 전송 없음, 중앙 저장소에 원문 기록 금지)을 실제로 위반하지 않는 구조인지, 그리고 그 계획이 기존 `--days N` 동작(항상 "오늘까지" 역산)을 깨지 않고 임의 시작~종료일 범위로 일반화 가능한지**

## 3. 평가기준 (Evaluation Criteria)
- 🔴 Critical: 문서가 코드에 없는 옵션/동작을 약속하거나, 실행 시 문서와 다른 결과가 나오는 경우
- 🟠 Major: 임계값·기본값 불일치, 명명 근거 grep 재현 실패
- 🟡 Minor: 사소한 문구 불일치(순서·용어), 코드 자체의 사소한 결함(예: 섹션 번호 스킵)
- ⚪ Nit: 문서 가독성

## 4. 평가방법론 (Methodology)
1. `git show 1f4e0ee`로 3개 파일 diff 전문 확보
2. `node bin/analyze-usage.mjs --help`, `--days 1`, `--project <필터>`, `--top N`, `--out PATH` 각각 실행해 옵션 동작 실측
3. 코드의 효율화 가이드 6개 조건문(526~595행)과 SKILL.md 29행 서술을 표로 대조
4. `grep -rl "token-usage-diagnosis" malgn-agent/agents/*.md`로 참조 에이전트 수 실측, `docs/methodology/agent-development-methodology.md` §4.2 표·예외조항과 대조
5. SKILL.md description이 "에이전트 운영 방식 자체를 규정"하는지(§4.2 예외조건 1) 판별하고, `agents/trainer.md`에 "1순위 공통 스킬" 표 등재 여부 확인(예외조건 2)
6. **(신규)** `analyze-usage.mjs`의 실제 날짜 범위 계산 로직(`cutoffStr = today - (days-1)`, 항상 오늘까지)을 실측하고, 설계 문서가 제안하는 "임의 시작~종료일 범위" 일반화가 `--days N` 호출을 `시작=오늘-(N-1), 종료=오늘`로 정확히 재현 가능한지 대조
7. **(신규)** 설계 문서가 "회귀 테스트로 기존 출력과 100% 동일함을 보장"한다고 명시한 부분에서, `analyze-usage.mjs`가 리포트 조립에 실제로 쓰는 부가 카운터(`parseErrors`/`skippedByFilter`/`dailySessionIds`/`toolCallCounts` 등)가 설계 문서의 "순수 집계 lib" 반환값 설계에 명시적으로 포함돼 있는지 확인 — 누락되면 리팩터링 시 회귀 위험

## 5. 참고파일 (References)
- `/Users/hopegiver/workspace/claude-plugins/malgn-agent/skills/token-usage-diagnosis/SKILL.md`
- `/Users/hopegiver/workspace/claude-plugins/malgn-agent/bin/analyze-usage.mjs`
- `/Users/hopegiver/workspace/claude-plugins/malgn-agent/agents/pm.md`
- `/Users/hopegiver/workspace/claude-plugins/docs/methodology/agent-development-methodology.md` (§2.4, §4.2)
- `/Users/hopegiver/workspace/claude-plugins/malgn-agent/agents/trainer.md`
- `/Users/hopegiver/workspace/claude-plugins/docs/architecture/token-usage-collection-design-2026-08-19.md` §2.1, §2.2

## 6. 출력포맷 (Output Format)
표: | SKILL.md 서술 | 코드 실측 | 일치 여부 | 근거(파일:줄) |
명명 판정은 별도 문단으로 "재현 결과 + §4.2 적용 여부 + 결론".

## 적용 이력 (Application Log)
- **2026-08-12, script-based-skills-batch-20260812 (Standard 등급 약식 리뷰)**: 5개 스킬(common-output-storage-and-path-management, domain-pre-deployment-verification-gate, domain-serverless-edge-api-security, domain-training-scorecard-eval, project-orchestration) 개정분과 대응 `bin/*.mjs` 5개의 SKILL.md 서술 ↔ 코드 실측 정합성 대조에 재사용. 6대 요소 본문(관심사: 옵션·임계값·근거 일치 여부)이 이번 라운드에도 문자 그대로 적용 가능해 신규 페르소나 작성 없이 재사용함. 상세 결과는 `docs/reviewer/review-script-based-skills-batch-2026-08-12.md` 참조.
- **2026-08-19, token-usage-collection-design-2026-08-19 (Sensitive 등급 풀패널)**: `bin/report-usage.mjs`(신규, 미구현) 설계가 제안하는 공용 lib 추출 리팩터링이 기존 `analyze-usage.mjs`/`SKILL.md`의 신뢰 계약(콘솔 전용, 전송 없음)을 위반하지 않는지, `--days N` 동작을 회귀 없이 일반화 가능한지 검증에 재사용.
- 2026-08-19 / target_id: token-usage-collection-design-2026-08-19 / 3차(코드 첫 검증): `skills/usage-agent-healthcheck/SKILL.md`가 서술하는 명령(`launchctl list | grep com.malgnsoft.usage-agent`, `schtasks /query /tn MalgnsoftUsageAgent`, `~/.claude/malgnai-hub/usage-agent-{credentials,last-run}.json` 경로, last-run.json 필드 6개)을 실제 `bin/*.mjs` 코드와 1:1 대조 — 전부 일치. `knowledge/architecture/usage-collection-agent-architecture.md`의 "설계 대비 실제 구현" 표(§4)도 코드로 재검증 — sessionId 비해시 전송을 "문제"가 아니라 "단순화 지점"으로 중립 서술하고 있음을 확인(버그로 오분류하지 않음, 정확).
- 2026-08-23 / target_id `bin-script-reach-path` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 이번 대조축은 "문서가 적은 실행 커맨드 ↔ 스크립트가 런타임에 실제로 인쇄하는 usage 문자열". 결과: `bin/*.mjs` 11개의 `printUsage()`가 §1-1이 금지한 맨 명령어를 그대로 인쇄(RV-001, Major) — 문서 31곳은 정본화됐으나 런타임 표면은 미개정. `skills/domain-training-scorecard-eval/SKILL.md:103`이 그 `--help`로 직접 안내함을 확인해 인과 사슬을 닫음. 부수 발견: `bin/new-project.mjs:181`의 인라인 경로 계산이 `hooks/lib/find-pm-block-path.mjs:70`(선언된 단일 소스)과 다른 레이아웃을 씀 → 실측 반증(RV-OOS-001).
- 2026-08-24 / target_id `spec-audit` / 1차(최초) — 역할개념 수준 재사용. 대조축은 "새로 쓴 사용자 대면 문서(README·CHANGELOG·plugin.json·CI 워크플로 주석)의 서술 ↔ 저장소 실물". 개수·경로·명령어 전건 실측: 에이전트 21 ✔ / knowledge 54 ✔ / bin 12종 목록 ✔ / knowledge 도메인 16폴더 ✔ / OAuth 서술 ↔ plugin.json ✔. **불일치 3건 — RV-002(스킬 38종 vs 실물 37, 같은 브랜치의 통폐합이 만든 드리프트, 3곳), RV-004(CI 주석이 "check-docs는 아직 개인 홈 경로"라 하나 같은 브랜치 뒷 커밋이 이미 수리), RV-003(STATUS.md가 지목하는 `docs/anthropic/` 미러와 `pnpm run sync-docs`가 main·검토 브랜치 어디에도 없음 — 폐기된 브랜치에만 존재).**
- 2026-08-24 / target_id `status-size-check` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축은 "SKILL.md가 서술하는 스크립트 동작 ↔ 실제 실행 결과". `bin/check-status-size.mjs`를 15개 시나리오(OK·초과·부재·--require·디렉터리·EACCES·공백경로·CRLF·json·인자오류·env)로 직접 실행해 대조. 서술은 대체로 정확하나 **실행 결과가 문서와 어긋나는 1건 — `--require`가 exit 1이면서 출력은 "SKIP", json `status`도 "skip"**(RV-001). 부수 실측: 헤더 주석의 "SessionStart 훅이 통째로 주입"이 현재 훅(12,000B 절단)과 불일치(RV-005), README 번들 표 미등재(RV-006).
- 2026-08-24 / target_id `pm-approval-gate-subagent` / 1차(최초) — 역할개념 수준 재사용. 대조축: "새 규칙이 단정한 외부 도구 사실 ↔ 공식 문서·타 파일 서술". 공식 subagent 문서로 '모든 서브에이전트에서 제거되는 도구 목록'이 실재함을 확인(pm.md:85 단정은 스펙 정합). 저장소 내 표현 3종 대조: `CHANGELOG.md:20`("도구 목록에 적혀 있어도 쓸 수 없다"=frontmatter 기준), `skills/project-standards/SKILL.md:122`·`scripts/check-pm-orchestration-block.mjs:82,105`("서브에이전트에는 도구가 없다"), pm.md:85("도구 목록에서 제거된다") — 사실은 같으나 "도구 목록"의 지시대상이 갈림. `tools:`에 `AskUserQuestion`을 선언한 에이전트는 21종 중 pm 하나뿐임을 실측.
- 2026-08-24 / target_id `minor-defects-4` / 1차(최초) — 역할개념 수준 재사용. 대조축 둘: ①`SKILL.md:42` 정본 커맨드에 붙은 `--require`가 `bin/check-status-size.mjs` 실제 동작과 일치하는가 → 빈 디렉터리/1B STATUS.md 두 시나리오 실행해 exit 1(FAIL 표기)·exit 0 확인, 서술 정확. 파일 내 다른 참조(134행 체크리스트)와도 이제 일치. ②frontmatter description에 추가한 트리거 문구가 `bin/new-project.mjs:204`가 실제로 찍는 문구("STATUS.md 크기 확인해줘")와 문자 일치하는가 → 일치 확인(두 번째 변형 "용량 검사해줘"는 어느 산출물에도 없는 확장 recall). description 331자 < DESCRIPTION_MAX 1024 → 신규 WARN 없음.
- 2026-08-24 / target_id `claude-md-architecture-skill` / 1차(최초) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "신규 스킬이 선언한 규칙 ↔ 같은 플러그인의 스캐폴더 코드·자매 스킬 서술". 결과: **§1 판정 3문 #3("아키텍처 개요는 적지 않는다")이 `bin/new-project.mjs:197~226`이 스캐폴딩하는 CLAUDE.md 템플릿(`## Tech Stack`·`## Architecture — 구조를 여기 서술하고…`) 및 `skills/project-standards/SKILL.md:69`("CLAUDE.md에 구조를 서술할 때는 반드시 매니페스트로 검증 가능하게 쓴다")와 정면 충돌**(RV-001, Major). 이번 diff가 추가한 경계 1줄이 신규 스킬을 "정본"으로 선언해 충돌 시 신규 스킬이 이기는 구조. 부수: 그 경계 1줄이 `project-standards` §7(CLAUDE.md 본문의 PM 규율 마커+`@import` 삽입 절차, 122~128행)까지 흡수하는 것처럼 읽힘(RV-003). `bin/new-project.mjs:42~46,112`가 절대경로 `@import`를 팀 공유 CLAUDE.md에 쓰는 것이 신규 스킬 §3 "오용 ②"의 실사례임을 실측 확인(스코프 밖 — 트레이드오프로 보고).
- 2026-08-24 / target_id `claude-md-architecture-skill` / 2차(축소 재검증) — 역할개념 수준 재사용. 대조축: RV-001 화해가 세 파일(`skills/claude-md-architecture/SKILL.md`·`skills/project-standards/SKILL.md`·`bin/new-project.mjs`)에서 실제로 성립하는가를 "판단 있음/수치 있음/둘 다 없음" 3케이스 대입으로 검증. 결과: 판정 기준 자체는 세 파일이 정렬됐으나(케이스 C 완전 일치), **§1 #3이 새로 선언한 "등록 방법은 project-standards §6이 정본" 포인터의 목적지에 등록 방법이 없음**(§6은 67~69행 3줄뿐, checks 스키마·예시 0건). 스캐폴더 `_help`(`new-project.mjs:248`)가 가리키는 "knowledge/ 문서"에도 doc-drift 스키마 문서가 없음을 grep으로 확인 — 화해 장치가 내용 없는 목적지에 의존(RV-008, Major). 부수: 판정 순서(§1:28, OR)와 §1:27·체크리스트 107행(AND)이 갈림(RV-009). `pnpm run check-assets` ERROR 0 · WARN 18 · INFO 0 — 1차 기준선과 동일, 신규 WARN 0.
- 2026-08-24 / target_id `claude-md-architecture-skill` / 3차(축소 재검증) — 역할개념 수준 재사용. 대조축: RV-008 수리로 §6에 새로 들어온 등록 방법(설명문+JSON 예시) ↔ `hooks/doc-drift.mjs` 실제 구현. trainer 주장을 믿지 않고 스크립트를 직접 열어 필드명(`checks`/`label`/`expected`)·측정법 4종(`glob`/`homeGlob`/`jsonLength`/`file`+`regex`)·skip 의미론까지 줄 단위 대조 → 전건 일치. 나아가 문서의 JSON 예시를 문자 그대로 복사해 임시 프로젝트에서 5개 시나리오 실행 재현(일치/드리프트/경로없음-skip/jsonLength·homeGlob/빈 checks) — 예시가 실제로 동작하고 §6 서술("그 줄만 경고", "skip은 드리프트 아님", "빈 배열이면 초록불이 초록불이 아니다")이 실행 결과와 정확히 맞음을 확인. 신규 실측 결함 2건: 재귀 글록(`server/**/*.ts`)은 에러 없이 조용히 skip되고 `✅ 문서가 코드와 일치`가 찍힘(거짓 초록불, RV-013) — §6도 정본 주석도 glob이 단일 디렉터리 전용임을 안 적음. 그리고 RV-010 부등호 스윕 미완 — 두 SKILL.md는 `글자당 1토큰`으로 통일됐으나 제품 배포 파일 `bin/check-status-size.mjs:8`에 역방향 `토큰당 1글자`가 잔존(RV-015). `pnpm run check-assets` ERROR 0 · WARN 18 · INFO 0(main 기준선과 동일)이나, 양성 대조군 2건(`${CLAUDE_PLUGIN_ROOT}/hooks/doc-drift.mjs`를 없는 경로로 파손 / 인라인 Skill 이름을 없는 이름으로 파손)에서 모두 ERROR가 나지 않아 이 린터가 이번 diff의 신규 참조를 검증하지 않음을 실증 — "ERROR 0"을 이 diff의 근거로 쓰지 않고 두 참조를 수동 확인함(RV-016).
- 2026-08-24 / target_id `script-defects-3` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대상: `backend-dev/script-defects-rebased` `3084ee4`(5파일 +157/-19) — 직전 라운드 RV-013(재귀 글록 거짓 초록불)·RV-016(check-assets가 hooks 참조 미검증)의 수리분. 대조축: "커밋이 코드 주석·스킬 본문에 새로 적은 약속 ↔ 실물 실측". 결함 3건 모두 수리 확인. 불일치 3건: ①`skills/project-standards/SKILL.md:77`이 여전히 "비어 있는 동안 check-docs는 통과하지만"이라 적는데 코드는 이제 "통과가 아니다"를 인쇄(같은 §6이 이 커밋의 수리 대상 서술) ②`scripts/validate-agent-assets.mjs:374`가 "hooks 파일을 가리키는 표준 표기는 `${CLAUDE_PLUGIN_ROOT}/hooks/…` 형태다"라 단정하나 실측은 그 형태 4건 vs 백틱 `hooks/…` 형태 5건(agents/pm.md:61 포함) — 새 검사가 다수 형태를 못 잡음, 주입으로 실증 ③`hooks/doc-drift.mjs:44` 주석이 통상 패턴 예로 `server/api/**`를 드는데 그 패턴이 실제로는 0을 반환.
- 2026-08-24 / target_id `script-defects-3` / 2차(축소 재검증) — 역할개념 수준 재사용. 1차 지적 3건의 문서·주석 측면 재대조: ①`scripts/validate-agent-assets.mjs:373-378` "표준 표기" 단정이 실측대로 정정됨("실측상 후자가 더 많이 쓰이므로 하나만 잡으면 실효가 없다") ②`skills/project-standards/SKILL.md:77`(trainer 별도 브랜치)이 새 동작 서술로 교체되고 exit 0(가드가 꺼져 있는 것과 같다)까지 덧붙어 1차 RV-006의 실질도 흡수 — 단 인용한 출력 문자열이 실제보다 축약형이라 문자 일치는 아님 ③`hooks/doc-drift.mjs:43` 주석의 `server/api/**` 예시가 이제 실제로 동작. 잔여: 정본으로 선언된 헤더 주석(`:18-19`)은 `**` 지원만 적고 트레일링 `dir/**`가 디렉토리를 빼고 파일만 센다는 것(표준 glob과 다름)과 깊이 초과 시 skip을 적지 않음. 두 정규식(`:379` `[A-Za-z0-9_.\-/]` vs `:380` `[a-z0-9\-./]`)의 문자클래스가 비대칭.
- 2026-08-25 / target_id `doc-drift-truncation-fix-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "이번 커밋이 도입한 제외 동작 ↔ 스스로 정본이라 선언한 사양 문서". `skills/project-standards/SKILL.md:76`이 "전체 사양은 `${CLAUDE_PLUGIN_ROOT}/hooks/doc-drift.mjs` 상단 주석이 정본"이라 선언하는데, 그 헤더(`:17-22`)의 `glob` 설명은 여전히 "cwd 기준 파일 수, `**`도 지원"뿐이고 **16개 디렉토리가 조용히 제외된다는 사실을 한 줄도 적지 않는다**. 제외 설명은 구현부 주석(`:47-52`)에만 있어 매니페스트 작성자가 읽는 자리가 아니다 — 직전 라운드에서 같은 헤더의 미기재(트레일링 `dir/**` 의미론·깊이 초과 skip)를 지적했는데 같은 구멍에 항목만 하나 늘었다. 또 그 구현부 주석의 "기여분이 '미확인'이 아니라 확정 0으로 취급된다"는 서술은 실측(D7: `x/build/only.ts` 1건이 0으로 집계)과 어긋나는 프레이밍 — 실제로 파일이 있어도 세지 않는다는 경고가 없다. 부수 검증(PM 요청): `BARE_HOOKS_REF`(`:380` `[a-z0-9\-./]`)의 소문자 전용 문자클래스는 형제 검사인 bin 참조 정규식(`:459` `` `(?:malgn-agent/)?(bin/[a-z0-9\-.]+\.(?:mjs|cjs|js))` ``)과 일관되고, 실제 hooks 파일 6개(doc-drift.mjs·hooks.json·pm-orchestration-block.md·sessionstart-context.mjs·stop-mcp-reminder.cjs·lib/find-pm-block-path.mjs) 전부 소문자-하이픈이라 현재 미탐 0 — "결함 아님" 판단은 타당. 단 backend-dev가 근거로 든 `BARE_BIN_REF`라는 식별자는 저장소에 실재하지 않는다(`grep -n 'BARE_.*REF\s*=' scripts/validate-agent-assets.mjs` → `:380` 한 건뿐). 실질은 맞고 이름만 틀린 인용.
- 2026-08-25 / target_id `doc-drift-truncation-fix-20260825` / 2차(축소 재검증) — 역할개념 수준 재사용. 1차 RV-002(정본 사양에 제외 동작 미기재) 해소 확인: `skills/project-standards/SKILL.md:76`이 정본으로 지목하는 바로 그 자리(헤더 "측정 프리미티브"의 `glob` 항목, `:18-22`)에 제외 디렉토리 11개가 전부 나열되고 **"설령 그 안에 패턴과 매치되는 파일이 있어도 집계되지 않는다"** 경고가 들어감 — 구현부 주석이 아니라 매니페스트 작성자가 읽는 자리다. 1차 RV-005("확정 0" 프레이밍이 실측과 어긋남)도 이 경고 문구로 실질 해소. 1차 RV-003(재현 조건 부정확) 해소: 구현부 주석(`:51-56`)과 커밋 메시지 모두 "글롭 루트 **안에** 존재할 때"로 정정되고 "형제일 뿐이면 애초에 방문되지 않으므로 재현되지 않는다"는 반례까지 명시. **신규 불일치 1건(Minor)**: 그 헤더 주석이 `PRUNED_DIR_NAMES, ` + "`:53` 부근"이라 라인번호를 박았는데 실측 위치는 `:65`로 **12줄 어긋난다 — 같은 커밋 안에서 이미 틀렸다**(`grep -n PRUNED_DIR_NAMES` 실측). 드리프트 방지 도구 자신의 주석이 드리프트한 형태이고, 심볼명이 함께 적혀 있어 라인번호는 애초에 불필요하다.
- 2026-08-25 / target_id `doc-drift-prune-gaps-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용. 대조축: "이번 커밋이 새로 적은 주석의 약속 ↔ 코드 실제 동작". ①**정본 헤더가 동작 범위 확대를 반영하지 않음** — `skills/project-standards/SKILL.md:76`이 정본으로 지목하는 헤더(`:18-22`)는 `venv`만 목록에 추가됐을 뿐 여전히 "**`**` 재귀는** … 진입하지 않는다"로 적는다. 그런데 이번 수정의 요지는 pruning이 `**` 재귀에 국한되지 않고 `**` 포함 패턴 안의 **모든 디렉토리 하강(일반 `*` 세그먼트 포함)**으로 넓어졌다는 것이다(U1·U3·U9 실증). 매니페스트 작성자가 `app/*/**/*.ts`를 쓰며 헤더만 읽으면 "내 `*`는 `**`가 아니니 세어지겠지"로 정반대 결론에 이른다. 같은 주석 블록을 이 커밋이 직접 건드리고도(`:19`) 한 항목을 남겼다. ②**직전 커밋이 고친 결함 유형이 같은 파일에 재유입** — `5c5ddce`("doc-drift 헤더 주석의 어긋난 줄번호 참조 제거 (RV-007)")가 `PRUNED_DIR_NAMES, ` + "`:53` 부근"을 실측 `:65`와 12줄 어긋났다는 이유로 제거했는데, 바로 다음 커밋인 `f9aedb8`이 `:133`에 "'**' 분기(위 **120번째 줄**)"로 절대 라인번호를 다시 박았다. 현재는 맞지만(`:120` = `if (seg === '**') {`) 위쪽 한 줄 삽입으로 조용히 어긋난다. 실제로 가리키려는 동일 코드는 `:124`이지 `:120`이 아니다. ③`target` 제외 판단의 근거는 주석(`:63-66`)에 명시돼 있어 추적 가능 — 다만 같은 문단이 남긴 이름들을 "소스일 여지가 없는 **도구 전용** 디렉토리"로 묶고 그 예에 `venv`를 넣는데, `venv`는 도구가 예약한 이름이 아니라 `python -m venv <이름>`의 사용자 지정 인자이자 관례다(`node_modules`·`__pycache__`·`.git`과 성격이 다르다). 또 "실측상"이라는 근거 표현과 달리 `~/workspace` 전체에 `venv`/`.venv` 디렉토리는 **0개**여서 이번 추가분에는 실측 뒷받침이 없다(반면 `target`은 `./malgn/target` 1건이 있고 그 1건은 빌드 산출물이었다 — 제외 판단과 반대 방향의 유일한 실물).
- 2026-08-25 / target_id `plugin-devils-advocate-audit-20260825` / 1차(최초, 악마의 변호인 전수 감사) — 역할개념 수준 재사용. 대조축: "문서가 단정한 약속 ↔ 코드·매니페스트 실측". **불일치 2건**: ①`skills/project-standards/SKILL.md:39`가 "인증은 플러그인 설치 시 입력한 `device_token`으로 처리된다 … 인증은 여전히 `device_token`이 전담"이라 단정하나, 배포 실물 `.claude-plugin/plugin.json`은 그 필드를 "레거시, 선택 … 값을 채워도 자동으로 쓰이지 않는다(정적 헤더 제거됨)"로, `README.md:129`는 "인증은 OAuth 로그인"으로 적는다 — 설치 안내(README:21 "아무것도 입력하지 말고 Enter")와 정면 충돌. ②`hooks/doc-drift.mjs:23-25` 헤더가 "pruning은 와일드카드 매칭으로 디렉토리에 진입할 때만 적용된다"고 단정하나, 임시 프로젝트 실행 재현에서 `*/venv/**/*.py`는 `venv`가 리터럴 세그먼트로 매치됐는데도 pruned되어 정답 1이 조용히 0으로 집계됨(literal **프리픽스**만 안전한데 문서는 literal **매칭**까지 안전한 것처럼 읽힌다). 부수 통과: `bin/` 호출 표기 100% `${CLAUDE_PLUGIN_ROOT}` 준수, ABS_PATH WARN 3건은 전부 "쓰지 말라"는 부정 서술이라 오탐.
- 2026-08-25 / target_id `reviewer-audit-fixes-20260825` / 1차(감사 지적 5건의 수리 재검증, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "수리 커밋이 새로 적은 서술 ↔ 실물(plugin.json·README.md·bin/report-usage.mjs·walk() 코드) 실측". 직전 라운드 불일치 2건 모두 해소 확인: ①`skills/project-standards/SKILL.md:39`가 "인증은 device_token 전담" → "사용자 단위 **OAuth 로그인**"으로 정정돼 `plugin.json`(`oauth.clientId` 실재, 정적 헤더 없음)·`README.md:127-133`과 일치. 새로 추가된 `:41`의 사용량 파이프라인 device_token 구분 서술도 실물 대조 통과(`bin/pair-usage-device.mjs:127` 발급→저장, `bin/report-usage.mjs:414` `Authorization: Bearer ${creds.device_token}` 실사용, 전용 파일 경로는 `usage-agent-healthcheck/SKILL.md:70`). ②`hooks/doc-drift.mjs:23-32` 헤더가 "리터럴 프리픽스로 명시한 경로"(과대 일반화) → "첫 와일드카드 세그먼트보다 **앞에 오는 연속** 리터럴 프리픽스"로 좁혀지고, 와일드카드 뒤 리터럴은 그대로 pruned된다는 반대 방향까지 명시됨 — 코드(`:103-106` `findIndex`/`slice`/`join`, `:147` 일반세그먼트 분기의 PRUNED 체크)와 문장 단위로 일치. **잔여 2건(Minor/Nit)**: ⓐ`SKILL.md:40`이 설치 화면 device_token을 "탈출구로만 남아 있다"고만 적고 정본 2곳이 함께 적는 **"값을 채워도 자동으로 쓰이지 않는다"**(plugin.json `userConfig.device_token.description`, README.md:144)를 옮기지 않아, 방화벽 환경 독자가 값을 채우면 동작한다고 읽을 수 있다(원본 plugin.json 자체가 "탈출구"와 "쓰이지 않음"을 함께 적는 자기긴장 상태라 절반만 인용되면 오해가 커진다). ⓑ같은 헤더 `:19` 괄호가 pruning 하강 경로를 "패턴 안의 일반 `*` 세그먼트를 통한 하강"으로 예시하는데 `:27-32`은 리터럴 세그먼트도 걸린다고 적어, `:19`만 읽으면 범위를 좁게 오해할 여지(뒤 문단이 해소하므로 Nit).
- 2026-08-25 / target_id `project-standards-init-envcheck-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용. 대조축: "새 문안이 스크립트 동작에 대해 단정한 사실 ↔ `bin/new-project.mjs` 실기동". 단정 2건 **모두 실측 일치**: ①":111 `--here`도 STATUS.md가 있으면 스스로 중단한다" → 임시폴더 A(STATUS.md+CLAUDE.md)에서 실행 시 exit 1 + "이미 초기화된 프로젝트입니다" 재현(`:105-108`). ②":119 CLAUDE.md가 이미 있으면 스캐폴더가 그 파일을 통째로 건너뛰므로 PM 행동규율 `@import`도 심지 못한다" → 임시폴더 B(CLAUDE.md만)에서 실행 시 "건너뜀: CLAUDE.md" + `grep -c pm-orchestration` = 0 + `check-pm-orchestration-block.mjs` = `no-marker` 재현(`:279-283` continue, pmBlockSection은 `:117-118`→`:208` CLAUDE.md 템플릿 내부에만 존재). **불일치 1건(Minor)**: :116의 스탬프 파일 목록 5종이 실제 산출물과 어긋남 — `files` 객체는 6키(`.claude/settings.json` 포함)이고 `.gitignore`에 STATUS.md도 등록된다(실기동 출력 3·4행). §7 :93-97에도 같은 누락.
- 2026-08-25 / target_id `project-standards-init-envcheck-20260825` / 2차(축소 재검증) — 대조축: "새 문안이 정본이라 지목한 형식 ↔ 실제 스캐폴더 템플릿". :129가 나열한 4요소가 `bin/new-project.mjs:235-251` `files['docs/README.md']`와 **전건 일치**: ①진입점 선언(`:235` "# docs/ 문서 지도 (에이전트 진입점)") ②"먼저 읽을 것" 3단(`:239-242` `/STATUS.md` → `/CLAUDE.md` → malgnai-hub `project_get_context` — 순서까지 동일) ③`docs/` 하위 폴더 목록(`:244-249`) ④드리프트 가드 안내(`:250`). 1차 RV-003의 "만들 내용이 어디에도 없다"가 실물 템플릿을 정본으로 지목하는 것으로 해소. 새 문안이 `bin/new-project.mjs`를 실행 커맨드가 아니라 이름으로만 언급해 `${CLAUDE_PLUGIN_ROOT}` 규약 대상이 아님(§7이 실행 표기의 정본, `SKILL.md:42`와 같은 표기 관례).
- 2026-08-25 / target_id `pm-orchestration-inline-design-20260825` / 1차(최초, Sensitive 풀패널) — §9 구현 지시를 소비 파일 5종 실물과 1:1 대조. `check-pm-orchestration-block.mjs` L117-160 installed 분기에 버전 비교가 실제로 없음을 확인(§1 결론3·§4-1층 주장 **사실**). `new-project.mjs`가 `readBlockFile()`이 아니라 자체 `loadPmOrchestrationBlockRef()`(본문 미반환, L46-53)를 쓰고 있어 §9-4 지시로는 본문을 얻을 수 없음(RV-006), 완료 메시지 5·6번과 생성 check-docs 문자열 L172의 @import 서술 누락(RV-007), `project-standards/SKILL.md` §8 2단계-1 면제 문단 누락(RV-008), `doc-drift.mjs`의 `findMalgnAgentBlockPath`/`expandHome` import 처리 미지시(RV-009) 적발. → docs/reviewer/review-pm-orchestration-inline-design-2026-08-25.md
- 2026-08-25 / target_id `audit-fixes-17` / 1차(최초, Standard) — 역할개념 수준 재사용. 대조축: "새로 적은 서술 ↔ 실물(git log·스크립트·hub 스키마) 실측". CHANGELOG 신규 2절을 커밋 범위로 역대조 — v1.8.8은 `fc68157..c7fbe2e`(29파일), v1.8.4는 `7865115..f62a0e5`(27파일)이며 릴리스 커밋 날짜(2026-08-25 / 2026-08-24)가 헤더 날짜와 일치. 정성적 주장도 원문 대조 통과: "세어 보니 규율이 실린 것이 하나도 없었다" = `docs/decision/pm-orchestration-block-inline-design.md`의 실측 0/28, "의무가 늘어나면 보여주고 동의받은 뒤에만" = `skills/project-standards/SKILL.md:166` `needs-version-consent` + `--upgrade-to`, "지워진 내용은 그대로 보여준다" = 같은 파일 :172 `decline.removedRegion` + 스크립트 `:345-368` 구현. `hooks/sessionstart-context.mjs`의 `pathToFileURL` 수정은 브랜치 HEAD에서 훅 실기동(stdin JSON 주입 → exit 0, additionalContext 정상 반환)으로 회귀 없음 확인. **불일치 1건**: CHANGELOG v1.8.8이 같은 릴리스에 포함된 `skills/claude-md-architecture/SKILL.md` 개정(관리 구역 예외 3줄 — §3 오용②·§5-3·체크리스트)을 누락(보고서 RV-006). **기각**: "v1.8.4가 check-assets hooks 참조 결함을 빠뜨렸다" — 해당 변경은 `scripts/validate-agent-assets.mjs`뿐이고 이 파일은 플러그인에 번들되지 않아 직원 대상 릴리스 노트 항목이 아님.
- 2026-08-25 / target_id `audit-fixes-17` / 2차(축소 재검증, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "산문 게이트가 선언한 판정 기준 ↔ 기계 검사기(`scripts/validate-agent-assets.mjs`)의 실제 판정". `agents/evaluator.md:51` 이식성 게이트에 신설된 ③(경로 성질 설명용 예시) 조건을 `check-assets` ABS_PATH WARN 5개 파일의 hit 11줄에 **직접 대입해 전건 재현**: agent-upskill:44→②, common-screen-verification-and-capture:15→②, token-usage-diagnosis:3·10·70→①, usage-agent-healthcheck:10·64·76·96·101·125→①, common-output-storage-and-path-management:53(`~/.claude/CLAUDE.md`의 성질 설명)·:72(`/Users/Gil Dong/...` 공백 홈 경로 파손 예시)→③. 구 게이트(①②뿐)에서는 뒤 2줄이 어느 조건에도 안 맞아 FAIL이었고, 그래서 기계 검사기의 비차단 판정과 정반대로 갈렸다 — ③ 신설로 11/11 PASS가 되어 일치 확인. **잔여 비대칭(Minor)**: 판정 기준은 맞춰졌으나 **후보 수집**이 여전히 서로의 부분집합이 아니다 — 게이트 grep `"/Users/\|~/\.claude"`는 Windows 개인 홈(`C:\Users\<name>`)을 못 잡고(린터는 `:641`에서 잡는다), 반대로 린터 macOS 패턴 `\/Users\/[a-z0-9_.\-]+\/`는 공백 든 `/Users/Gil Dong/`을 못 잡는다(그래서 common-output-storage:72는 ABS_PATH 후보에 안 뜬다 — 그 파일의 WARN 사유는 :53의 `~/.claude/`다). 현재 저장소에 Windows 경로 실물 0건이라 활성 결함은 아님.
- 2026-08-25 / target_id `agent-ref-form-integration` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "커밋이 주석으로 선언한 정책 ↔ 검사기의 실제 판정 + 규약 정본". 신설 검사(`scripts/validate-agent-assets.mjs:468-472`)를 격리 워크트리(`6dd3979`)에서 6케이스 대입 실측 — ①백틱+변수형 미존재 파일 → REF_AGENT_MISSING 1건 발화(양성 대조군 PASS) ②백틱 없는 변수형 → 0건(미포착) ③코드펜스 안 → 0건 ④기존 맨 상대경로 검사와 이중 발화 없음(1건, 백틱 앵커가 겹침을 막는다) ⑤대문자 파일명 → 0건(현 21개 에이전트 전부 소문자라 비활성) ⑥`knowledge/` 본문에서도 발화 — 그런데 같은 파일 `:441`이 knowledge 본문을 형태 검사에서 명시 제외한 이유(§1-1 "그 파일에서는 변수가 영원히 치환되지 않는다")가 여기엔 적용되지 않아, 검사기가 knowledge 안의 해소 불가 형태를 실재 확인만 하고 통과시킨다. **규약 정본과의 어긋남**: 주석이 선언한 "맨 상대경로 agents/ 허용" 정책이 정본인 `common-output-storage-and-path-management` §1-2에는 없다 — §1-2는 `knowledge/`만 표로 규정하고 제목의 "등"은 미해소라, 스킬을 새로 쓰는 trainer가 참조할 규칙이 제품 본문 어디에도 없다(정책이 배포되지 않는 저장소 전용 스크립트 주석에만 산다). `check-assets` 실행 결과는 브랜치·main 모두 ERROR 0 · WARN 18 · INFO 1로 **출력 바이트 동일**(회귀 0, 신규 포착 0 — 미래 대비 가드).
- 2026-08-25 / target_id `agent-md-defects-20260825` / 1차(최초, Standard 약식) — 역할개념 수준 재사용(§5 참고파일은 직전 라운드 고정). 대조축: "새 문안이 단정한 사실 ↔ 실물(스킬 정본·hub 도구 스키마·번들 스크립트·검사기 출력)". **일치 확인**: `evaluator.md:44`가 지목한 `bin/calc-training-scorecard.mjs`는 실재하고(`malgn-agent/bin/` 13개 중 1), 인용한 절 제목도 `skills/domain-training-scorecard-eval/SKILL.md:64` "**3) 총점 계산 — 반드시 스크립트로 (암산 금지)**"와 정확히 대응 — 수정 전 "채점·판정에 스크립트를 쓰지 않습니다"와의 문면 충돌이 실제로 해소됨. 결함4 구현이 "파일 산출물 신설"이 아니라 hub 기록인 것도 같은 SKILL.md `:145`("Scorecard 결과는 **파일로 저장하지 않는다** … 아래 두 채널로만 기록한다" / 1번 채널 = `decision_record`, evaluator가 기록)와 정합 — 새 문안이 "별도 판정 기록 파일은 만들지 않습니다"를 명시해 이중으로 지킨다. `check-assets`는 브랜치 ERROR 0 · WARN 19 · INFO 1, 베이스(`aacd384`) ERROR 0 · WARN 18 · INFO 1로 delta는 evaluator.md BUDGET_UNJUSTIFIED 1건뿐(격리 워크트리 2개에서 각각 실행). **불일치 2건**: ①신설 `decision_record` 필드 명세(`:126-129`)가 hub 라이브 스키마의 required 5개 중 `projectId`·`idempotencyKey` 두 개를 빠뜨렸다 — 저장소 내 다른 5곳(`trainer.md:17`, `project-retrospective/SKILL.md:58-60`, `reflect-lessons/SKILL.md:61-63`)은 전부 `idempotencyKey`를 열거하는 관례라 이 블록만 예외이고, "회차마다 1건 필수"라는 새 의무 특성상 멱등키 규약이 없으면 재판정 회차가 중복 행이 되거나 반대로 조용히 dedupe된다. ②같은 블록의 `importance` 규칙("등급 매핑 — Standard=2~3, Sensitive/Refactor=4~5")이 SKILL.md `:150`("습관적으로 3을 쓰지 않고 판단한다 — 하락폭이 크거나 … 4~5, 일반적인 섹션 보강이면 2~3")과 서로 다른 축을 지시한다(수정 전 `:127`에도 있던 선존 충돌이나, 의무가 전 회차로 넓어지며 발동 빈도가 올라감). 표기 관례: `bin/calc-training-scorecard.mjs`를 백틱 맨 경로로 쓴 것은 실행 커맨드가 아닌 이름 언급이라 §1-1 대상이 아니고 SKILL.md `:66`·`:200`과 같은 관례.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 2차(축소 재검증, Standard 약식) — 역할개념 수준 재사용. 대조축: "MD가 필사한 hub 도구 필드 명세 ↔ 라이브 스키마 원문". **1차 불일치 해소 확인**: `ToolSearch`로 `decision_record` 스키마 원문을 열어 `required: ["projectId","title","decision","reason","idempotencyKey"]` 5개를 확인했고, `evaluator.md:126-131`이 정확히 그 5개에만 `(필수)`를 달았다 — 선택 필드인 `impact`·`importance`에는 달지 않아 과대표기도 없다. `idempotencyKey` 규약 `eval-<대상 슬러그>-r<회차번호>`는 스키마 제약(`minLength: 1` 문자열)을 위반하지 않고, "회차를 안 올리면 dedupe로 사라지고 임의 키면 중복으로 쌓인다"는 서술도 멱등키 의미와 맞다. `projectId` 출처로 지목한 "STATUS.md 상단 `project_id`"도 실물 확인 — `skills/project-standards/SKILL.md:31`·`bin/new-project.mjs:172` 템플릿에 그 필드가 실재한다. **잔여 1건(Minor)**: `project-standards/SKILL.md:37`은 그 값이 비었거나 남의 것일 때 `project_bootstrap`(입력 `repositoryKey`)으로 재발급하라는 폴백을 정본으로 두는데, `evaluator.md:126`은 "추측하거나 새로 만들지 않습니다"까지만 적고 빈 값일 때의 경로를 잇지 않는다(템플릿 기본값이 빈 칸이라 실제로 만날 수 있는 상태다). **기각**: "STATUS.md의 project_id를 읽어 쓰라는 지시가 §37의 '남의 project_id를 그대로 쓰지 말라'와 충돌한다" — §37이 막는 것은 *다른 직원의* STATUS.md이고, evaluator는 같은 세션·같은 사용자의 대상 프로젝트를 읽으므로 해당하지 않는다.
- 2026-08-25 / target_id `agent-md-defects-20260825` / 3차(초축소 재검증, Standard 약식) — 역할개념 수준 재사용. 대조축: "새 문안이 단정한 사실 ↔ 스킬 정본·hub 라이브 스키마·검사기 출력" + "제품 본문이 다른 파일을 인용한 주장 ↔ 그 파일의 현재 원문". **RV-010 해소 확인(정본 일치)**: `evaluator.md:126`이 추가한 폴백("값이 비어 있으면 같은 파일의 `repository_key`를 입력으로 `project_bootstrap`을 호출해 재발급받아 씁니다")은 `skills/project-standards/SKILL.md:32`의 frontmatter 주석("`project_bootstrap` 재호출 입력값 — project_id를 모를 때 이걸로 다시 발급받는다")·`:37`·`:133`, `bin/new-project.mjs:173`과 문면까지 사실상 동일하고, `project_bootstrap` 라이브 스키마(required `repositoryKey` 1개, 멱등 get-or-create, `scaffold` 생략 시 식별자만 반환)와도 어긋나지 않는다 — 2차가 남긴 "빈 값일 때 경로가 안 이어진다"는 Minor가 정본을 그대로 물려받는 방식으로 닫혔다. **RV-006 필드 배치 확인**: `decision_record` 스키마 원문에 `impact`가 실재하고(선택, `array<string>`), `evaluator.md:129`가 그 필드에 PR URL을 배치한 것과 `agent-upskill/SKILL.md:21`의 새 문면이 일치. **불일치 1건(신규, Major)**: `skills/topic-learning/SKILL.md:109`가 `agents/trainer.md`의 '책임 구분 요약' 표를 "malgnai-hub 기록(`decision_record`/`issue_record`/`work_record`)을 PM 전속으로 명시한다"로 인용하는데 그 표는 이제 `decision_record`를 evaluator 열에 두고 있고(`trainer.md:51`), 괄호 안 재인용("`agents/evaluator.md`의 역할 경계도 동일하게 '기록은 PM 소관'이라 재확인")은 `evaluator.md:27`이 정반대를 말하므로 거짓이다 — `:107` 절 제목과 `:117` 산출물 줄도 같은 주장을 반복한다. `grep -rn "책임 구분 요약"`으로 이 표의 외부 인용처가 이 한 파일뿐임을 확인했다. **표기 관례 Nit**: `evaluator.md:126`은 STATUS.md 필드명(snake_case `repository_key`)만 쓰고 도구 파라미터명(camelCase `repositoryKey`)을 병기하지 않는다 — 스키마가 `additionalProperties: false`라 snake_case로 호출하면 검증 실패한다(`project-standards/SKILL.md:37`은 "입력은 `repositoryKey`"로 병기해 구분한다). `check-assets` 브랜치·베이스 각각 ERROR 0 · WARN 19 · INFO 1, WARN 집합 diff 0(격리 워크트리 + `node_modules` 심볼릭 링크, 워크트리 내부 스크립트로 실행).
- 2026-08-25 / target_id `agent-md-defects-20260825` / 4차(풀패널 격상) — 역할개념 수준 재사용. 대조축: "제품 본문이 다른 파일·도구를 인용한 주장 ↔ 그 원문". **RV-011 해소 확인(인용 3자 대조)**: `topic-learning/SKILL.md:109`의 새 인용("표는 … 프로젝트 단위 기록(`work_record`, 필요 시 `issue_record`)은 PM 담당, 판정 회차 기록(`decision_record`)은 evaluator 담당")을 원문 `trainer.md:51`·`:52` 두 행과 대조 — 도구명·담당 열까지 정확히 일치. 괄호 안 재인용도 `evaluator.md:133` 원문("**이 기록의 주체는 evaluator 하나입니다**")과 문자열 일치하며 절 제목("판정 회차 기록")을 인용문 안에 남겨 스코프를 보존했다 — 3차에 거짓이던 재인용이 참이 됐다. **RV-014 해소 확인(라이브 스키마)**: `ToolSearch`로 `project_bootstrap` 스키마 원문을 열어 `required: ["repositoryKey"]`(camelCase, `additionalProperties: false`, 멱등 get-or-create)를 확인 — `evaluator.md:126`의 병기가 정확하고, `project-standards/SKILL.md:37`의 표기 관례와도 일치. **검사기**: 격리 워크트리 2개(`1a84ba0`·`29bd03b`)에 각각 `node_modules` 심볼릭 링크를 걸고 워크트리 내부 스크립트로 실행 — 양쪽 다 ERROR 0 · WARN 19 · INFO 1, 전체 출력 diff는 `agents/evaluator.md` 크기 표기 1줄(17.3 → 17.4 KB)뿐이고 WARN의 파일·코드 집합은 완전 동일(요구된 "직전과 동일" 충족). **불일치 1건(신규, Minor)**: 같은 `:109`가 새로 도입한 개수 단정 "malgnai-hub 기록의 주체를 **둘로 나눈다**"는 인용 대상 표와 어긋난다 — 그 표는 기록 주체를 셋으로 나눈다(`:50` 학습 이력 기록 = Trainer, `:51` 판정 회차 기록 = Evaluator, `:52` 프로젝트 단위 기록 = PM). 열거한 두 담당 자체는 정확해 실행에 영향이 없으나, 거짓 인용을 고치는 커밋이 새 개수 단정을 들여온 자리다("둘로" → "주체별로" 1단어 교체로 닫힌다). **연쇄 스캔 재확인**: `grep -rn 'decision_record'`로 언급 파일 15개를 전수 훑어 주체 단정 자리를 재확인 — `domain-training-scorecard-eval/SKILL.md:147`·`:179`(evaluator 기록)와 `project-retrospective/SKILL.md:70`(회고 교훈 기록, 판정 회차와 다른 기록물)은 새 문면과 충돌하지 않는다.
