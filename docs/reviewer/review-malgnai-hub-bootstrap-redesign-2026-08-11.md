# 리뷰 보고서: malgnai-hub 대상 프로젝트 부트스트랩 원복 구현 (2026-08-11)

- 대상: `docs/decision/malgnai-hub-project-bootstrap-redesign.md`(설계, 최종 원복본) + 구현 커밋 `83d94fe`(trainer 구현) + `db48561`(PM fixup, 회귀 2건 수정)
- 워크트리: `/Users/hopegiver/workspace/claude-plugins/.claude/worktrees/agent-a1fba4b8d23d957bf`
- 등급: Sensitive 상당(회사 전체 배포용 신규 프로젝트 스캐폴딩 로직) — 풀 패널(4명, 발산형 1명 포함) 투입
- **종합 판정: 🟡 Amber — GO-with-fix** (Critical 없음. 기능은 깨지지 않으나 첫날부터 사용자가 두 가지 규율을 동시에 읽게 되는 자기모순 1건과, 설계문서 스스로의 스코프 선언과 실제 문면이 어긋나는 문서 정합성 결함 1건, 마이그레이션 안내 공백 1건을 배포 전 또는 배포 직후 빠르게 고칠 것을 권고)

## ⚠️ 리뷰 도중 발견: 워크트리 동시편집 (정직 명시 — 매우 중요)

이 리뷰는 커밋 `83d94fe`/`db48561`(리뷰 위임 시점의 "최종" 상태)을 대상으로 수행했다. **그런데 리뷰 작업 도중 `git status`를 재확인한 결과, 이 워크트리에 커밋되지 않은 변경 3건이 이미 working tree에 존재하는 것을 발견했다** — 내가 만든 변경이 아니다(`ps aux`로 확인한 결과 이 머신에서 다수의 Claude Code 세션이 동시에 실행 중이다). 즉 다른 프로세스가 이 리뷰가 진행되는 동안(또는 그 직전) 이미 "최종"이라고 전달받은 스펙을 다시 수정했다:

| 파일 | 변경 내용(미커밋) |
|---|---|
| `docs/decision/malgnai-hub-project-bootstrap-redesign.md` §0 부수 조치 문단 | "SessionStart 훅 제거 계획은 STATUS.md 쪽만 철회·복원했고, **PM 넛지 훅(`pm-orchestration-nudge.mjs`)은 별도 사용자 지시(decision `547c67be`)로 다시 SessionStart에서 빠졌다**"로 수정됨 |
| `docs/decision/pm-orchestration-block-import-design.md` | 내가 "제거 확인됨"으로 보고한 `[malgnai-hub 대상 프로젝트 한정 대체됨]` 계열 포인터가 `[일부 대체됨, 2026-08-11]`로 **다시 추가됨**(SessionStart 상시 넛지 레이어만 대체, `@import` 삽입 설계 자체는 유효라는 취지) |
| `malgn-agent/hooks/hooks.json` | `SessionStart`에서 **`pm-orchestration-nudge.mjs` 훅 항목이 실제로 제거됨**(2개 → 1개) |

**이것이 이 리뷰의 판정에 미치는 영향**:
- 위임 지시문의 "hooks.json/sessionstart-context.mjs/...가 정말 아무것도 안 바뀌었는지" 확인 요청에 대해, **커밋 `83d94fe`/`db48561` 기준으로는 "안 바뀌었다"가 사실**이었다(§본문의 정합성 감사관 검증 결과 그대로 유효). 하지만 **지금 이 순간의 워킹트리는 이미 그 전제가 깨져 있다** — hooks.json이 실제로 바뀌었고, "대체됨" 포인터도 다시 붙었다.
- 이 변경들은 아직 **커밋되지 않았고(unstaged)**, 이 리뷰가 검증을 위임받은 `83d94fe`/`db48561` 범위 밖이다. 나는 이 변경을 만들지 않았고, 내 커밋에도 포함시키지 않았다(내 커밋은 `docs/reviewer/` 산출물 5개 파일로만 한정했다 — `git add`로 파일을 명시 지정해 스테이징함, lesson `a171f562` 원칙 준수).
- **PM에게 명확히 요청한다**: 이 미커밋 변경이 (a) 정당한 후속 결정(decision `547c67be`)의 정상 진행 중 산출물인지, (b) 다른 세션과의 의도치 않은 충돌인지 먼저 확인할 것. 만약 (a)라면 이번 리뷰의 "GO-with-fix" 판정은 **`83d94fe`/`db48561`이라는 스냅숏에 대해서만 유효**하며, `547c67be` 후속 변경이 커밋되면 그 변경분(특히 hooks.json 재수정 + pm-orchestration-block-import-design.md 포인터 재추가)에 대한 **별도 리뷰가 다시 필요**하다.
- 아래 본문(페르소나 패널·지적 사항)은 모두 커밋 `83d94fe`/`db48561`을 기준으로 작성됐다 — 이 addendum 이후 내용을 읽을 때 "현재 워킹트리"가 아니라 "위임받은 두 커밋 시점"의 상태를 서술한다는 점을 유의할 것.

---

## 페르소나 패널 구성 (4명, 발산형 1명 포함)

| 페르소나 | 파일 | 역할 |
|---|---|---|
| 신규 프로젝트 온보더 | `docs/reviewer/personas/persona-scaffold-new-project-onboarder.md` | 오늘 처음 `new-project.mjs` 실행하는 직원 관점 — 생성물 자체의 자기모순 여부 |
| 기존 5필드 프로젝트 유지보수자 | `docs/reviewer/personas/persona-legacy-5field-project-maintainer.md` | 원복 이전(5필드) 버전으로 이미 만들어진 프로젝트 관리자 — 마이그레이션 경로 |
| 스펙-구현 정합성 감사관 | `docs/reviewer/personas/persona-spec-implementation-conformance-auditor.md` | 설계문서 조항 대 실제 diff 1:1 대조 |
| STATUS.md 존재당위 도전자 [발산형] | `docs/reviewer/personas/persona-status-md-necessity-challenger.md` | 오늘 세 번째로 바뀐 이 결정이 근본 해법인지, 패치 누적인지 재질문 |

## 지적 사항 (통합)

| ID | 심각도 | 위치 | 지적 | 담당 페르소나 |
|---|---|---|---|---|
| RV-01 | 🟠 Major | `malgn-agent/bin/new-project.mjs:67` | 새로 생성되는 `STATUS.md` **본문**(YAML frontmatter 아래 blockquote)에 구 규율 `"이 파일이 진행 상태의 단일 소스다. 착수 전 읽고, 상태가 바뀌면 끝내기 전 갱신."`이 그대로 남아 있다. 같은 파일 세트의 `CLAUDE.md` 템플릿(같은 파일 93~95행)은 이번 원복으로 "재작성은 6가지 상황으로 제한"을 명시했는데, `STATUS.md` 자신은 옛 문구("상태가 바뀌면 끝내기 전 갱신")를 그대로 말한다. `db48561`이 정확히 이 유형의 모순(`pm.md`의 "착수 전 읽고, 작업 사이클 끝마다 갱신" vs 6트리거)을 찾아 고쳤음에도, 같은 문구가 `STATUS.md` 템플릿 안에도 하나 더 있었던 것은 놓쳤다. `git show d1d44a1:malgn-agent/bin/new-project.mjs`로 대조해 이 줄이 이번 두 커밋 어디에서도 건드려지지 않았음을 확인함. | 신규 프로젝트 온보더 |
| RV-02 | 🟠 Major | `malgn-agent/skills/project-standards/SKILL.md:40~45` vs `docs/decision/malgnai-hub-project-bootstrap-redesign.md` §0, §2 | 설계문서 §0은 "이 저장소(claude-plugins) 자신은 provider: malgnai-mcp로... 이번 원복으로도 처음부터 영향이 없다"고 명시하고, §2는 "malgnai-hub 대상 신규 프로젝트는 이 상한을 목표로 한다"고 **범위를 malgnai-hub로 한정**해서 적었다. 그런데 §6 Tier1이 "오전 버전이 계획했던 provider 분기 신설은 철회 — 다시 단일 절차로 되돌아간다"고 명시한 대로, 실제로 작성된 `SKILL.md` §3 문면(1000토큰 상한/완료섹션 3~5개/6트리거)에는 provider 분기도, "malgnai-hub 대상 한정"이라는 스코프 qualifier도 전혀 없다 — 범용 규율로 적혀 있다. `SKILL.md`는 `knowledge/` 다수 문서가 "정본"으로 지목하는 파일이고, hooks(`sessionstart-context.mjs` 등)는 STATUS.md 내용을 파싱 없이 통째로 주입하므로 이 문서를 provider와 무관하게 참조할 수 있다. 즉 설계문서 자신이 "이 저장소는 영향 없다"고 약속한 것과, 실제로 작성된 SKILL.md 문면이 스코프를 한정하지 않은 것 사이에 괴리가 있다 — 기능은 안 깨지지만(§9 근거: 어떤 훅도 필드를 파싱하지 않음) 문서 자체의 자기주장이 어긋난다. | 스펙-구현 정합성 감사관, 기존 5필드 프로젝트 유지보수자 |
| RV-03 | 🟠 Major | `malgn-agent/skills/project-standards/SKILL.md` §8 전체 | 이번 원복 이전(5필드) 버전으로 이미 스캐폴딩된 기존 프로젝트가 오늘 이후 `repository_id`/`web_url` 필드를 그대로 들고 있는 STATUS.md를 계속 쓸 때, 그 필드를 지워야 하는지/그대로 둬도 되는지에 대한 안내가 SKILL.md §8("기존 폴더 초기화")·설계문서 어디에도 없다. `hooks/sessionstart-context.mjs:25,38-39`를 직접 읽어 확인한 결과 이 훅은 STATUS.md를 필드 단위로 파싱하지 않고 파일 전체를 문자열로 세션에 주입하므로 **기능은 깨지지 않는다** — 다만 이 결정 전체의 동기(중복 주입 낭비, 1,844토큰 실측)를 생각하면, 안내 부재로 기존 프로젝트들은 이번 저비용화의 혜택(필드 축소·1000토큰 목표)을 전혀 못 받고 옛 크기 그대로 남는다. 설계문서 §6 Tier2 후속 목록에도 "마이그레이션 안내"는 항목으로 없다. | 기존 5필드 프로젝트 유지보수자 |
| RV-04 | 🟡 Minor | `malgn-agent/knowledge/common/project-folder-structure.md:26`, `knowledge/common/project-management.md:10`, `knowledge/leadership/progress-status-templates.md:35` | 세 지식 문서 모두 여전히 "완료 섹션 5~7개"를 규율로 서술한다(이번 두 커밋이 손대지 않은 파일 — 설계문서 §6에도 knowledge/는 목록에 없어 명시적으로 스코프 밖). `SKILL.md`(§3, "정본")는 3~5개로 이미 바뀌어서, "정본은 SKILL.md"라고 스스로 말하는 이 knowledge 문서들과 수치가 어긋난 상태가 지금 막 생겼다. 이번 PR의 회귀는 아니지만, 이번 변경이 만든 새로운 드리프트이므로 후속 세션에서 놓치기 쉽다 — 여기서 명시해 둔다. | 스펙-구현 정합성 감사관 |
| RV-05 | ⚪ Nit | `malgn-agent/bin/new-project.mjs:93~97` | CLAUDE.md 템플릿 안에서 "6가지 트리거" 목록(①~⑥)과 바로 두 줄 뒤 "필수 규율" 목록(①②)이 같은 원문자 기호를 재사용한다. 시각적으로 두 목록이 이어지는 하나의 목록처럼 오독될 여지가 있다(내용상 오류는 아님). | 신규 프로젝트 온보더 |
| RV-06 | 🟡 Minor | `malgn-agent/agents/pm.md:70` | "Micro 등급: PM 직접 처리... 사후 STATUS.md 1줄 + `decision_record`/`work_record`만"이라는 기존 표 문구가, 이번에 추가된 "6가지 상황 외에는 건드리지 않는다"는 원칙과 정확히 겹치지 않는다(Micro 작업은 "중요한 작업 완료"에 해당하지 않을 수 있음). 설계문서 §7이 이미 이 지점을 "trainer가 개정 시 한 번 훑어볼 것을 권고"로 명시적으로 미확정 남겨뒀고, 실제로 trainer는 손대지 않았다 — 이는 설계문서가 허용한 범위 내의 판단이므로 위반은 아니나, 다음 세션이 pm.md를 다시 만질 때 함께 정리할 항목으로 재확인해 둔다. | 스펙-구현 정합성 감사관 |

## 🔵 Rethink (발산형 — STATUS.md 존재당위 도전자)

| 현재 구조 | 제안 구조 | 왜 더 나은가 | 예상 비용/리스크 |
|---|---|---|---|
| STATUS.md는 유지하되 필드 수·토큰 상한·재작성 트리거를 조여 "겹쳐도 감당 가능한 크기"로 만든다(오늘 결정). 훅 2개(`sessionstart-context.mjs`+`pm-orchestration-nudge.mjs`)가 세션마다 각각 STATUS.md를 (전자는 직접, 후자는 간접적으로 PM 블록 넛지와 함께) 주입하는 구조 자체는 그대로 둔다. | 두 훅의 책임을 명확히 분리한다 — `sessionstart-context.mjs`만 STATUS.md 본문을 주입하고, `pm-orchestration-nudge.mjs`는 STATUS.md를 다시 언급/인용하지 않고 PM 블록 넛지와 드리프트 가드에만 집중하도록 역할 경계를 문서(`pm-orchestration-block.md`)에 명시한다. 즉 "파일을 작게 만들어 중복 비용을 감당 가능하게" 하는 대신 "애초에 중복 주입을 안 하게" 만든다. | 원래 문제(issue `22789b9e`, 2회 중복 주입)의 **근본 원인**은 파일 크기가 아니라 "두 훅이 겹쳐서 같은 내용을 반복 주입"하는 구조다. 1000토큰 상한은 이 근본 원인을 없애지 않고 "겹쳐도 최악 2000토큰"으로 상한을 낮출 뿐이며, §9가 스스로 인정하듯 그 상한을 강제할 도구도 아직 없다 — 이 저장소 자신이 이미 압축 규율(5~7개)을 지키고도 1,844토큰까지 불어난 전례(설계문서 §2)가 있어, 새 프로젝트들도 결국 같은 경로를 밟을 개연성이 낮지 않다. 역할 분리는 발생 자체를 막아 "규율 준수에 의존"하는 리스크를 없앤다. | 두 훅이 실제로 무엇을 중복 주입하는지 정밀 실측이 먼저 필요하다(현재 설계문서는 "2번 중복 주입"이라고만 서술하고 어느 훅이 무엇을 얼마나 주입하는지 세부 breakdown이 없음 — 이번 리뷰 범위에서는 재확인하지 못했다). 역할을 분리하면 `pm-orchestration-nudge.mjs`가 지금 하던 일 중 STATUS.md 관련 부분을 제거해야 하는데, 그 부분이 실제로 존재하는지부터 코드 레벨로 다시 확인해야 한다(이번 리뷰는 설계문서 서술을 근거로 가설을 세운 것이며, `pm-orchestration-nudge.mjs` 실제 소스는 이번 세션에서 직접 열어보지 않았다 — 후속 검증 필요). |

## 페르소나별 관점 요약

- **신규 프로젝트 온보더**: 3필드+캐비어트+6트리거는 CLAUDE.md 쪽만 보면 잘 정리돼 있으나, 같은 스캐폴드가 만드는 STATUS.md 본문에 옛 문구가 살아남아 "첫날부터 두 파일이 서로 다른 소리를 하는" 상황을 만든다(RV-01). 콘솔 안내문 1~4단계는 실행 순서상 문제없이 따라갈 수 있었다.
- **기존 5필드 프로젝트 유지보수자**: 기능은 안 깨진다(훅이 필드를 파싱하지 않으므로 RV-02/RV-03 모두 크래시 리스크는 없음). 하지만 "그대로 둬도 되는가"에 대한 명시적 안내 부재(RV-03)와, 새 SKILL.md 규율의 적용 범위가 문면상 불분명한 점(RV-02)은 실제 손해(토큰 낭비 지속)로 이어진다 — 이번 저비용화의 수혜가 신규 프로젝트에만 국한되고 기존 프로젝트는 옛 크기 그대로 방치된다.
- **스펙-구현 정합성 감사관**: `git diff d1d44a1 HEAD -- malgn-agent/hooks/`가 빈 출력임을 직접 확인해 §6 Tier1 "훅 변경 없음" 주장은 실측과 일치한다. `pm-orchestration-block-import-design.md`의 "대체됨" 포인터도 현재 파일에 없음을 확인했다(다만 `d1d44a1..HEAD` 사이 그 파일의 diff 자체가 0이라, 애초에 이 워크트리에는 "대체됨" 문구가 커밋된 적이 없다 — 오전 세션의 편집이 이 브랜치에 반영된 적이 없거나 별도 경로였을 가능성이 있음. 결과 상태는 설계문서 요구와 일치하므로 문제는 아니지만, 정직하게 남긴다: "제거했다"는 커밋 로그상 확인되지 않고, 결과 상태만 확인됨). §9의 정직 명시(1000토큰 강제 수단 없음, project_get_context 스키마 미검증)는 문서에 실재한다.
- **STATUS.md 존재당위 도전자 [발산형]**: 위 Rethink 표 참조. 오늘 세 번째 방향전환이 근본 해법인지 재질문했고, 대안(훅 역할 분리)을 제시했다 — 단, 대안의 전제(두 훅의 실제 중복 주입 내용)는 이번 리뷰에서 코드 레벨로 재검증하지 못했음을 명시한다.

## 트레이드오프

- RV-02/RV-03은 "고쳐야 한다" vs "굳이 지금 안 고쳐도 된다"는 판단이 갈릴 수 있다. 기능적 크래시가 없다는 점에서 배포를 막을 이유는 아니지만, 이번 결정의 존재 이유(토큰 비용 절감)가 기존 프로젝트에는 적용되지 않는다는 점은 설계 의도와의 괴리이지 사소한 흠이 아니다 — Amber 판정의 핵심 근거다.
- RV-04(knowledge/ 문서 5~7개 잔존)는 이번 PR의 스코프 밖(설계문서가 명시적으로 knowledge/를 언급하지 않음)이라 이번 커밋의 결함으로 채점하지 않았으나, 방치하면 "정본"이라는 SKILL.md의 권위와 실제 참조되는 knowledge 문서 사이의 드리프트가 더 벌어진다.

## 잘 된 점

- `git diff d1d44a1 HEAD -- malgn-agent/hooks/`가 실제로 빈 결과 — 설계문서 §6 Tier1 "훅 5개 변경 없음" 주장이 코드 레벨로 정확히 지켜졌다.
- `db48561` fixup 커밋이 실제로 유효한 회귀 2건(new-project.mjs의 "필수 규율" 누락, pm.md의 자기모순 문구)을 찾아 정확히 고쳤다 — 다만 STATUS.md 템플릿 안의 같은 유형 문제(RV-01)는 놓쳤다.
- `SKILL.md` 전체(§3/§5/§7/§8/체크리스트)에서 `repository_id`/`web_url` 잔존 참조를 grep으로 확인한 결과 전부 "제거했다"는 설명 맥락에서만 등장하고, 실제 지시문에는 하나도 남아있지 않다 — 일괄 정합화 작업 자체는 꼼꼼했다.
- 설계문서 §9가 미확정 사항(1000토큰 강제 수단 없음, `project_get_context` 스키마 미검증)을 스스로 명확히 밝혀뒀다 — 없는 걸 있는 척하지 않았다.
- `project_id` 필드를 왜 남겨뒀는지에 대한 근거(§1 비고 — malgnai-hub 서버 소스를 직접 열어 확인한 사실)가 추측이 아니라 실측 기반이다.

## 생략한 부분 (정직 명시)

- 실제 malgnai-hub MCP 서버(`project_bootstrap`/`project_get_context` 등)에 대한 실기동 검증은 하지 않았다 — 이 리뷰는 로컬 파일/git 이력 대조에 한정했다(malgnai-hub 도구가 이 세션에 연결돼 있지 않음, 설계문서 §9와 동일한 제약).
- `pm-orchestration-nudge.mjs`의 실제 소스 코드는 이번 세션에서 직접 열어 STATUS.md 관련 로직 존재 여부를 확인하지 않았다 — 발산형 Rethink 제안(훅 역할 분리)의 전제는 미검증 가설로 남겨뒀다.
- UI/화면 리뷰가 아니므로 스크린샷 캡처는 수행하지 않았다(대상이 텍스트 문서·스크립트이므로 해당 없음).
- `docs/decision/pm-orchestration-block-import-design.md`에서 "대체됨" 포인터가 실제로 "제거된" 것인지, 애초에 이 브랜치에 "추가된 적이 없는" 것인지는 git 이력만으로 완전히 구분하지 못했다(위 정합성 감사관 항목 참조) — 결과 상태는 요구사항과 일치함을 확인했다.

## 실행 액션 여부

- 이번 리뷰에서 코드/문서를 수정하지 않았다. 승격·배포·병합 등 실행 액션은 수행하지 않았다(reviewer 역할 경계 — 검증만).
- 이 보고서와 페르소나 파일 4개는 이 워크트리(`agent-a1fba4b8d23d957bf`)에 커밋했다. push는 하지 않았다.

## PM 권고

1. **RV-01(Major)**: `new-project.mjs:67`의 STATUS.md 템플릿 blockquote 문구를 CLAUDE.md와 동일한 6트리거 요약으로 교체(또는 CLAUDE.md 쪽으로 통합하고 STATUS.md 쪽은 "갱신 규율은 CLAUDE.md 참조"로 단순화) — 배포 전 수정 권고.
2. **RV-02(Major)**: `SKILL.md` §3에 "이 상한/트리거는 malgnai-hub 대상 프로젝트를 우선 목표로 하며, provider: malgnai-mcp 프로젝트(이 저장소 포함)에는 소급 적용을 강제하지 않는다"는 한 줄 스코프 명시를 추가 — 설계문서 §0/§2의 원래 의도를 문면에 되살리는 저비용 수정.
3. **RV-03(Major)**: `SKILL.md` §8에 "기존 5필드 STATUS.md는 그대로 둬도 기능상 문제없다(어떤 도구도 해당 필드를 파싱하지 않음) — 다음 갱신 시점에 자연스럽게 3필드로 정리하되 급하지 않다"는 한 줄을 추가해 유지보수자의 불확실성을 없앨 것.
4. RV-04/RV-06은 다음 knowledge/pm.md 정리 세션에서 함께 처리할 백로그로 등록 권고(급하지 않음).
5. 발산형 Rethink(훅 역할 분리)는 이번 배포를 막을 사유는 아니지만, `pm-orchestration-nudge.mjs` 실소스 확인 후 별도 세션에서 근본 해법 여부를 재검토할 가치가 있다.
