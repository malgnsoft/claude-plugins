# `agents/autopilot.md` 신설 리뷰 보고서

리뷰 페르소나 패널: persona-subagent-runtime-constraint-auditor.md · persona-cross-skill-directive-conflict-auditor.md · persona-enforcement-gap-auditor.md · persona-process-mechanism-zero-based-challenger.md(발산형)
리뷰 대상: `/Users/hopegiver/workspace/claude-plugins/.claude/worktrees/agent-a526524a9c576c254/malgn-agent/agents/autopilot.md` (브랜치 `worktree-agent-a526524a9c576c254`, 커밋 `87991f2`, base `main` — 신규 1파일 +95/−0)
target_id: `autopilot-agent-new-20260919`
리스크 범주: 전역 자동실행 자산(전 직원 배포 + 사람 감독 없이 자기지속하는 오케스트레이션 계층)
리뷰 일자: 2026-09-19
모드: 최초(Full) — PM 위임에 재검토 3요소 없음 → `agents/reviewer.md` 안전측 기본값에 따라 풀패널
종합 판정: 🟡 Amber (Critical 0 · Major 7 · Minor 3 · Rethink 3)

## 요약 (2분 규칙)

trainer의 5개 주장 중 **4개는 실물 대조로 참**이다 — pm.md 포인터화(HOW 비복제), 승인 게이트 완화 금지 문구, `tools:`에서 WebFetch/WebSearch 제거 및 Edit/Write/Bash 용도 한정, hub 도구 5종 실재. 식별자·이력 금지 원칙 위반도 0건이다. 그러나 **"게이트를 무력화할 다른 우회로가 남아있지 않은지"에 대한 답은 '남아 있다'**이다: ①autopilot이 루프 지속 판단 때마다 열라고 명시한 필수 knowledge 파일 안에 *"사람 승인 게이트는 되돌리기 어려운 행동에만 둔다 — 내부 반복은 자율로 돌린다"*는 문장이 살아 있어 pm.md 권한표와 동시 준수가 불가능하고, ②등급 하향은 막았지만 **목표를 잘게 쪼개 정당한 Standard 연쇄로 만드는 경로**에는 문장이 하나도 없으며, ③회차·예산 상한과 승인 대기 상태가 **세션 경계를 넘지 못해** 자율 루프의 유일한 브레이크가 설계상 리셋된다. 여기에 더해 이 에이전트는 **어떻게 기동되는지가 문서에 없고**(서브에이전트냐 `--agent` 메인세션이냐에 따라 `AskUserQuestion` 가용성이 갈린다), **저장소 어디에도 autopilot으로 가는 라우팅 진입점이 없다**(매 세션 주입되는 PM 블록은 `malgn-agent:pm`만 안다).

## 지적 사항 (통합)

| # | 심각도 | 관점 | 위치 | 확인방법 | 문제 | 개선안 |
|---|---|---|---|---|---|---|
| M-1 | 🟠 | 런타임 제약 | `autopilot.md`:4·27·54 | `docs/anthropic/agents/sub-agents.md`:411·828·847 원문 대조 | 기동 방식이 문서에 없다. 공식문서 :411은 `AskUserQuestion`을 **모든 서브에이전트에서 `tools`에 적혀 있어도 무조건 제거**한다고 못박고, :828·:847은 플러그인 에이전트를 `claude --agent malgn-agent:autopilot`로 **메인세션 에이전트**로 띄울 수 있다고 적는다. 즉 같은 파일이 두 모드에서 능력이 갈리는데, :27("`AskUserQuestion`은 사람 승인 게이트용")과 :54(목표 미확정 시 `AskUserQuestion`으로 확정)는 서브에이전트 모드에서 **첫 단계부터 죽은 경로**다 — 전제 조건조차 못 채우고 즉시 반환한다 | "전제 조건"에 기동 방식을 1줄로 못박는다(어느 모드를 정본으로 할지 + 다른 모드에서 무엇이 달라지는지). :28이 부재를 안전하게 처리하고 있으므로 fail-safe 자체는 깨지지 않으나, 운영자가 기대하는 동작과 실제가 갈린다 |
| M-2 | 🟠 | 지시 충돌 | `hooks/pm-orchestration-block.md`:5·7 ↔ `autopilot.md`:26 / `hooks/hooks.json` | 훅 정의 전문 + 블록 전문 grep(`autopilot` 0건) | **진입점이 없다.** 매 세션 무조건 주입되는 유일한 라우팅 지시(PM 블록)가 `malgn-agent:pm`만 열거하고 autopilot을 모른다 — "목표만 줄 테니 알아서 진행해"라고 쳐도 라우팅이 autopilot으로 가지 않는다. 동시에 `hooks.json`에 matcher/조건 분기가 0이라 `--agent` 메인세션 모드에서는 블록이 그대로 주입돼 "Standard이고 후보 1~2종이면 직접 위임한다"(:5)가 autopilot의 "전문 에이전트를 직접 호출하지 않는다"(:26)와 같은 세션에 공존한다 | 진입점을 하나 정해 명시(PM 블록에 라우팅 1줄 추가 또는 `--agent` 기동 문서화). 둘 중 무엇이든 M-1과 같은 결정에 묶인다 |
| M-3 | 🟠 | 실행가능성 | `autopilot.md`:78 | `skills/common-output-storage-and-path-management/SKILL.md` §1-2 대조 + 저장소 실경로 확인 | "필수(작업 전 항상 참조)" 목록의 `agents/pm.md`가 **열리지 않는 경로 형태**다. §1-2는 "플러그인 자원을 열라고 지시할 때는 `${CLAUDE_PLUGIN_ROOT}/…` 한 형태로 적는다 / 맨 상대경로는 사용자 프로젝트에서 찾다가 `File does not exist`로 실패한다"고 규정하고, 바로 윗줄 :79는 그 규약을 지킨다. 실경로는 `malgn-agent/agents/pm.md`라 이 저장소에서도 실패한다. 승인 게이트 정본을 못 여는 것이 곧 이 설계의 안전 전제가 깨지는 지점이다 | :78을 `${CLAUDE_PLUGIN_ROOT}/agents/pm.md`로. (:16·:18의 인용형 언급은 §1-2 표의 "읽으라는 지시"가 아니므로 대상 아님) |
| M-4 | 🟠 | 지시 충돌 | `knowledge/leadership/autonomous-iteration-philosophy.md` 말미 "비용 측면 주의" ↔ `agents/pm.md`:72 (진입: `autopilot.md`:19·79) | 두 파일 전문 대조 | **남아 있는 우회로.** 그 knowledge 말미에 *"사람 승인 게이트는 **되돌리기 어려운 행동**(배포·삭제·외부 발송)에만 둔다. 내부 반복(리뷰→수정→재리뷰)은 자율로 돌린다"*가 그대로 있다. pm.md 권한표는 Sensitive/Refactor를 **가역성과 무관하게** 사람 승인 필수로 걸므로 동시 준수 불가다. autopilot.md:19는 이 파일을 "루프를 한 번 더 돌릴지 판단할 때마다", :79는 "작업 전 항상 참조"로 **승격**시켰다 — 즉 계속/정지를 정하는 바로 그 순간에 게이트를 좁히는 문장이 같이 로드된다 | 그 문장을 pm.md 권한표를 가리키도록 정정(가역성은 승인 요부의 기준이 아님을 명시). 대상 파일은 이번 diff 밖이지만 **이 diff가 그것을 필수 참조로 올림으로써 발현되는** 충돌이므로 같은 라운드에서 처리 권고 |
| M-5 | 🟠 | 강제력 격차 | `autopilot.md`:18·34 | `skills/common-task-grading-and-verification-depth/SKILL.md` 전수 grep(`쪼개\|분할\|슬라이스\|누적`) → 분할 방지 조항 0건 | **등급 하향은 막았고 목표 분할은 안 막았다.** :18이 금지하는 것은 "등급을 낮춰 게이트를 비껴가지 않는다"인데, :34는 "한 루프는 목표 1개만 다룬다"로 오히려 분할을 권장한다. 각 조각이 **진짜로** Standard면 PM 단독 승인 + Fast Path로 reviewer 없이 닫히므로, 누적하면 Sensitive급인 변경이 N개의 정당한 Standard 통과로 완성된다. 이것이 질문받은 "여러 루프를 거치며 감독·리뷰 체계를 서서히 침식시키는 경로"의 실체다 | 두 문장 중 하나: (a) 루프 분할이 등급 회피로 쓰이지 않게 하는 금지문, 또는 (b) RT-2의 누적 diff 감사 게이트. 후자가 M-6과 같은 장치로 닫힌다 |
| M-6 | 🟠 | 강제력 격차 | `autopilot.md`:19·44·46~50 | 체크포인트 4항목 전수 확인 + knowledge 종료조건 절 대조 | **브레이크가 세션 경계에서 리셋된다.** 회차 상한(3~5회)·예산·"같은 목표 2회 연속 실패"는 전부 autopilot 자신의 컨텍스트에서만 세어지는데, :46~50 체크포인트 4항목(작업트리·`nextAction`·이슈/WBS·STATUS.md 1줄)에 **누적 회차·누적 예산·연속 실패 횟수가 없다.** 그런데 :46은 "루프는 한 세션보다 오래 산다"를 전제한다 — 새 세션은 0회차로 재개하므로 상한 3종이 모두 설계상 무력화된다 | 체크포인트 항목에 누적 회차·예산·연속 실패를 추가해 `work_record`/STATUS.md로 영속화. 기계 상한이 필요하면 프론트매터 `maxTurns`(플러그인 서브에이전트에서도 무시되지 않음 — `sub-agents.md`:234 무시 목록은 `hooks`/`mcpServers`/`permissionMode`뿐) |
| M-7 | 🟠 | 강제력 격차 | `autopilot.md`:28 ↔ `agents/pm.md` "`AskUserQuestion`을 쓸 수 없는 실행" 3항 | 두 문단 나란히 대조 | **승인 대기 상태가 한 곳에만 산다.** pm.md 3항은 반환문 ①~④에 더해 "같은 내용을 `issue_record`로 남겨 **반환문이 요약 과정에서 유실돼도** 추적 가능하게 한다"를 요구한다. autopilot :28은 같은 상황에서 반환문 ①~④만 요구하고 그 영속화 의무를 물려받지 않았다. 세션 전환 절차(:48)에도 "승인 대기 건" 항목이 없어, 승인 대기 중 롤오버가 나면 무엇이 왜 멈췄는지가 사라진다 | :28에 pm.md 3항과 같은 `issue_record` 의무를 추가하고, :48 체크포인트 항목에 "열린 승인 대기 건"을 포함 |
| m-1 | 🟡 | 지시 충돌 | `malgn-agent/knowledge/README.md`:54 | grep `autonomous-iteration-philosophy` 전수(참조처 2곳) | 그 항목이 "(`agents/pm.md` 실참조)"라고 단정하는데 이제 `agents/autopilot.md`도 실참조한다. 저장소 [변경이력 관리 원칙]의 "정본을 고칠 때 그것을 인용·요약하는 다른 자리를 같은 커밋에서 함께 정정" 대상 | 실참조처를 둘 다 적는다 |
| m-2 | 🟡 | 강제력 격차 | `autopilot.md`:27 | pm.md:57과 동형 확인 | `Bash` "읽기 전용 검증용", `Edit`/`Write` "STATUS.md 체크포인트용"은 **산문 약속일 뿐** 기계 장치가 없다. `disallowedTools`가 프론트매터에 실재하지만 쓰이지 않았다. pm.md와 동형이라 신규 결함은 아니나, 사람이 매 턴 보지 않는 자율 루프에서는 같은 약속의 비용이 다르다 | 판단 대상 — 최소한 `permissionMode`는 플러그인에서 무시되므로(`sub-agents.md`:234) `disallowedTools` 쪽만 선택지임을 인지 |
| m-3 | 🟡 | 실행가능성 | `autopilot.md`:78 | `pnpm run check-assets` 출력(pm.md 36.2 KB) | 36.2 KB 파일을 "작업 전 항상 참조"로 걸었는데 실제로 쓰는 것은 권한 참조표 1개와 4개항이고, 그 요지는 :16·:18에 이미 요약돼 있다. 매 루프 재독 비용이 크다 | "권한 참조표를 여는 조건"으로 좁히는 편이 같은 효과에 싸다(RT-3 참조) |

## 기각된 지적

| 관점 | 지적 요지 | 처리 | 사유 |
|---|---|---|---|
| 런타임 제약 | 서브에이전트 spawn depth 한계(3층)로 autopilot→PM→전문가 체인이 끊긴다 | 자체 기각 | `sub-agents.md`:982 확인 — 기본 3층에서 autopilot(1)→pm(2)→전문가(3)가 정확히 들어맞고, 깊이 한계에서 제거되는 `Agent`는 전문가 에이전트들이 애초에 갖고 있지 않다(`tools:` 전수 확인 — `Agent` 보유는 pm·autopilot 2종뿐). 발현되지 않는 제약 |
| 지시 충돌 | hub 도구명(`agent_learning_record` 등)이 실재하지 않는다 | 자체 기각 | 스키마 원문 직접 조회 — `work_record`(`nextAction` 필드 포함)·`agent_learning_record`·`project_get_context`(`sections` enum에 state/issues/wbs 포함)·`issue_resolve`·`decision_record`/`issue_record` 전부 실재. trainer 주장 #5는 참 |
| 실행가능성 | `README.md`·`plugin.json`·`marketplace.json`의 "에이전트 21종" 불일치(`check-assets` ERROR 4건), `README.md` 에이전트 표에 autopilot 행 없음, MD 11.8 KB(권고 10 KB 초과) | 스코프 제외 | PM이 위임 시 "별도 처리할 미결 사항"으로 기명 제외. `check-assets`의 ERROR 4건은 **전부** 이 개수 드리프트이며, 그 외 신규 ERROR는 0건(main 대비 WARN +1도 크기 항목) |
| 지시 충돌 | `pm-orchestration-block.md`:9("승인은 사람과 대화하는 이 세션이 직접 받는다")가 autopilot 호출 시 성립하지 않는다 | 기각 | 두 모드 모두 성립한다 — 서브에이전트 모드에서는 블록이 autopilot에 주입되지 않고 :18이 상위로 올리며, 메인세션 모드에서는 `AskUserQuestion`이 살아 있어 블록 문면대로 동작한다. trainer 주장 #4는 이 지점에서 참 |

## 페르소나별 관점

### [런타임 제약 감사관] — 판정: 🟠 Amber
`tools:` 선언과 실제 가용 도구를 공식문서로 대조한 결과, **모드가 확정되지 않은 것이 유일하되 결정적인 문제**다. `Bash`/`Edit`/`Write`/`Agent`/MCP는 배경 서브에이전트 2차 필터(`sub-agents.md`:418 잔존 목록)에도 전부 포함돼 어느 모드에서나 살아 있다. 갈리는 것은 `AskUserQuestion` 하나인데, 하필 그것이 이 에이전트의 안전 설계 전체가 매달린 도구다. :28이 "기준은 frontmatter 선언이 아니라 지금 이 실행에서 실제로 호출 가능한 도구 목록이다"라고 정확히 적은 것은 **이 라운드에서 가장 잘 쓰인 문장**이다 — pm.md가 실측으로 얻은 교훈이 제대로 이식됐다.

### [지시 충돌 감사관] — 판정: 🔴→🟠 (강등)
인용망 전수 대조 결과 **빈 포인터 0건**이다 — autopilot.md가 지목한 Skill 6종, knowledge 1종, 절 앵커 4종(`project-orchestration` §4.1/§4.2, `domain-git-safety-and-concurrency` §1-1, `project-standards` §3, `common-learning-loop-knowledge-management` "기록 도구를 쓸 수 없을 때") 전부 실재를 확인했다. 충돌은 **필수 참조로 승격된 문서 안**에 있었다(M-4). 당초 Critical로 올렸으나, autopilot.md:18의 금지 문구가 명시적·비협상형이고 :19의 참조가 "종료 조건"으로 범위를 한정해 두어 동시 준수 실패가 확정적이지 않으므로 Major로 강등한다. 다만 **해석에 맡겨진 상태**라는 점은 변하지 않는다.

### [강제력 격차 감사관] — 판정: 🟠 Amber
"멈출 줄 안다"는 원칙이 **어디에도 물려 있지 않다**. 이 축에서 이 저장소는 여섯 라운드 연속 같은 결론에 도달했는데, 이번 대상은 성격이 다르다 — 이전 라운드들의 대상은 사람이 매 턴 보고 있는 메인 세션 규율이었고, 이번은 사람이 보지 않는 동안 스스로 돌아가는 루프다. 같은 "강제력 0"의 비용이 훨씬 크다. 반면 이번에는 기계 장치가 실제로 존재한다는 점이 다르다: `maxTurns`(프론트매터, 플러그인에서 무시되지 않음)와 `work_record.nextAction`(영속 필드). 지시를 늘리지 않고 필드 2개로 닫을 수 있는 구간이다.

### [제로베이스 도전자(발산형)] — 판정: 🔵 아래 별도 섹션

## 구조적 제언 (Rethink) — 발산형 페르소나 🔵

| # | 현재 구조 | 제안 구조 | 왜 더 나은가 | 예상 비용/리스크 |
|---|---|---|---|---|
| RT-1 | 정지 조건 4종(수확 체감·회차 상한·예산·2회 연속 실패)이 전부 **계속할 유인을 가진 당사자의 자기판단**에 놓이고, 그 상태값은 컨텍스트에만 산다 | 외부 회로차단기 3종 중 택1~2: ①프론트매터 `maxTurns`로 기계 상한(초과 시 partial 반환) ②`work_record.nextAction`에 누적 회차·예산을 실어 세션 밖에서 세어지게 ③N루프마다 사람 재인가 | 지시 문장을 늘리지 않고 **영속 필드·하네스 필드**로 닫는다. M-6이 같은 장치로 해소된다. 현재 구조는 세션이 바뀌는 순간 상한이 0으로 돌아가므로, 오래 도는 루프일수록 브레이크가 약해지는 역방향 특성을 갖는다 | 낮음 — `maxTurns`는 프론트매터 1줄, `nextAction` 서술은 기존 :41 RECORD 단계 안에서 해결 |
| RT-2 | VERIFY(:40)가 **루프 1회분 diff**만 본다. 누적 blast radius를 보는 눈이 파이프라인 어디에도 없다 | N루프마다(또는 같은 모듈 3회 이상 연속 변경 시) **그 구간 전체 diff**를 PM에 넘겨 등급을 재판정받는 누적 감사 게이트 | M-5(목표 분할로 Sensitive를 Standard 연쇄로 통과)와 RT-1이 하나의 장치로 닫힌다. 지금은 "각 조각이 정당하게 Standard"라는 사실 자체가 방어를 무력화하는데, 누적 단위로 재판정하면 그 논리가 성립하지 않는다 | 중간 — 루프마다 비용이 드는 게 아니라 N회마다 1회이므로 토큰 영향은 작으나, "N"과 트리거 조건을 정하는 설계 판단이 필요(변경동결 원칙상 사용자 판단 사안) |
| RT-3 | `agents/`의 22번째 **서브에이전트**로 두되, 본문은 "호출자: 사용자 직접(최상위)"이라 적어 두 모드 사이에 걸쳐 있다 | `claude --agent malgn-agent:autopilot` **메인세션 프로필**로 형태를 확정 | 그래야 ①`AskUserQuestion`이 살아 루프가 승인 지점에서 죽지 않고 ②`maxTurns`·`isolation` 같은 프론트매터가 의미를 갖고 ③"최상위 계층"이라는 본문 주장과 실제 실행 위치가 일치한다. 현재 형태는 능력이 기동 방식에 따라 갈리는데 그 기동 방식이 문서에 없다(M-1). 덧붙여 :78의 36.2 KB 필수 재독도 "권한 참조표를 여는 조건"으로 좁히는 편이 같은 효과에 싸다 | 낮음(문서 결정) — 다만 이 모드에서는 PM 블록이 함께 주입되므로 M-2의 충돌 처리가 선행돼야 한다 |

## 트레이드오프 (페르소나 간 충돌)

- **런타임 제약 감사관 vs 발산형**: 전자는 "서브에이전트 모드를 전제하고 :28의 fail-safe를 유일 경로로 명시하라"(안전 최대), 후자는 "메인세션 모드로 확정하라"(기능 최대 — 승인을 받아 루프를 이어갈 수 있음)를 권한다. → **권고: 후자(메인세션)**. 전자를 택하면 autopilot은 사람 승인이 필요한 순간마다 죽고, Sensitive/Refactor 목표는 한 번도 완주하지 못해 자율 루프의 가치가 Micro/Standard로 한정된다. 다만 그 선택은 M-2(PM 블록 동시 주입) 해결을 전제로 한다.
- **강제력 격차 감사관 vs 에이전트 업그레이드 원칙(1순위 성능, 2순위 토큰)**: 전자는 체크포인트·기록 항목 추가를 권하고 이는 상시 비용 순증이다. → **권고: 추가한다.** RT-1의 수단은 산문이 아니라 필드(`maxTurns` 1줄, `nextAction` 서술 편입)라 바이트 증가가 작고, 막는 실패(감독 없는 무한 루프)의 비용이 비대칭적으로 크다.

## 잘 된 점 (유지할 패턴)

- **:28의 "기준은 frontmatter 선언이 아니라 지금 이 실행에서 실제로 호출 가능한 도구 목록이다"** — pm.md가 실측(`No such tool available`)으로 얻은 교훈이 새 자산에 정확히 이식됐다. 이 한 문장이 M-1을 결함에서 fail-safe로 낮춘다.
- **:16의 HOW 비복제 원칙과 그 실행** — 등급 판정·팀 구성·WBS·위임 프롬프트·검증 절차를 한 줄도 옮겨 적지 않고 pm.md 포인터로 처리했다(trainer 주장 #1 참). 이 저장소가 반복적으로 당해 온 "두 벌이 되어 조용히 갈림"을 설계 단계에서 차단했다.
- **:18의 게이트 비협상 문구** — "완화하거나 / 자기 판단을 사람 승인으로 대신하거나 / 등급을 낮춰 비껴가지 않는다(강화만 가능)"는 pm.md:61의 로컬 정의 우선 조항과 문면이 정확히 맞물린다. "PM이 승인 대기로 반환하면 재위임 대상이 아니라 루프 정지 신호"도 pm.md 4항과 모순 없음을 원문 대조로 확인(trainer 주장 #2는 **적힌 범위 안에서는 참**이며, 남은 것은 M-4·M-5의 문서 밖 우회로다).
- **도구 권한 사유 서술이 pm.md:57과 동형** — WebFetch/WebSearch 제거, "도구를 가졌다는 사실이 경계를 열어주지 않는다"까지 동일 구문(trainer 주장 #3 참).
- **제품 본문 저작 규율 위반 0건** — 8자리 hex·ULID·커밋 해시 0건, 날짜 도장·이관 경위·버전 언급 0건(:42·:86의 "라운드"는 일반어). 빈 포인터 0건.

## 평가기준 충족 현황

| 기준 | 관점 | 중요도 | 충족 | 비고 |
|---|---|---|---|---|
| 지시가 명령하는 도구가 실행 컨텍스트에 실재하는가 | 런타임 제약 | 필수 | △ | `AskUserQuestion`만 모드 의존(M-1) |
| 포인터가 실재 절을 가리키는가(빈 포인터 0) | 지시 충돌 | 필수 | ✅ | Skill 6·knowledge 1·절 앵커 4·hub 도구 5 전수 실재 |
| 필수 참조 문서와 동시 준수가 가능한가 | 지시 충돌 | 필수 | ❌ | M-4 |
| 상시 주입 표면과 충돌하지 않는가 | 지시 충돌 | 필수 | ❌ | M-2 |
| 원칙 문장이 자기판단 밖에서 강제·관측되는가 | 강제력 격차 | 권장 | ❌ | M-5·M-6·M-7 (강제력 0) |
| 승인 게이트가 완화되지 않는가(문면) | 강제력 격차 | 필수 | ✅ | :18 |
| 제품 본문 저작 규율(식별자·이력 금지) | 전 관점 | 필수 | ✅ | 위반 0건 |
| 정적 검사 신규 ERROR 0 | 전 관점 | 필수 | ✅ | ERROR 4건 전부 스코프 제외된 개수 드리프트 |

## PM에게 권고

1. **병합 전 수정 필요(trainer 반환)**: M-3(경로 형태 1줄) · M-7(`issue_record` 의무 + 체크포인트 항목) · m-1(knowledge/README 실참조 표기). 셋 다 국소·명백한 결함이라 설계 판단이 갈리지 않는다.
2. **사용자 판단 필요(설계 결정)**: M-1·M-2가 한 덩어리다 — "autopilot을 서브에이전트로 둘 것인가, `--agent` 메인세션 프로필로 둘 것인가". 이 결정 없이는 :27·:54가 참인지 거짓인지 확정되지 않고 라우팅 진입점도 정할 수 없다. 발산형 권고는 메인세션 확정이다.
3. **같은 라운드에서 처리 권고**: M-4는 이번 diff 밖(`knowledge/leadership/autonomous-iteration-philosophy.md`)이지만, **이 diff가 그 문서를 루프 지속 판단의 필수 참조로 승격시킴으로써 비로소 발현되는** 충돌이다. 저장소 [백로그 처리 원칙]의 "명백한 결함 + 이미 열어본 파일 + 국소 반영"에 해당한다.
4. **백로그 후보(변경동결 보류 대상)**: M-5·M-6 + RT-1·RT-2. 기계 수단(`maxTurns`·`nextAction`)이 실재하므로 "미확인 추정치"가 아닌 저비용 항목이나, 어느 장치를 어디에 걸지는 구조 판단이라 사용자 승인이 필요하다.
5. **이 보고서는 리뷰만 수행했다 — 파일 수정·커밋·병합·배포 실행은 하지 않았다.** 워크트리 `agent-a526524a9c576c254`는 커밋 `87991f2` 상태 그대로다. 이번 라운드에서 reviewer가 쓴 파일은 `docs/reviewer/` 아래 페르소나 적용이력 4건 + INDEX.md 갱신 + 이 보고서뿐이다.

## 생략한 관점 / 미확인

- **화면 리뷰 없음** — 대상이 에이전트 MD 1개(UI 없음)라 `docs/screenshots/` 캡처는 해당 사항 없음.
- **실기동 미검증** — `claude --agent malgn-agent:autopilot`을 실제로 띄워 `AskUserQuestion` 가용성을 재현하지 않았다. 근거는 저장소가 보유한 공식문서 미러(`docs/anthropic/agents/sub-agents.md`:411·828·847) 원문 인용이며, **실행 재현이 아니라 사양 대조**다. M-1의 두 모드 동작은 이 수준까지만 확인됐다.
- **evaluator 판정 미수행** — 이 보고서는 reviewer 패널 결과이며 승격 판정(게이트 체크리스트)은 evaluator 소관이다.

---

# 2차 (축소 재검증) — 2026-09-19

- **대상 델타**: `87991f2..34eead3` (워크트리 `.claude/worktrees/agent-a526524a9c576c254`, 브랜치 `worktree-agent-a526524a9c576c254`)
- **모드**: 축소(C). 재검토 3요소 충족 · 풀패널 강제 승격 조건 비해당 · 동일대상 4조건 충족 확인
- **판정**: 델타 자체 🟢 **Green** / 자산 전체 🟡 **Amber**(차단 사유 0건, 스코프 밖 Major 1건 잔존)

## 페르소나 재사용 판정

| 페르소나 | 재사용/신규 | 사유 |
|---|---|---|
| persona-subagent-runtime-constraint-auditor.md | 재사용 | M-1 담당. 역할개념(실행 컨텍스트의 도구 실재) 그대로 |
| persona-cross-skill-directive-conflict-auditor.md | 재사용 | M-3·M-4 담당. 역할개념(정본 간 동시 준수 가능성) 그대로 |
| persona-enforcement-gap-auditor.md | 재사용 | M-5·M-6·M-7 담당. 역할개념(선언 vs 강제) 그대로 |
| persona-process-mechanism-zero-based-challenger.md | 재사용(발산형 슬롯) | 재설계의 비례성 재질문. 신규 리스크 표면 없음 |

신규 0건 — INDEX.md 역할개념 열 대조 결과 이번 확인 항목 전부가 위 4개 역할개념 안에 들어감.

## 1차 지적 해소 현황

| ID | 상태 | 확인 근거(원문 대조) |
|---|---|---|
| M-1 | ✅ 해소 | frontmatter `:4`에서 `AskUserQuestion` 제거. 죽은 경로 2곳(구 :27·:54)이 `:31`(멈추되 묻지 않는다)·`:57`(전제조건)로 재작성. "멈춤 ≠ 진행" 구분은 `:31`("실행하지 않고 멈춘다")·`:32`("승인이 떨어지기 전에는 그 행위를 실행하지 않는다")·`:48`("되묻고 기다리는 것이 아니라 남기고 끝낸다")·`:67` 4중으로 명시 — 오독 여지 없음 |
| M-3 | ✅ 해소 | 읽기 지시 4곳(`:16`·`:18`·`:83`·`:84`) 전부 `${CLAUDE_PLUGIN_ROOT}/…`. 맨 상대경로 0건(grep 전수) |
| M-4 | ✅ 해소(autopilot 경로) | `:21` 종속 단서 + `:84` 반복 + `knowledge/README.md:54` 표기. 잔여: 충돌 문장 원문(`autonomous-iteration-philosophy.md:119`)·`pm.md:150` 참조는 선존재 상태 그대로 → 백로그 |
| M-5 | ✅ 해소 | `:19`(원칙)·`:42`(DECIDE)·`:66`(체크리스트) 3중 배치 |
| M-6 | ✅ 해소 | `:22`·`:45`·체크포인트 ⑤·`:69`. 읽기 경로 성립 확인 — hub 스키마상 `work_record.nextAction` → `project_get_context.state.nextAction`, `:40` OBSERVE가 `state`를 당김 |
| M-7 | ✅ 해소 | `:32`가 pm.md와 동일한 `issue_record` 의무·동일 사유로 수용. 체크포인트 ⑤에 "열려 있는 사람 판단 대기 건" 추가 |
| M-2 | ⏸ 스코프 밖 | 전반부(기동 모드 선택)는 사용자 결정으로 무의미화. 후반부(진입점·라우팅)는 아래 잔여 |

## 이번 라운드 신규 지적

| ID | 심각도 | 위치 | 확인방법 | 내용 |
|---|---|---|---|---|
| R2-1 | 🟠 Major(스코프 밖·비차단) | `hooks/pm-orchestration-block.md` 전문 / `README.md`:47 | `grep -rn autopilot malgn-agent/hooks/` = 0건 | 진입점 미해결. 매 세션 주입되는 유일한 라우팅 지시가 `malgn-agent:pm`만 열거하고 autopilot을 모른다 — "목표만 줄 테니 알아서 진행해"가 autopilot으로 라우팅되지 않고, `autopilot.md:26`은 PM→autopilot 호출을 금지한다. 사용자가 이름을 직접 부르면 동작하므로 배포 차단은 아님 |
| R2-2 | 🟡 Minor | `autopilot.md`:32 | 구 `:28` 삭제분과 `:45` 폴백 대조 | 새 정지 절차가 `issue_record`(MCP)를 의무화하면서 그 도구 부재 시 폴백을 스스로 갖지 않음. `:45` RECORD의 일반 폴백이 간접 커버하고 전제조건이 hub projectId 확보를 요구해 위험은 낮음 |
| R2-3 | 🟡 Minor(선존재·백로그) | `knowledge/leadership/autonomous-iteration-philosophy.md`:119 ↔ `agents/pm.md`:150 | 두 파일 원문 대조 | M-4의 충돌 문장이 원문 그대로 남아 있고 pm.md 참조에는 종속 단서가 없다. pm.md가 권한표 자신의 정본이라 동시 준수 불가로는 발현되지 않음 |
| R2-4 | ⚪ Nit | `autopilot.md`:45 ↔ `:40` | hub 스키마 원문 | `:45` "여기에 싣는다"가 `nextAction` 아닌 `summary`로 해석되면 `:40`의 sections 예시(state/issues/wbs)에 `recentWork`가 없어 새 세션이 카운터를 못 읽는다 |

## 기각된 지적

- **"서브에이전트 모드에서 PM 행동규율 블록이 autopilot과 공존해 `:26`과 충돌한다"(1차 M-2 후반부)** — 기각. 블록은 `hooks.json`의 SessionStart 훅이 주입하는 세션 이벤트 산물이고, 서브에이전트가 자기 사본으로 로드하는 것은 CLAUDE.md뿐이다(`docs/anthropic/claude-code/context-window.md`:253·258). 이 저장소 CLAUDE.md는 블록 사본을 두지 않는다(규약상 금지). 서브에이전트 확정 하에서는 공존 자체가 성립하지 않는다.

## PM 처리분 검증 (커밋 `34eead3`)

| 항목 | 결과 |
|---|---|
| 에이전트 22종 동기화 | ✅ `ls agents/*.md` = 22, `check-docs` agents 문서=22 실측=22 |
| README 분류표 `autopilot` 행 | ✅ `:47` "최상위 자율 루프 \| `autopilot`" — 본문 `description`("최상위 자율 루프")과 일치 |
| plugin.json / marketplace.json | ✅ 양쪽 모두 "22종", version 1.8.44 동일 |
| CLAUDE.md 서술 | ✅ "autopilot 1종을 제외한 전원 pm.md 기준 위임모델" — 실물과 일치 |
| 예산 사유서 | ✅ `docs/refactor/autopilot-budget-rationale.md` 실재, 선언 16,099 B = 실측 16,099 B 일치 |
| 정적 검사 | ✅ `check-assets` ERROR 0 · `check-docs` 3/3 (이 워크트리에서 재실행) |

## PM에게 권고

1. **병합 가능** — 1차 Major 7건 중 6건 해소 확인. 남은 1건(R2-1)은 스코프 밖이자 비차단.
2. **R2-1은 사용자 판단 백로그** — 진입점을 블록에 1줄 추가할지, 이름 호출/`--agent` 기동만 문서화할지는 상시비용이 걸린 구조 판단이다(변경동결 원칙 보류 대상).
3. **의도된 경계 보존** — `:19`+`:33` 결합으로 Sensitive/Refactor 목표는 autopilot이 구조적으로 완주할 수 없다. 이는 결함이 아니라 사용자 결정의 귀결이므로, 이후 "루프가 자꾸 멈춘다"는 증상으로 이 문장들을 완화하지 말 것.

## 생략한 관점 / 미확인

- **실기동 미검증** — 1차와 동일. `AskUserQuestion` 제거는 frontmatter 원문 + 공식문서 미러 대조이며 실행 재현이 아니다.
- **M-2 전면 재검토 생략** — PM 결정에 따름(위 기각 항목만 사양 대조로 처리).
- **화면 리뷰 없음** — UI 산출물이 아님.

