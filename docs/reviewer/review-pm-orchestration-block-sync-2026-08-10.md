# PM 오케스트레이션 블록 상시동기화 구현 — 커밋 전 최종 검증

리뷰 페르소나 패널 (2026-08-09 라운드에서 이미 산출·검증된 자산 재활용 — 동일 파일군에 대한 후속 라운드이므로 관심사·평가방법론을 이번 diff 대조로 확장해 그대로 사용):
- `docs/reviewer/personas/persona-skeptical-verification-architect.md`
- `docs/reviewer/personas/persona-ops-maintainability-realist.md`
- `docs/reviewer/personas/persona-zero-based-redesigner.md` (발산형)

리뷰 대상 (전부 워킹트리 미커밋, `git diff`/`git status`로 실물 대조):
- `malgn-agent/hooks/pm-orchestration-nudge.mjs` (수정, +91/-38줄 상당)
- `malgn-agent/hooks/pm-orchestration-block.md` (수정, 버전마커 도입)
- `malgn-agent/bin/new-project.mjs` (수정, `.claude/settings.json` 스캐폴드 추가)
- `docs/decision/pm-orchestration-block-sync-strategy.md` (신규, 설계 근거)
- `malgn-agent/hooks/hooks.json` — **미변경 확인**(diff 없음, `git log -1`로 최신 커밋 해시가 이번 작업 이전임을 확인)

등급: Sensitive(전사 배포 플러그인의 SessionStart 훅 로직 변경 + 확정 안전장치 대상) → 풀 패널 적용
리뷰 방법: 정적 코드 대조(Read/git diff) + **실제 Node 실행으로 5가지 마커 상태 시나리오를 시뮬레이션**(무마커/installed/declined-저버전/declined-고버전/구버전 마커) — 이번 라운드는 정규식·버전비교 정확성이 핵심 검증 포인트라 정적 추론에 의존하지 않고 실행 검증함
리뷰 일자: 2026-08-10
종합 판정: 🟡 **Amber — GO-with-fix**. Critical 없음. 설계 의도 1~5는 신규/정상 경로(무마커 신규설치, installed 상시주입, declined 상태유지·버전비교)에서 모두 정확히 구현·실행 검증됨. 다만 **상태 전이 두 갈래**(구버전 마커 프로젝트의 인식, declined→installed 전환 시 마커 처리)에서 실행 검증으로 확인된 회귀/버그가 있어 병합 전 수정을 권고.

## 요약 (2분 규칙)
핵심 설계(마커만 남기고 본문은 매 세션 훅이 직접 읽어 주입)는 실행 시뮬레이션으로 정확히 동작함을 확인했다 — installed 상시주입, declined 버전비교(`N > X`) 방향 모두 통과. 그러나 두 가지 Major 결함을 실행/코드 대조로 발견했다: (1) **구버전(2026-08-09 이전) 방식으로 이미 설치된 프로젝트의 installed 마커**(`<!-- malgn-agent:pm-orchestration:v1 -->`, "installed:" 리터럴 없음)를 새 정규식이 인식하지 못해 "무마커"로 오판, 매 세션 재넛지가 발생하고 사용자가 다시 "예"를 누르면 구버전 전문 복사본이 영구히 남은 채 신규 마커+상시주입이 겹쳐 쌓인다 — 이번 리팩터가 없애려던 "stale copy" 문제가 정확히 이 경로로 재발한다. (2) **declined→installed 전환 시**(버전 개정으로 재넛지된 뒤 사용자가 "예"로 답함) 넛지 문구가 기존 declined 마커를 "교체"하라고 지시하지 않고 새 installed 마커를 그냥 "추가"하라고만 지시한다 — 두 마커가 공존하면 정규식이 앞쪽(기존 declined)을 먼저 매치해 이후 영구히 "여전히 거절 상태"로 오판할 수 있다.

## 지적 사항 (통합)

| # | 심각도 | 관점 | 위치 | 문제 | 개선안 |
|---|-------|------|------|------|--------|
| 1 | 🟠 Major | 회의적 검증 설계 / 운영 현실 | `hooks/pm-orchestration-nudge.mjs:23` `STATE_MARKER_RE = /<!--\s*malgn-agent:pm-orchestration:(installed\|declined):v(\d+)\s*-->/` | 2026-08-09 이전 방식으로 이미 설치된 프로젝트는 installed 마커가 `<!-- malgn-agent:pm-orchestration:v1 -->`(`installed:` 리터럴 없음) 형식이라 이 정규식에 매치되지 않는다. 실제로 Node 실행 시뮬레이션(구버전 마커+전문 복사본이 있는 CLAUDE.md)으로 재현 확인: 새 훅이 "무마커"로 판정해 AskUserQuestion 재넛지를 다시 띄운다. 사용자가 "예"로 재승인하면 구버전 전문 복사본은 그대로 남고 그 위에 새 `installed:v1` 마커+상시주입이 얹혀, CLAUDE.md에 동일 내용이 이중으로 존재하게 된다 — "stale copy 근절"이라는 이번 리팩터의 목적이 이 경로에서 무력화됨. 사용자 지시상 "마이그레이션 자체는 스코프 밖"이지만, 이건 미이관이 아니라 **재넛지·중복 발생이라는 새로운 회귀**라 감점 대상(사용자 지시 원문: "다만 회귀 위험이 있으면 지적") | `STATE_MARKER_RE`에 구버전 패턴(`installed:` 없는 `pm-orchestration:v(\d+)`, `declined:` 아닌 경우)을 별도로 매치해 "installed(legacy)"로 인식하고 재넛지를 하지 않는 분기를 추가한다. 최소 수정: 구버전 마커를 만나면 `installed:v{그버전}`과 동등하게 취급(상시주입은 하되, "구버전 전문 복사본이 CLAUDE.md에 남아있을 수 있으니 정리를 원하면 제거해도 된다"는 1회성 안내만 추가) |
| 2 | 🟠 Major | 회의적 검증 설계 | `hooks/pm-orchestration-nudge.mjs:69-77`(`askInstallNudge` 반환문, 특히 72행 `"예":` 지시) — line 91(무마커 호출)과 line 113(declined 재넛지 호출)이 **동일한 함수·동일한 "예" 문구를 공유** | "예" 응답 시 지시문이 "마커 한 줄만 **추가**한다"로만 되어 있다. 무마커(최초 설치) 경로에서는 문제없지만, declined 재넛지 경로(line 108-116, `state !== 'installed'`이고 `block.version > markedVersion`인 경우)에서는 **기존 declined 마커가 CLAUDE.md에 이미 존재**한다 — 그런데 "아니오" 지시문(74행)은 "기존 거절 마커가 있다면 새 버전으로 **교체**"라고 명시하는 반면 "예" 지시문은 기존 declined 마커를 제거/교체하라는 말이 전혀 없다. 모델이 문구 그대로 새 installed 마커만 "추가"하면 CLAUDE.md에 `declined:vX`와 `installed:vY`가 동시에 남는다. `STATE_MARKER_RE.match()`는 문자열상 먼저 나오는 매치를 반환하므로(대개 기존 declined 마커가 파일에서 더 앞쪽), 다음 세션부터 훅이 **영구히 "여전히 거절 상태"로 오판**해 사용자가 분명히 "예"라고 답한 프로젝트가 상시주입을 영원히 못 받을 수 있다 | `askInstallNudge`에 `reinstall` 컨텍스트가 있을 때는 "예" 문구도 "기존 거절 마커(`declined:vX`)를 지우고 아래 마커로 교체한다"로 분기해 명시한다. 최소 수정으로 정확성 확보 가능(같은 함수 안에서 `reinstall` 유무로 "예" 텍스트도 분기) |
| 3 | 🟡 Minor | 운영 현실 | `bin/new-project.mjs:136-143` 신규 `.claude/settings.json` 스캐폴드 | 마켓플레이스 이름(`malgnsoft-plugins`)·repo(`hopegiver/claude-plugins`)를 하드코딩 — `marketplace.json`의 실제 `name` 필드와 대조해 정확함을 확인했으나, 이 값이 향후 바뀌면(예: repo 이전) 이 스캐폴드가 조용히 깨진다 | 당장 수정 불필요(현재 정확), 다만 `marketplace.json`의 `name`/repo가 바뀌는 작업을 할 때 이 파일도 같이 갱신해야 함을 `.claude/doc-drift.json` 류 매니페스트나 커밋 체크리스트에 남겨두면 향후 드리프트를 잡기 쉬움 |
| 4 | ⚪ Nit | 검증 설계 | `hooks/pm-orchestration-block.md:1` 버전규칙 설명 주석 | 설명 주석이 다소 길다("이 파일이 단일 소스... 훅이 매 세션 이 파일을 직접 읽어...") — `readBlockFile()`이 이 줄을 본문에서 정확히 제외하는 것은 실행 확인했으므로 기능상 문제는 없음. 순수 가독성 취향 | 커밋을 막을 사유 아님. 참고만 |

## 페르소나별 관점

### 회의적 검증 설계자 — 판정: 🟠 Amber
설계 문서(`docs/decision/pm-orchestration-block-sync-strategy.md`)가 스스로 인정한 트레이드오프("자기완결성 포기")는 실제 구현에서 그대로 반영됐고, 정직하게 캐벗으로 남아있다 — 이 부분은 신뢰 가능. 그러나 "구현이 설계 의도를 실제로 만족하는가"를 상태 전이 다이어그램으로 재구성해보면 두 개의 간극(#1 구버전 인식, #2 declined→installed 마커 정리)이 드러난다. 둘 다 "정상 경로"(무마커 최초설치, installed 유지, declined 유지)에서는 드러나지 않고 **경계 전이**(레거시 데이터 마이그레이션, 상태 변경 시점)에서만 나타나는 전형적인 상태기계 버그 패턴이라, 코드를 한 번 읽는 것만으로는 놓치기 쉽다 — 이번에는 실제 5가지 시나리오를 Node로 실행해 재현했기 때문에 잡을 수 있었다.

### 운영/유지보수 현실주의자 — 판정: 🟠 Amber
"6개월 후에도 이게 살아있는가"의 관점에서 #1이 특히 아프다 — 이 플러그인은 실제로 어제(2026-08-09) 리뷰가 Green/GO로 끝나 전사에 배포됐을 가능성이 있고, 그 시점에 이미 몇몇 프로젝트가 구버전 마커로 "설치"를 완료했을 수 있다. 그 프로젝트들이 이번 배포 이후 매 세션 재넛지를 받게 되는 것은 "재넛지 피로 → 대충 수락" 패턴(설계 문서 자신이 옵션 (a)의 단점으로 이미 경고한 바로 그 현상)을 이번 배포가 스스로 촉발하는 역설적 결과다. `.claude/settings.json` 스캐폴드(#3)는 하드코딩이지만 현재 값이 실측과 일치하므로 당장의 위험은 낮게 평가한다.

### 제로베이스 재설계자 — 판정: 해당없음 (🔵 Rethink, RAG 미산정)
아래 "구조적 제언" 참조. 2026-08-09 라운드에서 이미 제시한 "결정론적 슬래시커맨드" 대안을 이번 라운드에서 발견된 구체적 증거(#2)로 재확인·강화함.

## 구조적 제언 (Rethink) — 발산형 페르소나 🔵

| # | 현재 구조 | 제안 구조 | 왜 더 나은가 | 예상 비용/리스크 |
|---|----------|----------|------------|----------------|
| 1 | 마커 읽기·쓰기 전체가 "훅이 프로즈로 지시 → 모델이 AskUserQuestion으로 묻고 → 모델이 스스로 Edit으로 마커를 추가/교체"하는 자연어 오케스트레이션. 마커의 "추가"와 "교체"를 구분하는 책임이 매번 프롬프트 문구 정확성에 달려있다 | 플러그인이 결정론적 설치 커맨드(예: `/malgn-pm-sync`)를 함께 제공한다. 이 커맨드가 CLAUDE.md를 읽어 현재 마커 상태를 코드로 파싱하고, 상태 전이(무→installed, declined→installed, installed 버전유지 등)마다 정확한 치환 로직(기존 마커를 정규식으로 찾아 **교체**, 없으면 **추가**)을 코드로 실행한다. 넛지는 AskUserQuestion으로 동의만 받고, 실제 파일조작은 커맨드에 위임 | 이번 라운드에서 발견된 #2(declined→installed 전환 시 마커 중복)가 바로 "기계적 쓰기 동작을 자연어 지시에 맡긴" 데서 나온 버그다 — "추가"와 "교체"를 코드가 정규식 치환으로 처리하면 이런 종류의 간극 자체가 발생할 수 없다. #1(구버전 인식) 역시 커맨드 안에 레거시 패턴 매칭을 한 곳에 모아두면 훅의 넛지 문구와 별개로 유지보수하기 쉬워진다 | 비용: 슬래시 커맨드 신설(중간, 마커 정규식·상태전이 로직은 이미 훅에 있어 재사용 가능). 리스크: 넛지가 "설치하려면 `/malgn-pm-sync` 실행"으로 안내를 위임하면, 사용자가 커맨드를 직접 실행하지 않는 한 설치가 진행되지 않아 현재의 "첫 세션 자동 개시" 장점이 희석될 수 있음 — 넛지 문구 품질에 발견성이 더 의존하게 됨(2026-08-09 리뷰에서 동일하게 지적된 트레이드오프) |

## 트레이드오프 (페르소나 간 충돌)
- 이번 라운드도 세 페르소나가 #1·#2(Major)에 대해 견해가 갈리지 않았다 — 다만 **"지금 당장 고쳐야 하는가" 대 "다음 사이클로 미뤄도 되는가"**에서 미세한 무게 차이가 있다. 회의적 검증 설계자는 "실행 검증으로 재현된 버그이니 병합 전 수정"을 우선하는 반면, 운영 현실주의자는 "#1은 이미 배포됐을 수 있는 프로젝트에 영향을 주므로 오히려 이번 커밋에 반드시 포함"이라고 더 강하게 본다. → **권고**: #1·#2 모두 이번 커밋에 포함해 수정(코드 변경량이 적어 병합을 늦출 이유가 되지 않음). 발산형의 슬래시커맨드 대안은 이번 커밋을 막지 않고 다음 개선 사이클 후보로 유지.

## 잘 된 점
- **핵심 설계 의도가 실행 검증으로 확인됨**: installed 상태에서 훅이 매 세션 `pm-orchestration-block.md`를 디스크에서 직접 읽어 본문을 상시주입하는 동작을 Node 실행으로 재현·확인(`case-installed` 시나리오) — "CLAUDE.md에 stale copy가 생길 여지 자체가 없다"는 설계 목표가 신규 설치 경로에서는 실제로 달성됨.
- **버전 비교 방향이 정확함**: `block.version > markedVersion`을 declined `v0`(구식)·`v2`(신식) 두 시나리오로 실행 검증 — 낮은 버전에서만 재넛지가 발동하고 이미 최신/그 이상인 경우 무동작함을 확인. 반대 방향 버그(재넛지가 영원히 안 뜨거나 매번 뜨는)는 없음.
- **확정 안전장치(파일 쓰기 금지) 유지 확인**: `grep -n "writeFileSync\|appendFileSync\|fs\.write\|unlinkSync\|rmSync\|renameSync"`로 전수 검사, 매치 0건 — `readFileSync`만 사용. 회귀 없음.
- **본문 추출 로직이 설명주석·마커 줄을 정확히 제외**: `readBlockFile()`이 버전마커 줄 다음부터를 본문으로 취급하는 로직을 실행 결과로 확인(주입된 텍스트가 "## PM 행동 규율..."로 정확히 시작, 설명주석·마커 잔존 없음).
- **`hooks.json` 미변경 판단이 맞음**: 이벤트명(`SessionStart`)·커맨드 인터페이스 모두 그대로이므로 손댈 이유가 없었고, 실제로 `git diff`상 변경 없음을 확인.
- **`new-project.mjs`의 신규 파일이 기존 파일을 덮어쓰지 않는 안전한 스캐폴드 패턴을 그대로 따름**(`existsSync(dest)` 체크로 skip) — 사용자가 이미 커스텀 `.claude/settings.json`을 가진 경우도 안전.

## 평가기준 충족 현황

| 기준 | 관점 | 중요도 | 충족 | 비고 |
|------|------|-------|------|------|
| CLAUDE.md에는 마커만, 본문은 절대 미복사 | 검증 설계 | 필수 | ✓ | 넛지 문구가 "절대 복사하지 말 것"을 명시, 훅 자신도 본문을 CLAUDE.md에 쓰지 않음 |
| installed 마커 시 매 세션 조용히 본문 상시주입 | 검증 설계 | 필수 | ✓ | 실행 검증(`case-installed`) |
| declined 마커 시 버전 비교로 1회 재넛지, 방향 정확 | 검증 설계 | 필수 | ✓ | 실행 검증(`case-declined-low/high/v0`) |
| 무마커 시 기존과 동일한 AskUserQuestion 넛지 | 검증 설계 | 필수 | ✓ | 실행 검증(`case-none`) |
| 훅 스크립트 자신은 어떤 파일도 쓰지 않음(확정 안전장치) | 검증 설계 | 필수 | ✓ | grep 전수 검사, 매치 0건 |
| 구버전 마커 호환(회귀 없음) | 운영 현실 | 권장(회귀는 지적 대상) | ✗ | 지적 #1 |
| declined→installed 전환 시 마커 정합성 | 검증 설계 | 필수 | ✗ | 지적 #2 |
| block.md 버전마커 문법이 훅 정규식과 실제로 맞물림 | 검증 설계 | 필수 | ✓ | 실행 검증, 설명주석도 정확히 제외됨 |
| hooks.json 무변경 판단의 타당성 | 운영 현실 | 권장 | ✓ | 이벤트명·인터페이스 불변 확인 |
| new-project.mjs 신규 스캐폴드 값 정확성 | 운영 현실 | 권장 | ✓ | marketplace.json 실측 대조 |

## COO에게 권고
1. **GO-with-fix**. Critical 없음 — 설계 의도의 핵심(마커-only + 상시 재주입)은 정상 경로에서 실행 검증까지 마쳤고 확정 안전장치(파일 미쓰기)도 회귀 없음을 확인했다. 다만 Major 2건(#1 구버전 마커 인식, #2 declined→installed 마커 정리)은 **회귀 위험**에 정확히 해당하므로 병합 전 수정을 권고한다.
2. **backend-dev에게 재작업 지시 시 구체적으로 전달할 내용**: `malgn-agent/hooks/pm-orchestration-nudge.mjs:23`의 `STATE_MARKER_RE`에 구버전 마커(`installed:` 리터럴 없는 `pm-orchestration:v(\d+)`) 인식 분기 추가(#1), `malgn-agent/hooks/pm-orchestration-nudge.mjs:69-77`의 `askInstallNudge`에서 `reinstall` 컨텍스트일 때 "예" 문구도 기존 declined 마커 교체를 명시하도록 분기(#2). 둘 다 국소 수정이라 재검토 주기는 짧을 것으로 예상.
3. Minor #3(new-project.mjs 하드코딩값)·Nit #4는 병합을 막을 사유가 아님, 참고만.
4. 발산형 소수의견(결정론적 슬래시커맨드)은 이번 커밋을 막지 않는다 — 2026-08-09 라운드부터 이어지는 제언이며, 이번 라운드에서 발견된 #2가 그 우려를 구체적으로 실증했다는 점만 기록하고 다음 개선 사이클 후보로 넘긴다.
5. **정직 명시**: 이번 리뷰는 Node 실행 시뮬레이션(마커 5종 시나리오)까지 포함했으나, 실제 Claude Code 세션을 새로 띄워 AskUserQuestion→모델이 실제로 Edit을 수행하는 전체 엔드투엔드 플로우(특히 #2가 실제 모델 응답에서도 재현되는지)는 검증하지 않았다 — 생략함, 사유: 정적/실행 코드 검증으로 문구상의 간극이 명확히 드러나 굳이 LLM 왕복까지는 필요치 않다고 판단했음. 실행 액션(커밋·배포 등)은 reviewer가 수행하지 않았다.
