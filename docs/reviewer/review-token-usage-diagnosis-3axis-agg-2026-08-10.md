# 리뷰 보고서: token-usage-diagnosis — 3축 집계(도구별/서브에이전트별/프로젝트별) + 프롬프트 요약 확장

- 대상 브랜치: `trainer/token-usage-diagnosis-3axis-agg-20260810` (main=`727cba8` 기준 분기)
- 대상 커밋: `d69bd60`(trainer 구현) → `05a7833`(코디네이터 후속 수정 — 프롬프트 요약 공유주의 문구 제거) → `af10014`(코디네이터 후속 수정 — `--out` 제거 + §3 캐비어트 최소 복원, RV-101 대응)
- 대상 파일: `malgn-agent/bin/analyze-usage.mjs`, `malgn-agent/skills/token-usage-diagnosis/SKILL.md`
- 등급: 위임 시 명시적 등급 표기 없음 → 기본 Standard로 간주. 다만 개인정보 노출 판단(RV-101)이 v1.2.1의 RV-003(Major, 개인정보) 전례와 동일 계열이라 Sensitive에 근접 — 풀 패널(4인, 발산형 1인 포함)로 진행
- 리뷰 방법(1차, `05a7833` 기준): `git diff 727cba8 05a7833` 전체 확보 후 코드 정독 + `~/.claude/projects`의 **실제 로컬 세션 로그(7일, 229세션, 21,878 API 호출)**로 `node bin/analyze-usage.mjs`를 여러 옵션 조합(`--days 7`, `--top 3/5`, `--project`, `--out`, `--help`)으로 실행해 검증. 추가로 디버그 계측을 임시 삽입해 신규 집계 3종의 총합 보존(conservation) 여부를 수치로 확인.
- 리뷰 방법(2차 재검증, `af10014` 기준, RV-101 단독 좁은 범위): 아래 "재검증(af10014)" 섹션 참고.

## 종합 판정(최신, af10014 반영): 🟢 Green (GO)

`af10014`로 RV-101(Major)이 실제로 닫힌 것을 코드 실측으로 확인했다(아래 "재검증(af10014)" 섹션). **1차 판정(🟡 Amber, GO-with-fix)은 `05a7833` 시점 기준으로는 유효했던 판정이며, 이번 좁은 재검증으로 그 유일한 Major가 해소됨에 따라 최신 커밋(`af10014`) 기준 판정을 🟢 Green(GO)으로 갱신한다.** Minor(RV-102~104)·Rethink(RV-108)는 이번 재검증 범위 밖으로, 백로그로 남겨둔 채 병합을 막지 않는다(1차 리뷰 시점부터 이미 non-blocking으로 판정됨).

---

## 재검증 (af10014, RV-101 단독 좁은 범위)

### 확인 대상
`git diff 05a7833 af10014 -- malgn-agent/bin/analyze-usage.mjs malgn-agent/skills/token-usage-diagnosis/SKILL.md` 전체 확보 + 스크립트 실행 검증.

### 1. `--out` 옵션 완전 제거 여부 — ✅ 확인
- 코드: `parseArgs`의 `opts.out`/`case '--out':` 분기, `printHelp()`의 `--out PATH` 안내, `run()` 말미의 `if (opts.out) { fs.writeFileSync(...) }` 블록이 모두 삭제됨(`git diff` 직접 확인).
- **실행 검증**: `node analyze-usage.mjs --out /tmp/should-not-exist.md` 실행 결과 `알 수 없는 옵션: --out (--help 참고)` 에러와 함께 exit code 1로 즉시 종료됨을 확인. `/tmp/should-not-exist.md` 파일이 생성되지 않았음을 `ls`로 확인(파일 시스템에 아무것도 남기지 않음).
- `node analyze-usage.mjs --help` 실행 결과 출력에 `--out` 언급이 전혀 없고 "콘솔 출력만 지원합니다(파일 저장 옵션 없음)."로 명확히 안내됨을 확인.
- `git show af10014:malgn-agent/bin/analyze-usage.mjs | grep -n -- "--out\|opts.out"` → **매치 0건**. 코드에 잔여 참조 없음.

### 2. SKILL.md 캐비어트 한 줄이 RV-101 취지를 충분히 커버하는가 — ✅ 확인 (충분한 최소치)
추가된 문장(109행): "**세션별 순위(§3)의 첫/마지막 프롬프트 요약도 원문 그대로(90자) 노출되니 공유 전 함께 확인한다.**"

RV-101이 요구한 최소 요건 3가지를 모두 충족한다: (a) 위치 특정(§3), (b) 노출되는 것이 무엇인지("첫/마지막 프롬프트 요약"), (c) 노출 방식과 정도("원문 그대로", "90자")까지 명시하고 공유 전 확인하라는 행동 지침까지 붙어 있다. 기존 cwd 캐비어트 문장과 같은 절 안에 같은 톤·같은 길이(한 문장)로 자연스럽게 붙어 있어 문서 구조상 이질감도 없다. RV-102(라벨이 IDE/훅 주입 텍스트까지 섞는다는 지적)까지 캐비어트에 명시할 필요는 없다고 판단한다 — RV-101의 핵심 요구는 "원문 노출 사실 자체의 고지"였고 이 문장은 그것을 정확히 충족한다. "장황할 필요 없다"는 사용자 요청 기준으로도 적절한 분량이다.

### 3. `--out` 제거로 인한 회귀(SKILL.md 잔여 참조) — ✅ 확인, 잔여 없음
`git show af10014:malgn-agent/skills/token-usage-diagnosis/SKILL.md`(전문)를 `grep -n "out"`으로 전수 검색한 결과 **매치 0건** — "자연어→옵션 매핑" 표의 `"기록으로 남겨줘"→--out` 행이 깔끔히 삭제됐고, "저장 규칙" 절이 "파일 저장 미지원 + 사용자가 파일 저장을 요청하면 기능 부재로 안내"하는 문장으로 자연스럽게 재작성됐다(거절이 아니라 "지원 안 함"으로 안내하라는 구체적 스크립트까지 포함, 터미널 리다이렉트(`> report.md`)는 스킬 통제 범위 밖이라고 명시해 과잉 제약도 아님). Example Usage·자연어 매핑 표 등 다른 절에도 깨진 참조 없음.

### 4. 이전 PASS 항목(RV-105~107 등)에 대한 회귀 여부 — ✅ 확인, 회귀 없음(전체 재실행으로 실증)
diff 범위가 `--out` 제거 + 캐비어트 한 줄 추가로 좁아 회귀 가능성이 낮다고 예상했지만, "그렇다고만 언급"하는 대신 실제로 확인했다: `05a7833` 버전과 `af10014` 버전 스크립트를 각각 동일 옵션(`--days 7 --top 5`)으로 실행해 `diff`한 결과 **출력이 완전히 바이트 단위로 동일**했다(§1~§10 전 구간 + 효율화 가이드, `diff` exit code 0). 즉 RV-105(균등분할 보존 법칙)·RV-106(`--top` 전파)·RV-107(콘솔=마크다운 동일성)이 검증했던 리포트 생성 로직 자체는 이번 커밋에서 단 1바이트도 바뀌지 않았음을 실측으로 확인했다 — "diff 범위가 좁으니 낮을 것"이라는 추정이 아니라 실행 결과 동일성으로 대체 확인.

### RV-101 최종 판정: ✅ CLOSED

| 확인 항목 | 결과 |
|---|---|
| `--out` 코드+문서 양쪽 완전 제거, 실행 시 에러로 거부 | ✅ 실측 확인 |
| §3 프롬프트 노출 캐비어트 최소 복원, 충분성 | ✅ 확인(충분한 최소치) |
| `--out` 제거로 인한 문서/코드 잔여 참조 | ✅ 없음 |
| 기존 PASS 항목(RV-105~107) 회귀 | ✅ 없음(바이트 동일 실측) |

---

## 지적 사항 요약 (1차 리뷰 기준, 이력 보존)

| ID | 심각도 | 위치 | 요약 | 담당 페르소나 | 상태(af10014 기준) |
|---|---|---|---|---|---|
| RV-101 | 🟠 Major | `SKILL.md` 109~111행 / `bin/analyze-usage.mjs` 144~159, 311~320, 525~542행 | §3 첫/마지막 프롬프트 원문(90자)이 노출되는데, `d69bd60`에서 추가됐던 관련 caveat이 `05a7833`에서 삭제되고 SKILL.md에 이 노출면에 대한 별도 언급이 남지 않음. RV-003보다 노출 표면이 큼(실증됨) | privacy-leakage-auditor | ✅ **CLOSED** (위 재검증 참고) |
| RV-102 | 🟡 Minor | `bin/analyze-usage.mjs` 144~159행 (`extractHumanPromptText`) | "첫/마지막 프롬프트"가 실제로는 IDE 이벤트 주입·Stop 훅 피드백·슬래시커맨드 caveat 래핑 등 "사람이 직접 타이핑하지 않은" 텍스트를 포함 — 라벨이 오도적, 노출 노이즈 증가 | token-attribution-accuracy-auditor | 미반영(백로그, 이번 재검증 범위 밖) |
| RV-103 | 🟡 Minor | `SKILL.md` 29행 / `bin/analyze-usage.mjs` 382~391행 | §8 "도구별 사용량"에 실측 결과 **가장 큰 비중을 차지하는** 합성 버킷 `(텍스트 응답, 도구 없음)`이 SKILL.md 설명에 언급 안 됨 | script-skill-consistency-auditor | 미반영(백로그, 이번 재검증 범위 밖) |
| RV-104 | ⚪ Nit | `bin/analyze-usage.mjs` 176~195, 311~320행 | `firstPrompt`/`lastPrompt`는 파일 처리 순서에 의존하는 로직(기존 `firstTs`/`lastTs`의 min/max 비교와 다른 패턴). 현재 로그 아키텍처(세션당 non-sidechain 파일 정확히 1개)에서는 안전함을 실측 확인했으나, 향후 아키텍처 변화 시 조용히 틀릴 수 있는 미검증 불변식 | token-attribution-accuracy-auditor | 미반영(백로그, 이번 재검증 범위 밖) |
| RV-105 | ✅ PASS(실측) | `bin/analyze-usage.mjs` 212~224, 355~391행 | 균등분할 귀속의 총합 보존 법칙을 디버그 계측으로 실측 확인 — 이중계산/누락 없음 | token-attribution-accuracy-auditor | af10014에서도 유지(바이트 동일 재확인) |
| RV-106 | ✅ PASS(실측) | `--top` 옵션 전파 | `--top 3`/`--top 5`로 재실행해 §8/§9/§10 모두 동일하게 반응함을 확인, SKILL.md 서술과 일치 | script-skill-consistency-auditor | af10014에서도 유지(바이트 동일 재확인) |
| RV-107 | ✅ PASS(실측) | 콘솔=마크다운 동일성 | `--out`으로 저장한 파일과 콘솔 출력을 `diff`로 대조, 저장 안내 줄 1개 차이 외 완전 동일 | script-skill-consistency-auditor | af10014에서 `--out` 자체가 제거됨 — 이 검증 방식은 더 이상 적용 불가하나, 콘솔 출력 로직 자체(§1~§10 렌더링)는 바이트 동일 유지 확인 |
| RV-108 | 🔵 Rethink | `bin/analyze-usage.mjs` 226행, 355~391행, 642~666행 | §9 "서브에이전트별 위임 집계"가 실제 서브에이전트 소비 비용(sidechain, 관측 25~59%)을 전혀 반영하지 못해 이름값을 못함. "연결 불가"라는 전제를 재검증한 결과 부분적으로만 사실 | subagent-cost-attribution-rethinker | 미반영(백로그, 이번 재검증 범위 밖) |

## 페르소나별 상세 (1차 리뷰, af10014 이전 시점 — 이력 보존)

### 1. privacy-leakage-auditor — RV-101 (핵심 쟁점, 요청받은 독립 재판단)

**실물 확인.** `node bin/analyze-usage.mjs --days 7 --top 5`를 실제 로컬 로그(7일, 229세션)로 실행한 결과, §3 표에 다음과 같은 프롬프트 원문이 실제로 그대로 찍혔다:

- 세션 `855b7e08`의 마지막 프롬프트: `"ui/ux 디자인 검증은 안했나? 보면 여백 처리도 안되어 있고 에이전트들도 적절히 조직도화 되어야 하는데 안되어 있네."` — 특정 산출물/작업에 대한 인물 비판성 발화가 90자 이내로 그대로 노출됨.
- 세션 `2460db3e`의 첫 프롬프트: `"우리 직원이 토큰을 너무 많이 사용하는데 아래와 같이 이력이 있어. event=api_request \| time=..."` — 인사 관련 맥락이 담긴 문장이 노출됨(참고로 `|` 문자는 `extractHumanPromptText`가 `\|`로 이스케이프해 마크다운 표는 깨지지 않음 — 이 부분은 잘 처리됨).

이 두 사례는 **v1.2.1 RV-003(도구 input JSON 조각 노출, Major 판정)**보다 노출 표면이 크다: RV-003은 `Read` 도구라면 file_path만 노출되는 등 조건부였지만, 이번 §3은 세션마다 항상, 자연어 문장 전체(최대 90자)가 노출된다.

**caveat 삭제 판단의 독립 재평가.** `d69bd60`은 다음 caveat을 추가했었다: `"세션별 첫/마지막 사용자 프롬프트 요약(§3 세션별 순위 표)"`을 "개인정보 유의" 절에 명시 + `"프롬프트 요약도 노출면이다"` 하위 불릿(원문 90자까지 truncate해 그대로 찍는다는 설명). `05a7833`이 이 caveat만 제거했다. 사용자 판단 근거는 "이 리포트는 순수 로컬 개인 진단용이고 외부 공유할 계획이 전혀 없다."

이 판단은 **이번 세션에서 사용자 본인이 만든 리포트 1건**에는 타당할 수 있다. 그러나 caveat이 살고 있는 곳은 `SKILL.md`이고, 이 파일은:
1. `SKILL.md` 13행: "특정 소수 전문 에이전트 전용이 아니다... 세션 중인 어느 에이전트든 사용자가 물으면 바로 실행한다" — **맑은소프트 전 직원**이 각자 자기 PC에서 실행하는 배포용 스킬이다. "이 사용자는 공유 안 한다"가 "모든 미래 사용자가 공유 안 한다"를 보장하지 않는다.
2. (`05a7833` 시점 기준) `SKILL.md` "저장 규칙" 절: "기록으로 남겨줘/파일로 줘"라고 명시하면 `--out`으로 마크다운 저장을 공식 지원한다 — 파일로 저장된 리포트는 로컬 세션 대화보다 공유(슬랙 첨부, 이메일 등) 가능성이 구조적으로 더 높다. **(af10014에서 `--out` 자체가 제거되며 이 경로는 원천 차단됨.)**
3. 남아있는 "개인정보 유의" 절(109행)은 cwd·§10만 명시하고 §3 프롬프트 노출은 언급이 없다 — RV-003 당시엔 "반복 호출 패턴" 표에 대해 같은 절 하위 별도 하위불릿을 남겼는데, 이번엔 동일 패턴(더 큰 노출면)의 하위불릿이 통째로 사라졌다. **(af10014에서 캐비어트 한 줄이 복원되며 해소됨.)**

**1차 독립 판정(당시): 🟠 Major, caveat 복원 권고.** → **2차 재검증(af10014) 결과: ✅ CLOSED.** 코디네이터는 caveat 복원과 `--out` 제거를 동시에 적용해, "SKILL.md 문구 보강"뿐 아니라 그 근거가 됐던 노출 경로(파일 저장 후 공유) 자체를 원천 차단하는 더 강한 조치를 취했다. 이는 reviewer의 권고("최소한 캐비어트 한 줄 복원")를 충족하고도 남는 수준이다.

### 2. token-attribution-accuracy-auditor — RV-102, RV-104, RV-105

**RV-105 (보존 법칙, PASS).** `toolAgg`(§8 전체, top 슬라이스 제외)의 4필드 합계를 디버그 계측으로 뽑아보면 `2,330,101,189`, 세션 전체 `tokenSum` 합계도 동일하게 `2,330,101,189`, `projectList`(§10 전체) 합계도 동일 값으로 정확히 일치한다. 균등분할(`addSplitTokens`)이 이중계산이나 누락 없이 원래 API 호출의 usage를 보존함을 실측으로 확인했다.

**RV-102 (라벨 정확성, Minor).** `extractHumanPromptText`가 잡아내는 "사람이 입력한 프롬프트"에는 실제로:
- IDE 통합 이벤트: `<ide_opened_file>The user opened the file /Users/.../STATUS.md...`(세션 `04653e7a` 첫 프롬프트로 실제 노출됨)
- Stop 훅 피드백: `Stop hook feedback: [malgnai-mcp 기록 점검]...`(세션 `04653e7a`/`395ea05e`/`2460db3e` 마지막 프롬프트로 실제 노출됨)
- 슬래시커맨드 caveat 래핑: `<local-command-caveat>Caveat: The messages below were generated by the user while running...`(세션 `395ea05e` 첫 프롬프트로 실제 노출됨)

가 섞여 있다. `isHumanPromptContent`(기존 로직, 이번 diff 대상 아님)는 "tool_result만 있지 않으면 사람 턴"으로 판별하는데, 이는 턴 *개수*를 셀 때는 사소한 오차였지만, 이번 diff로 그 턴의 *원문 텍스트*까지 노출하게 되면서 노출 노이즈가 커졌다(진짜 사용자 발화가 아닌 시스템 주입 텍스트가 90자 예산을 차지해 정작 유용한 정보는 안 보이는 경우도 있음). Critical/Major는 아니지만(계산 오류 아님, 문서가 "요약"이라고만 표현해 완벽한 정밀도를 약속하지 않음), 이번 재검증 범위 밖으로 백로그에 남긴다.

**RV-104 (순서 의존성, Nit).** `firstTs`/`lastTs`는 `if (!agg.firstTs || ts < agg.firstTs)` 식의 min/max 비교라 파일 처리 순서와 무관하게 항상 정확하다. 반면 `firstPrompt`/`lastPrompt`는 "처음 도달 시 1회 설정 / 이후 항상 덮어쓰기"로, **파일 처리 순서에 의존**한다. `findJsonlFiles`가 반환하는 파일 목록은 `fs.readdirSync` 순서(사실상 파일명 알파벳 순서에 가까움)이지 시간순이 아니다.

실측으로 위험도를 낮췄다: `find ~/.claude/projects -name '*.jsonl' -newermt '2026-08-04'`로 세션 `04653e7a`가 걸친 파일 206개를 전수조사한 결과, `isSidechain !== true`인(즉 §3에 반영되는) 파일은 **정확히 1개**뿐이었다(나머지 205개는 전부 서브에이전트 sidechain 파일이며, 코드가 이미 `if (!isSidechain)`로 걸러낸다). 단일 파일 내부는 JSONL append 로그라 자연히 시간순이므로, **현재 로그 아키텍처에서는 이 위험이 실현되지 않는다.** 다만 이 안전성은 "세션당 non-sidechain 파일은 항상 1개"라는 검증되지 않은 암묵적 가정에 기대고 있다 — 향후 로그 포맷이 바뀌어(예: 압축/재개 시 새 최상위 파일 생성) 이 가정이 깨지면 조용히 틀린 첫/마지막 프롬프트가 나올 수 있다. 방어적으로 `firstTs`/`lastTs`와 동일하게 타임스탬프 비교 방식으로 바꾸는 게 더 안전하지만, 지금 당장 병합을 막을 사안은 아니다.

### 3. script-skill-consistency-auditor — RV-103, RV-106, RV-107

**RV-106/RV-107 PASS(실측, `05a7833` 시점).** `--top 3`과 `--top 5`로 각각 실행해 §8/§9/§10 세 표 모두 행 개수가 정확히 top 값을 따름을 확인(SKILL.md 31행 "도구별/서브에이전트별/프로젝트별 표는 `--top` 옵션을 그대로 따른다"와 일치). `--out out-test.md`로 저장한 파일과 콘솔 출력을 `diff`한 결과 저장 완료 안내 줄(콘솔 전용, 기존 동작) 외 완전히 동일함을 확인했다. (af10014에서 `--out` 자체가 제거됐으므로 이 방식의 검증은 더 이상 재현 불가하나, 위 "재검증" 섹션에서 콘솔 출력 로직 자체가 바이트 동일함을 별도 확인함.)

**RV-103 (Minor).** SKILL.md 29행은 §8을 "도구별 사용량(Read/Bash/Task/mcp__* 등 도구별 호출 횟수·토큰)"으로 설명하지만, 실제 실행 결과 최상위(가장 큰) 행은 도구 이름이 아니라 합성 라벨 `(텍스트 응답, 도구 없음)`이다(실측: 8,194회 호출, 정렬기준 합계 76,139,552 — Top 5 중 1위). 이 라벨이 코드(382~391행)에는 있지만 SKILL.md 설명 어디에도 언급되지 않아, 사용자가 리포트를 보고 "이게 뭐지" 의아해할 수 있다. `--top` 관련 불릿(31행) 근처에 한 줄 추가를 권고한다. 이번 재검증 범위 밖으로 백로그에 남긴다.

### 4. subagent-cost-attribution-rethinker [발산형] — RV-108

**현재 구조의 문제.** §9 "서브에이전트별 위임 집계" 표는 이름과 달리 서브에이전트의 실제 소비 비용을 보여주지 않는다. 실측 데이터에서 1위 항목(`ux-designer`)의 "정렬기준 합계"는 237,652 토큰인데, 같은 기간 전체 사이드체인(서브에이전트 내부 실행) 비중은 총량의 25.4~59.3%(세션에 따라 최대 수억 토큰)에 달한다 — 표에 잡히는 숫자는 위임 *호출* 시점의 부수적 토큰(사실상 노이즈 수준)이고, 진짜 비용은 표 어디에도 subagent_type별로 분해되어 나타나지 않는다. 캐비어트(SKILL.md 33행)는 이 한계를 정직하게 밝히고 있으나, 표 자체의 "정렬기준 합계"·순위는 사용자가 실제로 궁금해할 질문(어느 서브에이전트가 토큰을 제일 많이 먹었나)에 답하지 못한 채 숫자처럼 보이는 표를 제공한다.

**"연결 불가" 전제의 실측 재검증.** sidechain 파일(`subagents/agent-<agentId>.jsonl`)을 직접 열어본 결과, 최상위 필드에 `subagent_type`은 확실히 없지만 고유한 `agentId`는 존재한다(예: `a265c3309eaf3b807`). 상위(non-sidechain) 세션 파일에서 같은 문자열을 검색한 결과, 해당 위임을 호출한 `Agent` 도구의 `tool_result` 콘텐츠 안에 `<output-file>.../tasks/a265c3309eaf3b807</output-file>` 형태의 경로가 포함돼 있었고, 이 `tool_result`가 응답하는 `tool_use.id`(`toolu_01TssH9ex7H5EGMdVk2qPJUB`)의 `input.subagent_type`은 `presenter`였다. 즉 **`agentId ↔ subagent_type` 상관관계를 복원할 단서가 실제로 존재한다** — "로그 구조상 연결 불가"는 정확히는 "현재 sidechain 파일 자체에는 없다"가 맞고, "상위 파일과 교차하면 복원 불가능"은 아니다.

단, 이 상관 경로는 **이 조직의 커스텀 `Agent` 도구**(`<output-file>` 태그를 남기는 하네스)에서만 실측 확인했다 — 로컬 로그 전체를 조사했으나 표준 `Task` 도구 호출 사례가 이 환경에 하나도 없어(이 조직은 사실상 전부 `Agent`를 사용), 표준 Claude Code `Task` 도구에서도 동일 패턴이 성립하는지는 **미검증**이다. 과대주장하지 않는다.

| 항목 | 현재 구조 | 제안 구조(대안) | 왜 더 나은가 | 예상 비용·리스크 |
|---|---|---|---|---|
| §9 서브에이전트 원가 귀속 | 위임 호출 시점의 미미한 토큰만 집계, sidechain은 §1 총량에만 뭉뚱그려짐 | sidechain 파일의 `agentId`를 상위 세션의 `tool_result` `<output-file>` 경로에서 역참조해, 해당 `agentId`에 대응하는 `subagent_type`으로 sidechain 총량을 롤업. §9에 "호출 오버헤드"와 "내부 소비(추정)" 두 컬럼을 병기 | 사용자가 실제로 묻는 "어느 서브에이전트가 제일 비쌌나"에 답할 수 있게 됨 | 파싱 로직 복잡도 증가(2-pass 필요: 먼저 상위 파일에서 agentId↔label 매핑을 만들고, 그 다음 sidechain 파일들을 집계). `Task`(표준 도구) 경로에서도 동일 상관키가 보장되는지 미검증 — 프로토타입 후 검증 필요. 이번 병합을 막을 사안은 아니며 후속 스킬 개선으로 제안 |

## 트레이드오프

- **RV-101(privacy-leakage-auditor)과 코디네이터의 원 판단 사이의 충돌 — af10014로 해소됨**: 코디네이터는 1차로 "순수 로컬 개인용, 공유 계획 없음"을 근거로 caveat을 지웠으나(개인 판단으로는 합리적이되 배포 스코프에는 부족하다고 reviewer가 지적), 2차(`af10014`)에서 caveat 복원 + `--out` 제거라는 더 강한 절충안으로 대응해 트레이드오프 자체를 해소했다.
- **RV-108(발산형)과 "이번 커밋 범위" 사이의 트레이드오프**: sidechain-agentId 상관 설계는 유효해 보이지만 표준 `Task` 도구 케이스가 로컬 데이터에 전무해 일반화를 확신할 수 없다. 이번 병합을 막을 근거로 쓰기엔 근거가 부족하다고 판단해 Rethink(후속 제안)로만 남긴다.

## 잘 된 점

- **RV-101에 대한 절충안이 reviewer 권고보다 강함**: reviewer는 "캐비어트 한 줄 복원"만 권고했는데, 코디네이터는 그 근거 경로(파일 저장 후 공유 가능성) 자체를 `--out` 제거로 원천 차단하는 구조적 대응을 택했다. 문서 문구보다 코드 레벨 제약이 더 확실한 보증이다.
- **`--out` 제거가 깨끗함**: 코드·SKILL.md 양쪽에 잔여 참조 0건, 옵션 사용 시 명확한 에러로 즉시 실패(조용히 무시되지 않음), 사용자 요청 시 응대 스크립트까지 SKILL.md에 남겨 에이전트가 자연스럽게 대응 가능.
- **보존 법칙이 실제로 지켜짐(RV-105)**: 균등분할 귀속이 이중계산·누락 없이 정확하다는 것을 디버그 계측으로 실측 확인했다. "근사치"라고 스스로 캐비어트한 부분(도구 여러 개 동시 호출 시 분할)은 애초에 로그에 도구별 개별 비용이 없어 불가피한 근사이며, 설계도 합리적이다.
- **`|` 문자 이스케이프 처리**: `extractHumanPromptText`가 사용자 프롬프트 원문에 포함된 `|`를 `\|`로 이스케이프해 마크다운 표가 깨지지 않도록 처리했다 — 실제 로그(파이프 문자가 포함된 프롬프트)로 검증했고 정상 작동했다.
- **`--top` 일관 적용**: 신규 3개 표 모두 기존 `--top` 옵션을 자연스럽게 재사용해 별도 옵션을 늘리지 않았다. API 호출 Top만 2배 유지하는 기존 관례와도 충돌 없이 공존한다.
- **무의존성 원칙 유지**: `af10014`에서도 새 import 없이 순수 삭제/문서 수정만으로 대응.
- **sidechain 한계를 SKILL.md·코드 주석 양쪽에 이미 정직하게 명시**: RV-108에서 "부분적으로만 사실"이라고 재평가했지만, 애초에 숨기지 않고 캐비어트를 남긴 점 자체는 이 스킬의 "verified/claimed 구분" 원칙과 일관된 좋은 습관이다.

## 실행하지 않은 것 (정직 보고)

- **이 브랜치에 fix 커밋을 추가하지 않았다(1차·2차 모두).** 위임 시 "Minor 오탈자 등은 직접 fix 커밋을 추가해도 된다"는 허용이 있었으나, reviewer 역할 경계(코드 직접 수정 금지 — 수정은 qa-engineer/trainer 담당)를 따라 어떤 커밋도 만들지 않았다.
- **RV-102~104·RV-108은 이번 좁은 재검증에서 다시 확인하지 않았다** — 코디네이터 요청에 따라 RV-101 단독 재검증만 수행했으며, 이 항목들의 상태는 1차 리뷰 시점 그대로다(백로그, non-blocking).
- **표준 Claude Code `Task` 도구의 sidechain 상관관계는 검증하지 못했다** — 로컬 데이터에 사례가 전무해 실증 불가(RV-108에 명시, 1차 리뷰와 동일).
- **전 직원의 실제 공유 관행은 조사하지 않았다** — RV-101 판단은 SKILL.md의 배포 스코프 서술(문서상 명시)에 근거했을 뿐, 실제 사용 행태 데이터는 없다(1차 리뷰와 동일).

## PM 권고 (갱신)

1. **RV-101은 닫혔다 — 이 항목 때문에 병합을 미룰 이유 없음.**
2. RV-102/RV-103/RV-104는 지금 당장 급하지 않음(Minor/Nit, 코드 계산 오류 아님) — 다음에 이 스크립트를 만질 기회에 함께 반영 권고.
3. RV-108(발산형)은 백로그에 "서브에이전트 원가 귀속 재설계(agentId 상관 프로토타입, `Agent` 도구 한정 확인·`Task` 미검증)"로 등록 권고.
4. **최종 판정: 🟢 Green (GO).** 코드 정확성·일관성은 1차 리뷰부터 GO였고, 유일한 Major(RV-101)도 이번에 실측으로 닫힌 것을 확인했다.

## 산출물

- 페르소나(4개, 전원 실재, 1차 리뷰에서 생성/갱신 — 이번 재검증은 신규 페르소나 파일 추가 없이 기존 산출물로 수행): `docs/reviewer/personas/persona-privacy-leakage-auditor.md`(갱신, 재사용), `docs/reviewer/personas/persona-script-skill-consistency-auditor.md`(재사용), `docs/reviewer/personas/persona-token-attribution-accuracy-auditor.md`(신규), `docs/reviewer/personas/persona-subagent-cost-attribution-rethinker.md`(신규, 발산형)
- 본 보고서: `docs/reviewer/review-token-usage-diagnosis-3axis-agg-2026-08-10.md` (2026-08-11 `af10014` 재검증 섹션 추가로 갱신)
