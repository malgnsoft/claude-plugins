# 리뷰 보고서: 조회 불가 8-hex id 제거 라운드 (`418b2a0..HEAD`)

**대상**: `malgn-agent/` diff, 베이스 `418b2a0` → HEAD `05e0273` (브랜치 `lesson-id-removal`, 32파일 / 195줄)

> ⚠️ **리뷰 중 대상이 움직였다.** 착수 시점 HEAD는 `4e27740`(31파일/194줄)이었으나, 리뷰 진행 중 PM이 `05e0273`(M5, html 1파일)을 커밋하고 `docs/refactor/lesson-id-removal-spec.md` §8·`CLAUDE.md`·`docs/methodology/audit-report.md`를 갱신했다(워킹트리 미커밋). 아래 판정은 **`05e0273` + 그 미커밋 docs 상태 기준**이며, 이 때문에 초안 지적 2건이 리뷰 도중 해소됐다(RV-002 일부·RV-003).
**정본 요구사항**: `docs/refactor/lesson-id-removal-spec.md` (커밋 `114ef7c`)
**리뷰일**: 2026-08-22 · **등급 처리**: 전 직원 배포 제품 32파일 동시 편집 → 풀패널(3페르소나, 발산형 1 포함)
**합격선**: 의미 무손실 — "id를 뺀 뒤에도 각 에이전트가 원래와 정확히 같은 행동을 하는가"

## 종합 판정: 🟡 Amber

**Critical 0 · Major 1 · Minor 2 · Nit 2 · Rethink 2 · (리뷰 중 해소 1).**
D 19줄의 **치환 품질 자체는 결함 0**이다. 유일한 Major는 이 라운드가 *만든* 결함이 아니라, 명세 §2.2-5가 사실 오류에 근거해 **제외한** 항목에서 나왔다(제품 본문에 조회 불가 참조가 2곳 그대로 남음). Minor는 전부 스코프·보고 정확성 문제이고 산출물 본문 결함이 아니다.

## 페르소나 패널 (재사용/신규)

| 페르소나 | 유형 | 재사용/신규 | 사유 |
|---|---|---|---|
| `persona-spec-implementation-conformance-auditor.md` | 수렴 | **재사용** | INDEX 스크리닝 결과 역할개념("명세=계약서, 커밋=납품물, 조항 1:1 대조")이 정확히 일치. 6대 요소 본문 무수정, "적용 이력"만 append. 단 §1·§2·§5가 이전 라운드(부트스트랩 재설계) 대상 파일에 고정돼 있어 **역할개념 수준으로만** 적용(INDEX 2026-08-10 RV-002 선례와 동일 처리) |
| `persona-semantic-force-preservation-auditor.md` | 수렴 | **신규** | 근접 후보 `persona-frontend-scope-consistency-auditor.md`·`persona-vue-zero-regression-guardian.md`("규칙 약화 감시")는 둘 다 vue-zero/frontend 스코프에 6대 요소가 고정. 이번 새 리스크 표면은 **"참조 삭제로 범위 한정자가 지시 대상을 잃는 것"** — 겹치지 않음 |
| `persona-dead-reference-scope-challenger.md` | **발산** | **신규** | 발산형 9종 중 `persona-self-service-scope-challenger.md`("스코프 축소 의심")가 근접하나 토큰진단 라운드에 고정. 이번 새 표면은 **"탐지 조건의 형태 기준 vs 목적 기준 괴리"** |

> INDEX 정합 이슈(스코프 밖, 보고만): 표에 등재되지 않은 페르소나 파일이 7개 존재 — 표 갱신이 몇 라운드 누락됨.

## 지적 사항

| ID | 심각도 | 위치 | 문제 | 권고 |
|---|---|---|---|---|
| RV-001 | 🟠 Major | `malgn-agent/agents/pm.md:36`, `malgn-agent/agents/reviewer.md:80` | 두 줄에 남은 `(사례: commit \`13bcd60\`)`의 `13bcd60`은 **이 저장소 어느 ref에도 없다**(`git cat-file -t 13bcd60` → `fatal: Not a valid object name`; `git log --all` 부분매칭 0건). 명세 §2.2-5는 "이 저장소에서 `git show`로 조회 가능한 살아있는 참조다. 유지"라고 단정했으나 사실이 아니며, 설치 직원 저장소에는 애초에 존재할 수 없다 — 즉 이 라운드가 제거하려던 **죽은 참조와 정확히 같은 부류가 살아남았다**. 게다가 제품 자신이 `agents/qa-engineer.md:66`에서 "인용한 커밋 해시가 `git log`로 실제 존재하는지 확인"을 의무화하고 있어 자기모순이다(reviewer.md:80은 하필 "무손실 diff 판정" 규칙 줄) | 두 곳 모두 `(사례: commit \`13bcd60\`)` 괄호째 삭제 — §3 A패턴과 동일 형태라 문장 재구성 불필요(실물 확인). **trainer에 반환**. 명세 §2.2-5도 정정 필요(PM 담당) |
| RV-002 | 🟡 Minor | `docs/refactor/lesson-id-removal-spec.md` §2.1·§4·§5.1·§5.2 (PM 담당) | 명세는 D를 **"21건(18줄)"**로 적었으나(`:30`, `:59`, `:116`, `:126`) §4가 실제로 열거하는 것은 **19줄 26건**이다(§4.3 헤더의 "13건"도 실제로는 표의 행 수). 리뷰 중 추가된 §8 실측 정정은 파일 수·id 총계·범위만 고쳤고 **D 수치는 그대로 남았다**. 이 오차가 PM 위임 브리프에 전파돼 D 목록에서 `skills/project-orchestration/SKILL.md:195`가 **누락**됐다 — 해당 줄은 A(괄호 전체가 id)·B(id 앞 쉼표+뒤 `)`) 어느 정규식에도 매칭되지 않는(id가 괄호 선두, 쉼표가 뒤) 사람 판단분이라, 브리프대로였다면 기계검증에도 사람리뷰에도 걸리지 않을 유일한 줄이었다 | 이번 리뷰에서 대신 검토함 — `(lesson \`e74c16e7\`, 상세 설계 기준은 architect 참조)` → `(상세 설계 기준은 architect 참조)`, **손실 0·파편 0**. **산출물 수정 불필요**. 명세 §8에 D 수치 정정(18줄→19줄, 21건→26건) 한 줄 추가만 권고 |
| ~~RV-003~~ | ✅ 리뷰 중 해소 | `malgn-agent/knowledge/design/html-style-guide/html-스타일가이드-가로형.html:54` | (초안 지적) 명세 스코프가 `**/*.md`인데 `.html` 1파일이 함께 바뀌었고, 그 id는 백틱이 없어(`lesson 965d45ff` 평문) G1·G3·G4 어느 게이트도 커버하지 못했다 | **리뷰 중 PM이 M5(`05e0273`) 단독 커밋 + 명세 §8③으로 범위 결함을 명시 정정해 해소.** 변경 내용도 재확인 결과 정확·무손실(`, lesson 965d45ff` 삭제, "2026-07-24 정정" 보존). 잔여 ⚪ 하나만: §8의 강화 G1 정규식은 여전히 **도구어 접두를 요구**해 접두 없는 `` \`8f36f1d3\` `` 형태는 못 잡는다 — 이번엔 실측 0건이라 무해(`grep -rnE '\`[0-9a-f]{8}\`' malgn-agent/` → 0) |
| RV-004 | 🟡 Minor | `malgn-agent/agents/devops.md:17` | 원문 `(QA 재탕 금지, 임의로 쉬운 시나리오 선택 금지, 선택기준 세분화 lesson \`2a83867a\`)` → `(… 선택 금지, 선택기준 세분화)`. "선택기준 세분화"는 원래 **id를 수식하던 경위 라벨**("이 항목은 해당 교훈으로 선택기준이 세분화됐다")인데, 앞 두 항목이 `~금지` 명령형이라 이제 **금지 나열의 세 번째 항목처럼 읽힌다**. 서술어가 없는 명사구여서 지시 내용이 없고, 오독하면 "선택기준을 세분화하라"는 없던 지시로 해석될 여지가 있음. 실제 행동(심각도 최고 항목 지정)은 본문 절에 이미 온전하므로 지시 자체는 불변 | `, 선택기준 세분화` 삭제 또는 `(… 선택 금지 — 선택기준은 2026-07-23 이후 세분화됨)`처럼 경위임을 드러내는 형태로. **trainer 판단에 맡김** |
| RV-005 | ⚪ Nit | `malgn-agent/agents/devops.md:27` | `(lesson \`0e97bdee\`, \`77753fe7\` 정정본)` 괄호째 삭제로 **"정정본"의 의미가 소실**됐다. 명세 §4.3은 "'정정본'이 가리키는 의미"를 보존 대상으로 명시했으므로 형식상 요구사항 미충족. 다만 "정정본"은 순수하게 id를 수식하던 말이라 남기면 지시 대상 없는 파편이 되고, 불릿 제목이 이미 `**[정정/보강]**`으로 시작해 정정 성격은 유지된다 | 조치 불요. 명세 요구를 문자 그대로 지킬 수 없는 케이스로 판정 — **기각 권고** |
| RV-006 | ⚪ Nit | `malgn-agent/agents/pm.md:29` | 이번 라운드 스코프 밖이지만 동종 죽은 참조: `memory \`feedback_coo-proactive-checkin-and-autonomous-handling\`` — hub에 `memory_*` 도구가 없고(CLAUDE.md 명시) 개발자 로컬 auto-memory 키라 설치 직원은 조회 불가. 8-hex가 아니라 스코프에 안 걸렸다 | 변경 동결 원칙상 이번 라운드에 끼워넣지 말고 **백로그**. RV-001과 함께 처리하면 저비용 |

## 페르소나별 관점

### 1. 스펙-구현 정합성 감사관 (재사용) — 조항 대조

- **§5.2 수용기준 G1~G7**: PM 재현분 위에서 재검증하지 않되(중복 노동 회피 지시), 게이트의 **커버리지**를 감사한 결과 RV-003을 발견 — G1의 백틱 앵커가 평문 id를 못 잡는다는 점은 이번엔 결과적으로 무해했으나(전 확장자 재스캔 결과 잔여 0건, 아래 확인 참조), 게이트 설계의 구멍이다.
  - 확인: `grep -rnE '(lesson|decision|issue|memory|project|세션)s? +\`?[0-9a-f]{8}\`?' malgn-agent/` → **0건**(전 확장자). `grep -rnE '\`[0-9a-f]{8}\`' malgn-agent/` → **0건**. 커버리지는 명세보다 넓게, 완전하다.
- **§4.1 (최대 위험) 판정: 합격**. `agents/qa-engineer.md:25`
  - 원문: `이 자율권은 lesson \`d0bee328\`·\`574534fa\` 이 두 건에만 한정되며`
  - 치환: `이 자율권은 위에 명시한 **라우팅 회귀·인증 스모크 이 두 유형에만** 한정되며`
  - 닫힌 경계 유지 ✅ / 후행 조항("세 번째 이후 패턴이 MD에 추가되어도 자동 확장되지 않고 별도 재검토") 무변경 ✅ / 같은 줄 전반부의 두 유형 정의와 지시 대상 일치 ✅ / 유형 판별 단서(`파일기반 라우터 파라미터 이름 불일치`, `로그인 성공=보호된 API 200`) 보존 ✅.
  - 단위가 "건"→"유형"으로 바뀐 점을 검토했으나 **확대 아님**: 불릿 제목 자체가 "자율 실행 가능 판단 **유형**"이고, 원문 전반부도 "정확히 이 두 건에 해당하는 실패"로 두 유형을 가리켰다. 오히려 지시 대상이 id에서 본문 문구로 바뀌어 **자기완결성이 올라갔다**.
  - **§6 역참조 확인 — PM이 이미 처리함**: `docs/methodology/audit-report.md:103`의 인용은 베이스에서 `"…정확히 2건의 lesson id로 한정…"`이었다. 치환으로 본문에서 lesson id가 사라졌으므로 **그대로 뒀으면 역참조가 깨졌을 자리**다. 리뷰 중 PM이 `"…정확히 2개 유형으로 한정…"`으로 갱신(워킹트리 미커밋, 17:59)했고, 현 본문(`위에 명시한 라우팅 회귀·인증 스모크 이 두 유형에만`)과 정확히 일치함을 확인했다. **추가 조치 불요 — 다만 이 갱신은 아직 커밋되지 않았다.**
- **§4.2 복제본 정합 판정: 합격**. `grep -rn '자율 실행 가능 판단 유형' malgn-agent/` → 5곳. 4곳이 `(2026-07-23 부하 인터뷰 기반 확대)`로 완전 동일, `frontend-dev.md:28`만 `; 2026-08-19 판단 주체 이전에 맞춰 범위 조정`이 추가돼 있다.
  - **이 비대칭은 베이스에도 있었고(`git grep -n ... 418b2a0` 확인), 실재하는 의미 차이다** — `pm.md:104`·`visual-designer.md:26`가 같은 2026-08-19 판단 주체 이전을 독립적으로 기술하고 있고, frontend-dev만 그때 자율권 범위가 실제로 조정됐다. 다음 세션이 이를 "의미 있는 차이"로 읽는 것은 **오독이 아니라 정독**이다. 조치 불요.
- **§4.4 앵커 역참조**: `### 5-1.` 제목에서 id 제거 → `grep -rn '5-1\|형제/유사' malgn-agent/` 결과 이 제목을 §번호로 역참조하는 곳 **없음**(`researcher.md:33`·`security.md:59`는 개념 서술이지 앵커 아님). 안전.
- **§7 커밋 분할 규약 준수**: M1(기계) / M2(qa-engineer 단독) / M3(복제본) / M4(나머지) 4커밋 — 명세대로. 위험 구간이 단독 커밋으로 분리돼 되돌리기 쉽다.

### 2. 의미강도 보존 감사관 (신규) — 원문 1:1 대조

`difflib.SequenceMatcher`로 32파일 전 변경줄의 편집 오퍼레이션을 뽑아 "순수 id 삭제" 형태에 매칭되지 않는 것만 육안 심사했다. 결과 **사람이 새 문구를 써 넣은 곳은 정확히 4곳**뿐이며 전부 검증했다.

| 위치 | 삽입된 문구 | 판정 |
|---|---|---|
| `agents/qa-engineer.md:25` | `위에 명시한 **라우팅 회귀·인증 스모크 이 두 유형에만**` | ✅ 같은 줄 전반부에서 가져옴. 창작 아님 |
| `agents/pm.md:104` | `"안 부르면 계속 안 불려짐" 실패 재발 방지` | ✅ **정당한 재사용**. 같은 줄 후반부(`이전 구조는 "안 부르면 계속 안 불려짐" 실패를 반복했으므로`)에서 인용했고, 원문에서 앞뒤 두 곳이 동일 id `ad845d8a`를 가리켰으므로 동일성 근거도 있다(`visual-designer.md:26`이 같은 id를 "안 부르면 계속 투입 안 됨" 위험으로 기술 — 교차 확인). 신규 정보 창작 아님 |
| `agents/localizer.md:20` | `같은 목업 엔티티의 정본이 확정되지 않으면 불일치가 생긴다는 기존 교훈이 로컬라이제이션에서 재현된 구체 사례` | ✅ id가 주어 자리였던 유일 케이스. 명세가 요구한 "교차 사례라는 정보"가 보존됐고 교훈 실질이 문장 안에 자립적으로 서술됨 |
| `knowledge/presentation/slide-design-guide.md:226` | `malgnai 구축 가이드(13페이지 PDF) 제작 세션 실측` / `해당 세션 트랜스크립트를` | ✅ 실측 수치 **전량 보존**(159턴/34.6M/$16.80, writer 120턴/$7, reviewer 91턴/$4.7, 6라운드, 13→4→4→7→13→2, 총 44장), 원인 분석 무변경. 조회 불가 식별자 2종(`세션 82291d88`, `agent-aab21b28208ecfdb6.jsonl`) 모두 제거 — 명세 요구 정확히 충족 |

- **살아있는 정보 보존 확인**: 파일 경로 참조 3건(`knowledge/common/screen-reuse-consistency-verification.md` × `reviewer.md:76`/`architect.md:26`/`frontend-dev.md:21`) 전부 잔존 ✅. 날짜(2026-07-23·2026-07-24·2026-08-19) 전부 잔존 ✅. POC 파일명 3개(`embed_kbdrafts.py` 등) 잔존 ✅. Skill 참조(`domain-pre-deployment-verification-gate`, `architect`) 잔존 ✅. *유일한 예외가 RV-001의 `13bcd60`인데, 이건 "보존됐으나 애초에 살아있지 않았다"는 반대 방향의 문제다.*
- **파편 스캔**: 전 변경줄 말미 60자 일괄 출력 + `(,` / `,)` / `()` / 이중공백 스캔 → **줄 끝 파편 0건**. 줄 중간 파편은 RV-004 1건뿐.
- **줄 수 불변**: 32파일 전부 베이스와 라인 수 동일(스크립트 확인) — 규칙이 통째로 사라진 곳 없음.
- **강조 표기**: `qa-engineer.md:25`에 원문에 없던 `**…**` 볼드가 추가됐다(⚪ Nit, 미기재). 한정자를 시각적으로 강화하는 방향이라 무해 이상 — 지적하지 않는다.

### 3. 죽은 참조 스코프 도전자 (발산형) — 🔵 Rethink

#### 🔵 RT-1: 스코프를 **id 형태**로 정의한 것이 이 라운드의 진짜 결함이다

- **현재 구조**: 탐지 조건 = `` `[0-9a-f]{8}` ``(백틱 + 8자리 hex). 목적 선언 = "설치 직원에게 조회 수단이 전무한 죽은 참조 제거".
- **어긋난 지점(실측)**: 두 기준이 세 곳에서 갈렸다.
  1. `commit \`13bcd60\`` — 형태 기준 밖(7자리) → 명세가 "살아있다"고 단정하고 제외. **실측 결과 이 저장소에도 없다**(RV-001).
  2. `memory \`feedback_coo-…\`` (`pm.md:29`) — 형태 기준 밖(hex 아님) → 손대지 않음. 설치 직원 조회 불가는 동일(RV-006).
  3. `agent-aab21b28208ecfdb6.jsonl` (`slide-design-guide.md:226`) — 형태 기준 밖이지만 **명세가 개별 예외로 지목해 제거**. `lesson 965d45ff`(백틱 없음, html) 역시 형태 기준 밖인데 trainer가 알아서 제거.
  → 즉 같은 라운드 안에서 판정 기준이 **형태**와 **조회 가능성** 사이를 오갔고, 그 결과 2건이 "게이트 통과" 도장을 받은 채 남았다.
- **대안 구조**: 스코프 정의문을 `백틱 8-hex`가 아니라 **"설치 직원이 자기 환경에서 확인할 수 없는 식별자 일체"**로 두고, 형태 목록(8-hex / 7자리 커밋해시 / auto-memory 키 / 로컬 트랜스크립트 파일명)은 그 정의의 *예시*로 열거한다. 게이트 G1도 같은 정의를 반영한 다중 패턴으로.
- **비용**: **저비용**. 근거 — 남은 3건(pm.md:36, reviewer.md:80, pm.md:29) 실물을 열어 확인한 결과 전부 순수 출처 꼬리표로 §3 A패턴과 동일 형태이며 문장 재구성이 불필요하다.
- **분류**: RV-001(2건)은 **결함** — "지금 무엇이 깨져 있는가"에 답이 있다(존재하지 않는 커밋을 근거로 제시 중). RV-006(1건)은 **개선/백로그**.

#### 🔵 RT-2: 재유입을 막는 기계 게이트가 없다 — 219건이 쌓인 경로가 그대로 열려 있다

- **실측**: `grep -rniE '8자리|8-hex|[0-9a-f]\{8\}' malgn-agent/ scripts/` → **0건**. `agents/evaluator.md:50`의 금지어 게이트는 **hub에 없는 도구명**만 검사하고 식별자 형태는 보지 않는다. `scripts/validate-agent-assets.mjs`(568줄)에도 hex/lesson 관련 검사 없음.
- **왜 문제인가**: 저장소 `CLAUDE.md`에 "제품 본문에 8자리 hex id를 새로 달지 않는다"는 **서술**은 있으나 기계 검사가 없다. 219건이 축적된 것이 바로 서술만 있고 게이트가 없었기 때문이다 — 이번 제거는 재고를 비웠을 뿐 유입관을 잠그지 않았다.
- **대안 구조**: `scripts/validate-agent-assets.mjs`에 WARN 규칙 1개 추가(`DEAD_IDENTIFIER`) — `malgn-agent/**`에서 백틱 8-hex / `memory \`feedback_*\`` / 7자리 커밋해시 인용을 잡아 WARN. hub ULID(26자)는 통과.
- **비용**: **저비용**. 근거 — 스크립트에 `warn(CODE, rel, msg)` 헬퍼(`:87`)와 파일 순회 구조가 이미 있어 규칙 함수 하나를 끼우는 형태다(실제로 열어 확인). 다만 WARN 기준선이 현재 18이라 **G2의 "WARN 18 유지" 회귀 기준선을 함께 갱신**해야 한다.
- **분류**: **개선 → 백로그**. 지금 깨진 것이 아니라 "이렇게 하면 재발을 막는다"이므로 변경 동결 원칙상 이번 라운드에 끼워넣지 않는다.

## 트레이드오프 / 페르소나 간 충돌

| 쟁점 | 정합성 감사관 | 발산형 | 권고 |
|---|---|---|---|
| RV-003(스코프 밖 html 파일 편집) | 명세 §2.1 위반 — 선언한 스코프 밖 편집은 검증 게이트를 우회한다 | 목적 기준으로는 **옳은 편집** — 형태 기준이 잘못이었지 편집이 잘못이 아니다 | **편집 유지**. 다만 보고 수치를 32파일/195줄로 정정하고 "스코프를 목적 기준으로 확장 적용함"을 명시 |
| RV-005("정정본" 소실) | 명세 §4.3 보존 요구 미충족 | 지시 대상 없는 파편을 남기는 게 더 나쁨 | **기각**(조치 불요). 명세 요구가 이 케이스에선 실현 불가 |

## 잘 된 점

1. **가장 위험한 줄(`qa-engineer.md:25`)이 가장 잘 처리됐다.** 닫힌 경계가 유지됐을 뿐 아니라, 지시 대상이 조회 불가 id에서 같은 줄의 본문 문구로 바뀌어 **자기완결성이 올라갔다**. (다만 `docs/methodology/audit-report.md:103`의 역참조는 이 치환으로 깨질 자리였고, PM이 리뷰 중 별도 갱신했다 — 산출물만으로는 자동 성립하지 않았다.)
2. **명세보다 넓은 커버리지 + 범위 결함의 자진 정정.** 백틱 없는 평문 id(html 파일)까지 잡아내 전 확장자 잔여 **0건**을 달성했고(재확인 완료), 그것을 M5 단독 커밋으로 분리한 뒤 명세 §8에 "확장자로 범위를 좁힌 것이 명세 결함이었다"고 명시 기록했다. 스코프 이탈을 조용히 묻지 않고 명세를 고친 처리가 모범적이다.
3. **창작 0.** 새로 써 넣은 문구 4곳 전부가 같은 줄/같은 파일 내 근거에서 왔다. 특히 `pm.md:104`는 원문에서 두 곳이 동일 id를 가리켰다는 점까지 성립하는 정당한 재사용이다.
4. **실측 수치 전량 보존.** `slide-design-guide.md:226`은 삭제 유혹이 가장 큰 줄(출처 줄)인데 159턴/34.6M/$16.80·라운드별 페이지 수까지 하나도 잃지 않았다.
5. **커밋 분할이 명세대로.** 위험 구간(M2)이 단독 커밋이라 부분 롤백이 가능하다.
6. **줄 수·규칙 불릿 수 불변** — 규칙이 통째로 사라진 곳이 없다.

## PM 권고

1. **RV-001을 trainer에 반환**(변경 동결 원칙의 "결함" 채택 대상). `pm.md:36`·`reviewer.md:80`의 `(사례: commit \`13bcd60\`)` 괄호째 삭제. 반환 전 PM이 `git cat-file -t 13bcd60`로 직접 반증 재현할 것 — 지적을 그대로 믿지 말라는 원칙 적용 대상이다.
2. **RV-004는 trainer 판단에 위임**(문안은 PM이 쓰지 않는다). "선택기준 세분화"를 삭제할지 경위임을 드러낼지.
3. **RV-002는 PM 직접 처리 영역**(`docs/`): 이미 추가한 명세 §8 표에 **D 수치 정정 행(21건/18줄 → 26건/19줄)**과 **§2.2-5 제외 근거의 사실 오류(`13bcd60`)** 두 줄을 더할 것. 그리고 워킹트리에 미커밋으로 남아 있는 `CLAUDE.md`·`docs/methodology/audit-report.md`·명세 §8을 커밋에 포함할 것(현재 미커밋).
4. **RV-005는 기각 권고.** 명세 §4.3의 "정정본 의미 보존" 요구가 이 케이스에선 실현 불가하며 불릿 제목이 이미 대체하고 있다.
5. **RT-2(린터 게이트)는 백로그.** 개선이지 결함이 아니다. 채택 시 G2 기준선(WARN 18)을 함께 올려야 한다.
6. **RV-006(`pm.md:29` memory 키)도 백로그.** RV-001 처리 시 같이 하면 저비용이나, 스코프를 늘리지 않는 편이 원칙에 맞다.
7. **RV-001 반영 후에는 재리뷰 불요.** A패턴 동일 형태 2줄 삭제이므로 PM 실물 대조로 충분하다.
8. **리뷰 중 대상이 움직인 점을 기록해 둘 것.** 이번엔 PM 갱신 방향과 리뷰 지적 방향이 같아 충돌이 없었으나, 리뷰 착수 후 대상 커밋이 추가되면 리뷰 보고서의 판정 기준선이 흔들린다 — 다음 라운드에서는 리뷰 위임 시 베이스·HEAD를 고정하고 리뷰 종료까지 동결하는 편이 안전하다.

## 생략한 것 (정직 보고)

- **PM이 이미 재현한 기계 검증(G1~G6)은 재실행하지 않았다** — 위임 지시의 중복 노동 금지에 따름. 다만 게이트의 **커버리지**는 독립 감사했고 그 결과가 RV-003이다.
- **UI/화면 리뷰 없음** — 대상이 전부 마크다운 문서라 해당 없음. 스크린샷 산출물 없음.
- **`docs/`·`malgn-agent/` 무수정** — 리뷰 보고서(`docs/reviewer/`)와 페르소나 파일 3개(1 재사용 append + 2 신규), `docs/reviewer/personas/INDEX.md` 갱신 외에는 어떤 파일도 편집하지 않았다. **실행 액션(커밋·병합·push) 없음.**
- **리뷰 중 HEAD가 `4e27740`→`05e0273`으로 전진했다.** 재확인은 했으나, 본 보고서의 모든 인용 라인 번호는 `05e0273` + 미커밋 docs 기준이다. 그 이후 추가 변경분은 검토 범위 밖이다.
- **A패턴 156건의 개별 육안 검토는 하지 않았다** — 스크립트로 "순수 id 삭제 형태"에 매칭되는 오퍼레이션을 자동 분류해 제외했고, 매칭되지 않은 것만 전건 육안 심사했다. 분류 로직이 틀렸다면 놓쳤을 수 있으나, 별도로 수행한 "전 변경줄 말미 60자 스캔"과 "줄 수 불변 확인"이 교차 검증망 역할을 한다.
