# `agents/pm.md` 예산 초과 사유서

- 대상: `malgn-agent/agents/pm.md`
- 권고 예산: 25 KB (`BUDGET_PM_KB` — 다른 메타 에이전트(15 KB)와 분리된 pm.md 전용 임계값. 근거: [pm-md-axis-split-design.md](pm-md-axis-split-design.md) §8)
- 이 문서가 변호하는 실측 크기: **40,178 B (39.2 KB)**
- 작성: PM. 근거: 재배치 라운드 커밋(`de0eea9`·`0cfc736`)의 실제 diff, `docs/reviewer/review-agents-md-relocation-2026-08-31.md`(풀패널 Amber 판정) 원문 대조, pm-md-slimming 라운드(중복 3건 병합 + 조건부 블록 2건 스킬 이관) reviewer/evaluator 판정 원문 대조, 현재 `pm.md` 전문 재독.
- 40,818 B 이후 증가분(+2,419 B)은 등급 판정을 위험·가역성 기준으로 바꾸고 Standard에 Fast Path를 신설한 라운드에서 붙었다 — Fast Path 5조건·이탈 시 즉시 보고 의무·WBS 트리거 축소·reviewer 조건부 호출 트리거를 핵심 원칙에 직접 적었다. 전부 위임 전에 PM이 스킬을 열지 않고도 판단해야 하는 문장이라 §1의 "감량 금지 구역"과 같은 성격이다(옮기면 상시 로드에서 조건부 로드로 바뀌어 위임 시점에 못 보게 된다).

## 1. 무엇으로 채워져 있는가 (섹션별 실측)

| 절 | 대략 범위 | 바이트 | 성격 |
|---|---|---|---|
| 토론 문화(비협상) | :14-25 | 718 B | 의사결정 5원칙 — 짧다 |
| 핵심 원칙 실행 규율(등급판정·WBS·검증·위임 등 20여 개 불릿) | :28-55 | 15,825 B | 전부 "이 문장이 없으면 무엇이 반복 재발했는가"가 붙은 행동 규칙 |
| 역할 경계 + PM 권한 참조표 + 승인 게이트 4항 + 전역 자산 승격 절차 | :56-117 | 16,997 B | 등급별 승인 주체·비가역 게이트·서브에이전트에서 `AskUserQuestion`을 못 쓸 때의 대응 — 이 저장소가 여러 세션에 동시에 위임되는 PM 오케스트레이터라는 구조 자체가 요구하는 분량 |
| 스킬 상세 + 전제조건 + 자기검증 체크리스트 | :118-140 | 2,289 B | 보고 전 실물 대조 체크리스트(9항목 — 유형별 추가 검증 5종은 `project-orchestration` 부록으로 이관) |
| 산출물 + 학습 자료(필수/참고) + 토큰 효율 | :141-174 | 3,756 B | 대부분 Skill/Knowledge 포인터 — 조건부 비용으로 이미 밀어낸 자리 |

핵심 원칙(28-55행)과 권한표(56-117행)가 전체의 약 82%(32.1 KB)를 차지한다. 이 두 구역은 재배치 라운드에서 "**절대 훼손 금지**" 대상으로 명시 지정됐고(승인 게이트 4항·권한 참조표), 위 리뷰 보고서의 감량 금지 구역 대조에서 **diff 헝크 자체가 없다**(원문 그대로) — 재량으로 줄일 수 있는 자리가 아니라, 재배치 라운드가 이미 "건드리지 않기로" 확정한 자리다.

## 2. 재배치로 이미 거른 것 — 그런데도 넘는다

라운드 전 45,094 B였다. 12건의 이관 후보(STATUS.md 6시점 규율 → `project-standards` §3 / 동시성·되돌릴 지점 → `domain-git-safety-and-concurrency` / 기록 폴백 → `common-learning-loop-knowledge-management` / 전역 승격 절차 상세 → `evaluator.md` / 검증 실무 4건 → `pm-verification-field-notes.md` / 위임 프롬프트 규율 → `project-orchestration` §4.1·§4.2)을 실행해 40,877 B로 줄었고, 승인 범위 초과분 정리(`0cfc736`)로 40,818 B가 됐다. **9.3% 줄었지만 권고 예산(15 KB)의 2.7배를 여전히 넘는다.**

이 라운드는 reviewer 풀패널에서 🟡 Amber(Major 3·Minor 5·Rethink 2)로 조건부 통과됐다. 그 보고서가 실측한 부수 효과: **옮긴 자리(skills+knowledge, +30,136 B)가 줄인 자리(agents, −10,535 B)의 2.9배로 부풀었다.** 즉 pm.md의 바이트는 사라진 게 아니라 상시 비용에서 조건부 비용으로 자리를 옮겼을 뿐이고, 이번에 남은 39.9 KB는 "더 옮길 수 있는데 안 옮긴 것"이 아니라 **옮기면 실행이 깨지는 것만 남긴 결과**다(§1의 감량 금지 구역이 그 경계).

이후 pm-md-slimming 라운드(중복 3건 병합 + 조건부 블록 2건을 `project-orchestration`/`domain-git-safety-and-concurrency` 스킬로 이관)에서 40,818→40,178 B로 추가로 줄었다. 옮긴 자리(skills, +4,682 B)가 줄인 자리(agents, −3,686 B)의 1.27배로, 직전 라운드(2.9배)보다 팽창비가 크게 개선됐다 — reviewer 풀패널 실측치(🟡 Amber, Major 1·Minor 4).

## 3. 잘라낼 것을 찾았는가 — 찾았고, 전부 구조 변경이라 백로그다

위 리뷰 보고서와 별도 감사(`review-backlog-consistency-20260825.md` RT-001)가 실제로 압축 가능한 자리를 지목했다. **전부 변경 동결 원칙상 개선이지 결함이 아니라 착수하지 않았다.**

- **RT-001 (표 중복) — 방향 반전, 사실상 해소**: PM 권한 참조표(`pm.md`:72-82)의 등급별 승인 주체 값이 Skill `common-task-grading-and-verification-depth`의 판정표(SKILL.md:15-21)에도 **값으로** 중복 존재한다. 애초 제안(등급 축 소유권을 스킬로 옮기고 pm.md를 포인터로 축약)은 [pm-md-axis-split-design.md](pm-md-axis-split-design.md) §2의 G축(게이트축) 정의와 충돌한다 — 권한표는 승인 경계라 조건부 로드로 못 내린다. **올바른 방향은 반대다: 스킬 쪽 사본을 포인터로 바꾸고 pm.md를 정본으로 그대로 둔다.**
- **리뷰 보고서 Minor #5 — 해소됨**: pm.md:42 "진행 상태 라벨은 실물로 대조" 규칙과 `domain-git-safety-and-concurrency` §1의 중복 서술을 pm-md-slimming 라운드에서 정리했다. 정본을 스킬 §1-1로 통합하고, pm.md에는 4갈래(git 상태 서술·WBS/이슈 상태·반복 지시·감사문서 "미반영") 이름만 나열한 트리거 문구로 축약했다 — 판정 가능성은 유지하면서 상시비용 중복은 제거됨(reviewer 실물 대조로 확인).
- **리뷰 보고서 Minor #6**: trainer.md 자기검증 3항목의 포인터 대상이 모드 1·4 전용 스킬인데, 정작 그 절차가 가장 필요한 모드 6(수동 MD 정리)은 그 스킬을 로드하지 않는다 — pm.md 쪽 직접 해당은 아니나 같은 라운드의 구조 문제.
- **리뷰 보고서 Rethink #2 — 결정됨**: pm.md를 불변축/가변축으로 가르자는 제안은 [pm-md-axis-split-design.md](pm-md-axis-split-design.md)에서 4축(G/S/C/X) + 배분 규칙으로 구체화돼 사용자 승인을 받았다. "가변축 단일 목적지 신설"은 채택하지 않고(이관 후보 대부분이 이미 소유 문서를 가지고 있어 새 그릇이 새 중복을 만든다), 기존 문서로 배분하는 규칙으로 대체했다. 실행은 Round A(이동+중복정리)부터 시작하고 Round B(트리거 슬리밍)는 별도 승인 대상이다.

세 제안 모두 **판단이 갈리지 않는 대상**(중복 서술·구조 재배치)이라 성능 훼손 없이 줄일 여지가 실재하지만, pm.md 본문의 다른 서술(권한표 참조 방식, 승인 게이트 절 번호 등)을 함께 고쳐야 해서 "성능에 기여하지 않는 것만 제거"를 넘는 구조 변경이다. 변경 동결 원칙(`CLAUDE.md`)상 개선 백로그로 남기고 지금 착수하지 않는다.

## 4. 비용의 성격

**상시 비용이다** — pm이 호출될 때마다 전량 로드된다. §1의 77%(핵심 원칙·권한표)는 재배치 라운드가 "이관하면 실행이 깨진다"고 판정해 남긴 것이고, 나머지는 이미 조건부 비용(Skill/Knowledge)으로 포인터화된 자리다. §3에서 찾은 잘라낼 여지는 전부 **구조 변경**이라 이 사유서가 변호하는 대상이 아니라 백로그의 몫이다.

## 5. 다시 감축을 논의할 조건

- RT-001은 방향이 반전됐다(위 §3) — pm.md 쪽을 줄이는 경로가 아니라 스킬 쪽 중복을 제거하는 경로다.
- 리뷰 보고서 Rethink #1(판단이 갈리는 지시는 MD에 한 벌만)은 Minor #5(pm.md:42류)에서 이미 실증됐다(pm-md-slimming 라운드).
- **Rethink #2는 결정 완료** — [pm-md-axis-split-design.md](pm-md-axis-split-design.md) Round A 실행이 다음 단계다. Round A 완료 후 예상치는 pm.md ≈ 36 KB(여전히 25 KB 예산 초과, 이 사유서가 계속 방어한다).
- Round B(트리거 슬리밍) 채택 여부, `project-orchestration` 색인 크기 규칙(설계안 §4) 적용은 별도 사용자 판단 대상.
