# `agents/trainer.md` 예산 초과 사유서

- 대상: `malgn-agent/agents/trainer.md`
- 권고 예산: 15 KB (`AGENT_BUDGET_KB`)
- 이 문서가 변호하는 실측 크기: **26,998 B (26.4 KB)**
- 작성: PM. 근거: 재배치 라운드 커밋(`de0eea9`)의 실제 diff, `docs/reviewer/review-agents-md-relocation-2026-08-31.md`(풀패널 Amber 판정) 원문 대조, 현재 `trainer.md` 전문 재독.

## 1. 무엇으로 채워져 있는가 (섹션별 실측)

| 절 | 범위 | 바이트 | 성격 |
|---|---|---|---|
| 핵심 원칙(자율실행·산출물 우선·저작 규율·중복 저작 방지·Skill/Knowledge 신설 판정) | :12-25 | 5,835 B | 특히 "신설 판정" 항목(:25)이 5개 기준을 전부 담아 단독으로 길다 — 매 학습 반영마다 서는 판단이라 압축하면 판정이 사람마다 갈린다 |
| 역할 경계 + 3자 책임 구분 요약 표 | :27-54 | 5,159 B | Trainer/Evaluator/PM 권한 경계 — 재배치 라운드에서 "**절대 훼손 금지**" 대상으로 지정, 리뷰 보고서 감량 금지 구역 대조에서 diff 헝크 없음(원문 그대로) |
| 실행 모드 표(6종) + 모드 5·6 상세 + MD 최적화 판단기준 4항 | :56-85 | 5,150 B | 모드별 명령어·실행경로·소요시간, 모드 6(MD 최적화)의 "1순위 성능·2순위 토큰효율" 판단기준은 이 저장소 CLAUDE.md "에이전트 업그레이드 원칙"의 실행판이다 |
| 전제 조건(git 저장소 필요·브랜치 규율·push 금지) | :87-91 | 1,081 B | 3줄, 짧다 |
| 자기 검증 체크리스트(11항목) | :93-106 | 3,746 B | 존재확인·보존확인·저작규율·정직보고·hub기록·다중대상·경로실재·역참조 등 |
| 산출물 | :108-114 | 757 B | 3줄 |
| 학습 자료(필수 6개 + 참고 9개) | :116-139 | 4,039 B | 대부분 Skill/Knowledge 포인터 — 조건부 비용 |
| 토큰 효율 | :141-149 | 321 B | 포인터 1줄 + 3불릿 |

핵심 원칙(12-25)과 역할경계+책임표(27-54)를 합치면 전체의 약 41%(11.0 KB)이고, 여기에 실행 모드 표(56-85, 5.2 KB)까지 더하면 60%를 넘는다. 이 세 구역이 trainer의 정체성(무엇을 언제 어떻게 하는가 + 다른 두 에이전트와의 경계)을 정의하는 핵심이라 압축 여지가 가장 적다.

## 2. 재배치로 이미 거른 것 — 그런데도 넘는다

라운드 전 30,029 B였다. 4건의 이관(식별자·이력 금지 저작 규율 → `domain-product-body-authoring-rules` / `work_record` 주인 판별 상세 → `common-learning-loop-knowledge-management` / 모드 1~4 절차 4개 절 → 모드 표 "참고" 열 + 각 실행 스킬 / 자기검증 3항목 상세 → `agent-upskill`·`reflect-lessons`)을 실행해 25,783 B로 줄었다. 이후 `agents/*.md` 3개 공통 커밋(`de0eea9`)에서 신규 knowledge 참조 등 소폭 조정이 반영돼 현재 26,998 B다. **10.1% 줄었지만 권고 예산의 1.8배를 여전히 넘는다.**

## 3. 잘라낼 것을 찾았는가 — 찾았고, 구조 변경이라 백로그다

- **리뷰 보고서 Minor #6**: 자기검증(:95-106)의 "문서경로 참조 실재 대조"·"기록 주체 서술 전수 확인"·"역참조 갱신 확인" 세 항목이 모드 1·4 전용 스킬(`agent-upskill`·`reflect-lessons`)을 정본으로 가리키는데, 정작 이 절차가 가장 필요한 모드 6(수동 MD 최적화 — 중복병합·죽은참조제거)은 그 스킬을 로드하지 않는 수동 실행이라 트리거가 비어 있다. 세 절차를 모드 중립 자리로 모으거나 모드 6 항목에서 별도 지목하면 포인터 구조가 정리되지만, 그러려면 자기검증 절 전체의 앵커 배치를 다시 짜야 한다.
- **`work_record` 주인 판별 잔여 중복**(재확인, 현재 시점): :52 "`work_record`는 역할이 아니라 이 기록의 주어가 누구인가로 주인이 갈린다"는 3분기 판별 문단이 `common-learning-loop-knowledge-management` "`work_record` 주인 판별" 절(SKILL.md:37-43)에도 거의 동일한 3분기로 전문 서술돼 있다 — 재배치 표는 이 항목을 "이관"으로 표기했지만 실제로는 원본이 trainer.md에 그대로 남고 스킬 쪽에 사본이 하나 더 생긴 형태다. pm.md Minor #5와 같은 패턴(상시비용에서 조건부비용으로 옮긴다는 재배치 목적에 반하는 잔여 중복)이다.
- **리뷰 보고서 Rethink #1**: "판단이 갈리는 지시는 MD에 단 한 벌만 남기고 나머지는 순수 포인터로" 기준을 통일하자는 제안이 trainer.md에도 그대로 적용된다 — 위 두 항목이 그 기준 미확정 상태의 구체 사례다.

세 항목 모두 **판단이 갈리지 않는 대상**(중복 서술·앵커 배치)이라 성능 훼손 없이 줄일 수 있지만, trainer.md 다른 절(자기검증 체크리스트 순서, 역할경계 절의 인용 방식)까지 함께 손대야 해서 "성능에 기여하지 않는 것만 제거"를 넘는 구조 변경이다. 변경 동결 원칙상 지금 착수하지 않고 백로그로 남긴다.

## 4. 비용의 성격

**상시 비용이다** — trainer가 호출될 때마다 전량 로드된다. §1의 60% 이상(핵심 원칙·역할경계·실행모드표)은 정체성·경계 정의라 재배치 라운드도 훼손 금지로 지정한 자리이고, 나머지 자기검증·학습자료 구역에서 §3이 찾은 잘라낼 여지는 전부 다른 절과 얽힌 구조 변경이라 이 사유서가 지금 변호하는 대상이 아니다.

## 5. 다시 감축을 논의할 조건

- `work_record` 주인 판별 중복(§3 2번째 항목)은 트리거 문장만 다시 쓰면 되는 국소 수정에 가깝지만, "정본은 스킬" 표기를 trainer.md 역할경계 절 서술과 정합시켜야 해서 편집 권한 경계상 trainer 위임 + PM 검증 사이클이 필요하다.
- Rethink #1(포인터 기준 통일)이 pm.md와 함께 채택되면 이 항목과 Minor #6이 같은 라운드에서 해소될 가능성이 크다 — 두 파일을 따로 손대면 기준이 다시 갈릴 수 있으므로 같은 라운드로 묶는 편이 낫다.
