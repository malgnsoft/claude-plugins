# STATUS 아카이브 — 2026-08 이전 라운드 이력

루트 `STATUS.md` 가 61KB까지 불어나 세션 시작마다 통째로 주입되던 것을 L0 오리엔테이션
크기로 줄이면서(2026-08-24), **지우지 않고 이리로 옮긴 원문**이다. 아래 내용은 옮길 당시의
`STATUS.md` 원문 그대로이며 한 글자도 고치지 않았다 — 그래서 시제·"현재"·"다음에 할 것"
같은 표현은 **작성 시점 기준**이다. 지금 살아 있는 상태는 언제나 루트 `STATUS.md` 가 정본이다.

옮긴 범위: 옮길 당시 `STATUS.md` 의 18~450행(보류 백로그 원본 + v1.7.8~v1.7.2 라운드 서술 +
폐기된 라운드 기록 + 열린 이슈 상세) 과 464~467행(최근 완료).

---

### 보류 백로그 (동결 해제 시 재검토)
- ~~**죽은 참조 재유입 방지 기계 게이트**~~ → **해소 (2026-08-23, 사용자가 "소소한 개선"으로 승인)**. 아래 「린터 게이트 신설」 절 참조. 원 기록:  `scripts/validate-agent-assets.mjs`에 검사 구멍이 둘이다. ⑴조회 불가 식별자 검사 **0건**(224건이 쌓였던 유입관이 그대로 열려 있다) ⑵**`knowledge/` 본문을 참조 원천으로 순회하지 않는다** — 참조 대상 인벤토리로만 수집하고 `REF_SKILL_MISSING`은 agents·skills 본문에서만 난다. 그래서 죽은 스킬 참조 16건이 개명 이후 약 2주간 **ERROR 0 초록불 아래에서 생존**했다(v1.7.5로 수리). 둘 다 `warn()` 헬퍼와 기존 3요소(`skillDirNames`·`knowledgeFiles`·`liveReferences`)로 저비용. 출처: reviewer RT-2·RT-001 (2026-08-22). 개선이므로 보류하나, **게이트 없이 가면 다음 개명 라운드에서 같은 사고가 재발한다**
- ~~**`agent-training-guide.md` 모드 체계 드리프트**~~ → **해소 (2026-08-23, v1.7.6)**. 아래 「모드 체계 드리프트 정합」 절 참조. 원 기록: **결함 후보, 사용자 판단 대기** (2026-08-22 발견). 이 가이드(576줄, `knowledge/README.md`가 MD 골격 정본으로 지목)는 "9가지 모드"로 `### 모드 7: 산출물 기반 진단`·`### 모드 8: MD 최적화`를 두고 모드 7 실행 지시를 10곳에서 한다. 그러나 `agents/trainer.md`는 **구 모드 7을 evaluator로 완전 이관**했고 trainer는 1~6이며 **모드 6이 MD 최적화**다 — 없는 모드를 실행하라는 지시 + 같은 작업의 번호가 6↔8로 갈린다. trainer.md에 정정 문구가 있어 즉시 오작동은 아니나 모순 지시가 공존한다. 이름 치환이 아니라 **내용 개정**이라 범위가 커 별건으로 분리
- ~~hub ULID 3건 조회 가능성 확인~~ → **해소**: 사용자가 확인 대신 삭제를 지시(2026-08-22). knowledge 3건 + `bin/report-usage.mjs` 주석 2건 = 5건 제거, v1.7.4 배포
- ~~**`~/.claude/hooks/`의 묵은 legacy `doc-drift.mjs`**~~ → **해소 (2026-08-23, v1.7.8)**. 홈의 파일을 지우는 대신 **자동 폴백 자체를 없앴다** — 파일 하나 지우면 그 PC만 조용해지지만, 폴백을 없애면 모든 PC에서 같은 은폐가 막힌다. 원 기록: 이 PC에 7월 2일자 파일이 남아 있어, 스캐폴딩된 `check-docs`가 잘못된 마켓플레이스 경로를 찾지 못해도 **조용히 그 옛 검사기로 폴백**하고 있었다. 그래서 결함이 이 PC에서는 증상을 드러내지 않았다(신규 직원 PC에서는 안내문이 떴다). 파일 자체를 지울지·경고할지는 별건
- ~~**`agent-training-guide.md` §8 스냅샷**~~ → **해소 (2026-08-23, v1.7.8)**. 숫자는 재산정 불가(측정 스크립트 미번들)이므로 한 글자도 바꾸지 않고 지위·시제만 "지나간 기록"으로 바로잡았다.

## 🟢 실행 못 할 지시 걷어내기 — v1.7.8 (2026-08-23, **배포 완료** — origin/main 반영됨)

사용자 질의 *"다음으로 개선할 2순위는 뭔가"*에서 출발했다. 제안서 표의 2순위(에이전트 `skills:` 프리로드)는 그 제안서 자신이 "비용 높음"으로 보류 권장한 항목이라 채택하지 않고, **비용 0에 가까운 후보 4(description)**를 위로 올렸다. 진행 중 대조에서 결함 3건이 추가로 나와 함께 처리했다.

### ① 무엇이 깨져 있었나
전부 **"지금 실행하면 안 되는 지시"**다. 개선이 아니라 결함이라 동결 원칙에 걸리지 않는다.

| 결함 | 실물 |
|---|---|
| 에이전트 5종이 **내부 번호**로 호출 시점을 서술 | `description`은 클로드가 위임 대상을 고르는 입력인데 "PM이 웹/앱 개발 STAGE 3에서 호출"이라 적혀 있었다. "API 만들어줘"에 매칭될 단서가 없다 |
| §8이 7월 스냅샷을 "현재 현황"으로 | 19종 표(evaluator·localizer 없음) + "이번 주" 액션 플랜이 현재 지시처럼 |
| `knowledge/README.md`가 "9가지 훈련 모드" | 가이드 자신은 세 곳에서 6가지 |
| **없는 폴더를 확인하라는 지시** | "모든 에이전트는 작업 전 `lessons/` 폴더를 확인" — 그 폴더가 없다. `security/`는 README 한 파일 안에서 앞뒤가 반대(19행 "폴더 있음" ↔ 96행 "전량 이관됨") |
| **없는 모드를 쓰라는 지시** | "trainer 모드 7은 이 골격을 공용 진단 잣대로 쓴다" — 모드 7은 evaluator로 이관됨 |
| **조용한 폴백** | 스캐폴딩된 `check-docs`가 플러그인 검사기를 못 찾으면 말없이 홈의 옛 검사기로 넘어갔다 |
| 찾아봐도 안 나오는 예시 | `pm.md:31`이 옛 `lesson_classify` 파라미터 `classified_agents`를 예시로 들었다 |

### ② 무엇을 했나
- **description 5종**: 실제 요청 문구를 단서로 넣고 단독 호출 경로 유지. 상시 비용 **+410B**. `use proactively`는 넣지 않았다 — 훅으로 PM 경유를 강제하는 이 제품 구조와 부딪힌다
- **§8**: 숫자를 한 글자도 바꾸지 않고 지위·시제만 "지나간 기록"으로. 재산정 불가한 값을 갱신하면 그건 측정이 아니라 창작이다
- **`lessons/`**: 학습 루프를 hub 조회·기록으로 교체. **쓴 도구는 스키마를 열어 확인한 것만** — hub에 `lesson_*`은 없다. 회고 절차 정본은 `retrospective-framework.md` "3단계"로 일원화
- **`security/`**: 폴더표 행 삭제(없는 폴더로 안내하는 색인). 이관처 정보는 같은 파일이 보존
- **모드 7**: 지시 2건만 고치고 **날짜 붙은 경위 서술 15건은 보존**. 전수 27건을 지시/이관표기/이력으로 분류
- **폴백**: 자동 폴백 제거. 못 찾으면 ⑴실제로 탐색한 경로를 인쇄 ⑵옛 검사기가 있으면 "있지만 실행하지 않는다 + 이유 + 수동 명령"

### ③ 어떻게 확인했나 (PM 독립 재현)
- **가짜 초록불을 실물로 재현했다.** 같은 스캐폴딩 프로젝트에서 현행 검사기는 `⚠️ PM 행동규율 @import 불일치`를 잡아내는데, 옛 검사기는 `✅ 문서가 코드와 일치` exit 0. 옛것에는 그 점검이 **통째로 없다**(grep 0건 대 현행 5곳). 지난 릴리스의 경로 결함이 개발 PC에서만 증상을 안 드러낸 이유가 이것이다
- 가짜 HOME 2종(옛 검사기 있음/없음)으로 폴백 분기 출력 육안 확인
- **hub 도구 5종은 PM이 스키마 원문을 열어 대조** — `project_search_history`는 `projectId`+`query` 필수에 `types` enum이 decision/issue/work, `agent_get_context`는 `agentName` 필수. 제품 본문 서술과 일치
- `agents/` diff가 정확히 description 5줄 · §8 삭제 9줄이 전부 대체된 제목·라벨(정보 손실 0) · 살아있는 `lessons/` 지시 잔존 0(남은 2건은 부정형) · 없는 모드 현재형 지시 0 · `classified_agents` 0
- `check-assets` **ERROR 0 · WARN 18**(기준선) · `plugin validate` 통과 · `node --check` 통과 · 식별자 유입 0 · `agents/` 265,926 → 266,333B

### ④ 이 라운드가 값을 한 지점
- **위임받은 쪽의 지적을 실물과 대조해 기각한 것.** trainer가 "`reflect-lessons` 스킬이 실행 불가일 수 있다"고 올렸고 PM이 그대로 사용자에게 넘겼는데, 확인해보니 **이미 hub로 이관이 끝나 있었다**(제품 전체 `lesson_*` 0건). 지웠다면 죽은 참조 5건이 새로 생기고, "기록까지만 하고 반영은 reflect-lessons가 전담"이라 넘기는 모드 2의 후속 절차가 끊겼다. *지적도 채택 전에 실물과 대조한다 — 이번엔 PM이 한 번 놓쳤다가 사용자 질문에 답하며 잡았다*
- **지시와 이력을 가른 것.** 모드 7 27건 중 지시 2건만 고치고 이력 15건을 남겼다. 기계적으로 밀었다면 "무엇이 그 지식을 낳았는가"가 지워진다
- **폴백은 파일이 아니라 코드에서 끊는 게 맞았다.** 홈의 옛 파일을 지우면 그 PC만 조용해진다

### ⑤ 미확인 (정직 표기)
- **위임 정확도 개선을 측정하지 않았다.** 새 description이 실제 세션에서 더 정확히 선택되는지 재현 실험은 돌리지 않았다. before/after 대조는 어휘 판단이지 관측이 아니다
- **Windows 미확인**: 폴백 안내문 앞머리에 `⚠️` 이모지가 처음 들어갔다. `cmd.exe` 코드페이지에서 깨질 수 있다. macOS에서만 확인
- **hub 도구를 실제 호출해보지는 않았다** — 스키마까지만 확인
- **v1.7.7의 미확인 3건이 그대로 남아 있다** (아래 절 ⑤ 참조): 캡처 png 산출물 관찰 · 설치본 경로 재확인 · Windows

### ⑥ 남은 판단거리
- `bin/new-project.mjs`가 플러그인을 못 찾았을 때 **종료코드 0**을 유지한다(원래 동작). 1로 바꾸면 플러그인 없는 PC에서 `check-docs`가 CI 실패로 뒤집힌다 — 별건
- 이 PC의 `~/.claude/hooks/doc-drift.mjs`(7/2자) 파일 자체는 **그대로 뒀다**. 자동 폴백이 사라져 무해하고, 지우는 것은 제품이 아니라 개인 환경 정리다

---

## 🟢 번들 스크립트 도달 경로 결함 수리 — v1.7.7 (2026-08-23, **배포 완료** — origin/main 반영됨)

앤트로픽 공식문서를 실제 조회해 제품과 대조하고, 효과가 가장 큰 **결함 1건**을 골라 수리했다.
제안서 `docs/roadmap/anthropic-docs-gap-proposal-2026-08-23.md`(후보 9건 비교표) · 리뷰 `docs/reviewer/review-bin-script-reach-path-2026-08-23.md`(Amber, Major 5건 전건 채택).

### ① 무엇이 깨져 있었나
번들 `bin/` 스크립트 12개에 도달하는 길이 **셋으로 갈라져 하나만 동작**했다.
- 추측형 플레이스홀더 `<malgn-agent 플러그인 경로>` **31건** — 절대경로를 알아낼 방법이 제품 어디에도 없었다
- 맨 명령어 — PATH 등재는 되지만 12개 중 **6개가 mode 644**라 `permission denied`(git에 커밋된 상태 = 전 직원 배포본이 그랬다)
- 결과: `capture.mjs`(에이전트 5명의 화면 검증 근거) · `check-edge-api-security.mjs` · `diff-env-keys.mjs` · `check-output-conventions.mjs` · `check-wbs-warnings.mjs`가 **첫 명령에서 실패** → 검증을 서술로 때우기 쉬운 구조

### ② 무엇을 했나
- **정본 한 갈래로 통일**: `node "${CLAUDE_PLUGIN_ROOT}/bin/<스크립트>.mjs"`. 규약은 `skills/common-output-storage-and-path-management/SKILL.md` §1-1 **한 곳**에만 둔다(31곳 반복 금지 = 조건부 비용 절약)
- **치환 범위를 관측으로 확정**: 스킬 본문·에이전트 본문·훅 커맨드는 치환됨 / Read로 연 파일은 치환 안 됨 / MCP·LSP는 **미확인으로 명시**. 기준은 디렉토리가 아니라 **"하네스가 컴포넌트로 로드하느냐"**
- **`bin/*.mjs` 11개가 `process.argv[1]`로 자기 실제 경로를 인쇄**한다 — 경로를 문서에 두 벌 두지 않으니 드리프트가 원천 불가
- **따옴표 필수**: 홈 경로에 공백이 있으면 무따옴표는 `MODULE_NOT_FOUND`(공백 경로로 재현). `hooks/hooks.json`과 형태 통일
- **회귀 게이트 2종** 신설(`scripts/`, 배포물 아님): `PLUGIN_PATH_PLACEHOLDER`·`BARE_SCRIPT_COMMAND`. 검사 대상 스크립트 이름을 **디스크에서 수집**하므로 새 스크립트도 자동 포함
- **`new-project.mjs` 결함 3건**: ⑴스캐폴딩된 `check-docs`가 `marketplaces/<별칭>/plugins/malgn-agent/`를 찾았으나 실제 레이아웃에 `plugins/` 세그먼트가 없음 ⑵`--help`가 도움말 대신 `~/workspace/--help/` 폴더를 생성 ⑶드리프트 발견 시 `execSync`가 예외를 던져 **정상 보고 위에 Node 스택 트레이스**가 쏟아짐(→ `spawnSync`, 종료코드 전파는 유지)

### ③ 어떻게 확인했나 (PM 독립 검증)
- `pnpm run check-assets` **ERROR 0 · WARN 18** (기준선 유지, 새 경고 0) · `claude plugin validate` 통과
- **양성 대조군 4종**을 PM이 직접 재실행 — `.md` 플레이스홀더/맨 명령어 주입 → ERROR, 스크립트 **런타임 문자열** 주입 → ERROR(신규 커버리지), **헤더 주석** 주입 → 미검출(의도된 제외). 원복 후 ERROR 0
- 임시 디렉토리에 실제 스캐폴딩 → `check-docs`가 올바른 설치 경로를 찾아 **드리프트 검사를 실제로 실행**, 스택 트레이스 없이 종료코드 1
- `--help`/`-h`/오타 플래그 실행 전후 `~/workspace/` 대조 → **폴더 생성 0건**
- `bin/` 실행비트 변경 0건 · 스크립트 인자문법·플래그·종료코드 불변

### ④ 이 라운드가 값을 한 지점
- **게이트를 먼저 만든 것이 수작업 누락 8건을 잡았다** — 손으로 훑은 뒤 남은 복사-실행용 코드블록을 린터가 검출
- **초안의 규약 문안 자체가 틀렸다**: "치환은 스킬·에이전트 본문에서만"이라 단정했으나 `hooks/hooks.json`이 반례(그 훅은 실제로 돈다). 그대로 배포했다면 **다음 라운드가 규칙대로 hooks.json을 "고쳐서" 전원의 SessionStart/Stop을 깨뜨릴** 수 있었다 → §1-1에 "바꾸지 마라"를 명시
- **"오탐 43건"이라는 제외 근거가 사실이 아니었다** — 한 줄의 중복 매치를 센 값이었고 실제로는 헤더 주석 + 런타임 문자열. 런타임 쪽을 고치자 제외 범위가 **헤더 주석 하나**로 좁혀졌다. *숫자를 근거로 스코프를 좁힐 때는 그 숫자의 산출 방식부터 본다*

### ⑤ 미확인 (정직 표기)
- **산출물 관찰 미수행** — 수정 후 ux-designer/reviewer가 실제로 캡처 png를 남기는지 보지 않았다. "경로가 원인이었나"에 답하는 **유일한 관측**인데 미완이다
- **Windows 미확인** — A안 채택으로 실행비트 의존은 사라졌으나, 치환된 경로로 `node`가 도는지는 보지 않았다
- **MCP·LSP 치환 여부 미확인** — 공식문서에는 있으나 관측 못 함. §1-1에 미확인으로 표기
- 공식문서 5종(`memory`·`slash-commands`·`settings`·`mcp`·`common-workflows`) 미조회 — 더 큰 갭이 있을 가능성 배제 못 함

### ⑥ 보류(제안서 후보 8건, 전부 개선이라 동결 유지)
에이전트 `skills:` 프론트매터 미사용 · `model:` 미지정 · `description`이 위임 트리거가 아님 · `tools:` 미지정 9종 · 상시비용 비대 · 훅 이벤트 2종만 사용 · `disable-model-invocation` 미설정 · 죽은 스킬 참조 1건(경미)

---

## 🟢 보류 백로그 2건 소진 — 린터 게이트 + 모드 드리프트 (2026-08-23, **배포 완료** — origin/main 반영됨)

사용자가 두 건을 "큰 변화가 아닌 소소한 개선"으로 승인해 동결 예외로 진행했다.

### ① 린터 게이트 신설 (`68ee332`, `scripts/` — 배포물 아님, 버전 무관)
백로그가 지목한 검사 구멍 2개를 막았다. **기준선 ERROR 0 · WARN 18 그대로 유지(오탐 0).**
- `knowledge/` 본문을 참조 **원천**으로 순회. agents·skills·knowledge 세 영역의 참조 검사를 `checkBodyReferences`로 일원화
- 경로형 참조 `skills/<이름>/SKILL.md`를 검사 대상에 추가(죽은 참조가 호출형·경로형에 섞여 있었다)
- **핵심 수리**: "…로 이관됨" 줄을 통째로 건너뛰던 것을 고쳤다. 한 줄에 *떠난 곳*과 *이사 간 곳*이 함께 적히므로, 떠난 곳(`구 X에서`/`X는`)만 제외하고 이사 간 곳은 실재를 확인한다 — 죽은 참조 16건 중 대부분이 바로 그 줄에 숨어 있었다
- "미번들/미포함" 표기가 있는 참조는 의도된 외부 참조로 보고 건너뛴다(줄바꿈으로 접힌 주석까지 인식) — 정직하게 표기한 문서가 벌받지 않게 한다
- `UNRESOLVABLE_ID` 신설. **형태가 아니라 "인용으로 쓰였는가"**로 잡는다(맨몸 8-hex를 다 잡으면 날짜·상수가 걸려 오탐이 실탐을 덮는다). `.md`뿐 아니라 `bin`·`hooks` 소스 주석까지 본다

**양성 대조군으로 검증했다**(규칙을 만들고 "잡히겠지"로 넘기지 않았다):

| 대조군 | 결과 |
|---|---|
| 식별자 제거 직전 트리 | `UNRESOLVABLE_ID` 207건 검출 · 그 트리의 8-hex/ULID 전수 대비 **미포착 0건** |
| v1.7.5 직전 트리 | 죽은 스킬 참조 16건 중 **14건** 검출(`knowledge/README.md`는 8/8) |
| 변이 주입 9종 | 죽은 호출형·경로형·이사 간 곳·떠난 곳·hex·커밋해시·ULID·플레이스홀더·§앵커 전부 기대대로 검출/무시 |

**남는 한계(설계상)**: 산문 속 **맨몸 스킬 이름**(백틱도 경로도 없는 형태)은 이 검사가 못 잡는다 — 위 미검출 2건이 그것이다. 개명 라운드에서는 표기형이 아니라 **이름으로 grep해 재수집**하는 절차가 여전히 필요하다. 코드 주석에 그대로 적어뒀다.

### ② 모드 체계 드리프트 정합 (`9a27336`, v1.7.6)
가이드의 "9가지 모드"를 정본 `agents/trainer.md`(1~6)와 1:1로 재매핑했다. 구 모드 7(산출물 진단)은 **한 줄도 지우지 않고** §5.7로 옮겨 evaluator 소관으로 재귀속. 문서 머리에 "훈련 모드 정본: `agents/trainer.md`"를 박아 다음 드리프트를 막는다.
같이 잡은 죽은 참조: `bin/sync-agents.js` 미번들 표기 1곳 · 실재하지 않는 `knowledge/lessons/` 3곳 · §4.1 트리 실물 대조(없는 `security/` 제거, 있는 `localization/` 추가) · §1.1 "19명"→"현재 21종".
**건드리지 않은 것**: 다른 파일 약 15곳의 "출처: … (모드 7 진단으로 역추출)" — 실행 지시가 아니라 과거 이력 서술이라 기계적으로 밀면 이력이 왜곡된다.
담당: trainer 초안(3라운드) → PM 실물 대조 검증. 게이트: `check-assets` ERROR 0 · 없는 모드 실행 지시 0건 · 트리 ↔ 실물 diff 과부족 0 · 순삭제 0.

### 별건으로 접수(미처리)
- `knowledge/common/skill-vs-knowledge-boundary.md` 머리 지위 블록의 섹션 번호 오기 — "§4의 'Trainer 모드 10' 제안"이라 하나 실제 위치는 §2 규칙 5
- 같은 파일의 옛 모드 번호 서술(모드 1~9)은 **유지**가 맞다 — 그 문서 머리가 이미 "모드 번호는 참고하지 말고 `agents/trainer.md`를 정본으로 볼 것"이라 전면 부인한다
- `agent-training-guide.md` §8 전체가 "(2026-07-09 기준)" 스냅샷이라 19명·점수표가 현재와 다르다. 스냅샷 갱신 여부는 별도 판정 사안

---

## 🟢 미처리 결함 2건 정리 — v1.7.5 (2026-08-22 작업 · **2026-08-23 배포 완료**, `f095ed4`·`1c392cf`·`ab7c2ad`, origin/main 반영됨)

사용자 질의 *"이전에 발견한 결함 중 아직 처리되지 않은 것"*에서 출발해, STATUS 기록·최신 리뷰 보고서를 실물과 전수 대조했다. 살아 있는 결함은 **2건**이었다.

| # | 결함 | 처리 | 담당 |
|---|---|---|---|
| ① | `knowledge/` 본문이 실재하지 않는 스킬 이름·경로 참조 **16건**(죽은 이름 8종 / 4파일) — `domain-` 접두 개명 라운드에서 `knowledge/`만 누락 | 1:1 치환, 줄 삭제 0 (`f095ed4`, v1.7.5) | trainer 초안 → reviewer 🟢 Green(지적 0) → PM 검증·커밋 |
| ② | 린터 `BUDGET_RATIONALE`이 실재하지 않는 사유서 4건을 등록 — 원복 때 `docs/refactor/`에서 삭제됨 | **복구 아닌 등록 해제**(`1c392cf`) | PM 직접(`scripts/`는 PM 소관) |

**①에서 표기형 스코프가 또 새어나갔다.** PM 실측 14건은 백틱·경로 앵커에 걸린 것뿐이었고, trainer가 **이름 기준으로 재수집해 산문 속 맨몸 이름 2건을 추가 발견**했다(총 16건). 식별자 제거 라운드가 세 번 반복한 "스코프를 형태로 잡는" 실패와 같은 형태다 — 이번엔 위임받은 쪽이 먼저 잡았다.

**②는 왜 복구가 아니라 해제인가**: 그 사유서들이 변호하던 크기는 원복으로 사라진 슬림본의 실측치이고, 현 배포본은 이미 그 변호분을 넘었다(pm 23,016 → 32,014B · frontend-dev 16,414 → 20,943B · reviewer 15,839 → 19,545B). 복구하면 실재하지 않는 본문을 변호하는 문서가 된다. 해당 4개는 `BUDGET_UNJUSTIFIED`(근거 없는 초과)로 정직하게 남는다 — WARN 총량 18은 그대로이며, 조용해진 것이 아니라 **재분류**됐다.

**검증(PM 독립 재현)**: knowledge가 참조하는 스킬명 24종 전부 `ls skills`와 일치 · 잔여 4히트는 전부 knowledge 파일명 · 4파일 줄 수 불변 · `sed 's/domain-//g'` 후 양 버전 해시 동일(= `domain-` 삽입 외 1바이트도 안 바뀜) · `check-assets` ERROR 0 · WARN 18 전후 동일 · 신규 식별자 유입 0 · `node --check` 통과.

**기록에만 남고 이미 해소된 것**(아래 「⛔ 열린 이슈」 절은 폐기 라운드 기록이다 — 새 세션이 살아있는 이슈로 오독하지 말 것): 🔴 PM 블록 버전 마커 단절은 **원복으로 소멸**했다(현 정본에 `version:2` 마커 존재, 소비자 3곳 정규식 정상 매칭 — 2026-08-22 실물 확인). 폐기 라운드의 RV-001~005·RV-F는 제품 미반영이라 무효.

**배포(2026-08-23)**: `git push origin main` — origin/main = `ab7c2ad`, 양쪽 매니페스트 1.7.5 정렬 확인. origin에는 `main` 단일 브랜치만 존재(규칙 준수). 원격 정본에서 죽은 스킬 참조 잔여 0 재확인.

**남은 것**: 없음. 보류 백로그 2건은 2026-08-23에 사용자 승인으로 모두 처리했다(아래 두 절).

---

## 🟢 식별자 근거 표기 항구 폐지 — v1.7.4 (2026-08-22, `956b1ee`, origin/main 반영 완료)

사용자 지시: "앞으로 레슨 아이디는 작성을 하지 말아줘요. 따라서 3개도 삭제."

v1.7.3에서 판단 보류로 남겼던 hub ULID를 삭제하고, 규칙을 "8자리 hex id 금지"에서 **"식별자 전반 금지"**로 확대했다.

| 위치 | 건수 | 담당 |
|---|---|---|
| `knowledge/architecture/usage-collection-agent-architecture.md` | 3 | trainer (`b49cf85`) |
| `bin/report-usage.mjs` 주석 | 2 | backend-dev (`f3945ef`) — `.mjs`라 trainer 범위 밖 |

**놓칠 뻔한 이유**: PM이 쓴 검증 grep이 백틱 앵커(`` `01…` ``)라 코드 주석의 맨몸 ULID를 통과시켰다. **이 라운드가 세 번째로 반복한 같은 실패** — 스코프를 형태로 잡은 것. `CLAUDE.md`의 검사식을 형태 무관으로 교정하고 그 실증을 함께 적었다.

보존한 것: `2026-08-19 사용자 최종 결정`(summary 120자 truncate 예외의 근거 — 사라지면 다음 세션이 임의 설계로 오인해 확장할 수 있다), 개인정보 제약 서술 전문. 코드 줄 변경 0(`node --check` 통과, `--dry-run` exit 0).

---

## 🟢 조회 불가 죽은 참조 전량 제거 — v1.7.3 (2026-08-22, `a7544c6`, origin/main 반영 완료)

**사용자 지시**: "중요한 결함 중의 하나는 레슨 아이디입니다. 이것을 제거해야 합니다."

**왜 결함인가**: 2026-08-19 provider 전환(malgnai-mcp → malgnai-hub)은 기록처만 바꿨다. 제품 본문에 박힌 `lesson \`8f36f1d3\`` 류 8자리 hex id는 폐기된 mcp 발급분이고 hub는 26자 ULID를 쓴다 — 설치 직원에게는 **조회 수단이 전무한 죽은 참조**. 변경 동결 원칙의 채택 대상(깨진 참조).

**결과 — 제품 32파일, 195줄 치환, 줄 추가·삭제 0**

| 종류 | 제거 |
|---|---|
| 8-hex id (`lesson`/`decision`/`issue`/`project`/`세션`) | 220건 |
| 실재하지 않는 커밋 해시 `commit \`13bcd60\`` | 2건 |
| 로컬 auto-memory 키 `memory \`feedback_coo-…\`` | 1건 |
| 경위 라벨 파편 (RV-004) | 1건 |
| **합계** | **224건 → 0** |

**손실검증**: A·B 정규식 정규화 동치 검사(PM 독립 재현) 위반 0 · 변경줄 전부 D 목록 내 · 규칙 불릿 계수 32파일 전부 동일 · `check-assets` ERROR 0 · WARN 18 불변 · 훅 exit 0.

**최대 위험 지점**: `agents/qa-engineer.md:25`. id 2개가 "승인 없이 `src/` 코드를 고칠 수 있는 범위"를 닫아두고 있었다 → `라우팅 회귀·인증 스모크 이 두 유형에만`으로 치환해 경계 유지(유형명을 다시 적어 이중 고정).

**명세가 두 번 실물과 어긋났다** (정본: `docs/refactor/lesson-id-removal-spec.md` §8)
- 범위를 `**/*.md`로 한정 → `.html` 1건 누락 (M5로 해소)
- `commit \`13bcd60\``을 "살아있는 참조"로 단정하고 제외 → **어느 ref에도 없음** (reviewer RV-001, M6으로 해소)
- 두 결함의 뿌리는 하나 — **스코프를 목적(설치 직원이 조회할 수 있는가)이 아니라 형태(8-hex·백틱·확장자)로 정의**한 것

**리뷰**: `docs/reviewer/review-lesson-id-removal-2026-08-22.md` — 🟡 Amber (Critical 0 · Major 1 · Minor 2 · Nit 2 · Rethink 2). Major·Minor 1건 채택 반영, Nit 1건 기각(근거: 지시 대상 없는 파편), Rethink 2건 백로그.

**커밋**: `114ef7c`(명세) · `835d5aa`(M1 기계) · `b7683f3`(M2 자율권 경계) · `9b2053b`(M3 복제본 정합) · `4e27740`(M4 D잔여) · `05e0273`(M5 html) · `c3c73c0`(M6 리뷰 반영)

---

## 🟢 배포본 위에 검증된 결함 수정만 재이식 — v1.7.2 (2026-08-22, `f72d553`, 브랜치 `restore-core-fixes`)

**사용자 지시**: "이번에 수정/추가된 작업들을 정말 냉정히 비교 분석해서, 솔직히 나아진 것이 크게 없으면 깃허브에 올라가 있는 버전(=설치본)으로 원상복구하자."

**판정 근거(4개 영역 병렬 검증 + PM 실측)**
간판 작업(Micro 판정 축 전환·V3 슬리밍 라운드)은 **순효과 없음 또는 마이너스**:
- 대규모 슬리밍 후에도 `agents/` 총량 271,761B → **272,885B (증가)**. pm/frontend-dev/reviewer −16KB를 evaluator +10.9KB·trainer +7.2KB가 더 먹음
- 상시 비용 `common-*` +3,411B
- 조회불가 8자리 id 221 → 208건(**94% 잔존**)
- "배포 차단 결함 해소" 작업의 대상은 **배포본에 없던 자작 결함**(문제 커밋이 origin/main 조상이 아님을 `merge-base`로 확인)
- 그 과정에서 `ux-designer.md:25`에 **새 부정합을 생성**(배포본은 정합했음)
- 86커밋 중 **31커밋은 제품을 한 줄도 안 바꿈**(`docs/` 3만 줄, 배포 안 됨)

**사용자 선택**: 3안 "핵심 수정만 재이식" — origin/main에서 재분기 후 검증된 결함 수정만 다시 얹음.

**재이식 내역 (제품 −5,110 B, 상시 −3,175 B)**
| | 내용 | 실측 |
|---|---|---|
| R1 | 실행 불가능한 도구 호출 지시 제거 | **72건 → 4건**(잔존은 evaluator 탐지 게이트의 금지어 목록 1줄) |
| R1a | `repositoryKey`로 적혀 호출이 튕기던 파라미터 | 22곳 → `projectId`(MCP 스키마 원문 대조) |
| R1b | 실재하는 `agent_learning_record`를 "없다"고 단언 | 거짓 5곳 정정 |
| R1c | `lesson_*` 의존 trainer 모드 2·4 | hub 도구만으로 닫히는 루프로 재작성 + 50건 상한 한계 명시 |
| R2 | 매 세션 주입 훅 블록의 없는 스킬명 | 정정 |
| R3 | 디자인 산출물 경로 | `docs/design/` 단일화(배포본은 visual-designer 자기검증 구조적 불가) |
| R4 | reviewer 오탐 기각 경로 | 신설(배포본엔 0건) |
| R5 | 설치 직원이 열 수 없는 경로 참조 | 10 → 0건 |
| R6 | 배포본 자산 실재 결함 | 정적검사 **ERROR 3 → 0** |

**검증 체인**: trainer 초안 → reviewer 1차(Critical 0/Major 5/**손실 0**) → 반영 → reviewer 2차(Critical 0/Major 3) → 반영. Major 중 **M3은 PM이 hub 스키마 원문 대조로 오탐 기각**(`wbs_list`의 `parentId`·`includeDone`, `project_get_context`의 `sections`는 전부 실재). 2차 지적이 몰린 "반영 커서"는 순부채 판정으로 철거.

**버전**: marketplace 0.3.0 ↔ plugin 1.7.1 불일치 해소 후 양쪽 **1.7.2** 정렬(배포 시 `/plugin update` 감지용).

### ✅ 배포 완료 (2026-08-22)
`main` fast-forward 후 `git push origin main` — origin/main = `9ec5183`. origin에는 `main` 단일 브랜치만 존재(규칙 준수). `restore-core-fixes`는 병합 후 삭제.

**후속 커밋 `9ec5183`**: 원복 때 저장소 `CLAUDE.md`의 운영 가드레일 2절("에이전트 업그레이드 원칙"·"편집 권한 경계")까지 함께 사라진 것을 복원. 제품이 아니라 세션용 지침이라 폐기 대상이 아니었음 — PM 범위 판정 오류. 복원하며 낡은 서술(skills 37→38, knowledge 53→55, 없는 플러그인 디렉토리 등)도 실측에 맞춰 갱신.

### 🔜 남은 결정 (사용자)
1. 폐기 브랜치 정리 여부 — 로컬 `principle-upgrade-priority` / `principle-upgrade-priority-round2` / `methodology-10-1-proxy-draft` (내용은 전부 백업 태그에 있으므로 지워도 손실 없음). 로컬 `main`은 이미 origin/main과 동기화됨
2. 되살릴 것이 있으면 태그 `backup/pre-revert-2026-08-22`(제품+docs 전량) / `backup/pre-revert-main-2026-08-22`에서 회수 가능

### 🗑️ 정리 필요
- `/Users/hopegiver/workspace/testproj` — PM이 스캐폴더 종단 검증에 쓴 폴더(삭제 권한 거부됨, 직접 삭제 요망)

---

## 📦 이하 폐기된 라운드의 기록 (태그 `backup/pre-revert-2026-08-22`에 보존, 제품엔 미반영)

## 🟡 Micro 판정 축 전환 — 사용자 지시로 중단 (2026-08-21, `6f36e48`, 브랜치 `principle-upgrade-priority`)

**출발점(사용자 지적)**: 자동 주입 블록의 Micro 정의가 **열린 예시 나열**이라 "작업 크기" 축으로 우회가 가능했다. 직전 확장(108자→243자)에 대한 평가는 *"내용만 길어지고 실속이 없다"*.

**반영(3라운드, trainer 위임 → reviewer 검증)**
- `hooks/pm-orchestration-block.md` — Micro를 **닫힌 연언**으로 재정의(⑴읽기만/고칠 곳 한 군데 ⑵새로 쓸 문장·코드를 판단하지 않음). 배제 사유 열거를 `둘 밖의 사정은 판정에 넣지 않는다`로 대체.
- `agents/pm.md` — **카테고리 오류 정정**: `STATUS.md 갱신`을 Micro 예시에서 제거. 진행 기록은 위임/직접을 물을 대상이 아니라 **PM 직무 그 자체**다(사용자 판단). PM 고유 직무 5개를 닫힌 목록으로 신설.
- `skills/common-task-grading-and-verification-depth` — 판정표·빠른판단의 `단일파일`(파일 개수) 기준 제거. 등급명 `Trivial`→`Micro` 통일.

**판정**: reviewer 🟡 **Amber**(Critical 0 · **Major 5**). P0 3건 수정 전 커밋 비권장이었으나 **사용자가 "여기서 멈춤. 커밋" 지시** → 미해결 상태로 커밋.

### 🔴 열린 채 남긴 것 — Major 5건 (감추지 않음)
| # | 위치 | 내용 |
|---|---|---|
| RV-001 | `SKILL.md:36` ↔ `:17` | 새로 넣은 *"파일 개수는 판정 기준이 아니다"*가 자기 판정기준 ⑴ *"고칠 곳이 한 군데"*를 되받는다 — 일괄치환이 Micro로 새는 근거가 **정본 안에** 있다 |
| RV-002 | `agents/pm.md:18` | 이번 라운드의 핵심인 닫힌 연언이 pm.md에 **미착지**(여전히 열린 예시) |
| RV-003 | `agents/ux-designer.md:25` | **삭제된 구 정의**(`문구·CSS·단일파일 수정`)를 인용하며 *"이 외에 새 예외 기준을 만들지 않습니다"*로 정본화 |
| RV-004 | `agents/pm.md:137-138` | 자가진단 스킬 2종의 *"Micro 등급 직접 처리"* 선언이 ⑵와 충돌 — 설계된 제품 기능이 위임 대상으로 뒤집힘 |
| RV-005 | `hooks/pm-orchestration-block.md:5` | PM 고유 직무가 pm.md의 안전장치(닫힌 목록·도구 결속) 없이 **열린 나열** — Micro 축에서 없앤 실패 형태를 **새 축에서 재생산** |

### 🔵 채택 보류한 대안 — 축 전환
Major 5건 중 **4건이 "같은 규칙을 여러 문면에 문장으로 복제"에서** 나왔다. reviewer 발산형 페르소나 권고는 1차 질문을 **작업 속성 → 산출물 소유권**으로 바꾸는 것("얼마나 작은 작업인가"가 아니라 **"이 산출물의 주인이 누구인가"**). 그러면 RV-005가 자동으로 닫히고 크기 판정은 검증 깊이를 정하는 부차 축으로 내려간다. 이 저장소 `CLAUDE.md`가 이미 같은 축(*"기준은 '대상 파일'이지 '작업 크기'가 아니다"*)으로 풀고 있는 **실증된 대안**이다. 재개 시 여기서 시작한다.

### 함께 남은 것
- **예산 사유서 드리프트 미해소**: `agents/pm.md` 25.0 KB vs 사유서 `docs/refactor/v3-pm-budget-rationale.md` 변호분 22.5 KB(초과 2,594 B). 드리프트의 **약 3분의 2(1,615 B)는 이번 라운드 이전 커밋에서 발생**. WARN 개수 회귀는 없다.
- **범위 밖 발견**: `knowledge/leadership/coo-rule-rationale.md:25`의 등급명 `trivial` 잔재 + `PM 고유 산출물(STATUS.md·통합 보고서 등)`이 **열린 목록**이라 이번에 닫은 5개 목록과 결이 다르다.
- 검증: `pnpm run check-assets` **ERROR 0 · WARN 16 · INFO 2**(회귀 0). 리뷰 보고서 `docs/learning/review-pm-micro-definition-closure-2026-08-21.md`.
- hub 이슈: `01m0jbjj4c8wgat55c90wd2kbj` (열린 채)


## ✅ trainer 담당 범위 정정 — 종결 (2026-08-21, 브랜치 `principle-upgrade-priority`)

**사용자 지시**: "트레이너는 agents·knowledge만이 아니라 `malgn-agent` 안의 **모든 `.md`**(agents·skills·hooks/\*.md·knowledge)를 책임진다." 저장소 `CLAUDE.md` §편집 권한 경계는 이미 그렇게 적혀 있었고 **제품 문서만 뒤처져 있던 것**을 맞췄다.

**커밋 3건** (`8b348ce` → `c6b9003` → `c269236`, 14파일 +69/−59, 전부 `.md`). trainer 저작 → evaluator 판정 2회전 → PM 실물 대조 3회전.

**로컬 main 반영 완료 (2026-08-21, 사용자 승인)**: 브랜치 통째 merge가 아니라 **이 3커밋만 cherry-pick**(`7c9f11f`·`357db16`·`5405d27`). 브랜치에 섞여 있던 병행 세션 커밋 3건(`ecaa285`·`dc221c7`·`3a00844` — 마지막 건은 제목에 "[초안·미승인]")은 **의도적으로 제외**했다. 검증: main 트리에서 `validate-agent-assets` **ERROR 0** · `claude plugin validate` **통과** · 브랜치 최종본과의 유일한 차이는 제외한 `dc221c7`분뿐임을 diff로 확인. **origin push 안 함.** 작업 브랜치·워킹트리 미커밋 4건은 병행 세션 작업이라 손대지 않았다.

| 회차 | 무엇이 나왔나 |
|---|---|
| 1차 `8b348ce` | trainer.md의 **자기모순** 해소(6분류 판정표는 Skill·Hook을 목적지로 드는데 §범위·§산출물은 agents+knowledge만 열거). 확장자 경계 조항 신설 — `.md`만 저작, `hooks/*.mjs`·`hooks.json`·`bin/`·`templates/`·`plugin.json` 제외 |
| 2차 `c6b9003` | evaluator가 **잔여 좁은 서술 6곳** 적발(trainer의 "잔여 0" 보고는 오류 — 1차 grep이 슬래시 결합형만 잡고 `Knowledge/MD` 역순·`MD와 knowledge` 서술형을 놓쳤다). evaluator.md에 **「훅 문서 대상」 채점 소절 신설** — 범위만 넓히고 rubric을 안 넓히면 훅 문서 diff에서 판정이 공회전한다 |
| 3차 `c269236` | 2차가 만든 **신규 결함 2건** 해소 — ①훅 신설이 "사용자 승인 필수" 목록으로 격상돼 같은 절 `:223`(문서형은 리뷰승인 1단계)과 충돌 ②모드 표에서 모드1은 훅 포함·모드3은 제외한 비대칭. 모드 표의 목적지 **열거 자체를 제거**(목적지는 모드가 아니라 6분류 판정표가 정한다)해 재발원을 없앰 |

**검증(PM 직접 실행)**: `pnpm run check-assets` **ERROR 0**(3회 모두) · 좁은 서술 잔여 grep 히트 3건 전부 이미 4자산인 오탐 · 훅 승인 언급 7곳 **전부 "PM 승인"**, "사용자 승인" 0건 · `hooks.json`에 `.md` 등록 0건.

### 이 라운드의 값 — 범위 확장이 게이트를 두 번 흔들었다
2차·3차의 결함은 **둘 다 "책임 확대"가 "권한 확대/격상"으로 새어나간 것**이다. 열거를 넓히는 편집은 조용히 게이트를 움직인다 — 넓히지도 좁히지도 않았는지가 검수 항목이어야 한다. 재발원 제거가 최종해였다(열거를 옮기지 말고 **없애고** 판정표 한 곳을 가리키게 한다).

### 🔵 이 라운드가 남긴 열린 것
- **`docs/methodology/agent-development-methodology.md`에 훅 문서 작성표준 절이 없다**(§3 에이전트/§4 스킬/§5 knowledge만). evaluator 체크리스트는 인라인으로 자립하게 만들어 당장 막히진 않으나, 근거 문서 쪽은 비어 있다. `docs/` 소관 = PM. **병행 세션이 `3a00844`로 이 파일을 만지는 중이라 착수 보류.**
- **`trainer.md` 예산 사유서 드리프트**: 28,144 → 29,597 B(이번 기여 +1,453). 사유서 변호분 22.3 KB 초과분은 대부분 이전 누적. "줄이라"가 아니라 **사유서 실측 갱신** 대상(`docs/refactor/v3-trainer-budget-rationale.md`, PM 소관) — 위와 같은 이유로 보류.
- **병행 세션 2개**가 같은 저장소에서 커밋 중(`ecaa285`·`dc221c7`·`3a00844`, 17:26~17:28). `dc221c7`과 이번 작업이 같은 파일(`skills/domain-training-scorecard-eval/SKILL.md`)의 다른 줄을 건드렸고 **양쪽 문안 모두 온전**함은 확인했다. **merge·push 전에 그쪽 세션 정리 필요.**

## ✅ 후보 큐 제거 라운드 — 종결 (2026-08-21)

**로컬 main 병합 `0599389`** (13커밋, 33파일 +1770/−470). 브랜치 삭제 완료. **origin 미push — main이 51커밋 앞섬.**

| 검증(도달하는 명령만) | 병합 후 재확인 |
|---|---|
| `claude plugin validate ./malgn-agent` | ✔ 통과 |
| `pnpm run check-assets` | ERROR 0 · WARN 18 (회귀 0) |
| dangling grep | **0건** |
| 훅 스모크 SessionStart | exit 0 · 유효 JSON · 15,769자 |

**판정**: evaluator ✅ **PASS**(게이트 2회차) · reviewer 🟡 **Amber**(round2, 지적 전건 반영). 검증 5회전(evaluator×2 · reviewer×2 + PM 실물 대조).

**함께 들어간 것**: PM 블록 승인게이트 `5736d04` · 검증 산출물(스코어카드·리뷰 보고서·페르소나 6) · V3-03b 계획 rev.1~3.

### 🔴 이 라운드에서 가장 값비쌌던 것 — 거짓 명제가 네 곳으로 증폭됐다
*"check-assets는 `bin/` 참조를 검사하지 않는다"*는 **거짓**이었다. `REF_BIN_MISSING`이 [validate-agent-assets.mjs:435](scripts/validate-agent-assets.mjs#L435)·[:468](scripts/validate-agent-assets.mjs#L468)에 실재하고 **표기형에 민감**할 뿐이다(백틱+플러그인 상대만 매치).
**경로**: evaluator가 뮤테이션 false negative를 일반명제로 해석 → **PM이 재현 없이 받아 이슈·STATUS·커밋 `ee0d464` 메시지 3곳에 증폭** → trainer가 사유서에 항구 기록. **재현한 것은 reviewer뿐.**
→ 이슈 철회 후 정확한 범위로 재발행(`01m0h2x7n81g6ez252z0yx0w5f`) · 사유서 §3.13.2 재정정(`e3588a1`) · `ee0d464` 메시지는 사후 수정 불가라 사유서로 갈음.
**규율**: 도구가 "안 잡는다"를 주장하기 전에 **그 도구가 잡는 표기형으로 양성 대조군을 먼저 만든다.**

### 🔵 미해결로 남긴 것 — RV-F (사용자 지시로 공백만 명시, `c21df26`)
*"쓰이지 않던 매체를 지우고 **쓸 수 없는** 매체로 착지 — 모드 4의 존재 이유가 비었다."* trainer 판단은 **조건부 동의**: 제거를 정당화한 근거는 큐의 **상시 비용**인데(39줄+스크립트+훅+테스트 31종이 모든 호출에 부과) `lesson_*` 90줄은 Skill 본문이라 invoke 시에만 로드된다 — *"쓸 수 없다는 같아도 상시로 물린다는 같지 않다."* 권고는 1안(부록 강등). 채택은 다음 라운드.

**귀결(감추지 않음)**: 배포 제품에서 모드 2→4는 **다시 꺼진다**. malgnai-mcp pending 11건은 **그대로 열린 채**이고 이 제거는 그것을 닫지 않는다.

## ✅ V3-03b + V3-03c 종결 (2026-08-21)

**로컬 main 병합** `6e8eb78`(03b, 10커밋) → `7e2beb9`(03c, 8커밋). 브랜치 2개 삭제. **origin 미push — 73커밋 앞섬.**

| 파일 | main 이전 | 현재 | |
|---|---:|---:|---|
| `frontend-dev.md` | 21,252 | **16,414** | −4,838 · 예산 초과 → 사유서 |
| `reviewer.md` | 19,217 | **15,839** | −3,378 · 예산 초과 → 사유서(E-1로 +479) |
| `ux-designer` / `visual-designer` | 12,695 / 10,745 | 12,980 / 11,040 | 경로 + C0 폴백 |

**병합 후 재검증**: `check-assets` ERROR 0 · WARN 18 · `claude plugin validate ./malgn-agent` 통과 · **프로젝트 경로 `design/` 잔존 0건** · 워킹트리 깨끗.
**판정**: evaluator 🟡 CONDITIONAL PASS(필수 1건 반영) · reviewer 풀패널 🟡 Amber(Critical 0·Major 3·Minor 5·Nit 1, **전건 반영**).

### ⚠️ 도달 증명 미완 — 배포 후에만 가능 (정직 표기)
`reviewer.md`의 런타임 로드 대상이 **산문 9건 → Skill 1건**으로 바뀌었으나 **설치본에 신설 Skill이 없다**(설치본 커밋 `1ba521a`). 서브에이전트는 설치본을 로드하므로 지금 리뷰를 돌려도 구버전을 읽는다. **확인법(배포 후)**: 아무 리뷰를 시켜 보고서에 Confidence 열과 변경유형 판별이 있는지. 정적 도달은 확인됨(`reviewer.md:65,99,121` 정확한 이름 · 디렉토리명 일치 · orphan 0건).

### 🔴 이 라운드의 교훈 — 같은 증폭이 두 번째다
**"신설 Skill description 606자로 상한 300자의 2배"는 거짓**이었다. **606은 바이트고 실제 263자**이며, 린터는 [validate-agent-assets.mjs:229](scripts/validate-agent-assets.mjs#L229)에서 `description.length`(문자)를 쓴다 — **원본은 이미 준수 상태**였다. **evaluator와 reviewer가 같은 바이트/문자 혼동**을 했고 PM이 검증 없이 지시서로 옮겼다. 직전 라운드의 `bin/` 사각지대 거짓 명제와 **같은 계열의 두 번째 사례**.
→ 규율: **검증자 둘이 같은 말을 해도 재현 전에는 지시서에 옮기지 않는다.**

### 🔵 Rethink 3건 — 기록만, 다음 라운드 (decision `01m0hgf63e8pbd6hjvkec710te`)
- **RT-2 knowledge 이동 시 "항상 로드되는 짝" 필수 — 우선 착수 후보.** 이번 라운드가 *"진입점 없는 knowledge는 존재하지 않는 것과 같다"*를 발견했는데 **RV-1이 그 위반 사례를 같은 라운드 안에서 만들어냈다**(`project-folder-structure.md` = 참조 0건 정본). 가설이 아니라 이미 실현된 실패. 비용 rubric 1줄 + `grep -rln <파일명> agents/ skills/` = 0이면 실패
- **RT-1 사유서 인식 → WARN 강등**: 조건부 동의. 단 **파일 실재만으로 강등하면 빈 파일도 면죄부** → 사유서가 변호하는 바이트 수를 명시하고 초과분이 그 값을 넘으면 WARN 복귀
- **RT-3 라운드 분리 기준을 불변량으로**: 조건부 동의, 확신도 낮음. **반례가 같은 라운드에 있다** — 게이트·G-c·I3′가 전부 통과했는데 RV-1은 그 전부를 빠져나갔다(있어야 할 것의 부재는 집합 비교로 안 잡힌다)

**남은 단계**: V3-03b 나머지 에이전트(backend-dev+architect+devops 한 커밋 → ux/visual·security·localizer·qa) → V3-04 frontmatter → V3-05 references/ → V3-08. **V3-06 보류.**

## 🚀 배포(origin push) 준비 — **완료. 남은 것은 push 하나뿐**

**main이 origin보다 76커밋 앞섬. push는 아직 하지 않았다(사용자 판단 대기).**

| 배포 전 선결 | 상태 |
|---|---|
| 🔴 PM 블록 마커 단절 `01m0fnv17myknytyhrzna9v02r` | ✅ **해소** — `6a491b2`, C안(무버전 마커) |
| 마켓플레이스 버전 불일치 `01m0fgn60jhp24dg62jkapxwj0` | ✅ **해소** — `7eef594`, 0.3.0→1.7.1 |
| 린터 표기형 민감성 `01m0h2x7n81g6ez252z0yx0w5f`(a) | ✅ **해소** — `7eef594`, 대조군 4종 검증 |
| 같은 이슈 (b) knowledge/ 참조 검사 | ⚠️ **미해소·비차단** — 린터에 knowledge 본문 순회 루프 자체가 없다. knowledge 8개 파일이 `bin/`을 인용 중 |

**최종 게이트**: `check-assets` ERROR 0 · WARN 18 · `claude plugin validate .` **및** `./malgn-agent` 둘 다 통과(경고 0) · 워킹트리 clean · 워크트리 잔재 제거.

### PM 마커 수리 — 실행 도달 증명 (PM 독립 재현)
| | 이전 | 이후 |
|---|---|---|
| `new-project.mjs --here` | "찾을 수 없어" 경고 + **`@import` 미삽입** | 마커 `:installed` + **`@import` 1줄 실제 삽입** |
| `readBlockFile()` | `null` | 정본 **684자** 읽음 |
| `check-…mjs` | fail-closed "설치를 진행하지 말고…" | `status: "ok"` |
**양성 대조군**: 잘못된 import 경로 → `drift` · import 줄 없음 → `legacy-no-import`로 **여전히 적발**(경로 축 생존 확인).

### ⚠️ 의도적으로 수용한 손실 (감추지 않음)
**블록이 실질 개정돼도 거절했던 프로젝트에 자동으로 다시 묻는 넛지는 사라진다.** `revisedSinceDecline` 제거의 대가다. B안("git 이력")은 **설치본이 shallow clone**(커밋 10 vs 157, 블록 이력 1/9, graft 경계라 fetch마다 sha 변동)이라 배제됐고, A안(콘텐츠 해시)은 오타까지 전사 넛지를 띄운다. 재상정 조건은 decision `01m0hkpd17582rx9b8n1rrj52n`과 `skills/project-standards/SKILL.md` §9에 기록.

### 📌 push 직후 확인할 것 (배포 후에만 가능한 도달 증명 2건)
1. **`reviewer.md`의 신설 Skill 로드** — 아무 리뷰를 시켜 보고서에 Confidence 열과 변경유형 판별이 있는지
2. 신규 프로젝트 스캐폴딩 1회 — 설치본 기준으로도 `@import`가 박히는지

## ⛔ 열린 이슈

**🔴 배포 차단 후보 — PM 블록 버전 마커 제거로 설치 체커가 깨진다** (issue `01m0fnv17myknytyhrzna9v02r`, high, 2026-08-20). `a92079f`(사용자 직접 편집)가 `hooks/pm-orchestration-block.md`에서 버전 주석 2줄 + `<!-- malgn-agent:pm-orchestration:version:2 -->`를 제거했으나(현 정본 594자, 마커 grep 0건), 마커를 파싱 전제로 쓰는 소비자 **3곳**(evaluator 대조로 1곳 추가 확인, 최초 기록의 2곳은 과소)이 안 따라갔다 — [new-project.mjs:36-41](malgn-agent/bin/new-project.mjs#L36-L41) 자체 인라인 정규식 `if (!m) return null` → 신규 스캐폴딩에 `@import` 미삽입(경고만, fail-open) · [find-pm-block-path.mjs:43](malgn-agent/hooks/lib/find-pm-block-path.mjs#L43) `if (!m) return null` → [check-pm-orchestration-block.mjs:85](malgn-agent/skills/project-standards/scripts/check-pm-orchestration-block.mjs#L85)의 "읽지 못했다(배포 누락 가능성) — 설치 진행 말 것" 경로. **origin push 시점에 신규 프로젝트의 PM 블록 설치 절차가 막힌다.** 지금은 설치본(마켓플레이스 클론, 커밋 `1ba521a`)이 아직 마커 있는 구버전(788자)이라 잠복. **배포 전 이 건부터 처리할 것.** 두 경로가 모두 끊겨 이 개선을 받는 모집단이 「오늘 이미 `@import`가 박힌 프로젝트」로 영구 고정된다(`revisedSinceDecline` 영구 false로 개정 재확인 넛지도 사망). 발견 경위: trainer가 마켓플레이스 경로로 측정해 "마커 있음·788자"로 보고 → 정본 대조에서 불일치 추적. ⚠️ **플러그인 파일 검증 시 설치본(origin, 37커밋 뒤처짐)과 저장소 정본은 다르다.**

**✅ pm-orchestration-block 역할 보강 — 완료·로컬 커밋 `5736d04`** (decision `01m0fpnbwhegmtsg3dt02p2xdv` · 예산 재정의 `01m0gs3qs37s5ba8smfjv7mzwb`, 2026-08-21). 594→**685자**. 승인 게이트 문단 신설 + dangling 스킬명 `common-` 교정 + 압축. **검증**: evaluator CONDITIONAL PASS(82.05) + reviewer 풀패널 Amber(Critical 0/Major 2) → Major 2건 반영(`(Standard 이하)`→`(그 외)` 정본 정합 · "착수 전" 삭제 · "중요한" 원복 — evaluator가 "무손실 압축"이 실은 다각합의 의무 확대였음을 잡음). 재론금지 (a)(b) 둘 다 유지. 예산 스펙은 자수→**본문 500토큰**으로 재정의(현재 475토큰).
- ⚠️ **"pm.md와 동일 수준"은 미달이다(정직 표기)** — `pm.md:86-87`(위임 시 등급+완료판정 1줄) 미도달. reviewer 실측.
- ⚠️ **도달 범위는 기존 installed 프로젝트 한정** — 위 마커 이슈 미해소 상태에서는 신규 프로젝트 도달 0.
- 후속 후보: reviewer Rethink R-1(승인 주체를 grading 스킬 5등급 표의 새 열로 — 상시 비용 0자) · `validate-agent-assets.mjs:133` 스캔 범위에 `hooks/` 추가(이번 dangling이 ERROR 0을 통과한 원인) · grading 스킬 frontmatter `Trivial` vs 본문 `Micro` 불일치

**🟡 후보 큐 되돌리기 — 사용자 결정 완료, trainer 제거 계획 대기 중** (decision `01m0fkvsr744gw3e9yjq9vy825` · issue `01m0fks5tdc91j997zeaweqry8`, 2026-08-20). 사용자가 `trainer.md`의 「후보 큐」 필요성에 의문 제기 → trainer 자체 판정 **"현 형태로는 불필요, 되돌리는 쪽 권고"**. 핵심: 매체 전환 사유(`trainer.md:92` "원장 조회 상한 때문에 후보가 밀려나 누락")가 **실측 0건**이고, 실제 실패는 *소비자 부재*라 매체를 옮겨도 해소되지 않는다. 큐는 도입 후 **한 번도 쓰인 적 없다**(`candidates/`에 `.gitkeep` 외 이력 0). 사용자 전제("없이도 잘 해왔다")는 대체로 사실 — 이전 `trainer.md`는 모드 2→4를 배포 제품에서 꺼두고 모드 1·3으로 돌렸다.
- **결정**: **되돌린다**(a). 제거 방식·커밋 단위는 **trainer가 계획으로 먼저 반환**하고 채택은 사용자/evaluator가 판정 — PM은 설계하지 않는다
- **판단을 뒤집을 수 있는 미확인 2건**: 직원 프로젝트 세션에서 모드 2가 실제 발동한 적 있는지(저장소 접근 불가) · malgnai-mcp pending 11건 중 미대조 7건
- **되돌린다면 지금이 가장 싸다** — `ee4af3e`는 로컬 main에만 있고 origin 미push
- ⚠️ 되돌릴 때 `docs/learning/scorecards/`(evaluator 채점 이력)는 **별개 경로**라 함께 걷어내지 않는다

**✅ 계획 채택**(사용자, decision `01m0fm8egd4fzg5w9tr471twjh`) → **trainer 실행 완료** — 브랜치 `v3-03b-revert-candidate-queue`, 4커밋 **22파일 +119/−431**(`f6d1dcc` C1 기계 · `2f89a6f` C2 제품문서 11 · `b3ab3cb` C3 저장소문서 · `cd0c399` C4 사유서 §3.13 append). push/PR/merge 없음. **검증 상태(2026-08-21 실물 대조): reviewer 완료 🟡 Amber / evaluator 판정은 기록 없음.**
- ✅ **reviewer 완료** — [review-v3-03b-revert-candidate-queue-2026-08-20.md](docs/reviewer/review-v3-03b-revert-candidate-queue-2026-08-20.md) · Critical 0 / **Major 3** / Minor 3 / Rethink 1. 제거 자체는 기계적으로 깨끗(잔존 참조 0·린터 ERROR 0·과다 삭제 0). Amber 근거는 전부 **문서의 사실 진술**이며 문장 수준 수정 4곳 + hub 도구목록 1회 조회로 닫힌다
  - RV-A 🟠 **과소 삭제** — `skills/project-retrospective/SKILL.md:109`에 큐 시절 「산출」 지시 잔존 → 같은 파일 `:100`의 새 정본과 반환 형식이 충돌(따르면 삭제된 큐를 다시 만들게 되는 경로)
  - RV-B 🟠 "hub에 대응 도구 없음" **단정을 제품 6파일에 재도입**했으나 이번 라운드 hub 도구목록 실측 기록이 브랜치 어디에도 없음 — `trainer.md:19`가 금지하는 형태(15파일 사고의 축소판). 명제 자체는 참일 개연이 높고, 지적 대상은 **절차**
  - RV-C 🟠 사유서 §3.13 **착지점 오기** — "큐 이전"이 아니라 두 세대 전이고, 그 사이 세대(`c36b602`)가 의도적으로 지웠던 `lesson_*`이 부활
  - Minor 3: RV-D(1차 리뷰가 요구한 hedge가 큐와 무관한데 동반 삭제) · RV-E(`SubagentStart` 등록은 지웠으나 `sessionstart-context.mjs:3~5,46,51` 서브에이전트 분기·헤더 주석 잔존) · RV-G("번들되지 않음" 각주가 실측과 어긋나며 3중 복제)
  - 🔵 **RV-F Rethink**: 쓰이지 않던 매체를 지우고 **쓰일 수 없는** 매체로 착지했다 — 모드 4의 존재 이유가 비어 있다
  - 페르소나: 재사용 4 + **신규 1**(`persona-removal-landing-state-auditor` — 제거 작업 특유의 「착지점 감사」가 INDEX 41행에 없었음)
- ❓ **evaluator 판정 소재 불명** — `docs/learning/scorecards/`는 `.gitkeep`뿐이고 hub `recentWork`에도 이 브랜치 채점 기록 없음. 직전 세션 구두 보고가 `/clear`로 유실됐거나 미완. **병합 게이트이므로 재수행 또는 소재 확인이 선행**
- ⚠️ **미커밋 reviewer 산출물 7건** — 페르소나 5 수정 + 신규 1 + 리뷰 보고서 1. 경로지정 커밋 필요(`git add -A` 금지)
- ℹ️ 이 브랜치에는 PM 블록 커밋 `5736d04`도 함께 올라가 있다 — 병합 시 두 건이 같이 들어간다
- trainer 자체 검증 보고(재확인 대상): `check-assets` ERROR 0·WARN 18(main과 동일 → 회귀 0) · `test-queue-root` 스크립트 소멸 · `claude plugin validate` 통과(경고 1건은 선재 버전 불일치) · 훅 스모크에서 훅 자신은 큐 문자열 미생성
- 계획과 달라진 점 4건: 큐 절 66~98로 정정 반영 · 사유서 §3.12가 이미 있어 신설을 **§3.13**으로(상호참조 4곳 정정) · 제품 문서→`docs/` 인용 3곳에 "번들 안 됨" 각주 · `isSubagent` 가드는 방어 코드라 존치
- ✅ PM 실물 대조: 4커밋·22파일 확인, **5필드 계약·6분류 판정표·역할 경계 온전**

**⚠️ 처분 대기 — 스코프 밖 워킹트리 변경 1건**: `malgn-agent/hooks/pm-orchestration-block.md`(+6/−4, 미커밋). trainer 4커밋 어디에도 없고 큐와 무관한 **PM 행동 규율 개편**(기본 작업방식=PM 명시 · 위임 프롬프트 3요소 · 승인 경계 신설 · `verifiable-output-and-honesty`→`common-` 접두어 정정 · pm.md 정본 참조). trainer가 손대지 않고 멈춰 보고했다. `CLAUDE.md`가 `@import`하는 단일 소스라 배포 영향 있음 — **출처·처분 사용자 확인 필요**
- **방식: 부분 제거(외과적). `git revert ee4af3e` 금지** — 전량 revert는 큐와 무관한 가치(5필드 계약·6분류 판정표·`learning-loop-design-rationale.md`·hub 도구명 재배선 12파일)까지 되돌리고, `c36b602`가 만들고 `6a3565a`가 이미 폐기한 「학습 원장」 상태기계를 되살려 **양쪽 끝점보다 나쁜 상태에 착지**한다
- **C1 기계**: `bin/queue-root.mjs`·`scripts/test-queue-root.mjs` 삭제 + `package.json` 스크립트 + `hooks/sessionstart-context.mjs` 큐 블록 + **`hooks.json`의 `SubagentStart` 등록 제거**
- **C2 제품 문서 11개**: `agents/trainer.md`·`evaluator.md` + skills 7종 + knowledge 2
- **C3 저장소 문서**: `docs/learning/{candidates,discarded}/.gitkeep` 삭제 · `docs/learning/README.md` · `docs/README.md:18` · `CLAUDE.md`
- **C4**: `docs/refactor/v3-trainer-budget-rationale.md`에 "2026-08-20 제거 판정" **append**(삭제 아님 — §3.11의 자기 진단이 재상정 차단의 가장 값싼 반례)
- **남긴다**: `docs/learning/scorecards/` + evaluator `:123`·`:130` · evaluator `:133`의 `test -f` 게이트 한 줄(scorecards도 상대경로) · `sessionstart-context.mjs`(SessionStart 등록) · 5필드 계약·6분류 판정표 · `docs/reviewer/` 리뷰 2건 + 페르소나(실측 **6개**, 계획서의 3종은 과소)
- **검증**: `test-queue-root` 31종 전량 삭제(대상 소멸) · `check-assets` 1회로 dangling 참조 0 확인 · `claude plugin validate` · 훅 스모크(STATUS.md만 주입, 큐 문장 없음) · 잔존 grep이 `docs/refactor`·`docs/reviewer`에만 남는지

**🚩 PM 실물 대조 — 계획서 오류 1건(정정 전달 완료)**
- ✅ 확인: [sessionstart-context.mjs:55-66](malgn-agent/hooks/sessionstart-context.mjs#L55-L66)의 STATUS.md 주입·드리프트 경고는 `if (!isSubagent)` 안, 큐 블록은 [68-75](malgn-agent/hooks/sessionstart-context.mjs#L68-L75)로 가드 **밖** → 큐 제거 시 **SubagentStart 페이로드가 빈 문자열**. `SubagentStart` 등록 제거 판단이 맞고, 위 「미완 1건」(도달 미증명)은 증명 불필요로 소멸한다
- ❌ **오류: `trainer.md` 「후보 큐」 절은 66~98행(33줄)이지 계획서의 66~104(39줄)가 아니다.** 다음 헤딩 `## 역할 경계`가 **99행**이며 99~104는 역할 경계 본문(초안≠평가≠승격 분리·단일 소스 편집·에스컬레이션). 계획대로 지우면 trainer가 스스로 "유일한 비가역 손실"로 지목한 사고가 다른 대상에서 발생한다

**되돌린 뒤 귀결(감추지 않음)**: 배포 제품에서 모드 2→4는 **다시 꺼진다**(`69cded9` 상태 복귀, 모드 1·3이 담당). **malgnai-mcp pending 11건은 그대로 열린 채**이고 이 제거는 그것을 닫지 않는다 — 진짜 원인(소비자 부재)은 제거 후에도 동일하다. 이 작업은 잘못된 해법의 상시 비용을 없애는 것이지 문제를 푸는 것이 아니다.

워킹트리 정리 완료(미커밋 0). main이 origin보다 **37커밋** 앞서 있음 — 배포 시점에 push.
## ✅ 최근 완료
- V3-03a 공통 정리 · V3-03b-pm(`pm.md` 31.8→24.0KB) · 린터 2차 강화(`CANONICAL_CIRCULAR`·`REF_ANCHOR_MISSING`)
- Anthropic 공식 문서 미러 `docs/anthropic/` 23건 + `pnpm run sync-docs` (`a5b75a3`) — **사양 확인은 기억·웹검색이 아니라 이 미러**
- v1.7.1 디자이너 투입 기준 · v1.7.0 usage-agent(직원 개별 설치 필요)

---

## [보관] 2026-08-24 STATUS.md 축소 전 원문 (8,580B)

크기 상한 3,000B 도입으로 아래 서술을 이 파일로 옮겼다. 위임 모델은 `CLAUDE.md` → "위임 운영 규칙"으로, 항구 규칙 2건은 `CLAUDE.md`에 정본이 있어 STATUS.md에서는 뺐다.

<details>
<summary>원문 전체</summary>

---
provider: malgnai-hub
project_id: "01kzr45j8bz88dy4rw8b4g10yw"
repository_key: "claude-plugins"
---

# STATUS — claude-plugins
_최종 갱신: 2026-08-24 (훅 결함 2건 수리 + 이 파일을 L0 크기로 축소 · 직전 배포는 **v1.7.8**, origin/main 반영됨 = 전 직원 배포)_

> **이 파일은 L0 오리엔테이션용이다** — 현 운영 모드 · 지금 열려 있는 것 · 다음 행동만 둔다.
> 지나간 라운드 서술(v1.7.2~v1.7.8)과 열린 이슈의 상세 원문은 지우지 않고 옮겼다:
> **[docs/archive/status-2026-08-history.md](docs/archive/status-2026-08-history.md)** (58.6KB, 원문 그대로).
> 세션 시작 훅이 이 파일을 자동 주입하므로 여기서 커지는 만큼 **모든 세션에 비용이 곱해진다** — 이력은 아카이브로 보내고 여기엔 포인터만 남긴다.

## 🔒 현재 운영 모드 — 변경 동결 (2026-08-22 사용자 지시)
배포 이후 업데이트는 **오류·결함 수정과 작은 수정으로 한정**한다. 리팩터링·재설계·슬리밍 라운드·신규 에이전트/스킬 신설은 백로그로 보류하고 사용자 판단을 기다린다.
판정 기준: "지금 무엇이 깨져 있는가"에 답할 수 있으면 결함(진행), "이렇게 하면 더 좋아진다"면 개선(보류).
정본은 `CLAUDE.md` → **변경 동결 원칙 — 배포 이후**.

### 📌 항구 규칙 — 제품 본문에 식별자를 근거로 달지 않는다 (2026-08-22 사용자 지시)
8자리 hex든 26자 hub ULID든 로컬 memory 키든 커밋 해시든, 설치 직원은 조회할 수 없다. **교훈의 실질은 id가 아니라 문장으로 적는다.** 정본은 `CLAUDE.md` → §"malgnai-hub 도구 사양…". 검증은 **백틱 앵커 없이** — 앵커를 걸면 코드 주석의 맨몸 id가 통과한다(실증: `.md` 224건을 다 지운 뒤에도 `bin/report-usage.mjs` 주석에 ULID 2건 생존).

### 📌 항구 규칙 — 제품 본문은 최신 상태만 담는다 (2026-08-24 사용자 지시)
`agents/`·`skills/`·`knowledge/` 본문에 **"언제 바뀌었나 / 예전엔 어땠나"를 적지 않는다.** 합의 날짜 도장·이관/폐기 경위·버전/라운드/커밋 언급이 대상이다. 위 식별자 규칙과 같은 이유 — 설치 직원은 우리 회의도 라운드도 조회할 수 없고, 상시 비용만 물린다.
**단, 규칙이 생긴 이유(실패 양상)는 현재형으로 바꿔 남긴다** — 이유를 지우면 규칙을 흘려버린다. 형식 예시 안의 날짜(`review-…-2025-07-10.md`, 캡처 양식의 Date)는 이력이 아니므로 그대로 둔다.
정본은 `CLAUDE.md` → §"제품 본문은 최신 상태만 담는다".

## 🟡 진행 중 — 훅 결함 2건 + 컨텍스트 예산 (2026-08-24, 브랜치 `fix/hooks-and-context-budget`, **미병합**)
로컬 브랜치에 커밋만 되어 있다. 사용자 승인 후 main 병합 → origin push 하면 배포된다.
- **Stop 훅 중복 억제가 한 번도 작동한 적 없었다** — `stop-mcp-reminder.cjs`가 찾던 이름은 `mcp__malgnai-hub__…`인데 플러그인 설치본의 실제 이름은 `mcp__plugin_malgn-agent_malgnai-hub__…`다. 로컬 트랜스크립트 실측 **접두어 형태 148건 : 훅이 찾던 형태 0건**. 접두어 무관 판정으로 고치고, 리마인더 본문이 안내하는 이름도 실측 접두어를 학습해 쓰도록 했다. 진짜 트랜스크립트 A/B로 확인(양성 11건 전부 FIRED→SILENT, 음성 5건은 FIRED 유지).
- **`check-docs`가 저장소 밖을 가리켰다** — `package.json`이 `$HOME/.claude/hooks/doc-drift.mjs`를 실행해, 그 파일이 없는 PC에서는 실패했다. 저장소 안의 `malgn-agent/hooks/doc-drift.mjs`로 교체.
- **SessionStart 주입량 상한 신설** — `sessionstart-context.mjs`가 STATUS.md를 크기 무관하게 통째로 넣던 것을 기본 12,000B로 제한. 넘으면 앞부분만 넣고 **잘렸다는 사실을 본문 앞·잘린 지점·`systemMessage` 세 곳에 명시**(조용한 폴백 금지). `MALGN_STATUS_MAX_BYTES=0`이면 종전 무제한 동작. STATUS.md도 매니페스트도 없으면 0토큰인 성질은 유지.
- **이 파일 축소** — 61,220B → 위 아카이브 분리. 세션당 주입량이 그만큼 줄어든다.

## ⛔ 열린 이슈 (상세 원문은 아카이브 — **현재 유효성은 재확인 필요**)
아래 3건은 2026-08-20~21에 기록된 뒤 v1.7.2~v1.7.8 라운드를 거쳤다. **당시 서술이 지금도 유효한지는 대조하지 않았다** — 손대기 전에 아카이브 원문과 실물을 먼저 맞춰볼 것.
1. **🔴 PM 블록 버전 마커 제거로 설치 체커가 깨진다** (2026-08-20 기록, 당시 "배포 차단 후보"). 마커를 파싱 전제로 쓰는 소비자 3곳(`bin/new-project.mjs`·`hooks/lib/find-pm-block-path.mjs`·`skills/project-standards/scripts/check-pm-orchestration-block.mjs`). ⚠️ 이후 라운드에 "PM 마커 수리 — 실행 도달 증명" 기록이 있어 **이미 닫혔을 가능성이 높다. 상태 확인부터 할 것.**
2. **pm-orchestration-block 역할 보강의 남은 후속 3건** — reviewer Rethink(승인 주체를 grading 스킬 표의 새 열로) · `validate-agent-assets.mjs` 스캔 범위에 `hooks/` 추가 · grading 스킬 frontmatter `Trivial` vs 본문 `Micro` 불일치.
3. **후보 큐 되돌리기 라운드의 미결 2건** — evaluator 판정 소재 불명(병합 게이트) · 미커밋 reviewer 산출물 7건(경로지정 커밋 필요, `git add -A` 금지).

**보류 백로그**
- **기존 이력 표기 걷어내기 (2026-08-24 등록, 변경 동결로 보류)** — 위 항구 규칙은 앞으로 쓰는 본문에만 적용했고, 이미 들어 있는 잔존분은 손대지 않았다. 실측: 날짜 표기 **255줄**(agents 48 · skills 62 · knowledge 145), 이관/폐기 경위 서술 **109줄**. 최다 밀집은 `knowledge/README.md`(항목 설명의 상당수가 "(…본문은 skills/X로 이관) 배경만 남음" 형태). 승인 시 trainer 위임 — 기계적 일괄 치환 금지(형식 예시 날짜와 규칙의 사유 문장을 살려야 함).

## 🔑 위임 모델 (2026-08-20 개정 — 사용자 지시)

**PM은 지시서에 설계를 쓰지 않는다.** 대상과 제약만 넘기고 전문 에이전트가 **계획을 먼저 반환**한다. 그 계획의 채택은 **사용자 또는 evaluator**가 정한다 — PM 단독 결정 금지. PM이 남기는 것: 의도 전달 · 실물 대조 검증 · 기록 · 순서.
**근거(실측)**: `trainer.md` 개편에 서브에이전트 10회·약 246만 토큰. 소모의 주원인이 전부 PM 판단이었다 — 커밋1d 설계 결정이 기능을 껐고(3라운드·약 100만 토큰 소모 후 원위치, 크기도 22.5→27.7KB로 증가), 지시서 사실 오류 6회. 같은 기간 **trainer의 거부·정정은 15/15 전부 옳았다.**

**운영 규칙**
1. **검증 사이클 중 설계 변경 금지** — 아이디어는 적어두고 사이클을 닫은 뒤 판단. 크기 초과는 사유서로 끝내고 그 자리에서 고치지 않는다.
2. **검증 강도는 기존 등급 판정표를 따른다**(Skill `common-task-grading-and-verification-depth`). ~~문서만 바뀌면 패널 생략~~ — **이 규칙은 폐기**(2026-08-20, trainer 반증): 풀패널이 잡은 Critical 2건은 **문서만 읽어서는 안 보이고 실행해야 보이는** 결함이었다. "문서형이니 가볍게"였으면 둘 다 배포됐다.
3. **evaluator·reviewer는 항상 병렬.**
4. **파일 목록·인용은 그 자리에서 grep·원문 재확인**(기억 재사용이 오판 6회 중 3회의 원인).
5. 유지: 목표 KB 제시 금지·사유서 요구 · "지시가 틀렸으면 실행 말고 보고" · **'도달 증명'**(기제를 바꾸면 실제 사용자에게 닿는지 실행으로 증명, 못 하면 그 사실을 적는다).

## ✅ 최근 완료 (상세는 아카이브)
- **v1.7.8** 실행 못 할 지시 걷어내기 · **v1.7.7** 번들 스크립트 도달 경로 · **v1.7.6** 모드 체계 드리프트 · **v1.7.5** 미처리 결함 2건 · **v1.7.4** 식별자 근거 표기 폐지 · **v1.7.3** 죽은 참조 전량 제거 · **v1.7.2** 전 직원 배포
- 린터 게이트 신설(`scripts/validate-agent-assets.mjs` — 배포물 아님)
- Anthropic 공식 문서 미러 `docs/anthropic/` 23건 + `pnpm run sync-docs` — **사양 확인은 기억·웹검색이 아니라 이 미러**
- v1.7.1 디자이너 투입 기준 · v1.7.0 usage-agent(직원 개별 설치 필요)

</details>

## 📋 백로그 상세 — 에이전트 description에서 "PM 호출/단독 사용" 문구 제거 (2026-08-24 분석, 변경 동결로 보류)

**무엇이 문제인가.** `malgn-agent/agents/*.md` 21개 frontmatter의 `description` 끝에 `PM이 ~할 때 호출하거나 단독으로 사용 가능` / `PM 위임·단독 호출 모두 가능` 류 문장이 붙어 있다. description 합계 5,547B 중 약 **1,480B(27%)**가 이 문구다. description은 Agent 도구 목록에 실려 **매 세션 상시 물리는 비용**이다.

**왜 불필요한가 (실물 근거).**
1. **변별력 0** — pm 제외 20개가 전부 같은 말을 한다. description을 읽는 쪽이 하는 일은 "여럿 중 누구를 부를까"인데, 전원에게 동일하게 붙은 문장은 그 선택에 기여하지 못한다.
2. **이미 참인 사실의 반복** — Agent 도구 목록에 올라와 있다는 것 자체가 "단독 호출 가능"이다. 허가 문구가 아니라 사실 서술이고, 그 사실은 메커니즘이 보장한다. 정보가 되려면 반대(“PM만 호출, 단독 금지”)여야 하는데 그런 에이전트는 없다.
3. **PM에게도 새 정보가 아니다** — `agents/pm.md`에는 에이전트 명단이 하드코딩돼 있지 않다(`필요한 팀원을 동적으로 구성`, 3행). planner·ux-designer·presenter·writer·marketer·localizer는 본문에 등장조차 안 한다. 즉 PM도 팀 구성 시 **바로 그 description 목록**을 본다 — 목록을 펼쳐 보고 있는 쪽에 "저는 이 목록에 있습니다"라고 다시 말하는 꼴이다.
4. **라우팅 정본이 따로 있고, 비용 구조가 반대다** — `knowledge/leadership/team-composition-patterns.md`가 업무 유형별 파이프라인(`planner → architect → ux-designer → backend-dev → frontend-dev → qa-engineer → devops`)과 조건부 투입 규칙까지 갖고 있다. `PM이 프론트엔드 개발 단계에 호출` 류가 담으려던 정보가 여기 더 정확하게 있다. 그쪽은 **조건부 비용**(PM이 필요할 때만 로드), description은 **상시 비용**이다.

**무엇으로 대체하나.** architect·backend-dev·devops·planner·qa-engineer 5개는 이미 `"API 만들어줘", "서버 기능 구현"처럼 설계가 정해진 뒤 구현이 필요할 때 사용` + `(기존 구조 내 단일 필드 변경이면 생략)` 형태다 — 사용자 발화 예시 + 안 쓸 경계 조건. 나머지 16개를 이 형태로 맞추고, 그 5개에 남은 꼬리(`PM 위임·단독 호출 모두 가능.`)도 뗀다. **즉 진행하다 만 방향이지 신규 설계가 아니다.**

**주의(전부 밀면 안 되는 것).** 남길 값어치가 있는 건 "PM이 부른다"가 아니라 **선행조건 자체**다 — evaluator의 `trainer 초안 완료 후`, trainer의 `초안 완료 후 evaluator가 판정`. 주어(PM)만 빼고 조건은 살린다. 사용자가 직접 부를 때도 판별에 쓰인다.

**곁가지 결함 1건.** `agents/security.md` description에 ``PM(`pm.md`)이`` — 설치 직원이 열어볼 일 없는 저장소 내부 파일명이 노출돼 있다.

**처리.** 21파일 frontmatter 일괄이라 PM 직접 편집 대상이 아니다(trainer 위임). 판정 기준상 "지금 깨져 있는 것"이 아니라 개선이므로 변경 동결 원칙에 따라 보류. 사용자 승인 시 착수.

## 📋 백로그 상세 — 제품의 STATUS.md 상한을 "1000토큰" → "3,000바이트"로 (2026-08-24, 승인됨·미착수)

trainer 위임. 대상 4곳: `skills/project-standards/SKILL.md` 41·133행, `bin/new-project.mjs` 204·305행. 41행의 근거 문장도 바이트 기준으로 재작성한다(토큰은 세션에서 셀 수 없다 · 한글은 UTF-8 3B/글자이므로 3,000B면 전부 한글이어도 1,000토큰 안에 들어온다). **훅 기본 12,000B는 스코프 밖**(사용자 판단). 검사 수단 미배포와 `+MCP id` 문구는 별건으로 다룬다.
