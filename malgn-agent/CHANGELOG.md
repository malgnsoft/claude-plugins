# 변경 이력 (Changelog)

`malgn-agent` 플러그인의 릴리스 이력입니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를
따르며, 버전은 [유의적 버전(SemVer)](https://semver.org/lang/ko/)을 씁니다.

플러그인 버전은 `malgn-agent/.claude-plugin/plugin.json`과 저장소 루트
`.claude-plugin/marketplace.json` 두 곳에 함께 기록됩니다. **두 값이 어긋나면
`/plugin update`가 변경을 감지하지 못하므로 반드시 같이 올립니다.**

---

## [1.8.26] — 2026-09-01

golden task 벤치마크 로드맵 2단계 케이스 3종(`planner`·`qa-engineer`·`security`)을 신설했습니다
(사용자 승인, Exploration 등급, trainer 초안 → reviewer 풀패널(Red→재작업) → PM 직접 재검증).
디스크에만 실리고 세션 컨텍스트에는 로드되지 않는 `malgn-agent/evals/` 아래 추가 파일입니다.

### 추가
- `malgn-agent/evals/planner-prd-obligations/` · `qa-engineer-test-report-obligations/` ·
  `security-dev-stage-discipline/` — 케이스 정의(prompt.md + graders/*.md)

### 수정
- `scripts/run-golden-eval.mjs` — 신설 3종의 위임 실패 하드게이트 등록, `Bash` 도구 승인 추가,
  기본 비용 상한(`max-cost-usd`) 강제 제거(수동·저빈도 실행 용도로 사용자 결정)

## [1.8.25] — 2026-09-01

세션 종료 후에야 뜨는 Stop 훅 리마인더가 AI에게 전달되지 않는다는 사실이
실사용 중 확인되어, 기록 책임을 세션 진행 중 지점으로 앞당겼습니다(사용자
확인 후 trainer 초안 → reviewer 풀패널(Amber) → evaluator 조건부 통과, PM이
경계 재현 테스트로 조건 종결).

### 수정

- **PM 행동 규율 블록**(`hooks/pm-orchestration-block.md`, 매 세션 자동 주입):
  실질 작업(결정·이슈·작업 결과)을 마치면 malgnai-hub 기록과 STATUS.md
  갱신을 세션 종료 리마인더를 기다리지 않고 그 자리에서 하도록 명시.
- **기존 폴더 초기화 절차**(`skills/project-standards` §8): 신규 프로젝트
  스캐폴딩에는 있던 "STATUS.md 재작성 트리거 + malgnai-hub 기록" CLAUDE.md
  규율이 기존 폴더를 malgn-agent 프로젝트로 전환할 때는 빠지던 결함을
  점검 항목으로 보완.

## [1.8.24] — 2026-09-01

골든 태스크 벤치마크 v1 파일럿 신설(사용자 승인). architect 초안 → reviewer 풀패널
(Red → Critical 1·Major 3건 반영 후 재검증) 순으로 진행했습니다.

### 추가

- **골든 태스크 벤치마크**: `malgn-agent`의 개별 에이전트를 같은 과제로 반복 채점해
  시간에 따른 변화를 추적하는 체계. `malgn-agent/evals/architect-design-obligations/`에
  첫 파일럿(architect 1종)을 두었고, Claude Code 네이티브 `claude plugin eval`
  하네스로 실행합니다. 실행은 `pnpm run eval:golden`(저장소 전용, 배포 안 됨) —
  세션 시작 시 로드되지 않아 평소 컨텍스트 비용은 0이고, 유료 실행 전 비용 안내와
  명시적 동의(`GOLDEN_EVAL_I_ACCEPT_COST=1`)를 요구합니다. 설계·리스크 전문:
  `docs/architecture/golden-task-benchmark.md`.
- 이 기능은 조직 단위 Anthropic 얼리액세스 승인이 필요합니다 — 미승인 상태에서는
  하네스 자신의 안내 메시지와 함께 실행이 실패합니다(우회 없음).

## [1.8.23] — 2026-09-01

백로그 재조사 라운드에서 확인된, 즉시 반영 가능한 결함 1건 정정. trainer 초안 →
reviewer(Amber, 병합 비차단) → evaluator PASS 순으로 진행했습니다.

### 수정

- **`project-retrospective` 모드 2 `work_record` 기록주체 미명시**:
  `skills/project-retrospective/SKILL.md`의 "작업 진행형" 산출물 항목이 이
  기록을 누가 남기는지 명시하지 않아 애매했던 것을, `skills/reflect-lessons`의
  기존 선례와 동일한 방식으로 정본(`common-learning-loop-knowledge-management`의
  "`work_record` 주인 판별" 절) 포인터 한 줄을 추가해 해소했습니다.

### 보류

- 같은 라운드에서 함께 검토한 `evaluator.md`의 경로 형식 표기(`malgn-agent/`
  접두어 vs bare) 자기모순 건은, 이전 리뷰(2026-08-26 RV-203)가 이미 "grep
  명령은 bare·경로 참조는 접두어"로 갈린 관례를 확인하고 동종 15개 사이트를
  한 라운드로 묶으라고 결론 낸 바 있어 이번 단독 반영에서 제외하고 백로그로
  되돌렸습니다.

## [1.8.22] — 2026-09-01

변경동결 모드에서 사용자 승인으로 진행한 결함 소품 정정. trainer 초안 →
reviewer·evaluator 병렬 검증 → PM 재현검증 순으로 진행했습니다.

### 수정

- **`scoreHistoryLimit` 권장값 누락**: `knowledge/leadership/agent-training-guide.md`가
  `agent_get_context` 호출법만 안내하고 `scoreHistoryLimit` 권장값(`1`이면 충분)을
  빠뜨렸던 것을, 이미 같은 안내가 있는 다른 두 정본과 같은 취지로 보강했습니다.
- **표기 불일치**: `agents/trainer.md`의 `persona-<관점>.md`를 다른 정본들과 같은
  `persona-[관점].md` 표기로 통일했습니다.
- **중첩 괄호 재발**: `skills/domain-training-scorecard-eval/scoring-procedure.md`의
  이미 긴 문장 안에 괄호로 중첩됐던 부연설명을 별도 문장으로 분리했습니다.
- **knowledge 설명형 규율 위반**: `knowledge/review/reviewer-personas.md`에 섞여
  있던 명령형 실행 지시를 뺐습니다(같은 지시는 절차 정본인
  `skills/reviewer-persona-panel-standard/SKILL.md`에 그대로 있어 정보 손실 없음).

## [1.8.21] — 2026-09-01

변경동결 모드에서 사용자 승인으로 진행한 결함 정리 2라운드. 두 라운드 모두
trainer 초안 → reviewer·evaluator 병렬 검증 → PM 재현검증 순으로 진행했습니다.

### 수정

- **잔여 중복 3건 정본화**: `trainer.md`의 work_record 판별 로직과
  `common-learning-loop-knowledge-management`의 중복 서술을 정본 1곳 +
  포인터로 정리했습니다. `pm.md`의 진행상태 라벨 서술과
  `domain-git-safety-and-concurrency`의 중복도 같은 방식으로 정리했습니다.
  등급표 이중화(RT-001)도 함께 해소했습니다.
- **설치 경로 오류 6건 해소**: 승격된 자산이 없는 리뷰 페르소나 안내판
  3곳(`trainer.md`·`reviewer-personas.md`·`reviewer-persona-panel-standard`)이
  0개 파일을 가리키는 죽은 안내판이었던 것을 실재하는 경로 인용으로
  재서술했습니다. SessionStart 훅(`sessionstart-context.mjs`)이 플러그인에
  번들되지 않는 외부 문서를 줄번호로 인용하던 5곳도 축자 인용문 + 공개 URL
  앵커로 교체했습니다(줄번호 인용은 원본 문서 갱신 때마다 조용히 드리프트).
- **평가 기록 안내 정정 3건**: `agent_get_context` 호출 시 `scoreHistoryLimit`
  권장값(1) 안내를 추가했고, `evaluator.md`의 모호한 절 참조("위 3) 참조")에
  대상 헤더명을 병기했으며, 자기검증 체크리스트의 어색한 문장 하나를 세
  문장으로 분리했습니다.
- **정적 검사기 사각지대 2건 보강**: 문자형 절 인용(§A~§F류 볼드 라벨)을
  검사하지 못하던 것과, 색인+절별 분할 스킬의 절 파일 총량이 컨텍스트 예산
  검사에 반영되지 않던 것을 고쳤습니다.

## [1.8.20] — 2026-09-01

사용자 승인으로 변경동결을 부분 해제하고 진행한 구조개선 1건(Refactor 등급) +
결함 수정 2건. trainer 초안 → reviewer 풀패널(조건부 Green, Minor 5) · evaluator
병렬 판정(PASS) → trainer 반영 → PM 재검증 순으로 진행했습니다.

### 변경

- **스킬 3종 로딩 구조 개선**: `domain-backend-api-implementation-patterns`
  (24KB) · `domain-training-scorecard-eval`(20KB) · `domain-devops-deployment-patterns`
  (15KB)을 project-orchestration과 같은 패턴(색인+절별 파일)으로 분할했습니다.
  프로젝트당 1회만 필요한 판단 절(DAO 도입 여부·배포 단계별 절차·기록 파라미터
  상세)만 파일로 뺐고, 매번 걸리는 내용은 색인에 남겼습니다. 대표 시나리오 기준
  로드량 최대 50~65% 감소(내용 손실 없음, 기존 교차참조 전부 유효).

### 수정

- `domain-backend-api-implementation-patterns`의 "DAO/Service 무관 — 모든
  Hono 프로젝트에 적용" 우수 패턴 블록이 "DAO 유무 분기" 절 하위에 잘못
  중첩되어 있던 것을 최상위 절로 이동했습니다.
- `learning-loop-patterns`의 원칙과 실행 체크리스트에서 중복 서술되던 6개
  항목을 병합했습니다.

## [1.8.19] — 2026-08-31

사용자 승인으로 변경동결을 부분 해제하고 진행한 구조개선 1건(Refactor 등급).
trainer 초안 → reviewer 풀패널(Amber, Major 2) → trainer 반영 → PM 재검증
순으로 진행했습니다.

### 변경

- **project-orchestration 스킬 로딩 구조 개선**: `Skill` 도구가 섹션 단위가
  아니라 `SKILL.md` 전체만 로드하는데, 실사용은 매번 1~2개 절만 필요해 위임
  때마다 38.4KB가 통째로 로드되고 있었습니다. 절 번호(§1~§7)는 그대로 두고
  긴 절의 본문만 같은 디렉터리의 절별 파일로 분리했습니다(공식 "supporting
  files" 패턴). 대표 시나리오 기준 로드량 40~58% 감소(내용 손실 없음, 기존
  교차참조 전부 유효).
- **참조 검사기 확장**: `scripts/validate-agent-assets.mjs`가 스킬 절별 파일도
  참조 원천으로 훑도록 넓히고, `${CLAUDE_PLUGIN_ROOT}`가 절별 파일에서는
  치환되지 않는 제약에 맞춰 검사·처방 문구를 갈랐습니다.

## [1.8.18] — 2026-08-31

사용자 승인으로 변경동결을 부분 해제하고 진행한 백로그 3건. 각각 trainer/architect 초안 →
reviewer·evaluator 병렬 검증(위반 시 재작업 1~2라운드) → PM 실물대조 순으로 진행했습니다.

### 추가

- **신규 자산 신설 판정 3지선다**: `skills/project-orchestration` §3.6이 "지식팩 vs 전담
  에이전트" 이분법이라 Skill 신설 선택지가 없었고, knowledge 폴백이 "반복 발동 필요 절차는
  Skill" 원칙과 충돌했습니다. Agent/Skill/Knowledge 3지선다로 개정하고, 애매한 경계에서는
  Skill/Agent를 우선하는 기준을 `agents/trainer.md` "신설 판정"(정본)에 반영했습니다.
- **설치본 도달성 검사기**: 제품 본문이 인용하는 경로가 실제 설치본에서 열리는지 목적 기준으로
  검사하는 `scripts/check-install-reachability.mjs` 신설(`pnpm run check-links`). 기존
  결함 6건(2026-08-29 라운드에서 백로그로 미룬 것)이 남아 있어 `check-assets`에는 아직
  체이닝하지 않았습니다.

### 변경

- **knowledge/skills 이중 레이어 정리**: knowledge 44종 중 skills와 절차 본문이 중복되던
  3개 문서(`architecture/system-design-patterns.md`·`review/screenshot-capture-guide.md`·
  `common/permission-policy-compliance.md`)를 삭제하고 배경을 대응 skill로 흡수했습니다
  (정보 손실 없음, 흡수처와 1:1 대조로 확인). knowledge 44→40종.

### 수정

- `agents/trainer.md:151`이 교훈 게이트 4부 구조를 `common-learning-loop-knowledge-management`
  소속으로 오귀속했던 것을 `reflect-lessons`/`project-retrospective`로 정정.
- `CLAUDE.md`의 knowledge 배치 기준 문구가 실제 적용 기준(스킬 상단에 흡수 가능한 분량인가)과
  달랐던 것을 정정.

---

## [1.8.17] — 2026-08-29

v1.8.16 배포 이후 전 영역(agents 21종·skills 38종·knowledge 44종·hooks·bin) 냉혹 검증 라운드에서
실물 실행·재현으로 확정된 결함을 수정한 릴리스입니다.

### 수정

- **PM 권한 자기모순 해소**: `agents/pm.md`의 "위험도 낮음/중간은 PM이 직접판단" 문구가 같은 문서의
  등급 판정표·PM 권한 참조표와 반대로 읽혔습니다. Micro 등급만 승인 없이 직접 처리한다는 단일 기준으로
  정리했습니다.
- **비가역 행동 롤백 지점 규칙 보강**: 서브에이전트로 실행되는 PM(`agents/pm.md`,
  `skills/project-orchestration/SKILL.md`) 경로에 "merge·대량 삭제 등 되돌리기 어려운 행동 전에
  브랜치·백업·스냅샷으로 되돌릴 지점을 먼저 만든다"는 규칙이 빠져 있었습니다. 메인 세션 훅
  (`hooks/pm-orchestration-block.md`)에는 있었지만 서브에이전트 실행 경로에는 없던 공백이었습니다.
  로컬 프로젝트 정의 우선 조항에도 사람 승인 게이트를 낮출 수 없다는 카브아웃을 추가했습니다.
- **SessionStart 훅의 STATUS.md 주입이 플랫폼 문자 캡(10,000자)을 조용히 넘던 결함**을 고쳤습니다
  (`hooks/sessionstart-context.mjs`). 바이트 절단과 문자 절단 로직을 단일 함수로 통합해, 절단 시
  안내 문구(몇 줄 중 몇 줄)가 실제 주입 내용과 항상 일치하도록 했습니다.
- **Stop 훅(`hooks/stop-mcp-reminder.cjs`)이 대용량 stdin에서 출력을 파손시킬 수 있던 경로**와
  **PowerShell 전용 세션에서 리마인더가 무음 종료되던 결함**을 고쳤습니다.
- **`bin/check-wbs-warnings.mjs`가 malgnai-hub `wbs_list` 실응답과 다른 필드명(snake_case)을 읽어
  지연·블로킹 신호가 항상 조용히 침묵하던 결함**을 고쳤습니다(camelCase로 정정, 그룹/리프 이중계상도
  함께 해소). 필터된 스냅샷(`includeDone=false`)을 넣었을 때 그룹이 리프로 오판되는 경로에는 경고를
  추가했습니다. 최종수정시각 필드가 hub 응답에 없어 판정 불가능한 신호 2종은 삭제 대신 사유를 남기고
  건너뛰도록 했습니다.
- **`skills/project-orchestration/SKILL.md`를 hub 실제 스키마·스크립트 구현에 맞춰 정정**했습니다
  (파라미터 camelCase, WBS 생성 도구 사용법 보강, `delayed`가 서버 파생값임을 명시, 스크립트가
  판정하지 않는 신호 표기 등).
- **리뷰 보고서 저장 경로 정본을 `docs/reviewer/`로 통일**했습니다(`bin/check-output-conventions.mjs`,
  `skills/common-output-storage-and-path-management/SKILL.md`).
- **`skills/learning-loop-patterns/SKILL.md`가 매 작업 후 STATUS.md에 3개 섹션을 추가하라고
  지시하던 것을, `project-standards`의 STATUS.md 3,000바이트 상한 규칙과 충돌하지 않도록
  `work_record`/`decision_record` 기록으로 대체**했습니다.
- 그 외 죽은 참조(존재하지 않는 `bin/capture-all.js`·`capture-nav.js`, 정본이 스킬로 이관된 뒤 남은
  knowledge 스텁 등), 조회 불가능한 내부 프로젝트 근거(자사 프로젝트명·테이블명·파일경로), hub 도구
  파라미터 표기 오류, agents/skills/knowledge 사이 소소한 참조 불일치를 다수 정정했습니다.

### 제거

- `malgn-agent/knowledge/common/token-efficient-collaboration.md` — 본문 정본이 스킬로 이관된 뒤
  "배경만 남음"으로 선언된 스텁이었고 마지막 인바운드 참조가 사라져 삭제했습니다(knowledge 44→43).

---

## [1.8.16] — 2026-08-29

PM 행동규율 전달 방식을 전환하고(관리구역 사본 → SessionStart 훅 라이브 주입), 그에 딸려있던
문서-코드 드리프트 체커를 제거하고, malgnai-hub `agent_score_record` 계약변경에 대응한
릴리스입니다.

### 제거

- **문서-코드 드리프트 체커 기능을 통째로 제거했습니다.** 없어지는 것은 다음과 같습니다.
  - `hooks/doc-drift.mjs`와 그것이 읽던 `.claude/doc-drift.json` 매니페스트 개념 전체
    (측정법 `glob`/`homeGlob`/`jsonLength`/`file`+`regex`/`docFile`+`docRegex` 포함).
  - SessionStart 훅(`sessionstart-context.mjs`)이 세션 시작 때 붙이던 드리프트 경고·매니페스트
    파싱 실패 경고·전부 측정불가 경고.
  - 스캐폴더(`bin/new-project.mjs`)가 새 프로젝트에 스탬프하던 `.claude/doc-drift.json`(빈 checks)과
    `pnpm run check-docs` 스크립트, 그리고 CLAUDE.md·docs 지도 뼈대에 들어가던 드리프트 안내 문구.
  - 사양-구현 대조 검사(`scripts/check-doc-drift-spec.mjs`).
  - PM 행동규율 관리 구역의 인라인 신선도 점검(`checkPmBlockInline`).

  제거 이유는 이 가드가 실제로는 거의 켜지지 않았기 때문입니다. 스캐폴더가 만드는 매니페스트는
  `checks`가 빈 배열이고 그 상태가 그대로 유지되는 경우가 대부분이라, 가드는 꺼진 채로 훅·스크립트·
  스캐폴더·스킬 문서 네 곳에 걸쳐 유지 비용만 냈습니다. 게다가 잴 수 있는 것이 파일 개수와 정규식
  매치 수로 한정돼, 실제로 자주 낡는 서술형 안내(어떤 문서가 어디 있다는 설명 등)는 애초에 대상이
  아니었습니다.

  이때 함께 없어진 것이 PM 행동규율 사본의 신선도를 세션 시작 때 자동으로 봐주던 부분입니다.
  그 사본을 두는 방식 자체가 아래 "PM 행동 규율 전달 방식 전환" 항목에서 사라지므로, 신선도를
  누가 봐줄 것인가라는 문제도 같이 없어집니다.

### 변경

- **PM 행동 규율 전달 방식 전환 — 각 프로젝트 CLAUDE.md에 사본을 심는 대신, SessionStart 훅이 매
  세션 직접 넣어줍니다.**

  종전에는 `hooks/pm-orchestration-block.md`를 정본으로 두고 그 내용을 프로젝트마다 CLAUDE.md 안에
  마커로 감싼 사본("관리 구역")으로 복사한 뒤, 낡으면 사람이 점검 스크립트를 돌려 다시 맞추는
  방식이었습니다. 이제 사본을 두지 않고 훅이 원본을 그대로 주입합니다. 따라서
  **규율이 개정되면 플러그인만 업데이트하면 다음 세션부터 바로 최신 내용이 적용되고**, 프로젝트마다
  재동기화를 돌릴 일이 없어집니다. 이 방식으로 바꾸면서 함께 없어진 것:

  - 점검·재동기화 스크립트(`skills/project-standards/scripts/check-pm-orchestration-block.mjs`)와
    그 표기·판정 로직 모듈(`hooks/lib/find-pm-block-path.mjs`). 상태 어휘(`ok`/`stale-version`/
    `declined` 등)와 `--write`/`--decline` 옵션도 함께 사라집니다.
  - 그 절차를 담고 있던 `project-standards` §9, 그리고 "초기화 해줘" 환경 점검의 PM 블록 항목
    (점검 항목이 4개에서 3개 — CLAUDE.md 배치·크기 / `docs/README.md` / malgnai-hub 연동 — 으로
    줄어듭니다).
  - 스캐폴더(`bin/new-project.mjs`)가 새 프로젝트 CLAUDE.md에 관리 구역을 삽입하던 단계.
  - 블록 첫머리의 버전 마커. 대조할 사본이 없어져 버전을 매길 대상 자체가 없어집니다.

  **규율 본문은 한 글자도 바뀌지 않았습니다.** 위에서 없어진 버전 마커는 본문이 아니라 파일 첫머리의
  주석 두 줄이었습니다. 세션이 실제로 받아 보는 규율 문장은 직전 배포본과 같습니다 — 이번 변경은
  **그 문장을 어떻게 전달하느냐만 바뀐 것**입니다.

  **규율만 골라서 끄는 스위치는 없지만, 프로젝트별로 다르게 가는 길은 열려 있습니다.** 종전에는 세
  갈래가 있었습니다 — 규율의 의무가 늘어날 때 무엇이 달라지는지 보여주고 동의를 받은 뒤에만 심던
  게이트, 심은 뒤 거절 상태로 되돌리는 `--decline`, 그리고 규율 마지막 줄의 "로컬 CLAUDE.md/STATUS.md가
  다른 역할·오케스트레이션 규칙을 정의했다면 그 로컬 정의가 우선한다"는 조항입니다. 이번에 앞의 둘이
  없어지고 **마지막 조항은 그대로 남았습니다.**

  그래서 주입 자체를 끄는 설정값(환경변수 등)은 이번 릴리스에 들어 있지 않습니다. 통째로 끄려면
  플러그인을 끄는 것(`/plugin disable`)뿐인데, 그러면 `STATUS.md` 주입·에이전트·스킬까지 전부 같이
  꺼집니다. 대신 **그 프로젝트 CLAUDE.md(또는 STATUS.md)에 다른 역할·오케스트레이션 규칙을 적어 두면
  그쪽이 이 규율보다 우선합니다.** 규율 본문이 각 프로젝트 바깥(플러그인 설치 경로)에 있게 된 지금은
  이것이 프로젝트마다 다르게 가기 위한 사실상 유일한 공식 수단입니다 — "우리 프로젝트는 위임 기준을
  이렇게 쓴다"를 그 프로젝트 CLAUDE.md에 적어 두면 됩니다. 규율만 끄는 별도 스위치가 필요하면 따로
  요청해 주세요.

  **서브에이전트에는 이 규율이 더 이상 닿지 않습니다.** CLAUDE.md 본문은 내장 탐색·계획 에이전트를
  뺀 모든 서브에이전트의 시작 컨텍스트에 실리지만, SessionStart 훅이 넣는 내용은 **메인 대화의 시작
  지점에만** 들어갑니다(서브에이전트에 넣으려면 `SubagentStart`라는 별개 이벤트를 등록해야 하는데,
  이번에 등록하지 않았습니다). 따라서 규율 구역이 심겨 있던 프로젝트에서는 에이전트 전원이 이 문장을
  받다가, 이제는 메인 세션만 받습니다.

  잃기만 한 것은 아닙니다. 규율 본문은 "Standard 이상은 전문 에이전트에게 위임하라"고 지시하는데, 그
  문장이 **위임을 받아 실제로 일하는 중인 서브에이전트에게까지** 실리면 정반대 지시가 됩니다 — 지금
  수행해야 할 에이전트가 그 일을 또 넘기려 합니다. 메인 세션에만 들어가는 쪽이 그 문제를 없앱니다.
  대신 **모든 에이전트가 반드시 알아야 하는 사실**은 이 규율 블록으로 전달되지 않으므로, 그런 내용은
  프로젝트 CLAUDE.md 본문이나 위임 지시서에 적어야 합니다. "왜 서브에이전트가 규율을 못 받지"는
  빠뜨린 것이 아니라 이 이유로 택한 결과입니다.

  **왜 훅인가 (되돌리기 전에 읽어주세요).** 클로드코드 공식 문서는 "변하지 않는 정적 지시는 훅 말고
  CLAUDE.md에 두라"고 권고합니다(훅은 스크립트를 돌려야 하고, CLAUDE.md는 그냥 로드되므로). 그럼에도
  훅을 택한 이유는 우리 경우 정본이 **각 프로젝트 바깥**(플러그인 설치 경로)에 있기 때문입니다.
  바깥 경로는 `@import`로 걸면 다른 사람 PC에서 승인 다이얼로그가 뜨고, 거절하면 그대로 조용히
  비활성으로 남습니다. 그렇다고 본문에 복사해 두면 원본이 개정돼도 사본은 모른 채 갈라지고, 그
  갈라짐을 잡으려고 마커·버전·점검 스크립트라는 기계장치를 프로젝트마다 이고 가야 했습니다.
  훅 주입은 이 둘을 모두 피합니다 — 사람이 손댈 사본이 없고, 최신화가 자동입니다. 그 대가로
  세션마다 짧은 스크립트가 한 번 도는 비용과, 훅이 내보내는 문자열이 10,000자를 넘으면 본문 대신
  파일 경로와 미리보기로 대체된다는 제약을 받아들입니다. 그래서 규율 블록은 짧게 유지해야 하고,
  `STATUS.md` 주입과는 **별도의 훅 항목으로 분리해** 각자 따로 캡을 받게 했습니다 — 한 값에
  이어붙이면 `STATUS.md`가 큰 프로젝트에서 규율이 먼저 잘려 나갑니다.

- 자동 대조가 사라진 자리를 메우기 위해, 구조 서술의 정확성을 지키는 규칙을 사람이 실행할 수 있는
  형태로 다시 썼습니다(`project-standards` §6). 수치는 매니페스트에 등록하는 대신 **무엇을 세면 그
  값이 나오는지까지 함께 적어** 누구든 다시 셀 수 있게 하고(`routes/ 12개`가 아니라
  `server/routes/*.ts 12개`), 구조를 바꾼 작업 안에서 해당 서술을 같이 고치며, 마감·정리 시점에
  `ls`/`grep`으로 실측 대조합니다. 이 규칙을 인용하던 `claude-md-architecture`(§1 판정 3문·§4·§6·
  체크리스트)와 `project-orchestration` §6도 같은 기준으로 맞췄습니다.

### 수정

- `agents/evaluator.md`·`skills/domain-training-scorecard-eval/SKILL.md`·
  `knowledge/leadership/team-composition-patterns.md`를 malgnai-hub MCP 도구
  `agent_score_record`의 새 입력 계약(`raterType` 필수 추가, `evaluatorNote`+
  `improvementNote`→`note` 병합, `projectId` 제거, 스코프가 개인에서 `agentName` 단위
  회사 공용 지표로 재정의)에 맞춰 갱신했습니다. malgnai-hub 쪽 신계약의 프로덕션 배포를
  독립 확인한 뒤 반영했습니다.

---

## [1.8.15] — 2026-08-28

결함 수정 릴리스입니다.

### 수정

- malgnai-hub 이슈를 여는 절차(`issue_record`)만 있고 닫는 절차(`issue_resolve`)를 부르는 규칙이 없어,
  다른 목적의 작업이 부수적으로 해소한 이슈가 열린 채로 방치되는 문제를 고쳤습니다.
  `common-learning-loop-knowledge-management` 스킬에 "이슈 종결(Close)" 절차를 신설하고,
  `pm`·`evaluator`·`trainer`·`project-orchestration`·`learning-loop-patterns`에서 그 절차를
  따르도록 연결했습니다. 이슈를 연 사람이 아니라 해소를 확인한 사람이 그 자리에서 닫고, 검증
  라운드를 마칠 때는 이번에 손댄 파일과 겹치는 기존 열린 이슈도 함께 재점검합니다.

---

## [1.8.14] — 2026-08-26

결함 수정 릴리스입니다.

### 수정

- `trainer`의 자기검증 체크리스트에 "기록주체 서술을 고칠 때는 문구가 아니라 도구명(`decision_record`
  등, 리네이밍되지 않는 식별자)으로 전수 grep해 사이트 목록을 먼저 만든다"는 절차를 신설했습니다.
  같은 뿌리 결함이 문구 기반 검색으로 반복 재발하던 것을 막습니다.
- `doc-drift.mjs` 헤더 주석이 "전부 측정불가 시 exit 1" 판정의 소비자를 CLI 하나로만 서술하던 것을
  `sessionstart-context.mjs`(세션 경고)까지 포함하도록 정정하고, 실재하지 않던 앵커를 실제 진입점
  마커로 고쳤습니다. 재발을 막는 사양-구현 대조 검사(`scripts/check-doc-drift-spec.mjs`)를 신설해
  `check-assets`에 편입했습니다.
- `topic-learning` 스킬의 완료 보고 절차에서 trainer 자신의 학습 이력 기록(`agent_learning_record`)
  언급이 빠져 자매 스킬 `agent-upskill`과 비대칭이던 것을 맞췄습니다.
- `evaluator`의 스킬 접두어 게이트 판정 기준을 "이름이 몇 번 grep되는가"에서 "상시 비용인가 조건부
  비용인가"(knowledge 경유 간접 도달 포함)로 재정의했습니다. 기존 기준은 공통 knowledge를 경유해
  전 에이전트에 상시로 깔리는 스킬을 좁은 이름으로 오판정해 불필요한 개명을 유발했습니다.
- `evaluator`의 자기검증 체크리스트에 `agent_score_record` 쓰기·읽기 왕복(판정 전 `agent_get_context`로
  이전 점수 조회, 판정 후 기록)이 완료 정의임을 명시했습니다. 기존엔 쓰기만 게이트돼 있어 다음
  판정 회차가 이전 점수를 조회하지 못하는 결함이 있었습니다.
- `agent-training-guide.md` §6.4가 "키워드 점수는 보조 지표"라는 §3.1 선언과 모순되게 학습 보고서마다
  키워드 점수 3칸을 필수로 요구하던 것을, Scorecard 총점(조회 가능한 값) 필수 + 키워드 점수 선택
  항목으로 정정했습니다.

---

## [1.8.13] — 2026-08-26

결함 수정 릴리스입니다.

### 수정

- `frontend-vue-zero-patterns` 스킬과 정본 아키텍처 문서(`vue-zero-architecture.md`)가 규정한
  `utils.js` 등록 절차가 실제 프로덕션 배포본과 어긋나 있던 결함을 고쳤습니다. `export`로 작성한
  뒤 별도 등록 스크립트(`index.js`)에서 `import`해 `window.*`로 재등록하던 절차를, 실제 배포본처럼
  `export` 없이 선언해 일반 `<script>` 태그로 로드하는 방식(최상위 `function` 선언은 자동 전역,
  `const`/`let`/`class` 값만 필요시 수동 등록)으로 정정했습니다. `agents/frontend-dev.md`에 남아
  있던 옛 절차 지시(존재가 금지된 `composables/index.js` 참조)도 함께 정정했습니다.
- `evaluator`의 판정 체크리스트 공백 4항목(등급고정·실행력 도메인 승인게이트·스킬 중복판정·
  Knowledge→Skill 링크 판정)을 보강했습니다.
- 토큰 사용량 수집 스크립트(`report-usage.mjs`)가 `import`만으로 수집·전송이 실행되던 결함에
  ESM 메인모듈 가드를 추가했습니다(symlink 우회까지 realpath 정규화로 방어).
- `doc-drift.mjs`가 매니페스트 값을 문서 캡처 결과와 대조하지 않고 자기 자신을 복제해 비교하던
  구조를 문서 원문에서 직접 캡처하는 방식으로 전환하고, 문서 캡처 실패를 skip이 아닌 드리프트로
  승격했습니다.

---

## [1.8.12] — 2026-08-26

7개 트랙으로 나눈 강화 검증단(영역별 reviewer 3인 + qa-engineer 실기동 + security 데이터흐름 추적 +
evaluator rubric + 이음매 전담)이 라운드1~4에 걸쳐 낸 결함을 고친 유지보수 릴리스입니다.

### 보안

- **토큰 사용량 수집 스크립트(`report-usage.mjs`)가 세션 첫 프롬프트 원문 120자를 그대로
  전송하던 결함을 고쳤습니다.** API 키·비밀번호형 크리덴셜(`key=value` 패턴)과 주민등록번호
  형태의 개인정보를 전송 전에 마스킹합니다. 사용자 홈 디렉터리 절대경로의 계정명도 가립니다.
  (자연어 문장 속 비밀번호는 이번 범위 밖으로, 알려진 잔여 한계입니다.)
- `doc-drift.mjs`가 `.claude/doc-drift.json`이 존재하지만 JSON 문법이 깨진 경우를
  "매니페스트 없음"과 구분하지 못해 드리프트 검사가 조용히 통과되던 결함을 고쳤습니다.

### 수정

- `frontend-vue-zero-patterns` 스킬이 정본 아키텍처 문서(`vue-zero-architecture.md`)와
  `<style scoped>` 사용 여부·전역 등록 방식에서 정면으로 반대되는 지시를 하던 결함을 고쳤습니다
  — 스킬대로 코드를 쓰면 플랫폼 규칙을 어기는 상태였습니다.
- agents·skills·knowledge 전반에 걸쳐 죽은 참조(존재하지 않는 섹션·파일 인용), 역할 경계
  자기모순(evaluator/reviewer 소관 혼동, PM 최종결정권 오인용), 잘못된 실행 예시(Dockerfile
  빌드 실패, liveness probe 경로 불일치, Cloudflare 제한치 수치 오류, ROI 산식 이중차감)를
  정리했습니다.

---

## [1.8.11] — 2026-08-26

저장소 전체 감사에서 나온 결함을 고치는 유지보수 릴리스입니다. 제품 문서가 서로 다른
말을 하던 곳, 실행 안전성 문제, 죽은 조회 경로를 정리했습니다.

### 수정

- **PM 문서(`pm.md`)에 이 저장소 자체의 맥락("에이전트 성장이 공동목표")이 잘못
  섞여 있던 것을 일반화했고, 이 연동판에 없는 기능("자율 박동" 등)을 실행하라는
  지시도 정리했습니다.
- **채점 판정 절차 문서가 서로 다른 규칙을 말하던 곳을 정리했습니다** — "채점에
  스크립트를 쓰지 않는다"는 문장과 "총점은 반드시 스크립트로 계산한다"는 문장이
  충돌하던 것, 그리고 "판정 결과를 누가 기록하는가"가 문서마다 다르게 적혀 있던
  것(evaluator·PM·trainer 여러 문서에 걸쳐 6곳)을 하나의 규칙으로 맞췄습니다.
- **에이전트 채점 점수가 다음 회차에 조회되지 않던 문제를 고쳤습니다** — 점수를
  구조화된 형태로 남기는 지시가 없어 "지난달 대비 이번달" 비교가 항상 빈 값이었던
  것을 배선했습니다.
- QA 담당 문서에 남아 있던 조회 불가능한 참조 식별자를 서술형 문장으로 바꿨습니다.
- 여러 에이전트가 공통으로 참조하는 배경 문서가 PM 문서에 없는 정책(`risk_level`
  체계)을 인용하던 것을 실제 승인 규칙으로 정정했습니다.
- knowledge 문서 안의 죽은 참조(존재하지 않는 절 번호), 파일 자기모순 숫자,
  번들에 없는 문서와의 대비 서술을 정리했습니다.
- 리뷰 진행 문서(`reviewer.md`)가 신규 설치 조직에는 없는 파일(`INDEX.md`)이
  이미 있다고 전제하던 부분에 대응 절차를 추가했습니다.
- `README.md`의 "전부 Node 내장 모듈만 쓴다"는 설명이 실제로 외부 패키지를 쓰는
  스크립트(화면 캡처용) 한 개와 어긋나 있던 것을 바로잡았습니다.
- **자동 실행 훅 3건의 안전성 문제를 고쳤습니다**(전 직원 매 세션 실행되는 코드):
  문서-코드 정합성 점검이 아무것도 측정하지 못했을 때도 "이상 없음"으로 표시되던
  거짓 통과, 같은 상황에서 세션에 아무 신호도 안 가던 침묵, 특정 입력에서
  오류를 내며 비정상 종료하던 결함.

---

## [1.8.10] — 2026-08-25

에이전트가 실제로 읽는 참조를 정리하는 유지보수 릴리스입니다.

### 수정

- **knowledge 자료 중 아무 에이전트·스킬도 읽지 않던 17개를 정리했습니다.**
  내용이 이미 다른 스킬에 통째로 들어가 있던 10개는 지웠고, 그 외 남아있던
  7개는 실제로 해당 업무를 할 때 읽히도록 담당 에이전트(프론트엔드 개발,
  PM, 기획, QA, 리뷰, 트레이너)에 연결했습니다.
- 에이전트 훈련 가이드 문서가 스스로와 모순되던 부분(문서 골격 설명이
  두 가지로 갈려 있던 것)을 하나로 정리했습니다.
- 스킬·에이전트 사이의 상호 참조 표기 방식을 하나로 통일하고, 깨져 있던
  경로 몇 곳을 바로잡았습니다.

---

## [1.8.9] — 2026-08-25

저장소 전체 결함 감사(적대적 관점을 포함한 리뷰 페르소나 패널 검토)에서 나온
사실 오류·실행 오류를 정리하는 유지보수 릴리스입니다.

### 수정

- **Windows에서 문서 오류 경고가 조용히 사라지는 문제를 고쳤습니다.** 세션
  시작 훅이 파일 경로를 잘못된 방식으로 불러와 Windows에서만 오류가 났는데,
  그 오류가 감춰져 있었습니다.
- **화면 캡처 스킬의 예시 명령을 그대로 실행하면 에러로 멈추던 문제**와
  **뷰포트 기본 해상도 안내가 실제 값과 다르던 문제**를 고쳤습니다.
- **여러 에이전트·스킬 문서가 조회할 수 없는 자료를 근거로 가리키던 것을
  정정했습니다** — 내부 회의 기록을 근거로 쓰거나, 절 번호만 있고 어느
  문서인지 안 밝힌 곳들입니다.
- **평가(evaluator) 절차의 판정 기준 하나가 정상적으로 배포된 스킬을
  오탈락시키던 문제를 고쳤습니다.**
- 그 외 보안 담당 배치 안내가 다른 파일을 잘못 가리키던 것, OWASP 보안 항목
  번호가 뒤바뀐 것 등 문서 오기 다수를 정정했습니다.

---

## [1.8.8] — 2026-08-25

PM 행동규율을 각 프로젝트에 전달하는 방식을 바꾸고, "초기화 해줘"가 점검하는 범위를
넓히는 릴리스입니다. **업데이트 후 클로드코드를 재시작하세요.**

### 변경

- **프로젝트의 `CLAUDE.md`가 플러그인 안에 있는 PM 행동규율 문서를 "불러오는" 방식으로
  배선돼 있었는데, 실제로는 거의 로드되지 않고 있었습니다.** 클로드코드가 저장소 밖
  파일을 불러올 때 승인 창을 띄우기 때문입니다(이 방식으로 배선된 프로젝트들을 실제로
  세어 보니 규율이 실린 것이 하나도 없었습니다). 이제는 규율 문장을 `CLAUDE.md` 본문에
  시작·종료 표시로 감싼 구역으로 직접 심습니다. 새로 만드는 프로젝트는 생성 시점에
  자동으로 심기고, **이미 쓰고 있는 프로젝트는 "PM 행동규율 다시 확인해줘"라고 요청하면**
  현재 상태를 점검해 옛 배선이면 새 방식으로 바꿔줍니다. 이때 규율의 의무가 늘어나는
  경우에는 무엇이 달라지는지 보여주고 동의를 받은 뒤에만 바꿉니다. 규율을 원하지 않으면
  거절 상태로 되돌릴 수 있고, 그 과정에서 지워진 내용은 되돌릴 수 있도록 그대로
  보여줍니다.

### 수정

- **"이 프로젝트 초기화해줘"라고 하면 진행 상태 문서(`STATUS.md`)가 있는지만 확인하고
  끝났습니다.** 이제는 그 확인과 별개로 네 가지(`CLAUDE.md` 상태 진단 · PM 행동규율 구역
  · `docs` 지도 · malgnai-hub 연동)를 항상 점검해 결과를 보고합니다. 점검은 현재 상태를
  알려주는 데서 그치고, 실제로 파일을 손대는 일은 사용자에게 물어본 뒤에 합니다.

---

## [1.8.7] — 2026-08-25

작은 결함 6건을 고치는 릴리스입니다. **업데이트 후 클로드코드를 재시작하세요.**
그 밖에 직원이 해야 할 설정 변경은 없습니다.

### 수정

- **프로젝트 시작 표준 문서가 malgnai-hub 인증 방식을 잘못 설명하고 있었습니다.**
  "인증은 설치 시 입력한 토큰으로 처리된다"고 적혀 있었는데, 실제로는 OAuth 로그인
  방식으로 바뀐 지 오래고 그 토큰 필드는 지금 채워도 쓰이지 않습니다. 실제 동작에
  맞게 정정하고, 같은 이름을 쓰는 다른 용도(사용량 수집 파이프라인)의 토큰과
  혼동되지 않게 구분해서 설명했습니다.
- **재무 담당 에이전트의 자체 점검 항목이 자신이 갖지 않은 도구(셸 명령)를 쓰라고
  지시하고 있었습니다.** 실행 가능한 방식으로 고쳤습니다.
- **리뷰 담당 에이전트 문서가 한 문장 안에서 "PR을 직접 병합할 수 있다"와 "직접
  병합할 수 없다"를 동시에 말하고 있었습니다.** 실제로는 산출물 등급에 따라 갈리는
  이야기인데 그 구분이 빠져 있었습니다. 명확히 정정했습니다.
- **문서 두 곳에서 기록 시스템을 옛 이름("MCP")으로 부르고 있었습니다.** 지금 쓰는
  이름(malgnai-hub)으로 통일했습니다.
- **문서와 실제 코드가 어긋나지 않았는지 확인하는 내부 점검 도구의 설명 주석이,
  특정 경로 패턴 조합에서는 실제 동작과 다르게 서술돼 있었습니다.** (도구 자체는
  정상 동작했고, 설명만 부정확했습니다.) 실제 동작에 맞게 정정했습니다.

---

## [1.8.6] — 2026-08-25

작은 결함 3건을 고치는 릴리스입니다. **업데이트 후 클로드코드를 재시작하세요.**
그 밖에 직원이 해야 할 설정 변경은 없습니다.

### 수정

- **"일반적인 버그 수정" 등급 산출물을 최종 승인하기 전에 누가 확인해야 하는지가
  에이전트 문서 4개(재무·리서치·기획·비주얼 디자인)에서 잘못 적혀 있었습니다.**
  직전 릴리스에서 대표 문서 2곳은 바로잡았지만 이 4개 문서는 그대로 남아 있었습니다.
  이번에 마저 정정했습니다.
- **프로젝트 운영 표준 문서에서, 진행 상태 문서의 "완료된 작업" 목록을 몇 개까지
  남겨두는지에 대한 설명이 같은 문서 안에서 서로 다른 숫자(5~7개와 3~5개)를 말하고
  있었습니다.** 하나의 숫자(3~5개)로 통일했습니다.
- **문서와 실제 코드가 어긋나지 않았는지 확인하는 내부 점검 도구가, 일부 흔한 폴더
  이름(Python 가상환경의 `venv` 등)을 점검 대상에서 제외하지 못했고, 특정 경로 패턴
  조합에서는 제외 규칙 자체가 적용되지 않는 경우가 있었습니다.** 두 경우 모두
  해결했습니다.

---

## [1.8.5] — 2026-08-25

작은 결함 4건을 고치는 릴리스입니다. **업데이트 후 클로드코드를 재시작하세요.**
그 밖에 직원이 해야 할 설정 변경은 없습니다.

### 수정

- **PM이 작업 완료를 STATUS.md에 기록할 때 "기록 번호를 함께 적으라"는 지시와
  "적지 말라"는 지시가 동시에 남아 있었습니다.** 매 세션 자동으로 불러오는 문서에
  불필요한 내용이 늘어나는 것을 막기 위해, 이제는 요약 한 줄만 남기고 번호는 적지
  않는 쪽으로 통일했습니다.
- **같은 산출물을 다시 검토할 때 필요한 확인 정보가 문서마다 다르게 요구돼
  있었습니다.** 한쪽 문서는 2가지만 확인하면 재검토로 넘어갈 수 있다고 했고, 다른
  문서는 3가지를 요구해 실제로는 조건이 어긋나 있었습니다. 이제 두 문서 모두 같은
  3가지 정보를 요구하도록 통일해, 조건을 다 갖추지 않은 재검토 요청이 빠짐없이
  걸러집니다.
- **작업 등급별로 "누가 최종 승인하는가"를 한눈에 보여주는 표가 없어, 매번 다른
  문서를 찾아봐야 했습니다.** 판정표에 이 정보를 담은 열을 새로 추가했습니다. 이
  과정에서 "일반적인 버그 수정" 등급의 확인 주체가 문서마다 다르게(한쪽은 A, 다른
  쪽은 B로) 적혀 있던 오기도 함께 바로잡았습니다.
- **문서와 실제 코드가 어긋나지 않았는지 확인하는 내부 점검 도구가, 프로젝트 안에
  흔히 있는 폴더(예: 설치된 패키지 폴더) 하나만 깊은 구조여도 정작 맞는 문서를
  "틀렸다"고 잘못 판정하는 결함이 있었습니다.** 이런 폴더는 점검 대상에서 제외해
  문제를 해결했습니다.

---

## [1.8.4] — 2026-08-24

작은 결함 5건을 고치고, `CLAUDE.md`를 어떻게 쓰고 정리할지 정하는 안내 하나를 새로
추가하는 릴리스입니다. **업데이트 후 클로드코드를 재시작하세요.** 그 밖에 직원이 해야 할
설정 변경은 없습니다.

### 추가

- **"이 규칙을 `CLAUDE.md`에 적어야 하나, 다른 자리로 빼야 하나"를 정해주는 안내가
  새로 들어왔습니다.** 무엇을 어디에 적을지 정하는 배치표, 파일이 길어졌을 때 내용을
  잃지 않고 정리하는 절차, 크기 기준, 다른 파일 불러오기를 잘못 쓰는 흔한 두 가지 경우를
  담았습니다. "CLAUDE.md 정리해줘"라고 요청하면 이 안내를 따릅니다.

### 수정

- **새 프로젝트를 만들 때 심기는 PM 행동규율 배선이 프로젝트를 만든 사람의 PC 홈 경로를
  그대로 박아 넣고 있었습니다.** 그 프로젝트를 다른 PC에서 열면 경로가 맞지 않아 규율이
  실리지 않았습니다. 이제 각자의 PC 홈으로 다시 해석되는 형식으로 심습니다.
- **문서와 실제 코드가 어긋나지 않았는지 확인하는 내부 점검 도구가, 하위 폴더까지 훑는
  패턴을 폴더 이름 그 자체로 오해해 해당 항목을 조용히 건너뛰었습니다.** 이제 하위
  폴더까지 제대로 훑습니다.
- **같은 점검 도구가, 점검 목록이 아직 비어 있는 상태(프로젝트를 막 만든 직후)를
  "검사해서 통과"인 것처럼 보고했습니다.** 이제 "아직 아무것도 검사하지 않았다"로 구분해
  알려줍니다. 이 상태를 정반대로 설명하던 프로젝트 표준 문서의 문구도 함께 정정했습니다.
- **PM 문서에서 같은 지시가 두 자리에 조금씩 다르게 적혀 있었습니다**(같은 산출물을 다시
  검토할 때 무엇을 함께 넘겨야 하는가). 실질 내용은 한 곳에만 두고 다른 자리는 그것을
  가리키도록 정리했으며, 기록 시스템을 쓸 수 없을 때의 대처도 여기저기 흩어져 있던 것을
  한 곳으로 모았습니다(요구하는 행동 자체는 그대로입니다).
- **진행 상태 문서의 크기 안내에서 바이트와 토큰을 비교하는 부등호 방향이 뒤집혀 있었습니다.**
  글자당 토큰 수를 기준으로 바로잡았습니다.

---

## [1.8.3] — 2026-08-24

PM(메인 에이전트)이 판단 없이 바로 행동하지 않도록 안내를 3가지 추가하는 릴리스입니다.
**업데이트 후 클로드코드를 재시작하세요.** 그 밖에 직원이 해야 할 설정 변경은 없습니다.

### 추가

- **작업이 위험하거나 어떤 전문 에이전트가 필요한지 한눈에 안 잡히면, PM이 개별
  에이전트를 직접 짜맞추는 대신 PM 오케스트레이터에게 진행 자체를 넘기도록
  안내가 추가됐습니다.** 사람 승인이 필요한 지점에 이르면 넘겨받은 쪽이 멈추고,
  실제 승인은 항상 사람과 직접 대화하는 세션이 처리합니다.
- **작업을 재개하거나 판단이 갈리는 결정을 내리기 전에, 기억이나 추측 대신 먼저
  조회하도록 안내가 추가됐습니다.** 과거 결정·이슈 같은 기록은 malgnai-hub에서
  조회하고, 지금 이 순간의 저장소 상태(git 상태·브랜치 등)는 기록이 대신 줄 수
  없으므로 직접 확인합니다.
- **병합·대량 삭제처럼 되돌리기 어려운 작업은 실행 전에 되돌릴 지점을 먼저
  만들도록 안내가 추가됐습니다.** 실행하고 나서 리뷰나 평가가 잡아주길 기대하지
  않습니다.

---

## [1.8.2] — 2026-08-24

작은 결함 2건을 고치는 릴리스입니다. **업데이트 후 클로드코드를 재시작하세요.**
그 밖에 직원이 해야 할 설정 변경은 없습니다.

### 수정

- **새 프로젝트를 시작할 때 안내받는 "STATUS.md 3,000바이트 이내 유지" 규칙을 확인할
  자동화된 방법이 없었습니다.** 지금까지는 `wc -c`(맥) / PowerShell(윈도우) 명령을
  손으로 입력해 확인해야 했습니다. 이제 플러그인에 검사 스크립트가 포함돼, 프로젝트
  표준 절차가 이 스크립트를 자동으로 실행합니다. 새 프로젝트를 시작할 때 뜨는 안내
  문구도 이 스크립트를 가리키도록 바뀌었습니다.
- **PM이 다른 에이전트에 의해 "하위 작업자"로 불려간 경우, 사람에게 직접 승인을
  구해야 하는 상황(중요하거나 되돌리기 어려운 작업 승인 등)에서 그 확인 절차를 건너뛸
  수 있었습니다.** PM이 하위 작업자로 실행되면 사용자에게 직접 질문하는 도구를 쓸 수
  없는데(1.8.1에서 다른 에이전트들은 고쳤지만 PM 자신은 남아 있었습니다 — PM은 보통
  직접 사람과 대화하는 역할이라 이 문제가 없다고 판단했었습니다), 이제는 이 상황에
  처하면 승인이 필요한 작업을 실행하지 않고 멈춘 뒤, "승인 대기 중"이라는 사실을
  자신을 부른 쪽에 명확히 알리도록 바뀌었습니다. 자신을 부른 쪽이 "괜찮다, 진행해도
  된다"고 답해도 그것은 사람의 승인이 아니므로 무시합니다.

---

## [1.8.1] — 2026-08-24

이전 버전이 놓친 것들을 마저 고치는 릴리스입니다. **업데이트 후 클로드코드를 재시작하세요.**
그 밖에 직원이 해야 할 설정 변경은 없습니다.

### 수정

- **에이전트가 사용자에게 직접 승인을 구하라고 지시받는 문장이, 실제로는 실행할 수 없는
  지시였습니다.** 사용자에게 직접 질문하는 도구(`AskUserQuestion`)는 위임받아 실행되는
  에이전트에서는 도구 목록에 적혀 있어도 애초에 쓸 수 없습니다(클로드코드 자체의 제약).
  그런데 `security`·`evaluator`를 비롯한 여러 에이전트와 스킬 본문이 "이 도구로 직접
  물어라"고 지시하고 있었습니다. 이제 그 절차는 "위임받은 에이전트는 확인이 필요하다는
  사실을 위임한 쪽(`pm`)에게 돌려주고, 실제 질문은 `pm`이 한다"로 바뀌었습니다. 승인
  자체를 건너뛰게 된 곳은 없습니다. 프로젝트를 새로 시작할 때 쓰는 절차(스킬
  `project-standards`)도 같은 이유로 같이 고쳤습니다 — 이 스킬은 특정 에이전트 전용이
  아니라 어떤 에이전트든 상황에 따라 스스로 찾아 열 수 있습니다.
  - **1.8.0 변경 이력 정정**: 그 버전의 "`capture-strategist`가 사람에게 승인을 물을 수
    있게 됐다"는 설명은 사실이 아니었습니다. 위임받아 실행되는 에이전트는 이 도구를
    도구 목록에 갖고 있어도 쓸 수 없다는 사실이 이번에 확인됐습니다. 그래서 이번
    버전에서는 반대로, 이 도구를 목록에 적어 둬도 아무 효과가 없는 8종(`security`,
    `evaluator`, `trainer`, `backend-dev`, `frontend-dev`, `devops`, `qa-engineer`,
    `capture-strategist`)의 도구 목록에서 그 항목 자체를 뺐습니다. `pm`은 실제로 이
    도구를 쓰는 세션이라 그대로 남겼습니다.
- 새 프로젝트를 시작할 때 안내하는 `STATUS.md` 분량 상한이 "1000토큰"으로 적혀
  있었는데, 토큰은 세션 안에서 셀 방법이 없어 지키고 싶어도 지킬 수 없었습니다. 눈으로
  셀 수 있는 바이트 단위(3,000바이트, `wc -c`로 확인)로 바꿨습니다. 같은 안내가 있는
  스캐폴딩 스크립트 출력도 맞췄습니다.
- 어느 에이전트를 부를지 판단하는 데 쓰이는 각 에이전트 설명 글에, "언제 부르는지"를
  알려주지 않는 문구("PM이 호출하거나 단독으로 사용 가능")가 21종 중 20종에 똑같이
  적혀 있어 설명으로서 변별력이 없었습니다. 이 문구를 빼고 그 자리에 실제로 언제
  쓰는지를 채웠습니다.
- 위임 등급을 설명하는 스킬 안에서 등급 이름 하나가 다른 문서들과 다르게 적혀
  있었습니다(`Trivial` → 나머지 문서와 같은 `Micro`로 통일).
- 프로젝트 운영 표준 스킬의 "완료 항목마다 id를 적으라"는 안내를 뺐습니다.
  malgnai-hub에서 이력을 조회할 수 있어 STATUS.md에 따로 적을 필요가 없고, 적으면 그
  자체가 매 세션 들어가는 분량이 됩니다.

### 정리

- 에이전트·스킬·참고자료 본문 곳곳에 남아 있던 "언제·누가·왜 이렇게 바꿨는지"류
  서술(합의 날짜, 이전 버전과의 비교, 내부 논의 경위)을 걷어냈습니다. 이런 서술은
  설치한 회사 입장에서 확인할 방법이 없는 내용이라 도움이 되지 않고, 매 호출마다
  분량만 차지하고 있었습니다. 규칙이 왜 필요한지에 대한 설명은 남겼습니다.

### 검사기

- 스킬이 실행하라고 지시하는 번들 스크립트, 복사해 쓰라고 안내하는 템플릿 파일이 실제로
  있는지 확인하는 검사를 추가했습니다. 지금까지는 이런 참조가 옮겨지거나 지워져도
  설치 직원이 실제로 열어보기 전까지는 아무도 몰랐습니다.

## [1.8.0] — 2026-08-24

에이전트가 **지시는 받고 있었지만 실제로는 실행하지 못하던 일들**을 되살린 릴리스입니다.
21종 중 12종은 1.7.8까지 쓸 수 있는 도구가 목록으로 제한돼 있었는데, 그 목록에 스킬
실행과 malgnai-hub 기록이 빠져 있었습니다. 그래서 본문이 문장으로 지시하던 절차가 그
12종에서 조용히 건너뛰어지고 있었습니다. 오류 메시지가 뜨지 않아 겉으로는 알아채기
어려웠던 부분입니다.

**업데이트 후 클로드코드를 재시작하세요.** 그 밖에 직원이 해야 할 설정 변경은 없습니다.
다만 보안 스킬 하나의 이름이 없어졌으니, 스킬 이름을 직접 지목해 부르시던 분은 아래
"제거" 항목을 확인하세요.

### 수정

- **`capture-strategist`가 사람에게 승인을 물을 수 있게 됐습니다.** 도구가 제한된
  에이전트라 `AskUserQuestion`이 없었고, 본문이 지시하는 "사람에게 직접 확인" 절차가
  실행되지 않았습니다. 승인을 묻도록 지시받는 나머지 8종(`pm`, `security`,
  `evaluator`, `trainer`, `backend-dev`, `frontend-dev`, `devops`, `qa-engineer`)은
  1.7.8에서도 정상 동작했습니다 — 도구가 제한돼 있지 않아 자동으로 갖고 있었습니다.
  이번에 도구 목록을 21종 전부 명시로 바꾸면서 그 8종이 승인 도구를 잃을 뻔했으나
  배포 전에 되돌렸습니다.
- **에이전트가 스킬을 부를 수 있게 됐습니다.** 도구를 제한한 에이전트 12종의 목록에
  `Skill`이 빠져 있었습니다. 공식 규격상 목록에서 `Skill`을 빼면 그 에이전트는 스킬을
  전혀 실행하지 못하므로, 본문이 "이 스킬을 따르라"고 가리키던 참조 55건이 전부 죽은
  지시였습니다. 이제 21종 모두 스킬을 실행합니다.
- **에이전트가 참고자료(`knowledge/`)를 실제로 열 수 있게 됐습니다.** 본문의 참조가
  플러그인 안이 아니라 작업 중인 프로젝트 폴더를 가리키는 형태여서, 에이전트가 파일을
  찾다가 실패했습니다(실측 확인). 참조 112곳을 플러그인 기준 경로로 맞추고, 이미 없는
  문서를 가리키던 참조 9건을 정리했습니다. 로고·스타일가이드 경로 9곳도 같습니다.
- **도구가 제한됐던 11종이 malgnai-hub에 결정·이슈·작업을 기록할 수 있게 됐습니다.**
  기록 지시는 대부분 본문이 아니라 스킬을 통해 전달되는데, 이 11종의 도구 목록에는
  hub 도구가 없어 지시가 도달해도 실행할 수 없었습니다(`finance`는 그 스킬들을 하나도
  참조하지 않아 대상에서 제외). 도구 제한이 없던 나머지 9종은 1.7.8에서도 기록이
  가능했습니다. 이번에 20종 전부에 hub 도구를 명시하고 `ToolSearch`를 함께 넣었습니다 —
  MCP 도구는 기본적으로 이름만 로드되고 정의는 나중에 불러오는 방식이라, `ToolSearch`가
  없으면 도구를 줘도 쓸 수 없습니다.
- **세션을 마칠 때 뜨는 hub 기록 리마인더**가 방금 기록을 남긴 턴에도 계속 뜨던 문제를
  고쳤습니다. 훅이 찾던 도구 이름이 실제 설치본의 이름과 달라서 "이미 기록했으면 건너뛴다"가
  한 번도 동작한 적이 없었습니다. 리마인더가 안내하는 도구 이름도 이제 실제 이름으로
  나옵니다.
- 도구 제한이 없던 9종은 자기가 또 다른 에이전트를 띄울 수 있었습니다. 팀을 꾸리는
  `pm`만 남기고 나머지 8종에서 그 권한을 뺐습니다(이미 제한돼 있던 12종은 원래
  불가능했고, 결과적으로 지금은 `pm` 1종만 가능합니다).
- `evaluator`에서 `Edit`을 뺐습니다. 판정이 `evaluator`의 일이고 편집은 `trainer`의
  일이라는 원칙에 맞춘 것입니다.
- `pnpm run check-docs`가 개인 홈 폴더의 파일(`$HOME/.claude/hooks/doc-drift.mjs`)을
  실행하도록 되어 있어, 그 파일이 없는 PC에서는 실행 자체가 실패했습니다. 저장소 안의
  파일을 쓰도록 고쳤습니다. 다만 이 명령이 어디서나 통과한다는 뜻은 아닙니다 — 검사
  항목 중 하나가 malgn-agent 설치본의 위치를 찾으므로, 플러그인이 설치되지 않은
  환경(CI 러너 등)에서는 여전히 실패합니다. 플러그인이 설치된 PC에서 쓰는 명령입니다.

### 추가

- **세션을 시작할 때 자동으로 들어가던 `STATUS.md` 분량에 상한이 생겼습니다**(기본
  12,000바이트). 이 훅은 모든 프로젝트의 모든 세션에 걸리기 때문에 파일이 길어질수록
  비용이 곱해집니다(실측으로 61,369바이트가 한 번에 들어간 세션이 있었습니다). 넘으면
  앞부분만 넣되 조용히 자르지 않고, 잘린 사실과 나머지를 읽는 방법을 함께 알려줍니다.
  줄 경계에서 자르므로 한글이 깨지지 않습니다. 프로젝트 사정에 안 맞으면
  `MALGN_STATUS_MAX_BYTES` 환경변수로 조정하고, `0`으로 두면 종전처럼 무제한입니다.
- 에이전트 21종에 사용 모델을 명시했습니다. 판단 자체가 산출물이거나 영향 범위가 조직
  전체인 6종(`pm`, `architect`, `evaluator`, `trainer`, `reviewer`, `security`)은
  `opus`, 나머지 15종은 `sonnet`입니다.
- 플러그인 폴더 안에 `README.md`·`CHANGELOG.md`·`LICENSE`를 배치했습니다. 설치·업데이트
  절차와 구성요소를 플러그인 폴더에서 바로 확인할 수 있습니다.
- `plugin.json`에 공식 규격 권장 메타데이터 `license`·`repository`·`homepage`를
  추가했습니다.
- GitHub Actions 워크플로를 신설했습니다. `main` push와 모든 PR에서 매니페스트 규격
  (`claude plugin validate --strict`)과 에이전트/스킬 자산 정합(`check-assets`)을
  자동 검증합니다. 마켓플레이스 매니페스트도 함께 검증해 `plugin.json` ↔
  `marketplace.json` 버전 불일치를 배포 전에 잡습니다.
- 저장소 쪽 자산 검사(`check-assets`, 플러그인에는 포함되지 않는 개발용 도구)에 회귀
  방지 규칙 3건을 넣었습니다 — 참고자료 참조가 열 수 없는 형태일 때, 훅·번들 스크립트에
  죽은 참조가 있을 때, 그리고 문서의 구성요소 개수 표기가 실물과 어긋날 때. 셋 다 이번에
  고친 결함이 그대로 통과하던 사각이었습니다.

### 변경

- **보안 스킬을 4종에서 3종으로 정리했습니다.** 네 스킬의 설명문이 서로를 지목하며
  "이 스킬은 저것과 중복되지 않는다"고 해명하고 있었는데, 실측해 보니 해명이 짝지은
  쌍과 실제로 겹치는 쌍이 달랐습니다. 항상 함께 읽히도록 설계돼 있던 한 쌍을 하나로
  합쳤습니다. 남은 3종은 "무엇을 보고 있는가" 한 축으로 갈립니다 — 라우트 한 건
  (`domain-backend-api-security`) / 이 스택의 코드베이스
  (`domain-serverless-edge-api-security`) / 프로젝트 전체 태세
  (`domain-security-audit-checklist`).
- `plugin.json`의 `description`을 규격에 맞는 짧은 소개문으로 정리했습니다. 기존에 이
  필드에 누적돼 있던 버전별 변경 이력은 이 문서로 옮겨 보존했습니다.

### 제거

- 스킬 `domain-backend-security-audit` — `domain-backend-api-security`에 흡수됐습니다.
  **이 이름을 직접 지목해 부르시던 분은 `domain-backend-api-security`로 바꿔 부르세요.**
  흡수된 내용(4계층 입력검증 구조, 외부 호출 안전성)은 그대로 옮겨졌고, 남은
  malgnai 코드 규약은 `domain-backend-api-implementation-patterns`에 더 자세한 형태로
  이미 있습니다. PM에게 맡기시는 평소 사용 방식에는 영향이 없습니다.

---

## [1.7.8] — 2026-08-23

### 변경
- 위임 판단이 내부 문서의 번호 체계에 묶여 있지 않도록 정리했습니다. 번호가
  바뀌어도 판단 기준이 깨지지 않습니다.

## [1.7.7] — 2026-08-23

### 수정
- 번들 스크립트(`bin/`)에 도달하는 실행 경로를 하나의 정본 규약으로 통일했습니다.
  추측성 경로 플레이스홀더와 맨 명령어 실행 지시를 제거하고, 회귀를 막는 린터
  게이트를 함께 넣었습니다.
- `new-project.mjs` 결함 3건 수정 — 스캐폴딩된 `check-docs`의 탐색 경로를 실물에
  맞추고, `--help`가 폴더를 만들지 않게 했습니다.

## [1.7.6] — 2026-08-23

### 수정
- 훈련 가이드의 모드 체계가 실제 정본(`agents/trainer.md`)과 어긋나 있던 드리프트를
  정합했습니다.

## [1.7.5] — 2026-08-22

### 수정
- `knowledge/` 본문이 가리키던 죽은 스킬 참조 16건을 정합했습니다(스킬 개명 라운드
  이후 누락분).

## [1.7.4] — 2026-08-22

### 제거
- 제품 본문에서 조회 불가능한 식별자를 근거로 다는 표기를 항구적으로 폐지했습니다.
  설치한 직원이 열어볼 수 없는 내부 식별자 5건을 제거하고, 교훈의 실질은 식별자가
  아니라 문장으로 적도록 규칙을 확대했습니다.

## [1.7.3] — 2026-08-22

### 제거
- 조회 불가능한 죽은 참조 224건을 제거했습니다.

### 수정
- `plugin.json`과 `marketplace.json`의 버전이 어긋나 `/plugin update`가 변경을
  감지하지 못하던 문제를 해소하고 양쪽을 같은 값으로 맞췄습니다.

## [1.7.2] — 2026-08-22

전 직원 배포 기준선. 이 시점부터 업데이트는 오류·결함 수정과 작은 수정으로
보수적으로 운영합니다.

### 수정
- 배포본 위에 검증된 결함 수정만 재이식했습니다.

## [1.7.1] — 2026-08-19

### 변경
- 웹개발 팀 구성 정책 개정 — `ux-designer`를 기본 투입으로 바꾸고,
  `visual-designer` 투입 판단 주체를 `ux-designer` 설계 단계로 옮겼습니다.
- 프레임워크별 기본 UI 라이브러리 원칙을 명문화했습니다
  (vue-zero → Bootstrap 5, Nuxt → Nuxt UI).

## [1.7.0] — 2026-08-19

### 추가
- 토큰 사용량 자동 수집 usage-agent 신설 — 직원 PC의 Claude Code 로컬 사용량을
  매시간 malgnai-hub로 집계·전송합니다(`bin/report-usage.mjs` 등).
- 수집 파이프라인 자체의 상태를 점검하는 `usage-agent-healthcheck` 스킬 추가.

## [1.6.1] — 2026-08-19

### 추가
- 로고 옆에 회사명 텍스트를 병기하지 않는 디자인 규칙 추가.

## [1.6.0] — 2026-08-19

### 추가
- 사내 노하우를 에이전트·스킬 전반에 접목한 배치 반영.

## [1.5.0] — 2026-08-12

### 변경
- 스킬 5종을 "설명형"에서 "스크립트 활용형"으로 개정 — 판단 근거가 되는 후보 탐지를
  번들 스크립트가 수행하고, 최종 판단은 사람/에이전트가 하도록 역할을 나눴습니다.

## [1.4.0] — 2026-08-11

### 변경
- malgnai-hub 인증을 OAuth 2.1(PKCE)로 전환했습니다.

## [1.3.0] — 2026-08-11

### 변경
- malgnai-hub 부트스트랩 재설계 — 상시 PM 블록 훅 방식에서 스캐폴딩 1회 + 온디맨드
  스킬 방식으로 바꿔 상시 토큰 비용을 줄였습니다.

## [1.2.3] — 2026-08-11

### 변경
- `token-usage-diagnosis` 3축 집계로 개선.

## [1.2.2] — 2026-08-10

### 추가
- 같은 파일을 반복해서 읽는 낭비를 막는 반복-Read 방지 패턴 도입.

## [1.2.1] — 2026-08-10

### 추가
- `token-usage-diagnosis` 스킬 신설 — 개인 세션 로그 기반 토큰 사용 패턴 자가진단.

## [1.2.0] — 2026-08-10

### 변경
- PM 오케스트레이션 블록의 전파 방식을 `@import` 우선 + 훅 드리프트 가드로
  전환했습니다.

## [1.1.0] — 2026-08-10

### 수정
- PM 오케스트레이션 훅 수정분을 실제 배포에 반영.

## [1.0.0] — 2026-08-07

### 추가
- 방법론 rubric 기반 전면 재구축. 신규 에이전트/스킬/knowledge 작성의 판정 기준을
  세우고 그 기준으로 자산 전체를 다시 만들었습니다.
- 이전 명칭 `malgn-dev`에서 `malgn-agent`로 개명.
